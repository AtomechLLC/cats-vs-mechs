---
phase: 02.1-token-authoring-inserted
verified: 2026-08-28T07:54:14Z
status: human_needed
score: 5/5 must-haves code-verified; 0/5 itemised-confirmed in a real browser
overrides_applied: 0
human_verification:
  - test: "Create a token type, close the editor (Done), then raise its tally from zero using the on-board stepper alone — no console."
    expected: "The just-created type's zero line is visible on every card/faction-head its scope names once the dialog is closed, and pressing the on-board +/- actually changes the number. This is the F-02.1-B fix (commit ce6f61e) and it has never been exercised in a real browser."
    why_human: "The whole mechanism depends on <dialog> modal/backdrop semantics (pointer capture while open, release on close) that are well-specified but were previously wrong once already in this exact phase (F-02.1-A shipped a reveal that could not be pressed; F-02.1-B fixed it). No browser or Playwright is available in this repo/session, so only a human running the artifact from file:// can close this out. This is the literal reason ALLOC-11 is unticked."
  - test: "02.1-HUMAN-UAT.md Check 2 — create a type, leave its tally at zero on all 12 cards, confirm no empty box/gap appears; then raise it on one card and confirm only that card grows."
    expected: "Zero-tally lines take no layout space; card heights are unaffected until a number is written."
    why_human: "The Node stub has no layout engine; `hidden` is asserted as a DOM property only."
  - test: "02.1-HUMAN-UAT.md Check 3 — type a name, press Escape (dialog stays open, text reverts), then press Escape again with focus on a button (dialog closes, focus returns to the Tokens button)."
    expected: "Dialog cancel is intercepted only while the name field is dirty; a second Escape with nothing pending closes normally."
    why_human: "The stub `<dialog>` has no close-request (`cancel` event) behaviour at all; gate check 37 covers only the revert half, never the not-closing half."
  - test: "02.1-HUMAN-UAT.md Check 4 — type 30 plain characters into the Name field, then paste 30 skull emoji, then Enter."
    expected: "Field stops at 24 characters when typed; admits ~11-12 emoji when pasted (maxlength counts UTF-16 units); no broken half-character; Enter commits with no error panel."
    why_human: "`maxlength` counts UTF-16 code units, the op's cap counts code points — the disagreement only exists on a real `<input>`. Gate check 38 drives the code-point cut through the op directly and never touches `maxlength`."
  - test: "02.1-HUMAN-UAT.md Check 5 — create six custom types (11 total rows), put the artifact on the actual workshop display, and read the list from the back of the room."
    expected: "Every row's name and token legible at distance; list scrolls without overflowing the dialog; selected row distinguishable at distance."
    why_human: "Carries forward Phase 2's unclosed projector gap (G-02-B); IN-02 additionally flags the list rows at 16px, under the file's own 18px legibility floor for anything carrying information."
  - test: "02.1-HUMAN-UAT.md Check 6/7 — rename Health to Vigor and confirm all three surfaces (list row, dialog heading, all 12 cards) update in lock-step and Ctrl+Z reverts them together; read the refusal message for an empty name."
    expected: "One word, three places, no drift; refusal text reads as a plain sentence, not a stack trace."
    why_human: "Mechanically covered by gate checks 30/35/38, but prose legibility and a literal keyboard walk (including the deliberate 'Ctrl+Z ignored with the caret in a text field' behaviour) were never confirmed by a person."
  - test: "02.1-HUMAN-UAT.md Check 8 — full end-to-end rehearsal: invent a type named `Poison ☠`, set circle/violet/skull, close the editor, raise the tally on two cats by hand (not console), remove the type, undo once."
    expected: "Two violet skull circles on the two chosen cats and nothing elsewhere; type + both tallies vanish together on remove with no confirmation dialog; one Ctrl+Z restores everything together."
    why_human: "This is ROADMAP criterion 3's literal acceptance test and it has never been run by a human even once, before or after the F-02.1-B fix — the only rehearsal reply on record ('token config works') predates the fix and did not attempt the tally-by-hand step."
  - test: "02.1-HUMAN-UAT.md Check 9.7-9.9 — middle-click autoscroll over an armed ramp, a physically held Enter at OS repeat rate, and the same two on a tally stepper specifically."
    expected: "No stray value change or autoscroll circle; a held key produces one undo step, not a burst."
    why_human: "No harness in this repo can drive a middle-click or a real OS-level key-repeat; carried unresolved from Phase 2 and now also untested on the phase's own new tally steppers."
  - test: "CR-01 review findings, cases A and C (row press mid-type, and 'New type on the whole side' mid-type) — confirmed only in the Node stub's emulated pointerdown-before-focus ordering."
    expected: "A half-typed name commits to the type it was typed for in both cases, exactly as case B (Health-renamed-to-Venom) was confirmed to do with real synchronous focus() semantics."
    why_human: "The review rates cases A/C MEDIUM-HIGH specifically because the browser's native pointerdown-before-focus-change ordering was never executed, only modelled."
  - test: "WR-02 (focus drop when a tally line collapses to hidden) and WR-04 (list scroll jump on repaint) — confirmed only against the stub's modelled `display:none`-blurs-active-element and `replaceChildren`-clamps-scrollTop behaviour."
    expected: "Focus does not visibly vanish to <body>; the type list does not visually jump to the top under a student's finger."
    why_human: "Both are specified browser behaviours faithfully modelled in the Node stub, per the fix report's own 'Carried forward, not fixed' section — neither was run in a browser."
---

# Phase 2.1: Token Authoring (INSERTED) Verification Report

**Phase Goal:** A student can name any token type and invent new ones of their own, so the board's
vocabulary becomes something they authored rather than something the tool handed them.
**Verified:** 2026-08-28T07:54:14Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (ROADMAP § Phase 2.1 Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | A student can give any token type a name, and that name appears wherever the token type is presented — picker and board row alike | ✓ VERIFIED (code) | `renameTokenType` (`cats-vs-mechs.html:2109`) writes `build.tokens[id].name`; `labelFor`/`[data-lbl]` sync pass repaints every stat line, every built-in included after the Damage-label fix (`e7f14ef`, confirmed still present at line 2667-2668 passing `'dmg'` as the 5th `statLine` arg); gate check 30 asserts list row + board label move together on rename; a dedicated `[S09.7]` row walks all 4 labelled built-ins (`hp,ap,shield,dmg`) so a 5th silent exception can't recur. Human confirmation exists only in aggregate ("token config works"), not itemised — see human_verification. |
| 2 | A student can create a new type choosing name/shape/colour/emoji from the same allowlists built-ins use, rendering exactly like a built-in | ✓ VERIFIED (code) | `createTokenType` (`:1995`) validates `p.shape`/`p.color`/`p.glyph` against `App.data.SHAPES`/`COLORS`/`GLYPHS` — the identical arrays built-ins draw from; `tallyLine`/`tokenRow` (`:2598`, `:2613`) reuse the exact board-rendering path built-ins use, no parallel renderer. Gate check 31 confirms a line is drawn on every unit card (unit scope) or every faction head (side scope). |
| 3 | A student-created token counts nothing on its own; it carries a hand-incremented tally the tool tracks and never infers | ⚠ CODE-VERIFIED, NEVER BROWSER-CONFIRMED | `nudgeTally`/`setTally` (`:2217`, `:2236`) are plain state ops with no model/derivation reading a tally anywhere in the file (confirmed by grep — only `readTally`/`writeTally`/`dropTally` touch it). Reachability was broken twice and fixed twice in this phase: F-02.1-A (reveal added, `19a23f1`) then found unreachable because the picker is modal (F-02.1-B); the actual fix (`ce6f61e`) lets the reveal survive the editor closing, so the revealed line is live on the board — not behind a backdrop — the moment the dialog shuts. **I independently confirmed this fix is real and load-bearing**: reverting just the behavioural line (re-adding `setReveal('')` to `onPickerClose`) while keeping the current assertions turns 2 rows of the full 430-row suite red (`actual="" expected="t1"`, `actual=true expected=false`); the fix is intact in the shipped file. **What no one has done is press it in an actual browser** — REVIEW.md and this session both state plainly there is no browser/Playwright in this environment, and 02.1-06's own must-haves (plan frontmatter) require "a student can invent a token type, ... tally it, ... end to end, by hand" confirmed by a human, which HUMAN-UAT.md Check 8 recorded as NOT RUN even before this fix existed. |
| 4 | A student can remove a type they created; the five built-ins cannot be removed | ✓ VERIFIED (code) | `removeTokenType` (`:2064`) refuses any `TOKEN_IDS` member by name before touching state; CR-01 case B (Remove silently renaming Health) and CR-02 (ghost row after undoing a create) were both found in code review and fixed (`57c087e`, `f2d2802`) — I independently reverted each fix in a scratch copy and confirmed the regression gate checks (39, 40) go red without them and green with them restored. |
| 5 | Creating a type with a colliding/prototype-key id is refused by name, not silently accepted | ✓ VERIFIED (code) | `requireTokenId` (`:1412`) checks `RESERVED_KEYS` (`__proto__`, `constructor`, `prototype`) **before** the shape pattern, with a comment explaining the ordering is load-bearing; `requireNewTokenId` additionally refuses the 5 built-in ids; ids are always machine-generated (`nextTokenTypeId`, never derived from student text) per D-13. REVIEW.md independently peeled each of the 3 defence layers in scratch copies and confirmed the other two still held in every case; I did not re-run that specific probe but read the resulting code and confirm the guard ordering matches the review's description. |

**Score:** 5/5 truths hold up under code inspection, static tracing and regression-style probing
(reverting each fix and watching the relevant gate check go red). **0/5 have an itemised human
confirmation from inside a real browser** — the project's own README-equivalent (CLAUDE.md /
tech-stack doc) and this phase's own review repeatedly flag that no browser or Playwright exists in
this environment, and 02.1-HUMAN-UAT.md's one recorded human reply ("token config works") both
predates the two review-driven fix rounds and never attempted the specific "tally by hand" step
criterion 3 turns on.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `cats-vs-mechs.html` `[S01]` `TOKEN_ID_PATTERN`/`TOKEN_ID_CAPTURE`/`RESERVED_KEYS`/`MAX_CUSTOM_TYPES`/`TOKEN_SCOPES`/`MAX_TOKEN_NAME` | Id-shape + refusal + cap constants | ✓ VERIFIED | Present at `:765-785`, exported on `App.data`, asserted against in `[S09.7]` |
| `cats-vs-mechs.html` `[S05]` `createTokenType`/`removeTokenType`/`renameTokenType`/`setTally`/`nudgeTally` | The five authoring ops | ✓ VERIFIED | All present, wired into `dispatch()` (`:2288-2294`), guards run outside the commit per the file's stated convention |
| `cats-vs-mechs.html` `[S06.1]` `labelFor` + `[data-lbl]` sync pass, `amountFor` scope branch, hide pass | Board renders custom types | ✓ VERIFIED | `:2598-2668`, `:3033-3043`; the hide pass now takes 3 inputs (amount, reveal, focused-line) after WR-02 |
| `cats-vs-mechs.html` `[S06.2]`/`[S07.2]` list-plus-editor picker, `showType`/`setReveal`/`revealFor`, topbar collapse | The authoring surface | ✓ VERIFIED | One `Tokens` button confirmed (`grep -c 'data-act="openTokenPicker"'` → 2, matching the documented count of 2 including the stub's own selector use), `showType` is the single owner of `dataset.tok` + reveal (`:4103`) |
| `tests/selftest-node.cjs` `KNOWN_IDS` / stub-drift gate | Shell/stub id parity | ✓ VERIFIED | `stub-drift gate: 35 shell ids, all built by the stub page` — measured, exit 0 |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `[S05] createTokenType`/`removeTokenType`/`renameTokenType` | `requireTokenId`/`requireNewTokenId`/`requireTokenName` | guard called before any commit | ✓ WIRED | Read directly; order is load-bearing per the file's own comment and REVIEW.md's layer-peeling probes |
| `[S06.1] sync()` | `build.tokens[id].name` | `[data-lbl]` pass through `labelFor` | ✓ WIRED | Confirmed for all 4 labelled built-ins + custom types via the `[S09.7]` regression row added for the Damage-label bug |
| `[S07.2] onPickerClose` | board (`.brd-line--opt` hide pass) | `revealTok` surviving close, read by `sync()`'s hide pass | ✓ WIRED (code) | Traced end-to-end: `showType` sets `revealTok` → `onPickerClose` no longer clears it → `sync()`'s hide pass reads `state.ui.revealTok` on every frame, independent of dialog open/closed state. Confirmed load-bearing by reverting the one line that used to clear it (see truth 3 evidence) — **wiring is real, but never observed rendering in an actual DOM/browser, only in the Node stub DOM** |
| `#topbar` `Tokens` button | `UI_HANDLERS.openTokenPicker` | `UI_ACTS` seam, not `[S05] dispatch` | ✓ WIRED | `:4135-4142`, confirmed by gate check 27 |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|---------------------|--------|
| Board tally line (`tallyLine`) | `amountFor(state, tokenId, side, unitId)` | `readTally` off `state.build[side].units[i].tally` / `state.build[side].tally`, written only by `setTally`/`nudgeTally` | Yes — sparse, real state, no static/hardcoded fallback | ✓ FLOWING |
| Picker list row / editor | `state.build.tokens` (whole vocabulary) | Same `build` slice every render reads | Yes | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Terminal suite (DOM-free + stub-DOM interaction gate) | `node tests/selftest-node.cjs` | `363 passed, 0 failed`; `interaction gate: 46 of 46 checks passed`; `stub-drift gate: 35 shell ids`; `scan: no forbidden patterns`; exit 0 | ✓ PASS |
| Full in-file suite incl. document-gated rows, run under a lifted `makeStubDom()` in `vm` with `Event`/`MouseEvent`/`KeyboardEvent`/`CSS.escape` supplied | scratch script per verification instructions | `TOTAL: 430 records; 430 passed, 0 failed` | ✓ PASS |
| CR-01 fix is load-bearing | reverted `commitName`'s type-binding fix in a scratch copy, kept current tests | Check 39 (and 41) → FAIL; restored → PASS | ✓ PASS (regression-proven) |
| CR-02 fix is load-bearing | reverted `syncPicker`'s ghost-row fix in a scratch copy, kept current tests | Check 40 → FAIL; restored → PASS | ✓ PASS (regression-proven) |
| F-02.1-B fix is load-bearing | reintroduced `setReveal('')` into `onPickerClose` in a scratch copy, kept current tests | 2 rows in the full 430-row suite → FAIL (`revealTok` empty instead of the type id, line hidden instead of shown); restored → PASS | ✓ PASS (regression-proven) |
| Naming-trap / acceptance greps | `grep -ci "counter\|rating\|balanced\|difficulty"`, `grep -c "verdict\|balanced\|rating\|difficulty"`, `grep -cE "https?://"`, script/style counts | 0, 0, 0, 1/1 | ✓ PASS |

### Probe Execution

No `scripts/*/tests/probe-*.sh` convention exists in this project; this phase's equivalent
"probe" is `node tests/selftest-node.cjs` (behavioral spot-check above) plus the full-suite and
regression-revert scripts run for this verification. No separate probe files were declared in any
PLAN/SUMMARY for this phase.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|--------------|------------|--------------|--------|----------|
| ALLOC-10 | 02.1-01 .. 02.1-06 | Student can give a token type a name, shown wherever presented | ✓ SATISFIED | Truth 1 above; REQUIREMENTS.md already ticks this and the evidence supports it |
| ALLOC-11 | 02.1-01 .. 02.1-06 | Student can create/remove own token types; a free hand-incremented tally the tool never infers | ? NEEDS HUMAN | Truths 2, 3, 4, 5 above. Code-level evidence is now strong for the "increments by hand" half specifically (previously blocked by F-02.1-A, subsequently reasoned-fixed by F-02.1-B), but **REQUIREMENTS.md still ticks this `[ ]` citing F-02.1-A as the open blocker — that note is now stale** (F-02.1-B superseded it), and neither the old nor the new blocker has ever been closed by an actual human trial. Recommend REQUIREMENTS.md be updated to point at F-02.1-B / this verification rather than re-ticking outright, until a human runs Check 8 in a browser. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | none found | — | `grep -nE "TBD|FIXME|XXX|TODO|HACK|PLACEHOLDER"` and "not yet implemented/coming soon" scans return empty across `cats-vs-mechs.html` |

Info-level findings IN-01 through IN-04 from `02.1-REVIEW.md` remain open by design (explicitly
marked out of scope in `02.1-REVIEW-FIX.md`) and do not block the phase goal. IN-02 (picker list
rows at 16px, under the file's own 18px legibility floor) is the one worth carrying into the
projector rehearsal (folded into the Check 5 human-verification item above).

### Human Verification Required

See YAML frontmatter `human_verification` for the full list with test/expected/why-human. Highest
priority, in order:

1. **Criterion 3, end to end, by hand, post-F-02.1-B fix.** This is the literal reason ALLOC-11 sits
   unticked and it has never been attempted by a human even once — not before the fix (Check 8 used
   the console by design) and not after (no rehearsal has happened since `ce6f61e`).
2. The four Node-unreachable behaviours 02.1-06's own must-haves name explicitly: the `<dialog>`
   `cancel` interception (Check 3), the zero-tally line taking no layout space (Check 2), `maxlength`
   on a real input (Check 4), and the 11-row list at projector distance (Check 5).
3. CR-01 cases A/C, WR-02's focus drop, and WR-04's scroll jump — all MEDIUM-confidence per
   `02.1-REVIEW.md`, modelled faithfully in the Node stub but never run in a browser.
4. The Phase-2-carried regression items (Check 9.7 middle-click autoscroll, Check 9.8 held-Enter at
   OS repeat rate) plus their new tally-stepper counterpart (Check 9.9).

### Gaps Summary

No must-have truth is FAILED at the code level — every one of the ROADMAP's five success criteria
has a real, wired, non-stub implementation, and I independently regression-tested the three
review-driven fixes (CR-01, CR-02, F-02.1-B) by reverting each one in a scratch copy and confirming
the shipped test suite would have caught its absence. The full 430-row in-file suite (not just the
363-row terminal-visible slice) passes at 430/430 when run with a real DOM stub, `Event`,
`MouseEvent`, `KeyboardEvent` and `CSS.escape` supplied, exactly as the verification brief specified.

The reason this is `human_needed` rather than `passed` is not a code defect — it is that **this
phase's own closing plan (02.1-06) requires an itemised human rehearsal that never happened**, and
the one thing that *was* rehearsed ("token config works") was recorded before two rounds of
review-driven fixes landed, including the fix for the exact capability (criterion 3, "increments by
hand") this requirement is built around. REQUIREMENTS.md's ALLOC-11 note is stale relative to
F-02.1-B and should be revisited once a human runs the artifact.

---

_Verified: 2026-08-28T07:54:14Z_
_Verifier: Claude (gsd-verifier)_
