---
phase: 02.1-token-authoring-inserted
plan: 02
type: execute
wave: 2
depends_on: [02.1-01]
files_modified:
  - cats-vs-mechs.html
autonomous: true
requirements: [ALLOC-10, ALLOC-11]
must_haves:
  truths:
    - "A student-made token type can be created with a name, a shape, a colour, an emoji and a scope, and it lands in the vocabulary under a produced id."
    - "A student-made token type can be removed; the five built-ins refuse removal by name."
    - "Removing a type takes its tallies with it, and one Ctrl+Z brings the type and every number back together."
    - "Any token type, built-in or student-made, can be renamed, and a rename that changes nothing commits nothing."
    - "A tally can be set and nudged, defaults to zero, and stores nothing at all when it is zero."
  artifacts:
    - path: "cats-vs-mechs.html"
      provides: "[S05] createTokenType / removeTokenType / renameTokenType / setTally / nudgeTally"
      contains: "function createTokenType"
    - path: "cats-vs-mechs.html"
      provides: "[S05] tallyBag / readTally / writeTally / dropTally"
      contains: "function readTally"
    - path: "cats-vs-mechs.html"
      provides: "[S05] dispatch arms and export entries for all five new ops"
      contains: "case 'createTokenType'"
    - path: "cats-vs-mechs.html"
      provides: "[S09.7] rows for both corruption shapes on both new write paths, plus the state-shape rows"
      contains: "[S09.7]"
  key_links:
    - from: "[S05] createTokenType"
      to: "requireNewTokenId"
      via: "the guard, before the commit is entered"
      pattern: "requireNewTokenId"
    - from: "[S05] renameTokenType"
      to: "requireTokenId"
      via: "the guard, before the read-then-write of the record"
      pattern: "requireTokenId"
    - from: "[S05] removeTokenType"
      to: "commitStructural"
      via: "one mutator that deletes the type and every tally, so undo restores them together"
      pattern: "commitStructural\\('token remove"
---

<objective>
Build the five state transformations a student's authoring actions map onto. No page work: this
plan is `[S05] OPS` and `[S09.7]` only.

Purpose: ALLOC-11's create and remove, ALLOC-10's rename, and D-00e's tally — the number the tool
tracks and never infers a meaning for. Plan 02.1-01 built the gate; this plan is every write path
that has to run through it.

Output: `App.ops.createTokenType`, `removeTokenType`, `renameTokenType`, `setTally` and `nudgeTally`,
reachable through the one `dispatch` entry point, with the Pitfall 2 rename shape asserted per
reserved key.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-token-authoring-inserted/02.1-CONTEXT.md
@.planning/phases/02.1-token-authoring-inserted/02.1-RESEARCH.md
@.planning/phases/02.1-token-authoring-inserted/02.1-PATTERNS.md
@.planning/phases/02.1-token-authoring-inserted/02.1-01-SUMMARY.md
@CLAUDE.md
@cats-vs-mechs.html

<interfaces>
Consumed from plan 02.1-01 — do NOT re-derive these:

  App.data.TOKEN_ID_PATTERN, RESERVED_KEYS, MAX_CUSTOM_TYPES, TOKEN_SCOPES,
  MAX_TOKEN_NAME, TOKEN_NAME_REFUSE
  App.ops.requireTokenId, requireNewTokenId, requireTokenName, clampTokenName, nextTokenTypeId

Established by this plan and consumed by plans 02.1-03 / 02.1-04 / 02.1-05:

  App.ops.createTokenType({ name, shape, color, glyph, scope }) -> 't<N>'   (structural commit)
  App.ops.removeTokenType(tokenId)                              -> undefined (structural commit)
  App.ops.renameTokenType(tokenId, name)                        -> boolean   (plain commit; false = no-op)
  App.ops.setTally(side, unitId, tokenId, n)                    -> boolean   (plain commit)
  App.ops.nudgeTally(side, unitId, tokenId, delta)              -> boolean   (plain commit)

State shape this plan writes (NESTED — measured as the only shape that costs no shipped assertion):

  build[side].tally             = { t2: 5 }        // scope 'side'
  build[side].units[i].tally    = { t1: 2 }        // scope 'unit'
  build.tokens.t1               = { shape, color, glyph, name, scope }

  A tally of zero deletes its key. A bag with no keys left deletes itself.
  For a scope-'side' type, unitId is null.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: createTokenType and removeTokenType, with removal that takes its tallies with it</name>
  <files>cats-vs-mechs.html</files>
  <behavior>
    - `createTokenType({name:'Poison', shape:'circ', color:'violet', glyph:'', scope:'unit'})`
      returns `'t1'`, and `build.tokens.t1` holds exactly `shape`, `color`, `glyph`, `name`, `scope`.
    - Creating a second type returns `'t2'`. Removing `t1` and creating again returns `'t3'` — the
      sequence takes the largest suffix in use, not the count.
    - An unknown shape, colour, glyph or scope is refused with a message naming the offending value.
    - A name that fails `requireTokenName` is refused, and nothing is committed.
    - Creating a 7th student-made type throws a message naming `MAX_CUSTOM_TYPES`.
    - `removeTokenType('hp')` throws a message naming Health and saying the board is built on it.
      Same for `ap`, `shield`, `dmg`, `dead`.
    - `removeTokenType('__proto__')`, `('constructor')`, `('prototype')` each throw, and
      `Object.getPrototypeOf(build.tokens) === Object.prototype` afterwards.
    - `removeTokenType('t9')` for a pattern-valid id naming no live type throws by name.
    - Create a unit-scope type, tally it on two units and on a faction, remove it, then `undo()`:
      the type AND both tallies come back in ONE step.
    - Every refusal above leaves `App.state.undoDepth()` unchanged.
  </behavior>
  <read_first>
    - `cats-vs-mechs.html` lines 1318–1400 — `commitStructural` at `:1335` and its trigger-definition
      comment at `:1318–1334`, `nextUnitId` at `:1348`, `addUnit` at `:1361` (note `MAX_UNITS` is
      checked INSIDE the mutator, and why), `removeUnit` at `:1383` (note it calls `findUnit` first
      so a missing id throws with a real message).
    - `cats-vs-mechs.html` lines 1502–1580 — `setTokenStyle` as rewired by plan 02.1-01: the
      guard-before-commit ordering, the `String()`-in-the-message habit, the field-by-field mutator
      and its merge-helper warning.
    - `cats-vs-mechs.html` lines 1584–1640 — `dispatch` and its `DELIBERATELY ABSENT` block at
      `:1605–1616`, plus the frozen export literal.
    - `cats-vs-mechs.html` lines 1206–1226 — `sideFromBuild`, which copies `{id, hp, shield, alive}`
      explicitly.
    - `.planning/phases/02.1-token-authoring-inserted/02.1-RESEARCH.md` § "The create op, end to
      end", § "The remove op", § Pitfall 1, § Pitfall 10, § Pattern 2.
    - `.planning/phases/02.1-token-authoring-inserted/02.1-PATTERNS.md` § "`[S05]` — create and
      remove a collection member".
  </read_first>
  <action>
    In `[S05] OPS`, after `setTokenStyle`, add `createTokenType(spec)` and `removeTokenType(tokenId)`
    plus the private helper `dropTally(owner, tokenId)`.

    `createTokenType(spec)` — guards first, all OUTSIDE the commit: `requireTokenName` on
    `spec.name`; `App.data.SHAPES` / `COLORS` / `GLYPHS` / `TOKEN_SCOPES` membership on the other
    four, each throwing a message naming the offending value with `String()` around it. Then produce
    the id with `requireNewTokenId(nextTokenTypeId(App.state.get().build.tokens))` — the double call
    is deliberate: the sequence produces it and the gate confirms it, so a future change to either
    one cannot quietly widen the other.

    Commit through `commitStructural` with a label of the form `'token new ' + made`, because
    `buildColumn` decides whether this type's stat line exists at all and `sync()` can neither create
    a line nor remove one — that is precisely `commitStructural`'s own stated trigger. Inside the
    mutator, count the ids that are not in `App.data.TOKEN_IDS` and throw naming
    `App.data.MAX_CUSTOM_TYPES` when the cap is reached. Put that ONE check inside the mutator and
    say why in a comment: it needs the vocabulary from the detached copy, which is exactly `addUnit`'s
    documented `MAX_UNITS` exception, and it is safe because a mutator that throws leaves no trace.
    Every other guard stays outside. Assign the record field by field — never a merge helper, for the
    reason `setTokenStyle`'s mutator comment already gives. Return the produced id, so `[S07.2]` can
    select it in the list once `dispatch` returns.

    `removeTokenType(tokenId)` — refuse a member of `App.data.TOKEN_IDS` FIRST, with a message that
    reads the live record's `name` so a student who renamed Health to Vigor is told about Vigor, and
    that offers rename as the supported alternative (D-15). Then `requireTokenId(tokenId)`. Then a
    single `commitStructural` mutator that: throws by name when the vocabulary has no own property
    for the id; deletes the vocabulary entry; and walks both sides calling `dropTally` on the faction
    and on every unit. All of that inside ONE mutator, so Ctrl+Z restores the type and every tally
    together (D-16). No confirmation dialog anywhere — D-17.

    `dropTally(owner, tokenId)` — return early when the owner has no own `tally`; delete the key;
    delete the whole bag when no keys are left. `Object.prototype.hasOwnProperty.call` throughout.

    Add `case 'createTokenType'` and `case 'removeTokenType'` arms to `dispatch`, reading only the
    payload keys each op needs, and add both functions to the `[S05]` frozen export literal.

    Add a short comment beside `sideFromBuild` recording that tallies deliberately do NOT enter the
    fight slice: a tally is a build-time annotation this phase, and whether one should be spendable
    during a fight is Phase 5's decision. Write it down rather than leaving the next reader to wonder
    whether it was missed.

    Then add the matching `[S09.7]` rows for every line of the `<behavior>` block above, in the frame
    plan 02.1-01 established (`savedAll` at the top, `App.state.restore(savedAll)` as the last act).
    The three reserved-key rows on the remove path each get their own row plus a
    `Object.getPrototypeOf(...) === Object.prototype` check — this is the CREATE-shape family and one
    row does not cover the class.
  </action>
  <verify>
    <automated>node tests/selftest-node.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/selftest-node.cjs` exits 0, **strictly more rows passing than plan 02.1-01 left**,
      0 failed, interaction gate **27 of 27**, stub-drift gate **28 shell ids**.
    - `grep -v '^ *//' cats-vs-mechs.html | grep -c "function createTokenType\|function removeTokenType\|function dropTally"` returns **3**.
    - `grep -c "case 'createTokenType'\|case 'removeTokenType'" cats-vs-mechs.html` returns **2**.
    - `grep -c "case 'openTokenPicker'" cats-vs-mechs.html` returns **0** — the must-not-regress grep
      that keeps page work out of `[S05]`.
    - `grep -c "commitStructural('token new\|commitStructural('token remove" cats-vs-mechs.html`
      returns **2**; `grep -c "App.state.commit('token new\|App.state.commit('token remove"` returns
      **0** — create and remove change what `structure()` would build.
    - The undo-together row passes: create, tally on two units and a faction, remove, one `undo()`,
      and the type and all three numbers are back.
    - **Naming trap, both greps re-run after every comment block:**
      `grep -ci "counter\|rating\|balanced\|difficulty" cats-vs-mechs.html` = **0** and
      `grep -c "verdict\|balanced\|rating\|difficulty" cats-vs-mechs.html` = **0**.
  </acceptance_criteria>
  <done>A student-made type can be created and removed through `dispatch`; the built-ins refuse removal by name; one Ctrl+Z restores a removed type and all of its tallies together.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: renameTokenType and the sparse tally ops, with the rename corruption shape asserted</name>
  <files>cats-vs-mechs.html</files>
  <behavior>
    - `renameTokenType('hp', 'Vigor')` returns `true` and `build.tokens.hp.name` is `'Vigor'`.
      `build.tokens.hp.shape` is unchanged and the id `'hp'` is untouched (D-09, D-10).
    - `renameTokenType('hp', 'Vigor')` again returns `false` and adds no undo entry.
    - `renameTokenType('  Vigor  ', ...)` — the NAME is trimmed by `requireTokenName`, so
      `renameTokenType('hp', '  Vigor  ')` returns `false` when the live name is already `'Vigor'`.
    - `renameTokenType('__proto__', 'x')`, `('constructor', 'x')`, `('prototype', 'x')` each throw,
      and afterwards `({}).name === undefined` and
      `Object.prototype.hasOwnProperty.call(Object.prototype, 'name') === false`.
    - `renameTokenType('hp', 'Poison ☠')` succeeds — names may carry BMP symbols and emoji (D-12).
    - `setTally('cats', 'c1', 't1', 3)` then `readTally` reports 3; setting it back to 0 leaves the
      unit with NO own `tally` key at all.
    - `setTally('cats', null, 't2', 5)` writes on the faction when `t2` has `scope: 'side'`, and is
      refused when `t2` has `scope: 'unit'`.
    - `setTally` with `n` above `MAX_ALLOC`, or non-integer, is refused by `int`.
    - `nudgeTally` with a non-numeric delta is refused by `requireDelta`; nudging below zero clamps
      at zero and leaves no key.
    - `setTally('cats', 'c1', '__proto__', 3)` and `nudgeTally('cats', 'c1', 'constructor', 1)`
      each throw, and `Object.prototype` is clean afterwards.
    - `addUnit('cats')` on a side whose last unit carries a tally produces a new unit with NO `tally`
      key.
  </behavior>
  <read_first>
    - `cats-vs-mechs.html` lines 1129–1200 — `int` at `:1131` (the value boundary: type test before
      arithmetic), `MAX_ALLOC` at `:1163`, `requireSide` at `:1167`, `findUnit` at `:1174`,
      `requireDelta` at `:1186`.
    - `cats-vs-mechs.html` lines 1275–1320 — `nudgeFactionAp`, `nudgeUnitMaxHp`, `nudgeUnitShield`:
      the shipped nudge shape, including the boolean return the press-and-hold ramp reads.
    - `cats-vs-mechs.html` lines 1521–1580 — `setTokenStyle`'s "no-op returns false" block at
      `:1539–1556` and the reason its comment gives (do-nothing Ctrl+Z steps between the student and
      the edit they want back).
    - `cats-vs-mechs.html` lines 1361–1381 — `addUnit`'s field-by-field seed from `last`.
    - `.planning/phases/02.1-token-authoring-inserted/02.1-RESEARCH.md` § Pattern 2, § Pitfall 2,
      § Pitfall 13.
    - The `[S09.7]` suite as plan 02.1-01 and Task 1 above left it.
  </read_first>
  <action>
    Add `renameTokenType(tokenId, name)`, `setTally(side, unitId, tokenId, n)` and
    `nudgeTally(side, unitId, tokenId, delta)` to `[S05] OPS`, plus the private helpers `tallyBag`,
    `readTally` and `writeTally`.

    `renameTokenType` — `requireTokenId(tokenId)` FIRST, then `requireTokenName(name)`, both outside
    the commit. This ordering is the whole Pitfall 2 defence: the shipped shape
    `var t = tokens[id]; t.name = x` resolves `t` to `Object.prototype` for a reserved id and writes
    persistent, global pollution that `JSON.stringify` and `deepFreeze` both walk straight past.
    Gating create but not rename ships the worse of the two shapes. After the guards, read the live
    record with `hasOwnProperty`, throw by name when there is none, return `false` when the trimmed
    name already matches, and otherwise assign `t.name` field by field inside a plain
    `App.state.commit` — a rename is NOT structural, because plan 02.1-03 makes labels a per-frame
    sync pass and a structural frame here would rebuild both columns on every keystroke.

    `tallyBag(owner, create)` / `readTally(owner, tokenId)` / `writeTally(owner, tokenId, n)` — the
    sparse-storage rules, exactly two of them and both worth stating in the comment: a tally of zero
    deletes its key, and a bag with no keys left deletes itself. `hasOwnProperty` on every read; a
    bare `owner.tally[id]` read for `'constructor'` resolves on the prototype instead of failing.

    `setTally(side, unitId, tokenId, n)` — guards outside the commit and in this order:
    `requireSide`, `requireTokenId`, then resolve the type from the live vocabulary with
    `hasOwnProperty` and throw by name when it is absent, then refuse a built-in id (a built-in has
    no tally — its quantity is `maxHp` / `ap` / `shield`), then check the scope agrees with the
    arguments: `scope: 'side'` requires `unitId` to be null-ish, `scope: 'unit'` requires a real
    unit. Clamp the value with `int(n, 0, MAX_ALLOC, ...)` — reuse `MAX_ALLOC`, do not invent a
    second bound. Return `false` when the value is already what it would be set to, matching the
    no-op contract. Commit with plain `App.state.commit`; the stat line already exists, so a tally
    change is sync-only.

    `nudgeTally(side, unitId, tokenId, delta)` — `requireDelta` outside the commit, then the same
    resolution as `setTally`, then `int(current + delta, 0, MAX_ALLOC, ...)`. Return the boolean the
    press-and-hold ramp's sixth stop condition reads.

    Do NOT add a `tally` line to `addUnit`'s seed (Pitfall 13). It is a rule about not writing a
    line, which is the easiest kind to lose in a later edit — so it gets an assertion rather than a
    comment alone.

    Add `case 'renameTokenType'`, `case 'setTally'` and `case 'nudgeTally'` arms to `dispatch`, and
    all three ops to the `[S05]` frozen export literal.

    Then add the matching `[S09.7]` rows for every line of the `<behavior>` block. The three
    reserved-key rows on the rename path are the Pitfall 2 family and each asserts BOTH that the call
    threw AND that `Object.prototype` is clean, because `'constructor'` fails with a different
    `TypeError` and a different message than `'__proto__'` does. Add the state-shape rows too:
    `Object.keys(App.state.get().build)` is still exactly `['schema','cats','mechs','tokens']` with
    six student-made types and sparse tallies present; the whole build slice still passes an
    integers-and-strings walk; `MAX_CUSTOM_TYPES` and `MAX_TOKEN_NAME` are asserted against the named
    constants. Close the suite by handing the board back with `App.state.restore(savedAll)` and
    asserting the round trip.
  </action>
  <verify>
    <automated>node tests/selftest-node.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/selftest-node.cjs` exits 0, more rows passing than after Task 1, **0 failed**,
      interaction gate **27 of 27**, stub-drift gate **28 shell ids**.
    - `grep -v '^ *//' cats-vs-mechs.html | grep -c "function renameTokenType\|function setTally\|function nudgeTally\|function tallyBag\|function readTally\|function writeTally"` returns **6**.
    - `grep -c "case 'renameTokenType'\|case 'setTally'\|case 'nudgeTally'" cats-vs-mechs.html`
      returns **3**.
    - `grep -c "commitStructural('token rename\|commitStructural('tally" cats-vs-mechs.html` returns
      **0** — rename and tally are plain commits, or a stepper press rebuilds both columns.
    - The rename-pollution rows have been observed to FAIL once: temporarily remove
      `requireTokenId` from `renameTokenType`, re-run, confirm the three rename rows go red AND that
      the create-path rows stay green (proving the two are genuinely different code paths), then
      restore. Record both counts in the SUMMARY.
    - `Object.keys(App.state.get().build)` row passes with six student-made types present — the
      nested tally shape adds no top-level `build` key.
    - **Naming trap, both greps re-run:**
      `grep -ci "counter\|rating\|balanced\|difficulty" cats-vs-mechs.html` = **0** and
      `grep -c "verdict\|balanced\|rating\|difficulty" cats-vs-mechs.html` = **0**.
  </acceptance_criteria>
  <done>All five authoring ops exist behind `dispatch`; the rename path is gated and proven separately from the create path; tallies are sparse, clamped to `MAX_ALLOC` and never inherited by a new unit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `[S07]` page -> `[S05] dispatch` | `data-tok`, `data-side`, `data-unit` read off the page are caller-supplied keys arriving at five brand-new write paths. |
| `[S05]` ops -> `build.tokens` and `build[side][.units[i]].tally` | Two object families now indexed by a caller-supplied key, not one. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-07 | Tampering / EoP | `createTokenType` — `s.build.tokens[made] = rec` | mitigate | `requireNewTokenId(nextTokenTypeId(...))` before the commit. The id is produced from a sequence and never derived from student text (D-13), so no reserved name can reach the key position at all. Asserted per reserved key. |
| T-02.1-08 | Tampering / EoP | `renameTokenType` — `var t = tokens[id]; t.name = x` | mitigate | `requireTokenId` before the read. This is the Pitfall 2 / CR-02 shape: persistent, global, and invisible to `JSON.stringify` and `deepFreeze`. Asserted per reserved key, with a deliberate red-then-green run recorded. |
| T-02.1-09 | Tampering / EoP | `removeTokenType` — `delete tokens[id]` and `delete owner.tally[id]` | mitigate | Built-in refusal first, then `requireTokenId`, then a `hasOwnProperty` existence check inside the mutator. `dropTally` uses `hasOwnProperty` on every owner. |
| T-02.1-10 | Tampering / EoP | `setTally` / `nudgeTally` — `owner.tally[tokenId] = n` | mitigate | `requireTokenId` plus a live-vocabulary `hasOwnProperty` resolution before anything indexes a bag. Asserted for `__proto__` and `constructor` on both ops. |
| T-02.1-11 | Denial of service | Unbounded vocabulary or unbounded tally | mitigate | `MAX_CUSTOM_TYPES = 6` inside the create mutator (the documented `addUnit` exception), `MAX_ALLOC = 99` through the shipped `int` boundary, and the `t99` sequence ceiling. All three asserted against the named constants. |
| T-02.1-12 | Tampering | A non-JSON value reaching `build.tokens` and vanishing on the next `thaw()` | mitigate | Field-by-field assignment of primitives only, never a merge helper. Measured: a `Map` committed into state becomes `{}` on the next thaw with no error, and `Object.create(null)` loses its null prototype — the container is not the defence, the gate is. |
| T-02.1-13 | Repudiation / integrity | A refusal that leaves a phantom undo step | mitigate | Every guard outside the commit, with the single documented exception of the cap check that needs the detached copy. Asserted by an `undoDepth()` row after each refusal family. |
| T-02.1-SC | Tampering | npm / pip / cargo installs | mitigate | Not applicable: CLAUDE.md forbids any dependency or build step, and this plan installs nothing. No `[ASSUMED]` / `[SUS]` package exists, so no legitimacy checkpoint is owed. |

</threat_model>

<verification>
1. `node tests/selftest-node.cjs` exits 0, 0 failed, interaction gate 27 of 27, stub-drift gate
   28 shell ids, and the passed count is strictly higher than plan 02.1-01 left it.
2. `grep -ci "counter\|rating\|balanced\|difficulty" cats-vs-mechs.html` returns 0.
3. `grep -c "verdict\|balanced\|rating\|difficulty" cats-vs-mechs.html` returns 0.
4. `grep -c "case 'openTokenPicker'" cats-vs-mechs.html` returns 0.
5. `grep -c 'data-act="openTokenPicker"' cats-vs-mechs.html` still returns 5 — the topbar is
   untouched until plan 02.1-05.
6. Both deliberate red-then-green runs (create gate removed, rename gate removed) have been
   performed and their counts recorded.
</verification>

<success_criteria>
- Five new ops exist, all reachable only through `App.ops.dispatch`, none of them page work.
- Create and remove commit structurally; rename and tally commit plainly.
- Both corruption shapes are gated on every path that indexes the vocabulary or a tally bag, with an
  assertion per reserved key per path.
- The nested tally shape adds no top-level `build` key and costs no shipped assertion.
- Removing a type and its tallies is one undo entry.
</success_criteria>

<output>
Create `.planning/phases/02.1-token-authoring-inserted/02.1-02-SUMMARY.md` when done. Record: the new
passed/failed totals, both red-then-green results, the exact op signatures as shipped, and the exact
text of the built-in-removal and cap refusal messages so plan 02.1-05 surfaces the same copy.
</output>
