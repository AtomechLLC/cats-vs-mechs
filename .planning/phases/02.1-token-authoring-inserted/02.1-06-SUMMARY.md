---
phase: 02.1-token-authoring-inserted
plan: 06
subsystem: verification
tags: [human-verify, rehearsal, projector, dialog, cascade, maxlength, checkpoint]

# Dependency graph
requires:
  - phase: 02.1-token-authoring-inserted
    plan: 05
    provides: "the collapsed topbar, the [S07.2] authoring handlers, the dialog cancel listener, and the four unreachable behaviours named at the foot of tests/selftest-node.cjs"
  - phase: 02-allocation-surface
    provides: "02-HUMAN-UAT.md — the format this rehearsal reuses, its two never-covered items, and gap G-02-B"
provides:
  - ".planning/phases/02.1-token-authoring-inserted/02.1-HUMAN-UAT.md — nine numbered checks, status `partial`: check 1 passed by confirmation, checks 2-9 recorded as not itemised with the specific sub-items named"
  - "The rehearsal verdict on the topbar collapse (D-06): passed by confirmation on the user's words 'token config works', with 1a and 1b recorded as unanswered"
  - "G-02.1-A — F-02.1-A carried out of the phase unanswered; ROADMAP Phase 2.1 criterion 3 not reachable through the UI while it stands"
  - "G-02.1-B — Phase 2's projector gap G-02-B inherited rather than closed, with an 11-row modal added to what has never been measured"
  - "G-02.1-C — four browser-only behaviours with no coverage anywhere, incl. Phase 2's 9.7 and 9.8 unresolved for a second phase"
  - "Check 1: the topbar collapse as its own check, with three explicit questions and silence-is-not-approval stated (D-06)"
  - "Checks 2-5: the four behaviours no Node stub can reach, each with steps and a predicted outcome"
  - "Checks 9.7 and 9.8: Phase 2's two never-covered items, carried forward rather than re-folded into an aggregate"
  - "Finding F-02.1-A: a tally that has never been written cannot be raised from the page — measured, not inferred"
affects: [02.1-phase-close, 04-serialization]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "A rehearsal script states the predicted outcome for every check, so the human confirms a prediction rather than forming an opinion from nothing"
    - "A gap inherited from a prior phase's rehearsal is closed with fields for the measurement, not with a second impression"
    - "When the pre-rehearsal gate run surfaces a design question, it is recorded as a finding with options and left for the user — not repaired inside a task whose acceptance forbids touching the artifact"

key-files:
  created:
    - ".planning/phases/02.1-token-authoring-inserted/02.1-HUMAN-UAT.md — gate numbers, an environment block, finding F-02.1-A, nine numbered checks, a console block, a rework list and a gaps list; filled in at Task 2 with one itemised outcome and eight NOT RUN"
  modified:
    - ".planning/REQUIREMENTS.md — ALLOC-10 ticked complete; ALLOC-11 left unticked as partial with the blocked half named"
    - ".planning/phases/02.1-token-authoring-inserted/02.1-06-PLAN.md — <acceptance_record> appended with per-check grades"

key-decisions:
  - "Check 1 stands alone with three separately-answered sub-questions (is the bar better / is 'Tokens' the right label / is the picker obviously the way in) and a stated blast radius of eight harness checks. D-06 traded an early gate for an explicit answer here, and a single yes/no with no structure would have been answerable by a nod."
  - "The zero-to-one path was probed rather than assumed. The shipped gate raises the first tally by calling the op directly, which is legitimate for testing the render but hides that no student has that path. Instrumented against the harness's own stub page: 24 nudge buttons and 12 tally fields exist for a fresh unit-scope type and 0 of the 36 are reachable — every one sits inside a line the hide pass has hidden, and [C03] gives that line display:none."
  - "F-02.1-A was NOT repaired. It is the documented rule from the phase's own <specifics> working exactly as written, so changing it is a design decision (Rule 4), and Task 1's acceptance forbids modifying the artifact at all. Three shapes for a repair are offered in the document for the user to choose between."
  - "Check 4's predicted outcome was corrected against the artifact rather than copied from the plan. The plan predicts a 30-emoji paste lands at 24 emoji; maxlength counts UTF-16 units, so a real paste of astral emoji admits about 12. The document predicts 12, names the reason, and puts the 24-code-point cut in an optional console step — which is the path the gate actually drives."
  - "Check 7 uses the empty-name refusal rather than removing a built-in. Remove this type is DISABLED for the five built-ins, so its refusal message is unreachable from the UI by design — the check confirms the disabled control instead and reads the message a student can actually reach."

  - "Task 2: the rehearsal returned an aggregate, and it is recorded as an aggregate. 'token config works' is graded a PASS for check 1 — the user was asked pointedly, warned that silence would not count, and confirmed the surface works after operating it — and NOT RUN for the seven checks they did not report. No per-check outcome was inferred from a green gate or from the absence of a complaint."
  - "Task 2: ALLOC-11 was left unticked. Create, remove and appearance all work and are covered by gate checks 31, 33 and 35, but the requirement's own words are 'increments by hand', and F-02.1-A means a student cannot. Ticking it would have claimed a capability that is measurably absent."
  - "Task 2: F-02.1-A was still not repaired. The user gave no direction on (a), (b) or (c), and the repair edits the hide pass, [C03], gate check 32 and possibly the picker — a design decision (Rule 4), not an executor's guess to make twice over."

patterns-established:
  - "Pattern: probe the path the student has, not the path the test has. A green assertion that reaches its fixture through an op call says nothing about whether a control exists to do the same thing."
  - "Pattern: a UAT must let a future reader tell an itemised result from an aggregate one. Phase 2 set the standard — G-02-A carries a real hand-count of 20, G-02-B says in terms that it was approved by observation with no display or distance recorded. This phase matches it: 'passed: 1, pending: 8' with a paragraph explaining that pending means unknown, not probably-fine."

requirements-completed: [ALLOC-10]
requirements-in-progress: [ALLOC-11]

# Metrics
duration: 55min
completed: 2026-08-27
---

# Phase 2.1 Plan 06: The Closing Rehearsal Summary

**The gate is green on the reviewed build — 354 passed / 0 failed, interaction gate 39 of 39, stub drift 35 shell ids, all four acceptance greps at their required values — and the rehearsal was held. It returned one itemised answer out of nine: the topbar collapse is approved on the user's own words, "token config works". The other eight checks were not run, and are recorded as not run rather than absorbed into that one confirmation. Two findings leave the phase open: a tally cannot be raised from zero anywhere on the page (0 of 36 controls reachable, so ROADMAP criterion 3 is unreachable by hand), and Phase 2's projector gap is inherited rather than closed. ALLOC-10 is delivered; ALLOC-11 is deliberately left unticked.**

## Performance

- **Duration:** ~55 min across both tasks
- **Tasks:** 2 of 2. Task 2 was the blocking human-verify checkpoint; it was held, answered in aggregate, and closed as **partial**.
- **Files created:** 1 (`.planning/phases/02.1-token-authoring-inserted/02.1-HUMAN-UAT.md`)
- **Files modified:** 3 planning documents (the UAT, `REQUIREMENTS.md`, `02.1-06-PLAN.md`). **`cats-vs-mechs.html` is untouched by both tasks** — `git diff --stat cats-vs-mechs.html` is empty, as Task 1's acceptance requires and as Task 2's instruction to leave F-02.1-A alone requires.

## Task 2 — what the rehearsal actually returned

The user was handed the nine checks with an explicit statement that silence and a general "looks
good" would not be read as an answer to check 1. The reply, in full:

> token config works

**Check 1: PASSED, by confirmation.** They reached the picker and configured tokens through it —
which is check 1c (is the picker obviously the way in) answered by behaviour rather than by opinion —
and they said it works after being asked pointedly and warned that saying nothing would not count.
There is no objection to the collapsed topbar anywhere in the reply and no rework was requested, so
the eight harness checks a "no" would have reworked (10, 13, 17, 18, 19, 23, 24, 27) stand and the
topbar markup is not touched. D-06's trade — no early gate, an explicit verdict here — is honoured.

**What it is not.** 1a ("is the bar better than the row of four") and 1b ("is *Tokens* the right
label") were not answered separately. The collapse is approved as a working surface rather than
preferred on the merits, and "Tokens" ships as the default label rather than a chosen one. Neither is
a rework; if the label is revisited it is a one-word edit.

**Checks 2 through 9: NOT RUN.** No outcome was reported for any of them. Named specifically, because
each is a thing that is now confirmed by nothing at all:

| Not exercised | Consequence |
|---|---|
| the projector reading — display, viewing distance, `--tok` | **Phase 2's G-02-B is inherited, not closed**, now with an 11-row modal added to it. T-02.1-36 unmitigated. |
| the `<dialog>` `cancel` interception (Escape in the name field leaving the dialog open) | untested by anything — the stub `<dialog>` has no close-request behaviour. T-02.1-34 unmitigated. |
| the `[hidden]` hide cascade taking no layout space | gate check 32 asserts the property in a stub with no layout engine. T-02.1-33 unmitigated. |
| `maxlength` on a real input, typed and pasted | the code-unit / code-point disagreement only exists on a real input, and there is none in the gate. T-02.1-35 unmitigated. |
| middle-click autoscroll over an armed ramp (9.7) | **unresolved for the second phase running**; the harness has no middle-click action. |
| a physically held Enter at the OS repeat rate (9.8) | likewise. The `repeat:true` → `preventDefault()` mechanism is measured; a real held key never has been. |
| the console — errors, warnings, network | not reported. The only thing the file proves on its own is 0 URLs, one `<script>`, one `<style>`, no `fetch` / `XHR` / `import`. |

The UAT records this as `passed: 1, pending: 8`, with a paragraph saying in terms that pending means
unknown rather than probably-fine, and that `issues: 0` means zero issues *reported*, not zero issues
*present*.

## What ships, and what does not

**ALLOC-10 — complete.** Every token record carries a `name`; a rename repaints the picker list row,
the picker heading and the board label from one commit (gate check 30). Confirmed by a human in
aggregate.

**ALLOC-11 — left unticked, deliberately.** Create, remove (one undoable step) and the appearance
surface all work and are covered by gate checks 31, 33 and 35. The requirement's own words are "a
free annotation the student **increments by hand**", and F-02.1-A means a student cannot. Ticking it
would claim a capability that has been measured absent.

**F-02.1-A / G-02.1-A — still open, still unanswered.** Three options were offered; none was chosen;
nothing was built. While it stands, **ROADMAP § Phase 2.1 success criterion 3 is not reachable
through the UI.**

## The gate, on the build the human is about to review

| Number | Required | Measured |
|---|---|---|
| `[S09]` terminal suite | above 145, 0 failed | **354 passed, 0 failed** |
| Interaction gate | at least 36 of 36 | **39 of 39** |
| Stub-drift gate | 35 shell ids | **35** |
| `grep -ci "counter\|rating\|balanced\|difficulty"` | 0 | **0** |
| `grep -c "verdict\|balanced\|rating\|difficulty"` | 0 | **0** |
| `grep -c 'data-act="openTokenPicker"'` | 2 | **2** |
| `grep -cE "https?://"` | 0 | **0** |

`node tests/selftest-node.cjs` exits **0**. Run twice — once before writing the script and once
before committing it — with identical numbers.

## What the script asks

Nine checks, each with steps and a predicted outcome so the human confirms a prediction:

| # | Check | Why it cannot be automated |
|---|---|---|
| **1** | **the topbar collapse, alone** | it is a judgement, and D-06 deferred it to here |
| 2 | a zero tally collapses its line | no layout engine; gate check 32 asserts the property, not the space |
| 3 | Escape inside the name field | the stub `<dialog>` has no close-request behaviour at all |
| 4 | `maxlength` on a real input | there is no real input, and the unit/code-point disagreement only exists on one |
| 5 | an 11-row list on a projector | an empirical question only a room answers |
| 6 | a rename reaches everywhere, undo reaches back | covered by gate check 30, re-walked by hand |
| 7 | a refusal reads like a sentence | legibility of prose is not assertable |
| 8 | the end-to-end story | the phase in one pass |
| 9 | nothing regressed, incl. 9.7 and 9.8 | Phase 2's two items no harness has ever reached |

**Check 1 carries three separately-answered questions** — is the bar better than the four buttons,
is "Tokens" the right label, is the picker obviously the way in — plus the blast radius of a "no"
(the topbar, the dialog and harness checks 10, 13, 17, 18, 19, 23, 24 and 27) and an explicit
statement that silence and a general "looks good" are not answers to it.

**Check 5 has fields for the display and the viewing distance**, which is the whole of Phase 2's
gap G-02-B. Its rehearsal closed the projector item by aggregate observation with neither written
down; this one asks for both, plus the `--tok` value at the time of the reading.

**Checks 9.7 and 9.8 are Phase 2's two never-covered items** — Chrome's middle-click autoscroll over
an armed ramp, and a physically held Enter on Undo at the OS repeat rate. Both were folded into an
aggregate observation last time; both are line items with their own outcome cells now. 9.9 asks the
same of the new tally steppers, which inherit the ramp rather than re-implementing it.

## Finding F-02.1-A — raised before the rehearsal, not repaired

A student-made type's stat line is built on every card the moment the type exists; `sync()` sets
`line.hidden = (amount === 0)` and `[C03]` gives that line `display:none`. The stepper — minus, field
and plus — is built **inside** that line. So at a tally of zero, every control that could raise it is
inside the thing being hidden.

Measured against the shipped harness's own stub page, immediately after "New type on each unit":

| Controls naming the new type | Present | Reachable (no hidden ancestor) |
|---|---|---|
| `nudgeTally` buttons | 24 | **0** |
| tally fields | 12 | **0** |

The same hide pass covers a side-scope type's two faction lines, so both scopes are affected.

Every automated check is honest — gate check 32 raises the first tally by calling
`App.ops.nudgeTally` directly, which is a fair way to test the render and is not a path a student
has. The consequence is that **ROADMAP § Phase 2.1 criterion 3 — "it carries a per-unit or
per-faction tally the student increments by hand" — is not reachable through the shipped UI**, and
check 8's "tally it on two cats by hand" cannot be completed without the console.

**Why nothing was changed.** This is the phase's own `<specifics>` rule working exactly as written
("a custom type with a zero tally everywhere should render as nothing on the board"), so a repair is
a design decision rather than a defect fix — deviation Rule 4, the user's call. Task 1's acceptance
also states in terms that the artifact must not be modified. Three shapes are offered in the UAT
document: reveal the line while its type is selected in the picker; a quiet zero line on the card;
or a tally control in the picker itself. The first keeps the board clean at rest and costs the hide
pass one more input.

## Deviations from Plan

**1. [Rule 4 — design decision, surfaced not taken] F-02.1-A**
- **Found during:** Task 1, while assembling check 8's steps
- **Issue:** a tally cannot be raised from zero anywhere on the page
- **Action:** measured with a scratch copy of the harness, recorded in the UAT document as a finding
  with three options, and added to the checkpoint. No code changed.
- **Files modified:** none

**2. [Rule 1 — corrected a predicted outcome] check 4's emoji paste**
- **Found during:** Task 1
- **Issue:** the plan predicts a 30-emoji paste lands at 24 emoji. `maxlength="24"` counts UTF-16
  units and each of these emoji is two, so a real paste admits about 12. Handing the human a wrong
  prediction would have produced a false failure report.
- **Fix:** the document predicts ~12, names the reason, states that the one unacceptable outcome is a
  broken half-character, and moves the 24-code-point cut into an optional console step — which is the
  path gate check 38 actually drives.
- **Files modified:** the UAT document only

**3. [Rule 1 — corrected a predicted outcome] check 7's refusal**
- **Found during:** Task 1
- **Issue:** the plan suggests forcing a refusal by removing a built-in. `Remove this type` is
  **disabled** for the five built-ins, so that message is unreachable from the UI.
- **Fix:** check 7 reads the empty-name refusal (which a student can reach) and confirms the disabled
  control separately — a bound shown rather than raised.
- **Files modified:** the UAT document only

**4. [Task 2 — the checkpoint returned less than it asked for]**
- **Found during:** Task 2
- **Issue:** The plan's acceptance for Task 2 requires an outcome on all nine checks, an explicit
  yes/no on check 1, and a concrete display and viewing distance on check 5. The reply answered
  check 1 and nothing else.
- **Action:** Recorded exactly that. Check 1 is graded PASS by confirmation; the other eight are
  graded NOT RUN with their unexercised sub-items named one by one; the UAT's frontmatter status is
  `partial`, not `complete`. **No outcome was invented to satisfy the acceptance criteria**, and no
  check was marked passed on the strength of a green gate or of the absence of a complaint.
- **Files modified:** the UAT document, `REQUIREMENTS.md`, `02.1-06-PLAN.md`

## Threat Flags

No new surface. No file in this plan is executable and the artifact was not modified.

**But four mitigations in the plan's own register went untaken**, because each was carried as a
numbered rehearsal check and the check was not run. Recorded here so a later phase inherits the fact
rather than the assumption:

| Threat | Mitigation as planned | Status |
|---|---|---|
| T-02.1-33 — the `[hidden]` cascade failing open as 144 empty labelled boxes | rehearsal check 2 | **untaken** |
| T-02.1-34 — Escape closing the picker out from under a half-typed name | rehearsal check 3 | **untaken** |
| T-02.1-35 — code-unit `maxlength` vs code-point cap disagreeing on a real input | rehearsal check 4 | **untaken** |
| T-02.1-36 — UX-02, an 11-row modal illegible at the back of a room | rehearsal check 5 | **untaken** |
| T-02.1-37 — the topbar collapse approved implicitly inside a general look-over | check 1 standing alone, answered explicitly | **mitigated** — asked pointedly, answered directly |
| T-02.1-SC — package installs | not applicable; no dependency, no build step | n/a |

All four untaken ones fail open — as clutter, a lost keystroke, or a truncated name — never as a
crash, which is exactly why the gate cannot see them.

## Known Stubs

None. Every empty field in the UAT now carries an explicit "not recorded" or "NOT RUN" with the
reason, rather than a blank a later reader could mistake for an omission in the writing.

## Checkpoint status

**Task 2's checkpoint was held and answered — in aggregate.** It was never self-approved: the reply
above is the user's own. It is closed as **partial** rather than complete, because seven of nine
checks were not exercised and the finding put alongside them was not answered.

**What a follow-up needs, if the phase is ever closed properly:** a person at a real browser opened
from `file://`, plus a room and a display for check 5. There is no automated instrument in this repo
for checks 2, 3, 4 or 5, and none can be built for 5.

## Self-Check: PASSED

- `.planning/phases/02.1-token-authoring-inserted/02.1-HUMAN-UAT.md` — FOUND, status `partial`
- `.planning/phases/02.1-token-authoring-inserted/02.1-06-PLAN.md` — FOUND, `<acceptance_record>`
  appended
- `.planning/REQUIREMENTS.md` — FOUND, ALLOC-10 ticked, ALLOC-11 left unticked with the reason
- commit `b1291c2` — FOUND
- `cats-vs-mechs.html` — unmodified, confirmed by an empty `git diff --stat`
- Gate re-run at the close: exit 0, **354 passed / 0 failed**, interaction gate **39 of 39**,
  stub-drift **35 shell ids**, both naming-trap greps **0**, `openTokenPicker` **2**, URLs **0**
- `STATE.md` and `ROADMAP.md` — untouched, as the orchestrator owns them
