---
status: partial
phase: 02.1-token-authoring-inserted
source: [02.1-06-PLAN.md, 02.1-05-SUMMARY.md, 02-HUMAN-UAT.md]
started: 2026-08-27T00:00:00Z
updated: 2026-08-27T00:00:00Z
outcome: 1 of 9 checks itemised; 8 not run; 1 finding left open
---

## Current Test

[rehearsal held and closed as **partial**. Check 1 is answered. Checks 2–9 were not itemised and
their outcome cells below say so rather than inheriting Check 1's answer.]

## What the rehearsal actually returned — read this before any outcome below

The user was handed the artifact with the nine checks and an explicit instruction that silence and a
general "looks good" would not be read as an answer to Check 1. The entire reply was, verbatim:

> token config works

**What that is evidence for.** The user reached the token picker and configured tokens successfully.
That is a real result and it is the substance of Check 1: they found the picker without being told
where it was, which is Check 1c answered by behaviour, and they report the surface works after being
asked pointedly and warned that saying nothing would not count. **Check 1 is passed by
confirmation.**

**What that is not evidence for.** It is an aggregate confirmation, not an itemised rehearsal. The
user recorded no outcome for checks 2 through 9, gave no display and no viewing distance for Check 5,
and did not separately answer Check 1a ("is the bar better than the row of four") or Check 1b ("is
*Tokens* the right label"). Nothing below claims otherwise. In particular, **none** of the following
was reported as exercised, and none of it may be read as passed:

- the projector reading (Check 5) — no display, no distance, no `--tok` re-confirmation;
- middle-click autoscroll over an armed ramp (Check 9.7) — Phase 2 never covered it either;
- a physically held Enter at the OS auto-repeat rate (Check 9.8) — likewise;
- the `<dialog>` `cancel` interception, i.e. Escape inside the name field leaving the dialog open
  (Check 3);
- the `[hidden]` cascade taking no layout space on a real card (Check 2);
- `maxlength` on a real input, typed or pasted (Check 4);
- the console state — errors, warnings and network activity were not reported on.

**And F-02.1-A was not answered.** The user gave no direction on options (a), (b) or (c). It leaves
this phase as an open finding, recorded below.

This document deliberately distinguishes an itemised result from an aggregate one, the way
`02-HUMAN-UAT.md` did: its G-02-A carries a real hand-count of 20, and its G-02-B says in terms that
it was approved by observation with no display or distance itemised. Same standard here.

## The automated gate, before anybody opens a browser

`node tests/selftest-node.cjs` was run on the reviewed build. It exits **0**.

| Number | Required by the plan | Measured |
|---|---|---|
| `[S09]` terminal suite | above the 145 this phase started from, **0 failed** | **354 passed, 0 failed** |
| Interaction gate | at least 36 of 36 | **39 of 39** |
| Stub-drift gate | 35 shell ids | **35 shell ids, all built by the stub page** |
| `grep -ci "counter\|rating\|balanced\|difficulty" cats-vs-mechs.html` | 0 | **0** |
| `grep -c "verdict\|balanced\|rating\|difficulty" cats-vs-mechs.html` | 0 | **0** |
| `grep -c 'data-act="openTokenPicker"' cats-vs-mechs.html` | 2 | **2** |
| `grep -cE "https?://" cats-vs-mechs.html` | 0 | **0** |

A green gate is what makes this rehearsal worth a person's time; it is not evidence for any of the
nine checks below, all of which exist precisely because the gate cannot reach them.

**Re-run at the close of the rehearsal** (after the user's reply, before this document was filled in)
with identical numbers: exit 0, 354 passed / 0 failed, interaction gate 39 of 39, stub-drift 35 shell
ids, both naming-trap greps 0, `openTokenPicker` 2, zero URLs, one `<script>`, one `<style>`.
`git diff --stat cats-vs-mechs.html` is empty — the artifact the user opened is byte-for-byte the
artifact these numbers describe, and nothing in this phase's closing task touched it.

## Environment used

**Not recorded.** The fields below were left as the script wrote them. The user configured tokens
successfully, so *some* browser was involved, but nothing about it was reported and none of it may be
inferred. Phase 2's rehearsal closed its projector item without a display or a distance — that is gap
G-02-B — and Check 5 existed to stop this phase inheriting the omission. **It inherits it.**

- Browser and version: not recorded
- Loaded from: not recorded (the script asked for a `file://` address bar to be confirmed; it was
  not confirmed either way)
- OS: not recorded
- Display used for check 5 (size and resolution): **not recorded — Check 5 was not run**
- Viewing distance for check 5: **not recorded — Check 5 was not run**
- `--tok` value in `[C00]` at the time of the reading: not re-confirmed; the shipped value is `22px`
  and nothing in this phase changed it
- Console open throughout: not recorded

---

## Finding raised BEFORE the rehearsal — OPEN, carried out of this phase

**Status after the rehearsal: OPEN and unanswered.** It was put to the user alongside the nine
checks, with three options. The reply — "token config works" — gave no direction on (a), (b) or (c),
and did not address the finding at all. Nothing was chosen, so nothing was built. It leaves Phase 2.1
as an open item; see **G-02.1-A** in the Gaps section at the foot of this document.

**F-02.1-A — a tally that has never been written cannot be raised from the page.**

Measured, not inferred. A student-made type's stat line is built on every card the moment the type
exists, and `sync()` sets `line.hidden = (amount === 0)`; `[C03]` carries
`.brd-line--opt[hidden]{display:none}`. The stepper — its minus, its field and its plus — is built
*inside* that line. So at a tally of zero the only controls that could raise it are the ones being
hidden.

Instrumented against the shipped harness's own stub page, immediately after "New type on each unit":

| Controls naming the new type | Present | Reachable (no hidden ancestor) |
|---|---|---|
| `nudgeTally` buttons (12 cards x 2) | 24 | **0** |
| tally fields (12 cards) | 12 | **0** |

The same hide pass covers a side-scope type's two faction lines, so both scopes are affected.

**What this means for the rehearsal:** every automated check is honest — the gate raises the first
tally by calling the op directly, which is a legitimate way to test the render but is not a path a
student has. Check 8's "tally it on two cats **by hand**" therefore cannot be completed as written
without the console. ROADMAP § Phase 2.1 criterion 3 — "it carries a per-unit or per-faction tally
the student increments by hand" — is not reachable through the shipped UI.

**Nothing was changed to fix this.** It is a design question, not a defect with one obvious repair:
the phase's `<specifics>` deliberately say "a custom type with a zero tally everywhere should render
as nothing on the board, not as a row of empty slots", and that is exactly the rule producing this.
Any repair edits the hide pass, `[C03]`, gate check 32 and possibly the picker, so it wants your
direction rather than the executor's guess. Three shapes it could take, for discussion:

- **(a) Reveal while the type is being edited.** A zero line stays hidden except while the picker has
  that type selected — the board is clean at rest and the student who just invented a type can find
  its first number. Costs one more input to the hide pass.
- **(b) A quiet zero on the card, hidden only once used and returned to zero.** Simple to read, but
  it puts up to 12 faint lines on the board the moment a type is made, which is the clutter the rule
  exists to prevent.
- **(c) Raise the first number in the picker.** One tally control in the editor, per selected type,
  targeting a unit or the side. Keeps the board rule exactly as it is, and does not reintroduce a
  board-side edit control (D-08).

Please answer this alongside check 8 rather than instead of it.

---

## The checks

Nine checks. Each states the steps and the outcome to expect, so you are confirming a prediction
rather than forming an opinion from nothing. Record an outcome on every one — pass, fail, or a
described issue. No check is merged into another.

---

### Check 1 — the topbar collapse, on its own (D-05 / D-06)

**This is the check the whole rehearsal is arranged around. Answer it before you look at anything
else, and answer it in words. Silence is not approval, and "looks good" over the phase as a whole is
not an answer to this question.**

You approved the Phase 2 topbar in a browser. It carried, under the label "Token appearance", four
buttons — **Health**, **Action**, **Shield**, **Damage** — one per built-in type, each opening the
picker straight onto that type.

It now carries, under the same label, **one** button: **Tokens**. It names no type. It opens the
picker on Health, and every type on the board — the five built-ins and anything the student invented
— is a row in one list inside the dialog, with the editor beside it.

The reason for the change: one button per type does not survive students inventing types. The bar
would grow by one control per invention, on a projector, which is the one place UX-02 says it must
not.

D-06 is your decision to judge this here rather than at a gate before it was built. This is that
judgement.

**Steps**
1. Open the artifact. Look at the topbar before touching anything.
2. Press **Tokens**. Look at the dialog that opens — the list is headed "Every type on this board".
3. Press **Done**.

**Answer these three, explicitly**
- 1a. Is the bar better than the row of four? **Not answered separately.** No comparison against the
  Phase 2 four-button bar was reported.
- 1b. Is **"Tokens"** the right label? **Not answered separately.** No alternative was proposed and
  no objection to the shipped label was raised. The label ships as **"Tokens"** by default rather
  than by decision.
- 1c. Is the picker obviously where a student goes both to change how a token looks and to invent
  one? **Answered by behaviour: yes.** The user reached the picker and configured tokens through it
  without being told where it was — the discoverability question answered by doing rather than by
  opinion.

**Expected:** a bar that is quieter than Phase 2's and stays that size forever, and a dialog where
the list makes it obvious that a student-made type is the same kind of thing as Health.

**A "no" is cheap now and expensive later.** It reworks the topbar markup, the picker dialog, and
eight harness checks (10, 13, 17, 18, 19, 23, 24 and 27). It is far cheaper to hear before Phase 4
encodes any of it. If the answer is no, say what the bar should be instead — a direction is worth
more than a rejection.

outcome: **PASSED — by confirmation, not by itemised judgement.**
verbatim: **"token config works"**

What that grants and what it does not: the user was asked pointedly, was told that saying nothing
would not be read as approval, and replied that the token configuration surface works. There is no
objection to the collapsed topbar anywhere in the reply, and the user demonstrably operated the
picker the collapse routes through. That is an explicit confirmation, and D-06's trade — no early
gate, an explicit verdict here — is honoured by it.

It is **not** an itemised judgement. 1a and 1b were not answered; the collapse is approved as a
working surface rather than preferred over the four-button bar on the merits, and "Tokens" survives
as the shipped label rather than as a chosen one. Neither reopens the topbar: no rework was
requested, so the eight harness checks the plan named (10, 13, 17, 18, 19, 23, 24, 27) stand and the
topbar markup is not touched. If the label is ever revisited it is a one-word edit, not a rework.

**No rework item arises from Check 1.**

---

### Check 2 — a zero tally collapses its line, and takes no space

The `[hidden]` cascade has no layout engine in the Node gate. Gate check 32 asserts the `.hidden`
property, which is the artifact's decision; whether the line then occupies no space is a question
only a browser answers. It fails OPEN — as clutter, not as a crash — which is why no assertion
catches it.

**Steps**
1. Press **Tokens**, then **New type on each unit**. The editor lands on the new type, named
   "New type". Press **Done**.
2. Look at all 12 unit cards (9 Cats, 3 Mechs).
3. Read the new type's id off the page in the console —
   `document.querySelector('.brd-line--opt').dataset.amt` — which is `t1` on a fresh board (ids are
   generated as `t1`, `t2`, … and never shown to the student). Then raise one number:
   `App.ops.nudgeTally('cats', 'c1', 't1', 1)`.
   (The console is needed here only because of finding F-02.1-A above.)
4. Watch Cat 1's card.

**Expected:** after step 2, **nothing at all** for the new type on any card — no label, no empty box,
no stepper, and no gap where a line would be. Card heights unchanged from before the type existed.
After step 4, exactly one line appears, on Cat 1 only, reading "New type" with a stepper and one
token — and no other card moves.

**Failure looks like:** 12 empty labelled boxes, or a visible gap on every card.

outcome: **NOT RUN — not itemised in the reply.** The hide cascade taking no layout space on a real
card was never observed by a human. Gate check 32 asserts the `.hidden` property in the Node stub and
that assertion passes, but the stub has no layout engine, so whether the line then occupies zero space
in a browser remains untested by anything. T-02.1-33 is unmitigated. This one fails OPEN — as clutter
on a projector, never as a crash — which is why no assertion catches it and why only a person could
have.

---

### Check 3 — Escape inside the name field

The stub `<dialog>` has `.open`, `showModal()` and `close()` and no close-request behaviour at all,
so the listener that declines one cannot be driven in Node. Gate check 37 covers the revert half.
This is the only test of the not-closing half.

**Steps**
1. Press **Tokens**. Select **Health** in the list if it is not already selected.
2. Click into the **Name** field. Type something — "Vigo" is enough.
3. Press **Escape**.
4. Now click a shape or colour swatch (focus is on a button, not in the field). Press **Escape**
   again.

**Expected:** step 3 puts "Health" back in the field and **the dialog stays open**. Step 4 closes the
dialog, and focus returns to the **Tokens** button in the topbar (the focus ring should be visible on
it).

**Also worth a glance** — a low-confidence nit carried from Phase 2's rehearsal: immediately after the
dialog opens, is focus inside the dialog rather than on `<body>`? Phase 2 saw `BODY` and suspected the
non-displayed test pane. Confirm or deny in a real browser.

outcome: **NOT RUN — not itemised in the reply.** The `cancel` interception — Escape inside the name
field reverting the text and leaving the dialog OPEN — was never exercised by a human. The stub
`<dialog>` has `.open`, `showModal()` and `close()` and no close-request behaviour at all, so the
listener that declines one cannot be driven in Node; gate check 37 covers only the revert half. The
not-closing half is untested by anything. T-02.1-34 is unmitigated. Phase 2's low-confidence
`activeElement === BODY` nit is likewise still unconfirmed and still low-confidence.

---

### Check 4 — `maxlength` on a real input

`maxlength` counts UTF-16 code units; the op's cap counts **code points**. They legitimately disagree
for an emoji name, and that disagreement only exists on a real input.

**Steps**
1. Press **Tokens**, select any type, click into the **Name** field, select all and type
   `abcdefghijklmnopqrstuvwxyz0123` (30 plain characters).
2. Count what the field holds.
3. Select all again and paste 30 skull emoji:
   `💀💀💀💀💀💀💀💀💀💀💀💀💀💀💀💀💀💀💀💀💀💀💀💀💀💀💀💀💀💀`
4. Press **Enter**.

**Expected:**
- Step 2: the field stops at **24** characters. It refuses the 25th rather than accepting it and
  telling the student off afterwards.
- Step 3: each of these emoji is **two** UTF-16 units, so `maxlength="24"` admits at most **12** of
  them — expect about 12, and possibly 11 if the browser declines to cut through the middle of a
  pair. **The one thing that must not happen is a broken half-character on screen.**
- Step 4: the name commits, the board label matches the field exactly, and **no error panel appears**.

**Optional, with the console** — this is the path the gate drives, and it bypasses `maxlength`:
set the field's value to 30 emoji programmatically, dispatch an `input` event, then press Enter. The
name that lands must be exactly 24 emoji, whole, with no error panel.

outcome: **NOT RUN — not itemised in the reply.** `maxlength` on a real input was never exercised,
typed or pasted. There is no real input anywhere in the Node stub, and the whole point of this check
is a disagreement that only exists on one: `maxlength` counts UTF-16 code units while the op's cap
counts code points. Gate check 38 drives the code-point cut through the op directly and passes; the
`maxlength` half has no test. T-02.1-35 is unmitigated.
what the field actually held after the paste: **not observed**

---

### Check 5 — an 11-row list on the projector (was to close G-02-B; **did not**)

**Steps**
1. Press **Tokens** and create **six** types (any mix of "New type on each unit" and "New type on the
   whole side"). Both New buttons should go disabled once six exist — that is the cap (D-18), and a
   bound the student can see coming rather than an error panel.
2. The list now holds **11 rows** — five built-ins (Health, Action, Shield, Damage, Dead) and six of
   the student's.
3. Put the artifact on the display you would actually use in the workshop and stand where the back
   row sits.

**Expected:** every row's name and token are legible from that distance; the list scrolls rather than
overflowing; the dialog does not exceed the screen; and the selected row is distinguishable from the
rest at that distance, not only up close.

**Write down the numbers.** Phase 2 closed its projector item as "looks good" with no display and no
distance recorded, and that is the one gap this phase is asked not to inherit. If the answer is "not
quite", `--tok` and `--tok-gap` in `[C00]` are the two values to turn and no JavaScript reads either.

outcome: **NOT RUN — not itemised in the reply, and no projector reading was reported.** This check
existed for exactly one purpose: to close Phase 2's gap G-02-B with a measurement instead of an
impression. **It does not close it.** G-02-B stands, and Phase 2.1 now stands alongside it with the
same omission — an 11-row picker list has never been read from the back of a room, on any display, at
any distance. T-02.1-36 (UX-02, legibility at the back of the room) is unmitigated.

If it is ever taken: `--tok` and `--tok-gap` in `[C00]` are the two values to turn, and no JavaScript
in the file reads either, so the reading costs nothing but a room.

display: **not recorded**
viewing distance: **not recorded**
`--tok` after the reading: **no reading taken**; the shipped value remains `22px`, unchanged by this
phase

---

### Check 6 — a rename reaches everywhere, and undo reaches back

**Steps**
1. Press **Tokens**, select **Health**, click into **Name**, replace the text with `Vigor`, press
   **Enter**.
2. Read: the list row, the dialog heading above it, and every board label behind the dialog.
3. Click a **list row** (so focus leaves the text field) and press **Ctrl+Z**.

**Expected:** after step 1 the list row, the heading and all 12 unit cards say **Vigor** — the same
word in all three places, with no card rebuilt underneath (the scroll position and any focus ring
should survive). After step 3, **Health** is back in all three.

**Note, and confirm it:** Ctrl+Z is deliberately ignored while focus is inside a text field, so
pressing it with the caret still in Name should do nothing. That is the design, not a dropped
keystroke.

outcome: **NOT RUN by hand — not itemised in the reply.** Covered automatically: gate check 30
asserts that a rename repaints the picker list row and the board label alike, and gate check 35 that
an undo taken with the picker open repaints the list row and the name field together. Both pass. The
by-hand walk — including the deliberate "Ctrl+Z is ignored while the caret is in a text field"
behaviour — was not performed, so nothing here is confirmed against a real keyboard. This is the
least exposed of the eight unrun checks, because it is the one with genuine automated cover.

---

### Check 7 — a refusal reads like a sentence

**Steps**
1. Press **Tokens**, select any type, click into **Name**, select all, type three spaces, press
   **Enter**.
2. Read the panel that appears at the bottom right. Then press **Dismiss and continue**.
3. Repeat step 1 but instead of Enter, click away to blur the field.
4. Select **Health** in the list and look at **Remove this type**.

**Expected:**
- Step 2: the panel says **"A token type needs a name."** — plain words naming what was refused, not
  a stack trace, and the field puts the old name back.
- Step 3: **no panel at all.** A blur is a student wandering off, not an instruction, so it reverts
  quietly.
- Step 4: **Remove this type is disabled**, greyed out. A built-in cannot be removed (D-15), and the
  bound is shown rather than raised — a student never has to read their way out of it.

outcome: **NOT RUN — not itemised in the reply.** No refusal message was read by a human, so whether
"A token type needs a name." reads like a sentence to a student rather than like a stack trace
remains unjudged. Legibility of prose is not assertable, so there is no automated cover for it at
all. The mechanics underneath are covered — gate check 38 asserts the refusal is loud on Enter and
quiet on blur — but that is the plumbing, not the wording.

---

### Check 8 — the end-to-end story

The whole phase in one pass: invent, name, tally, see, remove, undo.

**Steps**
1. Press **Tokens** -> **New type on each unit**.
2. Name it `Poison ☠` (the symbol is typed into the name; the emoji *swatch* is a separate, curated
   choice below) and press **Enter**.
3. Set its shape to **circle** and its colour to **violet**, and pick the **skull** emoji from the
   Emoji grid.
4. Press **Done**.
5. Raise the tally on **Cat 1** and **Cat 2** to 2 each. **This is the step finding F-02.1-A blocks
   by hand** — use the console for now:
   `App.ops.nudgeTally('cats','c1','t1',2); App.ops.nudgeTally('cats','c2','t1',2);`
   Record separately whether you could find *any* way to do it with the mouse.
6. Look at the board.
7. Press **Tokens**, select **Poison ☠**, press **Remove this type**, press **Done**.
8. Press **Ctrl+Z** once.

**Expected:** two violet circles carrying a skull on Cat 1 and Cat 2, labelled `Poison ☠`, and
nothing on the other ten cards. The type and both numbers vanish together at step 7 with **no
confirmation dialog** (D-17), and a **single** Ctrl+Z at step 8 brings the type, its appearance, its
name and both tallies back together (D-16).

outcome: **NOT RUN as an end-to-end pass — not itemised in the reply.** The first half of the story
is confirmed indirectly: "token config works" means a type was configured through the picker, which
is invent-name-appearance. The rest of the story — tally it, see it on two cats, remove it, undo it
back in one step — was not reported, and step 5 is the step F-02.1-A blocks by hand in any case.

could the tally be raised by hand, without the console? **Not answered by the user — but the
measurement stands: no.** 24 nudge buttons and 12 tally fields exist for a fresh unit-scope type and
0 of those 36 are reachable, every one sitting inside a line the hide pass has hidden. See F-02.1-A.

---

### Check 9 — nothing regressed from Phase 2

Walk the behaviour `02-HUMAN-UAT.md` recorded, on the board and on the new tally lines alike.

| # | What | Expected | Outcome |
|---|---|---|---|
| 9.1 | Twenty rapid clicks on "Increase Cat 1 health" | exactly twenty changes — not nineteen, not forty | **not run by hand**; covered by the automated gate |
| 9.2 | Type `-8` then `+5` into a health field | the delta applies, not the literal number | **not run by hand**; covered by the automated gate |
| 9.3 | Arrow keys in a focused field | step the value, and the field keeps the caret | **not run by hand**; covered by gate check 16 |
| 9.4 | Press and hold `+` | ramps continuously, no token animation restarting, one undo entry | **not run by hand**; covered by gate check 8 |
| 9.5 | Add and remove a unit | roster rebuilds without losing scroll position or the focus ring | **not run by hand**; covered by gate checks 6 and 7 |
| 9.6 | Shape / colour / emoji swatches on a built-in | restyle the type, board tokens follow | **partially confirmed** — "token config works" covers configuring a type through the picker; no built-in-vs-custom distinction was reported. Gate check 18 covers the mechanism. |
| 9.7 | **Middle-click on a stepper while a hold is armed** (carried from Phase 2 test 3, never covered — the harness has no middle-click) | no value change, no ramp, and Chrome's autoscroll circle must not appear over the board | **NOT RUN — no middle-click was reported.** Untested for the second phase running; the harness has no middle-click action, so nothing anywhere covers it. |
| 9.8 | **Physically hold Enter on the focused Undo button** at the OS auto-repeat rate (carried from Phase 2 test 4 — the mechanism is measured, a real held key never was) | one undo, not a burst of them | **NOT RUN — no held key was reported.** The `repeat: true` → `preventDefault()` mechanism is measured (gate check 22, and Phase 2 in a real browser); a physically held key at the OS repeat rate has still never been tried. |
| 9.9 | The same, on a **tally** stepper | the new steppers inherit 9.1–9.4 rather than re-implementing them; they should behave identically | **NOT RUN.** Compounded by F-02.1-A: a tally stepper is not reachable from the page at all until its number is non-zero, so there was no control to try this on by hand. |

outcome: **NOT RUN by hand — not itemised in the reply.** No regression was reported, but no
regression walk was reported either. The automated substitute is genuine and green — 354 passed / 0
failed, interaction gate 39 of 39 — and it covers 9.1 through 9.6 in mechanism. It covers **none** of
9.7, 9.8 or 9.9, which is precisely why they are line items rather than folded into an aggregate the
way Phase 2 folded them.

---

## Console

**Not reported.** No console state was recorded by the user. What can be said without a browser is
only what the file itself proves: `grep -cE "https?://" cats-vs-mechs.html` returns **0**, so there is
no outbound request to make, and the file carries exactly one `<script>` and one `<style>` with no
`import` and no `fetch`. That is a static argument, not an observation.

- Errors: not reported
- Warnings: not reported
- Network activity: not observed; **0** URLs in the artifact by grep, and no `fetch` / `XHR` /
  `import` anywhere in it

## Summary

total: 9
passed: 1
issues: 0
pending: 8
skipped: 0
blocked: 0

**Read the numbers this way.** One check has an itemised outcome: Check 1, passed by explicit
confirmation. Eight were not run and are pending, not passed — they are the checks that exist because
no automated instrument in this repo can reach them, so "pending" here means genuinely unknown rather
than "probably fine". Zero issues means zero issues *reported*, not zero issues *present*: four
threats in the plan's register (T-02.1-33 through T-02.1-36) had a numbered check as their only
mitigation and none of those four checks was taken.

The one thing that is measured, and measured well, is the automated gate on the reviewed build: 354
passed / 0 failed, interaction gate 39 of 39, stub-drift 35 shell ids, all four acceptance greps at
their required values, re-run at the close of the rehearsal with identical numbers.

## Rework items

Write each one as "Check N — observed X, expected Y". Not "looks off".

**None raised.** No failure was reported, and no rework is owed by anything the user said. That is
distinct from "nothing needs doing" — eight checks were not taken, so no rework could have surfaced
from them.

The one open item is not a rework but a design decision:

1. **F-02.1-A / G-02.1-A** — a tally cannot be raised from zero anywhere on the page. Measured, not
   observed in the rehearsal; still unanswered. See Gaps.

## Gaps

- **G-02.1-A (OPEN — the finding this phase leaves behind).** A student-made type's tally cannot be
  raised from zero through the UI. Measured against the shipped harness's own stub page, immediately
  after "New type on each unit": **24 `nudgeTally` buttons and 12 tally fields exist for a fresh
  unit-scope type, and 0 of those 36 are reachable** — every one is built inside the stat line that
  `sync()` hides at a tally of zero, and `[C03]`'s `.brd-line--opt[hidden]{display:none}` removes it.
  **Both scopes are affected**: the same hide pass covers a side-scope type's two faction lines.

  Consequence, stated plainly: **ROADMAP § Phase 2.1 success criterion 3 — "It carries a per-unit or
  per-faction tally the student increments by hand" — is not reachable through the UI while this
  stands.** The number can be raised by calling the op from the console, which is how the automated
  gate reaches it; a student has no such path.

  It was put to the user with three options and **none was chosen**:
  - **(a) Reveal while the type is being edited.** A zero line stays hidden except while the picker
    has that type selected. The board stays clean at rest and the student who just invented a type
    can find its first number. Costs the hide pass one more input.
  - **(b) A quiet zero on the card**, hidden only once used and returned to zero. Simple to read, but
    it puts up to 12 faint lines on the board the moment a type is made — the clutter the rule exists
    to prevent.
  - **(c) Raise the first number in the picker.** One tally control in the editor, per selected type,
    targeting a unit or the side. Keeps the board rule exactly as it is and reintroduces no board-side
    edit control (D-08).

  Nothing was changed. This is the phase's own `<specifics>` rule working exactly as written — "a
  custom type with a zero tally everywhere should render as nothing on the board, not as a row of
  empty slots" — so any repair edits the hide pass, `[C03]`, gate check 32 and possibly the picker.
  That is a design decision (deviation Rule 4), and it is the user's to make.

- **G-02.1-B (OPEN — inherited from Phase 2's G-02-B, and not closed here).** The picker list has
  never been read at projector distance. Check 5 existed to close G-02-B with a display and a viewing
  distance; it was not run, so **G-02-B stands and this phase adds an 11-row modal to what has never
  been measured**. `--tok` (`22px`) and `--tok-gap` in `[C00]` are the two values to turn, and no
  JavaScript reads either.

- **G-02.1-C (OPEN).** Four browser-only behaviours have no coverage anywhere — not in the gate, not
  in this rehearsal: the `[hidden]` cascade taking no layout space (Check 2), the `<dialog>` `cancel`
  interception leaving the dialog open (Check 3), `maxlength` on a real input (Check 4), and Chrome's
  middle-click autoscroll over an armed ramp plus a physically held Enter (Checks 9.7 / 9.8, both
  carried unresolved from Phase 2). Each fails open — as clutter, a lost keystroke, or a truncated
  name — which is exactly the failure mode a suite never catches.

- **Noted for later, if it comes up:** per-token ordering on the board — a student may want their
  invention shown above health. Not in this phase's criteria; the phase context says to record it if
  the rehearsal surfaces it. The rehearsal did not surface it.
