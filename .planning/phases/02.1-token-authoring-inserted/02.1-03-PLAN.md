---
phase: 02.1-token-authoring-inserted
plan: 03
type: execute
wave: 3
depends_on: [02.1-02]
files_modified:
  - cats-vs-mechs.html
autonomous: true
requirements: [ALLOC-10, ALLOC-11]
must_haves:
  truths:
    - "Renaming a token type changes every label that names it on the board, without rebuilding a column."
    - "A student-made type draws a stat line on the board at whichever site its scope names — the faction head for 'side', the unit card for 'unit'."
    - "A student-made type's tokens look exactly like a built-in's: same shapes, same colours, same emoji, same compaction above the threshold."
    - "A student-made type whose tally is zero shows nothing at all — no empty box, no label."
    - "A tally stepper behaves like every other stepper: click, delta typing, arrow keys, press-and-hold ramp."
  artifacts:
    - path: "cats-vs-mechs.html"
      provides: "[S06.1] labelFor + the [data-lbl] sync pass + statLine carrying a label key"
      contains: "function labelFor"
    - path: "cats-vs-mechs.html"
      provides: "[S06.1] amountFor scope branch, custom-type builder loops in factionHead and unitCard, the .brd-line--opt hide pass"
      contains: "brd-line--opt"
    - path: "cats-vs-mechs.html"
      provides: "[C03] .brd-line--opt[hidden]{display:none}"
      contains: ".brd-line--opt[hidden]"
    - path: "cats-vs-mechs.html"
      provides: "[S07.1] tokenId in the fire/nudgeField/applyField payload, FIELD_OPS.tally, nudgeTally in HOLD_ACTS, selectTokenType in UI_ACTS"
      contains: "selectTokenType"
  key_links:
    - from: "[S06.1] sync()"
      to: "build.tokens[id].name"
      via: "the [data-lbl] pass calling labelFor"
      pattern: "data-lbl|\\[data-lbl\\]"
    - from: "[S06.1] amountFor"
      to: "unit.tally / faction.tally"
      via: "the scope branch, placed before the findUnit early return"
      pattern: "tally"
    - from: "[S07.1] fire"
      to: "[S05] setTally / nudgeTally"
      via: "tokenId added to the dispatch payload"
      pattern: "tokenId: "
---

<objective>
Make the board draw what plan 02.1-02 can now store: a renamed label everywhere a type is named, a
stat line per student-made type at the site its scope names, and nothing at all where a tally is
zero.

Purpose: ALLOC-10's "shown wherever that token type is presented" and ALLOC-11's "renders on the
board exactly like a built-in one" and "a tally the student increments by hand".

Output: `[S06.1]` renders and syncs student-made types, `[C03]` collapses a zero line, and `[S07.1]`
carries the one extra payload key a tally stepper needs. Still no picker changes — those are plans
02.1-04 and 02.1-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-token-authoring-inserted/02.1-CONTEXT.md
@.planning/phases/02.1-token-authoring-inserted/02.1-RESEARCH.md
@.planning/phases/02.1-token-authoring-inserted/02.1-PATTERNS.md
@.planning/phases/02.1-token-authoring-inserted/02.1-02-SUMMARY.md
@CLAUDE.md
@cats-vs-mechs.html
@tests/selftest-node.cjs

<interfaces>
Consumed from plans 02.1-01 / 02.1-02 — do NOT re-derive:

  App.data.TOKEN_IDS, TOKEN_SCOPES, MAX_CUSTOM_TYPES
  App.ops.setTally(side, unitId, tokenId, n)      -> boolean
  App.ops.nudgeTally(side, unitId, tokenId, delta) -> boolean
  build.tokens[id] = { shape, color, glyph, name[, scope] }
  build[side].tally / build[side].units[i].tally  — sparse; a zero stores nothing

Already shipped and used unchanged (read them, do not rewrite them):

  App.render.styleFor(state, amt)      — hasOwnProperty lookup, draws an unknown id today
  App.render.safeShape / safeColor     — enum id -> fixed class suffix, never a raw caller string
  App.render.COMPACT_AT = 12           — a row at 12 renders 12x + one token, for any data-amt
  stepper(spec)                        — delta typing, arrow keys, ramp, aria-label, the data-k contract
  tokenRow(amt, side, unitId)          — built EMPTY, always
  setData(node, pairs)                 — skips undefined and null

Established by this plan and consumed by plans 02.1-04 / 02.1-05:

  App.render.labelFor(state, tokenId) -> string   — the display label, hasOwnProperty-guarded
  UI_ACTS includes 'selectTokenType'              — claimed and ignored until 02.1-05 registers it
  data-k on a tally control: side + '/' + unitId + '/' + tokenId   (unit scope)
                             side + '/' + tokenId                  (side scope)
  data-amt on a tally control and its row: the token id
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make labels sync-driven, and teach amountFor to read a scoped tally</name>
  <files>cats-vs-mechs.html</files>
  <behavior>
    - `labelFor(state, 'hp')` returns `build.tokens.hp.name`. After `renameTokenType('hp','Vigor')`
      it returns `'Vigor'`.
    - `labelFor(state, 'constructor')` and `labelFor(state, 'nope')` return the default Health name
      rather than a function or `undefined`.
    - A rename schedules a SYNC-only frame: the board's structural signature is unchanged and no unit
      card is rebuilt, yet every `[data-lbl]` node's `textContent` is the new name.
    - `amountFor(state, 't2', 'cats', null)` returns the faction tally for a `scope: 'side'` type —
      it must NOT fall through the `findUnit` early return.
    - `amountFor(state, 't1', 'cats', 'c1')` returns the unit tally for a `scope: 'unit'` type, and
      `0` for a unit that has never been tallied.
    - `amountFor(state, 'constructor', 'cats', 'c1')` returns `0`.
    - `amountFor` for the five built-ins is byte-for-byte unchanged in behaviour.
  </behavior>
  <read_first>
    - `cats-vs-mechs.html` lines 1755–1770 — the Tier-1 builder contract comment: `data-k` is
      IDENTITY and unique document-wide; `data-amt` names the quantity a node shows.
    - `cats-vs-mechs.html` lines 1685–1690 — `setData`, the only way a `data-*` is written here.
    - `cats-vs-mechs.html` lines 1820–1832 — `statLine`, the function that gains one attribute.
    - `cats-vs-mechs.html` lines 1939–1975 — `findUnit`, `amountFor` (note the `findUnit` early
      return sits above the `hp` / `shield` arms) and `styleFor` with its hasOwnProperty comment.
    - `cats-vs-mechs.html` lines 2059–2105 — `sync()`, especially the `.brd-value` pass and the
      `.tok-row` pass, and the `SYNC_HOOKS` call at the end.
    - `.planning/phases/02.1-token-authoring-inserted/02.1-RESEARCH.md` § Pattern 5, § "The tally
      read, in `amountFor`".
    - `.planning/phases/02.1-token-authoring-inserted/02.1-PATTERNS.md` § "`[S06.1]` — the tally
      read" (note the ORDERING CAUTION) and § "`[S06.1] sync()` — the new label pass".
  </read_first>
  <action>
    Add `labelFor(state, tokenId)` to `[S06.1]`, immediately beside `styleFor`, mirroring it exactly:
    `Object.prototype.hasOwnProperty.call` on the vocabulary, a `typeof … === 'string'` check on the
    record's `name`, and `App.data.DEFAULTS.tokens.hp.name` as the fallback. The comment must give
    `styleFor`'s reason: from Phase 4 this object can arrive from a pasted build code, and a bare
    lookup for `'constructor'` resolves on the prototype instead of failing.

    Give `statLine` one optional extra argument — the token id the label names — and write it with
    `setData` as `lbl` when it is present. Do not change the existing four-argument call sites'
    behaviour; `setData` already skips `undefined`.

    Add a `[data-lbl]` pass to `sync()`, immediately beside the `.brd-value` pass and written line for
    line like it: `querySelectorAll('[data-lbl]')`, then `textContent = labelFor(state, node.dataset.lbl)`.
    The comment must record WHY this exists rather than baking the label into `structure()`: making a
    rename structural would rebuild both columns on every keystroke of a name edit, and it is what
    lets a built-in rename (D-10) travel the same single path a student-made one does.

    Pass the token id from `factionHead` and `unitCard` into their existing `statLine` calls for
    `ap`, `hp` and `shield`, and replace those three hard-coded label strings with nothing — the
    label now arrives from state on the first sync. Keep the literal as the build-time text so the
    first painted frame is never blank; the sync pass overwrites it immediately.

    Then add the scoped-tally branch to `amountFor`. **This is a REORDER, not an append** — the
    side-scope arm must run BEFORE `var unit = findUnit(faction, unitId)` returns early on a null
    `unitId`, or a faction-scope tally is unreachable. Resolve the type from
    `state.build.tokens` with `hasOwnProperty`; treat it as a tally only when the id is not in
    `App.data.TOKEN_IDS`; pick the owner from the record's `scope` (`'side'` -> the faction,
    otherwise the unit); and read the bag with `hasOwnProperty` twice, returning `0` for a missing
    bag or a missing key. Nothing derived is stored — this is read on every frame from the build
    slice, and the comment should say so.

    Add the matching `[S09.7]` rows for every line of the `<behavior>` block. `labelFor` and
    `amountFor` are both already exported or exportable from `[S06]`; export `labelFor` alongside
    `amountFor` so the suite asserts the function rather than a re-typed copy of it.
  </action>
  <verify>
    <automated>node tests/selftest-node.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/selftest-node.cjs` exits 0, more rows passing than plan 02.1-02 left, **0 failed**,
      interaction gate **27 of 27**, stub-drift gate **28 shell ids**.
    - `grep -v '^ *//' cats-vs-mechs.html | grep -c "function labelFor"` returns **1**.
    - `grep -c "\[data-lbl\]" cats-vs-mechs.html` returns at least **1**.
    - `grep -c "labelFor: labelFor" cats-vs-mechs.html` returns **1**.
    - Ordering proof: in `amountFor`, the line number of the tally branch is LOWER than the line
      number of `var unit = findUnit(faction, unitId);`. State both line numbers in the SUMMARY.
    - A rename is sync-only: assert in `[S09.7]` that `renameTokenType` does not go through
      `commitStructural` —
      `grep -c "commitStructural('token rename" cats-vs-mechs.html` returns **0**.
    - **Naming trap, both greps re-run after every comment block:**
      `grep -ci "counter\|rating\|balanced\|difficulty" cats-vs-mechs.html` = **0** and
      `grep -c "verdict\|balanced\|rating\|difficulty" cats-vs-mechs.html` = **0**.
  </acceptance_criteria>
  <done>A rename reaches every board label through one per-frame pass with no column rebuild, and `amountFor` reads a scoped tally with the side arm above the `findUnit` early return.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Draw a student-made type on the board, and collapse the line when its tally is zero</name>
  <files>cats-vs-mechs.html</files>
  <behavior>
    - After creating a `scope: 'unit'` type, every unit card carries one more `.brd-line`, keyed
      `side + '/' + unit.id + '/' + tokenId`, with `data-amt` equal to the token id.
    - After creating a `scope: 'side'` type, each faction head carries one more `.brd-line`, keyed
      `side + '/' + tokenId`, and no unit card does.
    - Both new lines carry class `brd-line--opt` and enough `data-*` for the hide pass to re-read
      their quantity: the token id, the side, and (unit scope only) the unit id.
    - A line whose tally is 0 has `hidden === true` after a sync; setting the tally to 1 leaves
      `hidden === false`.
    - A built-in line is NEVER hidden — a unit at zero health is meaningful and stays on screen.
    - The new line's token row obeys `COMPACT_AT`: at 12 it renders `12x` + one token, at 11 it
      renders eleven tokens.
    - The new line's tokens use the same `safeShape` / `safeColor` class suffixes a built-in uses.
    - Creating and removing a type is a structural frame; nudging its tally is not.
  </behavior>
  <read_first>
    - `cats-vs-mechs.html` lines 1776–1815 — `stepper(spec)` in full (the `data-k` / `data-act` /
      `data-amt` contract it writes) and `tokenRow` with its "built EMPTY, always" rule.
    - `cats-vs-mechs.html` lines 1834–1913 — `factionHead` (the `side + '/ap'` stepper, the
      faction-level site) and `unitCard` (the `maxHp` and `shield` steppers, the per-unit site).
    - `cats-vs-mechs.html` lines 1915–1938 — `structure()` and `buildColumn`.
    - `cats-vs-mechs.html` lines 2005–2058 — `syncRow` and `COMPACT_AT`, including the comment about
      `display:none` and the `animate = row.children.length > 0` read that happens before the loop.
    - `cats-vs-mechs.html` lines 380–405 — `.err-panel[hidden]{display:none}` at `:387` and
      `.st-report[hidden]{display:none}` at `:400`: the file's own two instances of this exact
      cascade fix.
    - `cats-vs-mechs.html` line 191 — `.unit-line`. Confirm for yourself with
      `grep -c "unit-line" cats-vs-mechs.html` that it has no builder.
    - `.planning/phases/02.1-token-authoring-inserted/02.1-RESEARCH.md` § Pattern 6, § Pitfall 5.
    - `.planning/phases/02.1-token-authoring-inserted/02.1-PATTERNS.md` § "`[S06.1]` — the two render
      sites", § "`[C03]` CSS — the hideable stat line" (note correction 3: `.unit-line--opt` is dead).
  </read_first>
  <action>
    In `factionHead`, after the action-points line, walk the vocabulary and append one `statLine` per
    entry that is not in `App.data.TOKEN_IDS` and whose `scope` is `'side'`. In `unitCard`, after the
    shield line, do the same for entries whose `scope` is `'unit'`. Both loops build a `stepper` with
    `act: 'tally'`, `nudge: 'nudgeTally'`, `amt: <the token id>`, and a `key` of
    `side + '/' + tokenId` (faction) or `side + '/' + unit.id + '/' + tokenId` (unit), plus a
    `tokenRow(<token id>, side, unitId)`. Read the label from state, not from a literal — pass the
    token id into `statLine` so the `[data-lbl]` pass owns it.

    `data-k` must be unique document-wide, because focus restore takes the first match. The keys
    above are unique by construction for the same reason the existing ones are; say so in the
    comment, in the voice of the Tier-1 contract at `:1755`.

    Mark each new line with `classList.add('brd-line--opt')` and `setData` it with the token id, the
    side and (unit scope only) the unit id. Do NOT mark a built-in line.

    Add a hide pass to `sync()`, after the `.tok-row` pass and written like it: walk
    `.brd-line--opt` and set `node.hidden = amountFor(state, node.dataset.amt, node.dataset.side, node.dataset.unit) === 0`.
    Three things the comment should record, because each was checked: nothing animates when a line
    unhides (a hidden line holds zero children by construction, and `syncRow` reads `animate` from
    `row.children.length` before its loop, so 0 -> 1 appends without the entry class); the line
    already exists, so a tally 0 -> 1 is a plain commit and a sync-only frame unhides it; and a
    built-in line is never hidden.

    In `[C03]`, add `.brd-line--opt[hidden]{display:none}` — one rule, matching the file's own two
    existing instances. Do NOT add a `.unit-line--opt` rule: `statLine` builds `brd-line` for both
    sites and `.unit-line` has zero builders, so a second selector would ship dead. The comment must
    say why the rule is needed at all: `.brd-line{display:flex}` is an author rule and `[hidden]` is
    a user-agent rule, so author wins and `hidden` alone collapses nothing.

    Add the `[S09.7]` rows for every line of the `<behavior>` block. Note that the harness's
    `matches()` supports only a tag name, `.class` and `[data-*]` — `[hidden]` and `#id` silently
    match nothing — so assert on the `.hidden` PROPERTY, on `className`, or on `data-*`, never on an
    attribute selector.
  </action>
  <verify>
    <automated>node tests/selftest-node.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/selftest-node.cjs` exits 0, more rows passing than Task 1 left, **0 failed**,
      interaction gate **27 of 27**, stub-drift gate **28 shell ids**.
    - `grep -c "brd-line--opt" cats-vs-mechs.html` returns at least **3** (the CSS rule, the
      `classList.add`, the sync-pass selector).
    - `grep -c "unit-line--opt" cats-vs-mechs.html` returns **0**.
    - `grep -c "\.brd-line--opt\[hidden\]" cats-vs-mechs.html` returns **1**.
    - Zero-tally behaviour is asserted on the `.hidden` property, not an attribute selector:
      `grep -c "\[hidden\]" tests/selftest-node.cjs` returns **0**.
    - No forbidden sink introduced: the section-1 scan in the run above still passes.
    - **Naming trap, both greps re-run:**
      `grep -ci "counter\|rating\|balanced\|difficulty" cats-vs-mechs.html` = **0** and
      `grep -c "verdict\|balanced\|rating\|difficulty" cats-vs-mechs.html` = **0**.
  </acceptance_criteria>
  <done>A student-made type draws at the site its scope names, its tokens are indistinguishable from a built-in's, and a zero tally shows nothing at all.</done>
</task>

<task type="auto">
  <name>Task 3: The one deliberate [S07.1] edit — carry a token id through the field machinery</name>
  <files>cats-vs-mechs.html</files>
  <read_first>
    - `cats-vs-mechs.html` lines 2290–2360 — the `[S07.1]` banner (it promises later plans do not
      touch this sub-region; this task is the recorded exception), `UI_ACTS` at `:2327`,
      `UI_HANDLERS` at `:2339` and its claimed-and-ignored paragraph, `HOLD_ACTS` at `:2350` with
      its "an allowlist, not a data-step sniff" comment, `LATE_BINDERS` at `:2356`.
    - `cats-vs-mechs.html` lines 2465–2475 — `fire(btn)` and the three payload keys it sends today.
    - `cats-vs-mechs.html` lines 2482–2535 — `parseField`, `FIELD` and `FIELD_OPS` with its
      "a guard that runs in only one direction is not a guard" comment.
    - `cats-vs-mechs.html` lines 2536–2612 — `isField`, `showFieldValue`, `nudgeField`, `applyField`,
      `revertField`, `commitField`.
    - `cats-vs-mechs.html` lines 3960–4058 — `[S09.5] SUITE: interaction contract`, especially the
      `UI_ACTS.indexOf('openTokenPicker')` row at `:3977` and the `querySelector('[data-act="openTokenPicker"]')`
      at `:4029`.
    - `.planning/phases/02.1-token-authoring-inserted/02.1-RESEARCH.md` § Pitfall 12, § Pitfall 7.
    - `.planning/phases/02.1-token-authoring-inserted/02.1-PATTERNS.md` § "`[S07.1]` — the one
      deliberate edit".
  </read_first>
  <action>
    Four small, additive edits inside `[S07.1]`, each of which must be recorded in this plan's
    SUMMARY as a conscious exception to the section's banner.

    1. Add `tokenId: d.amt` to the payload built in `fire()`, and to the equivalent payloads in
       `nudgeField()` and `applyField()`. One key, ignored by every existing act — `dispatch`'s arms
       read only the payload keys they need.
    2. Add a `tally` entry to `FIELD_OPS`: `{ set: 'tally', nudge: 'nudgeTally', amt: null }`, and
       change the two `.amt` lookups to read `FIELD_OPS[d.act].amt || d.amt`, so a tally field reads
       its quantity from its own `data-amt` rather than from a table that cannot know it. The
       existing three entries keep their fixed `amt` and behave identically.
    3. Add `'nudgeTally'` to `HOLD_ACTS`, or the press-and-hold ramp will not run for a tally
       stepper. Its comment already explains why sniffing `data-step` is not acceptable.
    4. Add `'selectTokenType'` to `UI_ACTS`. It is page work — `[S05] dispatch` has a
       `default: throw` arm, and routing a UI act there shows a student the styled error panel on a
       first click. No handler is registered yet: plan 02.1-05 assigns
       `UI_HANDLERS.selectTokenType` from `[S07.2]`. Between now and then the act is CLAIMED AND
       IGNORED, which the `UI_HANDLERS` comment already documents as deliberate rather than
       unfinished.

    Nothing else in `[S07.1]` changes. In particular, `isField` still keys on `.stp-field` and
    `parseField` stays numeric — the name field in plan 02.1-05 gets its own plumbing and must NOT
    carry `.stp-field`, or "Poison" is parsed as a number and refused on Enter.

    Add `[S09.7]` rows: `FIELD_OPS` carries exactly four entries and `tally`'s `amt` is null;
    `HOLD_ACTS` contains `nudgeTally`; `UI_ACTS` contains both `openTokenPicker` and
    `selectTokenType`; a `data-act` a field may not drive is still refused at the page boundary
    (the existing gate check 25 covers this — confirm it still passes rather than duplicating it).
  </action>
  <verify>
    <automated>node tests/selftest-node.cjs</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/selftest-node.cjs` exits 0, more rows passing than Task 2 left, **0 failed**,
      interaction gate **27 of 27** (checks 1–4, 12, 16, 20, 22 and 25 all still green — they are the
      stepper and field-boundary checks this task's edits sit under), stub-drift gate **28 shell ids**.
    - `grep -c "tokenId: d.amt" cats-vs-mechs.html` returns **3** — `fire`, `nudgeField`, `applyField`.
    - `grep -c "FIELD_OPS\[d.act\].amt || d.amt" cats-vs-mechs.html` returns **2**.
    - `grep -c "'nudgeTally'" cats-vs-mechs.html` returns at least **2** (the `FIELD_OPS` entry and
      `HOLD_ACTS`).
    - `grep -c "UI_ACTS = \['openTokenPicker', 'selectTokenType'\]" cats-vs-mechs.html` returns **1**
      (or the equivalent multi-line literal — assert the array contents in `[S09.7]` either way).
    - `grep -c "case 'selectTokenType'" cats-vs-mechs.html` returns **0** — a UI act never reaches
      `[S05]`.
    - `grep -c "stp-field" cats-vs-mechs.html` is unchanged from before this task. Record the number.
    - **Naming trap, both greps re-run:**
      `grep -ci "counter\|rating\|balanced\|difficulty" cats-vs-mechs.html` = **0** and
      `grep -c "verdict\|balanced\|rating\|difficulty" cats-vs-mechs.html` = **0**.
  </acceptance_criteria>
  <done>A tally stepper is routed like every other stepper, with a token id in the payload, a ramp allowlist entry, and `selectTokenType` claimed by the UI seam ahead of its handler.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `build.tokens` (state) -> `[S06.1]` render | The vocabulary is read on every frame and, from Phase 4, can arrive from a pasted build code. Every read here is a caller-supplied-key read. |
| `data-*` on a rendered node -> `[S07.1]` -> `[S05] dispatch` | The renderer's own attributes become a dispatch payload. A `data-act` read off the page is a caller-supplied key. |
| A student-typed name -> the page | ALLOC-10 puts arbitrary student text on screen for the first time. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.1-14 | Tampering | `labelFor` / the `amountFor` tally branch — bare `vocab[id]` reads | mitigate | `Object.prototype.hasOwnProperty.call` on the vocabulary AND on the tally bag, copying `styleFor` exactly. Measured: `vocab['constructor']` and `vocab['toString']` both resolve to functions on an ordinary object. Asserted with a `labelFor(state, 'constructor')` row. |
| T-02.1-15 | Tampering | DOM injection via a student-typed token name | mitigate | Structurally impossible and kept that way: the name reaches the page only as `textContent` in the `[data-lbl]` pass, and the document-wide forbidden-pattern scan fails the build if any markup-parsing sink appears. The name is never written into `className`, into a selector, or into `data-k` — the id is the identity (D-09). |
| T-02.1-16 | Tampering | CSS class injection via a student-made type's shape or colour | accept -> already mitigated | `safeShape` / `safeColor` map an unknown id to the shipped health entry rather than pasting it through, and `createTokenType` already allowlisted both values. A student-made type inherits this with no change. |
| T-02.1-17 | Elevation of privilege | A `data-act` on a new stepper naming an op a field may not drive | mitigate | `FIELD_OPS` stays an allowlist by construction in both directions, and the new `tally` entry is a table row rather than a widened check. Existing gate check 25 is the regression test; confirm it still passes. |
| T-02.1-18 | Denial of service | 6 types x 24 units = 144 empty labelled boxes cluttering the projector | mitigate | The `.brd-line--opt` hide pass plus the `[hidden]` cascade rule. This is a UX-02 failure, not a crash — it fails open, which is why it needs the assertion rather than an eyeball. |
| T-02.1-SC | Tampering | npm / pip / cargo installs | mitigate | Not applicable: no dependency, no build step, no install in this plan. No legitimacy checkpoint is owed. |

</threat_model>

<verification>
1. `node tests/selftest-node.cjs` exits 0, 0 failed, interaction gate 27 of 27, stub-drift gate
   28 shell ids, passed count strictly higher than plan 02.1-02 left it.
2. `grep -ci "counter\|rating\|balanced\|difficulty" cats-vs-mechs.html` returns 0.
3. `grep -c "verdict\|balanced\|rating\|difficulty" cats-vs-mechs.html` returns 0.
4. `grep -c 'data-act="openTokenPicker"' cats-vs-mechs.html` still returns 5 — the topbar is
   untouched until plan 02.1-05.
5. `grep -c "unit-line--opt" cats-vs-mechs.html` returns 0.
6. The `amountFor` reorder is proven by line numbers recorded in the SUMMARY.
</verification>

<success_criteria>
- A rename appears everywhere the board names a type, through one sync pass and no column rebuild.
- A student-made type draws at the site its scope names and is visually indistinguishable from a
  built-in one.
- A zero tally renders as nothing — no empty box, no orphan label.
- A tally stepper has click, delta typing, arrow keys and the press-and-hold ramp, all inherited
  rather than re-implemented.
- `[S07.1]` grew by exactly four additive edits, each recorded as a conscious exception.
</success_criteria>

<output>
Create `.planning/phases/02.1-token-authoring-inserted/02.1-03-SUMMARY.md` when done. Record: the new
passed/failed totals, the two `amountFor` line numbers proving the reorder, the four `[S07.1]` edits
verbatim, and the unchanged `stp-field` count.
</output>
