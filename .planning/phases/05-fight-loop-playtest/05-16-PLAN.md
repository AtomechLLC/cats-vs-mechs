---
phase: 05-fight-loop-playtest
plan: 16
type: execute
wave: 16
gap_closure: true
depends_on: ["05-15"]
files_modified:
  - tests/selftest-node.cjs
  - tests/browser-checks.mjs
  - .planning/phases/05-fight-loop-playtest/05-11-PLAN.md
autonomous: true
requirements: [FIGHT-03, FIGHT-09, FIGHT-12, FIGHT-13, PROJ-05, REF-03]
section_ownership: >
  tests/selftest-node.cjs — check 92's drive and its `FIGHT_FLOOR` history comment; every fight
  interaction-gate row's CLAIM (93, 93b, 93c, 94, 94b, 96, 97, 98, 99, 100, 101, 102 and the rows
  plans 05-12, 05-14 and 05-15 added); the harness limitations list. tests/browser-checks.mjs — new
  numbered checks appended, its header untouched. 05-11-PLAN.md — surgical edits only, each marked
  inline as a D-27 edit. **It edits `cats-vs-mechs.html` NOT AT ALL** — if a finding here needs an
  artifact change, it is logged to `deferred-items.md` with its measurement, which is the executor's
  scope boundary and plan 05-10's shipped precedent. It does not touch check 95 or the re-homed
  disabled row (plan 05-14 turned both and owns their claims).

must_haves:
  truths:
    - "The fight-mode Layer C harvest reads the NEW tab — the battlefield's own strings included — with declarations standing on both sides, and FIGHT_FLOOR is re-measured off the board that row actually harvests."
    - "Every gate row that drove the old declaration form now asserts what the new grid actually claims — rewritten, never deleted and never quietly weakened."
    - "The phase's acceptance run is re-driven through the new grid, and the pool line it reads back is a spoken-for reading that MOVES rather than a spent reading that never did."
    - "A full round can be declared and advanced by real clicks in real Chrome and real Edge at 1920x1080 and at 1366x768, with both battlefield clusters laid out, the change-target flow completed by real clicks, the board region reachable and #strip still pinning."
    - "Plan 05-11 is still the blocking playtest, still numbered 11, and its script now describes the surface that ships."
  artifacts:
    - path: "tests/selftest-node.cjs"
      provides: "the re-measured FIGHT_FLOOR, the rewritten fight rows, and the limitations list"
      contains: "FIGHT_FLOOR"
    - path: "tests/browser-checks.mjs"
      provides: "the tab, the battlefield's geometry, both viewports, both browsers, and a round played by real clicks"
      contains: "msedge"
  key_links:
    - from: "check 92's drive"
      to: "the new grid's controls"
      via: "declarations standing on BOTH sides when the harvest is taken"
      pattern: "harvestInto"
    - from: "row 102"
      to: "the spoken-for reading"
      via: "read back verbatim off the page, not asserted around"
      pattern: "102\\."
---

<objective>
**Gate surgery, honestly.**

Four consecutive plans each set a dial against a page the next plan changed, and the record of that
is limitations entry 21 and a viewport fix taken outside the plan sequence. The failure mode this
plan exists to prevent is the mirror of it: **a gate row left pointing at a surface that has moved,
still green, asserting nothing.** Every one of the following is a measured precedent in this repo:

- probe X: a single-string declaration line was invisible to check 92, because the harvest was taken
  on a board where nothing had been declared. The drive moved. **It moves again — the grid draws a
  different set of words, and the harvest must be taken with declarations standing.**
- probe W: the Advance control disabled for an unaffordable declaration, and 147 of 147 passed.
- probe U(c): a stub dataset spelling drifted from the shell and the run was **spotlessly green over
  a control nothing would be listening to.**
- the viewport fix's own closing line: *"Nothing in this repository will go red if any of it is
  undone."*

`FIGHT_FLOOR` has a history comment with four entries, each naming its plan, its boards and its
per-card arithmetic, and it ends *"A plan that adds a fight surface after this one inherits the same
obligation and the same method."* **This plan is that plan, and three surfaces have arrived since:**
the view switch, the declaration grid and the battlefield. It writes the fifth entry, and the
battlefield is the reason the arithmetic is harder than the four before it — a unit's string count on
the fight page now moves with the action count, with how many token types that unit holds, and with
whether each of its token rows has compacted.

Purpose: make the gate able to fail on the surface that actually ships.
Output: a re-measured floor, rewritten rows, an acceptance run through the new grid, browser checks
at both sizes in both browsers, and 05-11's script brought up to date without being renumbered.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-fight-loop-playtest/05-REDIRECT-D27.md
@.planning/phases/05-fight-loop-playtest/05-CONTEXT.md
@.planning/phases/05-fight-loop-playtest/05-VIEWPORT-FIX.md
@.planning/phases/05-fight-loop-playtest/05-01-SUMMARY.md
@.planning/phases/05-fight-loop-playtest/05-10-SUMMARY.md
@.planning/phases/05-fight-loop-playtest/05-12-SUMMARY.md
@.planning/phases/05-fight-loop-playtest/05-13-SUMMARY.md
@.planning/phases/05-fight-loop-playtest/05-14-SUMMARY.md
@.planning/phases/05-fight-loop-playtest/05-15-SUMMARY.md
@.planning/phases/05-fight-loop-playtest/05-11-PLAN.md
@.planning/phases/05-fight-loop-playtest/deferred-items.md
@./CLAUDE.md
@cats-vs-mechs.html
@tests/selftest-node.cjs
@tests/browser-checks.mjs

<interfaces>
<!-- Read off the live harness. Re-grep every line number before trusting it. -->

check 92's drive :5432-5453 — `startFight`, one declaration a side, `advanceRound`, one declaration
  a side again, then `harvestInto(dom.byId['app'], [], '#app')`. The second pair exists because
  `advanceRound` EMPTIES the declaration list and a harvest taken straight after an Advance would
  close the ledger's hole and leave the declaration list's open. **That reason still holds.** The
  board is put back with `endFight` and the putting back is asserted (`fightBoardBack`).
`FIGHT_FLOOR = 120` :5663, with the history comment :5490-5662 — four entries, each with its
  three-roster table and its per-card arithmetic:
    41 → 56 (05-07 task 1, +15 flat on every board)
    56 → 83 (05-07 task 2, per-card 5 → 11)
    83 → 108 (05-08, per-card 11 → 14, drive changed)
    108 → 120 (05-09, per-card 14 → 25, three strings of it read TWICE through `aria-label`)
  **The method is the requirement, not the number:** trim the roster BEFORE `startFight` — a
  mid-fight `removeUnit` moves the build and leaves the fight slice, the ledger and the record
  holding every unit, and a per-card figure taken that way measures the setup chrome alone.
`SUITE_FLOOR` :614. Layer C's `#app` (setup) floor 117. `DIALOG_FLOOR` 138. `PROPOSE_FLOOR` 23.
Fight rows :8240-9100 — 93 (act partition, six ops named), 93b (listeners + floors), 93c (the
  keyboard's own ramp), 94 (no `data-k`/`data-act`/`data-amt`/`.brd-value`/`.brd-line--opt` inside
  `#ledger`), 94b (every `data-k` on the page unique), 96 (an Advance moves state AND page), 97
  (D-13's rendered half, a LEAF walk), 98 (the dead marker off the stored flag, four readings each),
  99 (the focus contract over a rebuilt list), 100 (an op that changes what is drawn without moving
  a stepper — probe S's character), 101 (REF-03 with a fight being PLAYED), 102 (the acceptance run,
  six values off the page).
  Row 102's recorded reading includes `"CatsAction points0 of 3 spent3 left to spend"` — **read back
  VERBATIM rather than asserted around**, deliberately, to put the gate on the record about
  FIGHT-09. That is limitations entry 30 and D-27 closes it.
The limitations list — entries 1-20 stand; 5 and 13 were re-read and extended; 21-32 are plan
  05-11's script. Entry 21 (viewport) was rewritten by the viewport fix; 30 (the spent reading) and
  32 (whether a student finds the declaration step) are the two D-27 moves most.
`tests/browser-checks.mjs` — 22 checks, both browsers, `PLAYWRIGHT_DIR`, `channel:'chrome'` /
  `'msedge'`, skips cleanly with exit 0 when Playwright is unresolvable, and is **not shipped**.
  Its header's RUNNING IT block and its skip contract are not to be changed.
`.planning/REHEARSAL.md` B3 and `deferred-items.md` items 1 and 2 — read both before writing either.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: check 92's drive through the new grid, and FIGHT_FLOOR's fifth history entry</name>
  <files>tests/selftest-node.cjs</files>
  <action>
**Layer C must read the fight tab's rendered words.** Re-drive check 92's harvest so it is taken on
the page the student actually plays on:
  - the view is on the fight tab (drive it the way a student would — through `startFight`, and
    assert the view followed, so this row also covers plan 05-12's edge);
  - **declarations are standing on BOTH sides when the harvest is taken.** Probe X's finding is the
    reason and it has not changed: the same board harvests clean with the shipped spelling and red
    with a single-string one, and a row that harvests an empty declaration list cannot see either
    outcome. The grid's per-row readings, its cost nodes and its requirement sentences are the new
    strings, and not one of them exists as a literal anywhere — Layers A and B read the source, so
    this layer is the only one that can ever read them;
  - a round has resolved, so the ledger has a row and the what-changed reading has something to say
    (plan 05-08's reason, unchanged);
  - **the battlefield is painted, on a board carrying a student-RENAMED token type and a
    student-authored one.** New with plan 05-15, and it is the largest single block of strings this
    surface has ever added: a unit name per unit, a compacted count wherever a row passes
    `COMPACT_AT`, a token label per type per unit, and a dead marker with its accessible name. **It
    is also where a renamed type's word enters the harvest**, which is check 47d's territory arriving
    on a third surface — so drive the rename through the real op, never a planted string, and assert
    the harvest stays clean;
  - **at least one unit is ruled dead and one is at zero health unruled**, so the dead marker and the
    still-standing reading are both on the page when the walk runs;
  - **a retarget is half-made on one side**, so the lit state's strings and the change-target
    button's label are harvested;
  - **at least one action button is DISABLED when the harvest is taken.** New with D-27, and it is
    the one state the walk has never read: drive a side's pool or a requirement so the three
    conditions bite, and assert the harvest is still clean. A disabled control's text and accessible
    name are still harvested, so this is where a "can't afford" sentence would be caught;
  - the board is still put back afterwards and the putting back is still asserted.

**Then re-measure `FIGHT_FLOOR` by the method the comment prescribes, not by reasoning about it.**
Measure on ONE artifact, on three roster sizes, trimming the rosters **before** `startFight`:
  - the suite's board at check 92 (12 cards), a 4-card board and a 2-card board;
  - derive the per-card marginal cost from the deltas, not from the totals — it has moved on every
    single one of the four previous entries (5 → 11 → 14 → 25) and a plan reading only the totals
    would have got the floor wrong four times running;
  - the roster-independent part is `total − cards × per-card`, and it must come to the same number
    on all three boards. If it does not, that disagreement is the finding — record it and find out
    why before setting the floor.
Set `FIGHT_FLOOR` to the roster-independent part measured **off the board this row actually
harvests**, and write the fifth history entry in the shape of the four above it: the plan, the
three-roster table, the per-card move with the extra strings **named one at a time**, and the axes.

**Name the axes that move the roster-independent part, because three of them are new.** The existing
three are the action count, the number of resolved rounds and the number of sides. This phase adds:
  - **one picker row per unit × one button per action**, so the per-unit cost now moves with the
    ACTION count too — a unit on the fight page is no longer a fixed number of strings;
  - **one battlefield shape per unit**, whose own string count moves with **how many TOKEN TYPES that
    unit holds a non-zero amount of** and with whether each of its rows has compacted — a compacted
    row is a count string where an uncompacted one is none at all, so **a bigger number can mean MORE
    strings, not fewer**, and a floor taken on a fresh board can be cleared by a board mid-fight;
  - **the token vocabulary itself**, because a student who invents a type adds a label per unit that
    holds it.
Work out whether the per-unit figure is now a product rather than a constant, and if it is, say so
and set the floor at the minimum of every axis (the six shipped actions cannot be removed and the
three shipped token types cannot be removed, so both have floors). **This is the single most likely
place for this plan to ship a floor that bounds nothing**, and it is why the measurement is taken on
three roster sizes rather than reasoned about from the totals.

Also re-read and re-record, without moving unless the reading moves: `#app` (setup) 127 and its floor
117; `DIALOG_FLOOR` 138 across 4 roots; `PROPOSE_FLOOR` 23; `SUITE_FLOOR`. **A floor moves when the
surface it bounds gains something roster-independent, not because a plan touched the file** — plan
05-09's and the viewport fix's recorded correction, and it binds here in both directions.

**PROBE AS — the harvest taken with NOTHING declared** (probe X's shape, driven again against the
new grid). Record what the floor would have been and what the walk would have missed. **Expect the
row to still pass on the old drive, and record that GREEN as the finding**, in probe X's and probe
U(c)'s register: it is the measurement that says the drive is load-bearing, not the assertion.

**PROBE AT — one grid string rendered as a single node**, with an action really named after a
comparative word, driven through the real rename op and the real declare op and never a planted
string. Expect check 92 to redden naming the word. **If it stays green, the harvest is not reaching
the grid — fix the drive and record what was wrong.**

Both probes: run after the task's commit, record verbatim, revert with a `cp` from a scratchpad
snapshot, `git status --short` clean after. **`git checkout --` is never used.**
  </action>
  <verify>
    <automated>node tests/selftest-node.cjs</automated>
  </verify>
  <done>
`node tests/selftest-node.cjs` exits 0. `FIGHT_FLOOR` re-measured with the three-roster table, the
per-card arithmetic and the axes written into the history comment as entry five. Every other floor
re-read and either unmoved with the reading recorded, or moved with the move recorded. Both probes
recorded verbatim with a clean tree after each revert.
  </done>
</task>

<task type="auto">
  <name>Task 2: every gate row about the old form rewritten, and the acceptance run re-driven</name>
  <files>tests/selftest-node.cjs</files>
  <action>
Go through the fight rows one at a time. **A row whose claim has moved is rewritten to the new claim.
A row is never deleted and never quietly weakened, and a row that no longer has anything to assert is
replaced by the row that does** — 72b's own words about a deleted boundary assertion, and the
treatment `[S09.10]`'s rows and check 95 both got.

For each row below, state in the summary: what it asserted, what it asserts now, and its reading.

  - **93 (the act partition).** Six ops were named in its label; the surface now dispatches a
    different set — `declare` and `clearDeclaration` survive, `declare`'s payload is built
    differently, and the private-attribute count has moved twice (plan 05-12's `data-vw`, plan
    05-14's retired `data-fg` kinds). Re-read the partition off the page and re-name every op in the
    label. The two halves that must survive: a control naming an act nobody registered is caught,
    and a state op quietly moved into `UI_ACTS` is caught.
  - **93b (listeners and floors).** `#views` is a third root. Extend, floor it, and keep the
    argument: a root carrying no listeners passes a per-listener test spotlessly.
  - **93c (the keyboard's own ramp).** It presses a control that may no longer exist. Re-point it at
    Advance and at an action button. **Then answer, in writing, whether an action button needs the
    suppression at all** — a held Enter on it now toggles a declaration at the OS auto-repeat rate,
    which is a different cost from Advance's forty rounds but not obviously an acceptable one.
    If it needs it, it is already covered by `[data-fg]`; assert that, do not assume it.
  - **94 / 94b (the ledger's attributes, and key uniqueness).** The grid writes a `data-k` per unit
    per action — the key space grew by roughly `units × actions`. Re-read the count, keep the
    uniqueness claim, and keep probe AG's measured hazard in the comment: `keyed(#board)` survives
    because `#ledger` sits outside it, and `keyed(#app)` returns a ledger row, so any future plan
    widening a focus restore to the shell hands the keyboard to a history entry.
  - **96 (an Advance moves state AND page).** Its drive declares through the old form. Re-drive
    through the grid; the claim is unchanged and must stay unchanged.
  - **97 (D-13's rendered half).** A leaf walk over the live reading. Re-read its leaf count. **And
    extend it over the grid**, which now renders per-side readings a leaf at a time: no single leaf
    of the fight tab may name both factions.
  - **98 (the dead marker off the stored flag).** Its four readings are on the board's unit card and
    survive. **Add the fifth reading D-27 creates:** a unit ruled dead has its action buttons
    disabled on the grid, and a unit at zero health that nobody ruled does **not** — which is the
    direction a tidy implementation loses, and it is now a disable rather than a marker.
  - **99 (the focus contract over a rebuilt list).** The rebuilt list is now the grid. Re-point it,
    keep both halves — the node was replaced, and the keyboard came back onto its `data-k` — and
    keep the recorded note that the POINTER path drops to `<body>` file-wide (`deferred-items.md`
    item 1) and this row does not see it.
  - **100 (an op that changes what is drawn without moving a stepper).** Probe S's character. A
    rename still does it; assert the bar says the new action name, the ledger says the new token
    name, and the already-drawn ledger row is the SAME NODE.
  - **the battlefield rows plan 05-15 added.** Re-read each and record its reading. **And add the
    one that only this plan can add**: the battlefield's strings are in check 92's harvest, counted,
    so a later plan that stops painting it moves the floor rather than passing quietly.
  - **101 (REF-03 with a fight being PLAYED).** Re-drive **on the fight tab**, because REF-03 is
    *"readable without leaving the fight view"* and the tab is now what "the fight view" means. This
    row and plan 05-12's 103b are the two halves of that requirement and neither is the other.
  - **102 (the acceptance run).** Re-drive every press through the grid: start, declare on both
    sides — **one action declared in a single press and landing on the lowest-health living enemy by
    default, and one retargeted through the change-target button and a click on the battlefield** —
    advance, rule a unit dead and back, undo, reset. Read the default target back off the RECORD and
    against `App.model.defaultAt`'s own answer, so the run asserts the surface uses the derivation
    rather than re-implementing it. Read the six values back off the page. **Read the pool line back
    VERBATIM**, as the row already does, and this time it is a spoken-for reading that MOVES:
    record it before and after a declaration, and before and after undoing one. That is limitations
    entry 30 closed by measurement rather than by assertion, and this row is where the gate goes on
    the record about it. Keep the row's existing honest clause about the board's health row drawing
    the BUILD allocation, so a later plan pointing it at the fight slice still reddens.
  - **the rows plans 05-12 and 05-14 added.** Re-read each and record its reading; change a claim
    only if this plan's own drives show it asserting something it does not mean. **Check 95 and the
    re-homed disabled row are plan 05-14's and are not touched here** — record their readings and
    say so.

Then update the **limitations list**, which is plan 05-11's script:
  - **entry 5** — re-read with the tab built. Say what the fight-mode harvest now covers and what it
    still does not.
  - **entry 13** — re-read. Name what is still not driven: a fight carried past two rounds so a
    record rolls off `MAX_PAST_ROUNDS`; a declaration naming nobody; an action whose cost is in a
    type a student invented; and every line the error panel would carry.
  - **entry 21 (the viewport)** — the tab is the structural answer. Rewrite it with the measured
    before/after, and be explicit about what the tab did **not** fix.
  - **entry 24** — whether a past round reads as *past* rather than as *disabled* is now sharper,
    because the grid ships a real disabled state a few hundred pixels away. Say so.
  - **entry 30 (the spent reading)** — closed by D-27. Rewrite it into a fixed item pointing at the
    spoken-for reading and row 102's before/after.
  - **entry 32 (whether a student finds the declaration step)** — D-27 is the answer to it and it
    stays open in a different form: whether a student finds the **tab**.
  - **new entries** for what only a room can settle about the new surface: whether a disabled action
    reads as *the board says no* rather than as *the tool says no*; whether a grid of
    `units × actions` buttons is legible from the back or is a wall; **whether a student notices that
    a target was chosen for them, and whether the change-target button reads as a change rather than
    as a correction**; **whether a lowest-health default nudges a room toward focus fire, which is the
    one thing in this phase where the tool makes a suggestion**; **whether the battlefield reads as
    the fight and the picker rows as the paperwork, or the two compete**; **whether a unit's tokens
    are legible at battlefield scale from the back of the room, or want their own `--tok`**; and
    whether the two-column grid plus two battlefield clusters hold at 24-a-side on a real projector.
    Number them from where 32 leaves off.

**PROBE AU — the acceptance run's pool line asserted rather than read back verbatim.** Change the
row to compare against a hard-coded expected string and record what that hides. **Then revert it** —
the verbatim reading is the point, and this probe exists to record why in the file rather than only
in a summary.
  </action>
  <verify>
    <automated>node tests/selftest-node.cjs</automated>
  </verify>
  <done>
Suite green, interaction gate at its full count with every fight row's OLD claim and NEW claim
written out side by side in the summary, each with its reading. The limitations list updated with
every entry named. `SUITE_FLOOR` and every Layer C floor re-read and recorded. Probe AU recorded
verbatim and reverted.
  </done>
</task>

<task type="auto">
  <name>Task 3: browser checks at both sizes in both browsers, and 05-11's script brought up to date</name>
  <files>tests/browser-checks.mjs, .planning/phases/05-fight-loop-playtest/05-11-PLAN.md</files>
  <action>
**Extend `tests/browser-checks.mjs`.** Keep its header, its `PLAYWRIGHT_DIR` loader and its
skip-cleanly-with-exit-0 contract exactly as they are — a fresh checkout must stay a working
checkout and CI must go on not caring. Add numbered checks that run in **real Chrome and real Edge**
at **1920x1080 and 1366x768**, four combinations, every reading taken in both:

  1. **The tab.** Both controls present and pressable; pressing each switches the view; `#views`,
     `.fg-band` and `#board` all report the same `left` and `width`.
  2. **The grid lays out, in the addendum's order.** With a fight running: both columns on screen at
     once, and each column reading top to bottom as `[SIDE]` → battlefield cluster → team resources
     → picker rows, verified by comparing each child's `getBoundingClientRect().top` rather than by
     reading the DOM order. `[ROUND]` spans above both columns. One picker row per unit,
     `units × actions` action buttons counted, and the whole grid's box inside the viewport at both
     sizes — on the shipped 9-and-3 board **and** at 24-a-side.
  2b. **The battlefield's geometry.** Two clusters, one per column; Cats' cluster left of Mechs'
     (compare their `left` values, so "left" and "right" are measured rather than assumed); one
     labelled shape per unit, counted against the roster; each shape's box non-zero; the token
     mini-shapes inside a shape non-zero and their computed `clip-path` non-`none` for a shape that
     is not a plain square — which is the claim that they really are CSS shapes. Taken on the shipped
     board, at 24-a-side, and on a board carrying a student-authored token type with an authored
     glyph. Record the cluster heights, because they are what moves `.fg-sides`' bound.
  2c. **A unit ruled dead stays drawn.** Rule one dead through the board tab, switch back, and read
     the battlefield: the shape is still there, its box is still non-zero, and its marker node is
     present with its accessible name. FIGHT-06 measured rather than asserted.
  3. **The live board and the fight state are reachable.** Switch to the board view mid-fight and
     read `#board`'s top; it must be reachable without the board being below the fold. Record the
     number rather than asserting a threshold nobody measured.
  4. **`#strip` still pins.** `position` is `sticky`, every ancestor's `overflow` is `visible`, and
     its viewport top at page scroll 0 / 800 / 1600 / 2400 settles, in **both** views. `#refband`'s
     box is non-zero in both views.
  5. **A full round by real clicks.** Start the fight; declare an untargeted action on one side and
     a target-directed one on the other **in one press each**; read the team resources before and
     after each declaration; press Advance; read the round and the ledger back. Every press a real
     click.
  5b. **The change-target flow, end to end by real clicks.** Read the default target off the row's
     own reading; press the change-target button and confirm the opposing side's battlefield shapes
     light (counted, and the count equals that roster); click one and confirm the row's reading names
     the unit that was clicked; press the change-target button twice and confirm the declaration is
     unchanged; press the declared action again and confirm it and its target both go. **Every one a
     real click on a real element**, and the lit state read from the computed style and the `aria-`
     attribute rather than from the class alone.
  6. **The disabled state is distinguishable without colour.** Drive a board where the three
     conditions bite and read back, for a disabled button and an enabled one: computed opacity,
     border style and width, and `disabled`. Assert they differ in **at least two** channels that
     are not hue.
  7. **Page errors and console errors are `[]`** on every run, in both browsers.

Record every number in the summary as a four-column table (Chrome/Edge × 1080/768), in the viewport
fix's manner. Where the two browsers disagree, that disagreement is the finding.

**Then update `05-11-PLAN.md` surgically. Do not renumber it, do not rewrite it, and do not move it
— it remains the blocking playtest and it runs after these plans, on the new surface.** Edit only
where it references a surface that no longer exists, and **mark every edit inline as a D-27 edit**
so a reader can see what moved and why:

  - **item 3 (D-23).** It asks the developer to confirm that a declaration you cannot afford is
    reported and never prevented. **D-27 overrules that, on their own word, for the fight
    declaration surface only.** Rewrite the item to ask the question that is now open: the
    declaration surface disables what cannot be declared, the never-disable rule still holds
    everywhere else, and Advance still lets a side over-commit through a mid-fight build edit — is
    that the line they meant, and does a disabled action read as *the board says no* or as *the tool
    says no*?
  - **item 14 (does the fight region fit above the board).** The tab dissolved the question. Replace
    it with the one that is left: with the fight and the board on separate tabs, is anything you
    need to see at once now on two screens?
  - **item 20 (spent visibly distinct from available).** Rewrite onto the spoken-for reading.
  - **items in section C** that quote plan 05-10's limitations entries by number: re-point each at
    the entry as task 2 left it.
  - **the pre-rehearsal gate figures** quoted anywhere in it: re-quote from this plan's final run.
  - **item 2 (PROJ-05, the two readings side by side).** The projection and the fight are now on the
    same tab as each other but the board's unit cards are not. Re-point the item at what a student
    actually has in front of them: the battlefield, the projection strip and the reference band, all
    on the fight tab at once.
  - **add the questions D-27 and its addendum opened and did not settle**, each as a numbered item in
    the file's own register, verbatim-tagged where the answer must be the developer's words:
      * the default target is the **lowest-health living enemy**, ties broken by roster order. That is
        a mild focus-fire suggestion by the tool, chosen explicitly by the developer and changeable in
        one press. **Did it feel like help or like the tool playing?** And did you ever want a
        different default — highest health, most recently hit, or none at all?
      * the target flow lights only the **opposing** side, which narrows `declareAction`'s
        recorded ruling that a target may be any unit on the board — a heal that lands on a friend is
        unreachable from this surface, and a heal-shaped action given an enemy by default is the case
        that surfaces it. **Widening it is one line.** Confirm or widen.
      * the battlefield is **read-only except as the target picker**. Did you reach for it to rule a
        unit dead or to change a number, and have to go to the board tab? Should the hand rulings
        come to it?
      * the battlefield draws the **fight's** health while the board tab's unit cards draw the
        **build's allocation** (FIGHT-10's division). Two health readings on two tabs. **Is that the
        line you meant, or should the board tab follow the fight?**
      * the row's unit name is a **label and not a button**, because the addendum moved targeting onto
        the battlefield and a control with nothing to do is worse than a label. Confirm, or say what
        pressing a unit in the picker should do.
      * the round figure is on the topbar **and** at the top of the tab. Keep both, or name the one
        to retire.
      * the per-declaration Clear and the "Declared so far" list are gone; re-clicking the action is
        the undo. Is that discoverable?
      * requirements are read on the **caster side**, not on the performing unit — the shipped
        machinery's own rule, unchanged by D-27. So a unit's own health does not decide whether its
        action is available. Confirm, or say it should be per-unit.
  - **Do not touch** its playtest core: two played fights, hot-seat, adjudicated as a student would,
    judged on whether the shipped default feels contested. That is the gate and nothing here dilutes
    it. **`DEFAULTS.cats.ap` stays untouched (D-25) — the playtest still owns it.**

Also update `.planning/REHEARSAL.md` B3 and `deferred-items.md` where they now describe a surface
that has moved, and log any browser finding this plan cannot fix (it edits no artifact) to
`deferred-items.md` with its measurement.
  </action>
  <verify>
    <automated>node tests/selftest-node.cjs</automated>
  </verify>
  <done>
`node tests/selftest-node.cjs` exits 0.
`PLAYWRIGHT_DIR=... node tests/browser-checks.mjs` reports **22 + N passed, 0 failed**, with N named
check by check, in both browsers, at both viewports — including the battlefield's geometry, the
dead-stays-drawn reading and the change-target flow driven end to end by real clicks. Running it with Playwright unresolvable still
exits 0 with the SKIP line — verify, do not assume. `05-11-PLAN.md` still numbered 11, its playtest
core untouched, every edit marked inline as a D-27 edit and listed in the summary. `git diff` over
`cats-vs-mechs.html` is **empty** for this whole plan — prove it.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness drive → rendered page | a row that drives a control that no longer exists asserts nothing and stays green |
| dev-only browser checks → CI | `browser-checks.mjs` must never become a dependency or a required gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-63 | Repudiation | a floor left unmoved when the surface it bounds gained a whole grid, so it bounds nothing | mitigate | `FIGHT_FLOOR` re-measured on three rosters by the prescribed method, with the fifth history entry and the new axes named; **probe AS records what the old drive misses** |
| T-05-64 | Repudiation | a row rewritten until the old number comes back rather than until the new claim is true | mitigate | every row's OLD and NEW claim written side by side with its reading; **probe AT drives a verdict word through the real ops** |
| T-05-65 | Information disclosure | the acceptance run asserting around a reading instead of printing it | mitigate | the pool line read back verbatim before and after a declaration and an undo; **probe AU records what asserting would hide** |
| T-05-81 | Repudiation | the battlefield's strings absent from check 92's harvest, so the largest new surface in the phase is invisible to the only layer that reads render-time copy | mitigate | the drive paints it, on a board carrying a renamed and an authored token type, with a unit ruled dead and a retarget half-made; the string count is recorded against the floor |
| T-05-82 | Repudiation | a floor cleared by a fresh board and unreachable mid-fight, because a compacted token row is MORE strings than an uncompacted one | mitigate | the floor is measured at the minimum of every axis, on three roster sizes, with the compaction axis named in the fifth history entry |
| T-05-66 | Denial of service | a browser check becoming a required gate and breaking a fresh checkout | accept | the skip-cleanly contract is re-verified with Playwright unresolvable; the file is dev-only and not shipped |
| T-05-67 | Tampering | 05-11 renumbered, rewritten or diluted while being brought up to date | mitigate | surgical edits only, each marked inline; the playtest core untouched; the plan stays plan 11 and stays blocking |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | zero packages installed; Playwright resolved through `PLAYWRIGHT_DIR` and nothing added to the repository |
</threat_model>

<verification>
- `node tests/selftest-node.cjs` exits 0; suite, `SUITE_FLOOR`, interaction gate, stub-drift, `#app`
  (setup), `#app` (fight) + `FIGHT_FLOOR`, dialogs, proposal pane, Layer A, Layer B, perf and the
  no-writer gate all recorded before and after, with every move explained.
- `node tests/browser-checks.mjs` green in both browsers at both viewports; and green-by-SKIP with
  Playwright unresolvable.
- `git diff cats-vs-mechs.html` is empty for this plan. Any artifact finding is in
  `deferred-items.md` with its measurement.
- Both naming greps 0 over the artifact; `url(` 0; `innerHTML` 0.
- No test label carries a comparative word or one of the eight mechanically-clean-but-unshippable
  words. A round is never a "Day".
- `DEFAULTS.cats.ap` untouched (D-25).
</verification>

<success_criteria>
Every row that watches the fight watches the surface that ships. The floor bounds a page somebody
measured. The acceptance run presses the grid a student presses and prints back a pool figure that
moves. Two browsers at two sizes agree that a whole round can be played by clicking. And plan 05-11
— still plan 11, still blocking — asks the developer about the tool they will actually be holding.
</success_criteria>

<output>
Create `.planning/phases/05-fight-loop-playtest/05-16-SUMMARY.md` when done.
</output>
