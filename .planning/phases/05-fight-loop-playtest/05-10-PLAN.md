---
phase: 05-fight-loop-playtest
plan: 10
type: execute
wave: 10
depends_on: ["05-09"]
files_modified:
  - cats-vs-mechs.html
  - tests/selftest-node.cjs
autonomous: true
requirements: [FIGHT-01, FIGHT-02, FIGHT-04, FIGHT-05, FIGHT-07, FIGHT-12, FIGHT-13, REF-03, SHARE-07]
section_ownership: >
  cats-vs-mechs.html — a new `[S07.5]` sub-region attached through the four seams, its `[S00]` line,
  and the `[S07]` export block including `UI_HANDLED`. It edits NOT ONE LINE of `[S07.1]` and adds no
  `fire()` payload key. tests/selftest-node.cjs — the interaction gate's new checks, numbered from
  **92** onward after plan 05-01's check 92 (renumber from the next free number that plan actually
  left), and the harness limitations list. Touches no `[S01]`–`[S06]` region, no `<style>` block and
  no static shell markup.

must_haves:
  truths:
    - "A person can start a fight, declare for both sides, advance, undo, apply a hand ruling, toggle a unit alive, and reset the fight — every one of them through a real press on a real control."
    - "Every fight control is either page work handled in this region or a real op that is dispatched, and the gate reads that partition off the page rather than out of a comment."
    - "No ledger row carries an attribute the interaction layer dispatches on or the sync pass writes, asserted by walking the rendered region."
    - "The phase's own acceptance run drives a whole round from the page and reads six values back off it."
  artifacts:
    - path: "cats-vs-mechs.html"
      provides: "[S07.5] — every fight listener, through App.boot.wrap, attached from the four seams"
      contains: "[S07.5]"
    - path: "tests/selftest-node.cjs"
      provides: "the interaction gate's fight checks, including the act partition and the acceptance run"
      contains: "advanceRound"
  key_links:
    - from: "[S07.5]"
      to: "UI_ACTS / UI_HANDLERS / HOLD_ACTS / LATE_BINDERS"
      via: "pushing and assigning; not one line of [S07.1] is edited"
      pattern: "LATE_BINDERS"
    - from: "every listener"
      to: "App.boot.wrap"
      via: "an anonymous zero-arity function, which check 68c reads back structurally"
      pattern: "App\\.boot\\.wrap"
---

<objective>
Make every one of it pressable, and make the gate able to fail on it.

`[S07]`'s banner reserved this: *"A plan adding a dialog, a reference card or a fight control writes
into `UI_ACTS` / `UI_HANDLERS` / `HOLD_ACTS` / `LATE_BINDERS` from its own `[S07.N]` sub-region and
edits no line of `[S07.1]`."* And `UI_ACTS`' own comment names *"Phase 5's fight controls"* by name as
an arrival it was built for. Plans 02-03, 03.1-05 and 04-06 each did exactly this and each recorded
*"changed NOT ONE LINE of `[S07.1]`"*. This makes four.

The partition matters more here than it has anywhere, and check 90b reads it off the page:

| Kind | Goes in | This phase's |
|---|---|---|
| Page work | `UI_ACTS` + `UI_HANDLERS`, handled here, **never dispatched** | picking which action a declaration names; moving between declaration slots; scrolling the ledger |
| State work | a real `[S05]` op, **dispatched, deliberately absent from `UI_ACTS`** | `startFight`, `resetFight`, `declareAction`, `clearDeclaration`, `advanceRound`, `setUnitHp`, `setFightShield`, `nudgeFightHp`, `setAlive` |

`UI_ACTS`' own words for why: an entry there is a name this region handles itself instead of
dispatching, *"which is exactly how a refusal is made to stop being raised."* **No fight op may be
parked in `UI_ACTS`.**

And this plan carries the phase's own acceptance run, because 16a's split is unforgiving: every
`[S09.*]` row this phase wrote sits above a no-DOM bracket and runs in CI, and **every surface
assertion has to live here instead.** The declaration surface's presses, the ledger's growth, the
dead marker, the mid-fight notice and the whole of Layer C's fight-mode reading are page work. This is
where they become able to fail a build.

Purpose: the presses, and the gate that watches them. Output: `[S07.5]`, and the checks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-fight-loop-playtest/05-CONTEXT.md
@.planning/phases/05-fight-loop-playtest/05-RESEARCH.md
@.planning/phases/05-fight-loop-playtest/05-PATTERNS.md
@.planning/phases/05-fight-loop-playtest/05-01-SUMMARY.md
@.planning/phases/05-fight-loop-playtest/05-06-SUMMARY.md
@.planning/phases/05-fight-loop-playtest/05-07-SUMMARY.md
@.planning/phases/05-fight-loop-playtest/05-08-SUMMARY.md
@.planning/phases/05-fight-loop-playtest/05-09-SUMMARY.md
@./CLAUDE.md
@cats-vs-mechs.html
@tests/selftest-node.cjs

<interfaces>
<!-- Extracted from the live artifact. Re-grep before trusting a line number. -->

[S07.1] the four seams :9752 (UI_ACTS), :9764 (UI_HANDLERS), :9779 (HOLD_ACTS), :9785 (LATE_BINDERS).
  Live registrations to extend BY PUSHING: UI_ACTS.push :11785, :12652; LATE_BINDERS.push :10814,
  :11516, :11776, :12616, :12641; SYNC_HOOKS.push :7886, :8198, :8351, :9411, :9661.
[S07.1] HOLD_ACTS :9765-9778 — "only the nudge ops return a boolean; undo, addUnit and removeUnit
  return undefined, so the ramp would run until release and then throw non-terminally on the first
  bound it met … flashing the error panel mid-demo."  AN ADVANCE MUST NOT BE HELD (a hold would
  advance forty rounds). A fight-health nudge legitimately may be — plan 05-05 gave it a boolean
  return precisely to leave the choice open.
[S07.1] fire() :9901-9909 carries FOUR payload keys and tokenId is "plan 02.1-03's one recorded
  exception to the section banner" (:9894-9900). [S07.3] declined a second; [S07.4] declined again
  (:11833-11840). PHASE 5 DECLINES A THIRD TIME — every control inside the fight surface carries a
  private data-* its own delegated listener reads.
[S07.1] the FIELD regex /^([+-]?)(\d{1,3})$/ :9925 — ASCII + and − only, which is why signed() writes
  ASCII.
[S07.4] :11788-12653 — THE TEMPLATE. Its banner :11790-11840 is the partition table to copy
  literally. bindShare / bindResetAsk :12580-12641 — every listener through App.boot.wrap:
      dlg.addEventListener('pointerdown', App.boot.wrap('reset ask press', onResetPress));
  bindResetAsk's no-`cancel` paragraph :12618-12630 is the model for DECLINING a listener in writing:
  "A no-op listener bound to make the four roots look alike would be dead code inside the error
   boundary, which is a worse thing to hand the next reader than this paragraph."
[S07] export block :12655-12694, especially UI_HANDLED :12679, "read off the live registration at the
  moment the section closes."
onPickerPress :10222-region — DISPATCH FIRST, PAGE WORK AFTER: "An op that refuses throws out of here
  into [S08]'s listener boundary and the page work never runs." [S07.4] inverted this exactly once,
  for the clipboard, and RECORDED the inversion. Phase 5 has no reason to invert it.
isPrimaryPress(e) :10104 — the shared front gate; the click arm is `if (e.detail !== 0) { return; }`.

tests/selftest-node.cjs, the check shapes to copy verbatim:
  68c  the listener-boundary check, FLOORED on the count — "App.boot.wrap returns an ANONYMOUS
       zero-arity function … so a raw binding is visible by name and by arity. Floored on the count,
       because a root with no listeners at all passes a per-listener test spotlessly."
  68d / 90b  the act partition collected off the page: walk for every data-act, split against
       UI_ACTS / UI_HANDLED / App.ops, assert both halves.
  71c  the whole-`disabled`-set comparison across 33 controls keyed by data-k, on a board at zero
       action points AND a roster below every requirement.
  72/73 the nothing-lands pair — "asserts state identity AND page movement in one row. A row that
       compared only the state would be spotlessly green over a pane whose every press did nothing at
       all — and probe Y proved that is not hypothetical."
  63b  no reference card ON THE PAGE carries an attribute the interaction layer dispatches on or the
       sync pass writes.  <- THE PARALLEL FOR LEDGER ROWS
  65   the static row count read OUT OF THE REAL MARKUP, not the stub, "which is hand-written from
       the same source and would be asserting itself."
  62   :3308-3324 — starting a fight leaves every action card on the page while the setup-only Add
       button goes away. REF-03 pre-satisfied. Already ships; extend rather than duplicate.
matches() :610-region — a tag name, classes and [data-*] ONLY. No #id, no descendant combinator,
  no [hidden]. Stub textContent is a plain own property and is NON-RECURSIVE. Any probe reasoning
  over a subtree's text must WALK it.
The harness limitations list, entries 5, 12, 13 as plan 05-01 left them.

Baseline before this plan: interaction gate 147 of 147 (146 shipped + plan 05-01's fight harvest).
  Confirm the actual next free number from 05-01's SUMMARY rather than assuming it.
</interfaces>
</context>

<the_line_this_phase_walks>
**Dispatch first, page work after.** `onPickerPress`' rule: an op that refuses throws out of the
handler into `[S08]`'s listener boundary and the page work never runs. `[S07.4]` inverted this exactly
once, for the clipboard's user-gesture requirement, and recorded the inversion in writing. **This
phase has no reason to invert it and must not.**

**No fight op in `UI_ACTS`.** Every one of them is a real state transformation, every one is
dispatched, and check 90b reads the partition off the page so a move reddens rather than shipping.

**Nothing is disabled for anything a student did.** Check 71c's comparison is extended to this
phase's controls: the Advance control stays enabled on a side that cannot afford its declaration, the
declaration controls stay enabled at zero action points, and the alive toggle stays enabled on a dead
unit — that last one is the one this phase would break first, and it is what makes a Shield ruling
recoverable.

**An Advance is never held.** `HOLD_ACTS` is a press-and-hold ramp; a held Advance would resolve forty
rounds. A fight-health nudge may be held, and plan 05-05 gave it a boolean return so that this plan
can choose. Choose, and say which and why.
</the_line_this_phase_walks>

<naming_trap>
    grep -ci "counter\|rating\|balanced\|difficulty" cats-vs-mechs.html   # must print 0
    grep -c  "verdict\|balanced\|rating\|difficulty" cats-vs-mechs.html   # must print 0

`tests/selftest-node.cjs`'s own check labels are **not** scanned by Layer A, which reads the artifact
only. Writing verdict language into a check label would still be a policy failure. Do not — and note
that plan 05-01 widened the lists, so the words a fight check would reach for are the ones now closed.

The round is **never a "Day"**. A comment about **iterating** presses fails on `rating`.
</naming_trap>

<tasks>

<task type="auto">
  <name>Task 1: [S07.5] — every fight press, through the four seams, editing not one line of [S07.1]</name>
  <files>cats-vs-mechs.html</files>
  <read_first>
    - cats-vs-mechs.html :11788-11845 ([S07.4]'s banner and its partition table — copy it literally)
    - cats-vs-mechs.html :12570-12700 (bindShare, bindResetAsk, the no-`cancel` paragraph, and the
      [S07] export block with UI_HANDLED)
    - cats-vs-mechs.html :9740-9800 (all four seams and their comments, including HOLD_ACTS' boolean
      rule and UI_ACTS naming this phase)
    - cats-vs-mechs.html :9890-9930 (fire()'s four payload keys, its one recorded exception, and the
      FIELD regex)
    - cats-vs-mechs.html :10095-10115 and :10215-10240 (isPrimaryPress and the dispatch-first rule)
  </read_first>
  <action>
    Open `[S07.5]` as a sub-region, add its `[S00]` line, and open it with `[S07.4]`'s banner shape —
    including **the partition table, written out**, naming every act this region handles itself and
    every op it dispatches, and closing with `[S07.4]`'s own sentence: neither name is in `UI_ACTS`
    and neither ever may be, because an entry there is how a refusal stops being raised, and check 90b
    reads it off the page.

    **Attach, do not edit.** Push into `UI_ACTS`, assign into `UI_HANDLERS`, push into `LATE_BINDERS`,
    and — if a held control is wanted — push into `HOLD_ACTS`. Record *"changed NOT ONE LINE of
    `[S07.1]`"* the way the three prior plans each did, and prove it: `git diff` over `[S07.1]` must be
    empty.

    **The presses:**
      - **Start the fight** and **reset the fight** — topbar controls carrying `data-act`, dispatched.
        SHARE-07 is reachable here or nowhere;
      - **declare** — the action, the performer and what it points at, each a control inside the fight
        region carrying a **private `data-fg`** its own delegated listener reads, never `data-act`.
        `#act-prop-open`'s idiom is the shape: a private attribute plus a `data-k` slash path naming
        the id;
      - **clear a declaration**, and clear all;
      - **Advance** — dispatched, and **not** in `HOLD_ACTS`;
      - **the hand rulings** — health, shield and the alive toggle, dispatched. Decide whether the
        health nudge is held: plan 05-05 gave it a boolean return so it can be, and `HOLD_ACTS`'
        comment says exactly what happens to a ramp over an op that returns `undefined`. Say which and
        why, in the register `bindResetAsk` used for declining a listener — *a no-op bound for
        symmetry would be dead code inside the error boundary, which is a worse thing to hand the next
        reader than a paragraph.*

    **No second `fire()` payload key.** `[S07.3]` declined one and `[S07.4]` declined again; this
    declines a third time, and the region's own delegated listener builds each call field by field
    from the pressed control — which is exactly what 03.1-05 recorded doing.

    **Every listener through `App.boot.wrap`.** Check 68c reads the registrations back structurally —
    `wrap` returns an **anonymous zero-arity** function, so a raw binding is visible by name and by
    arity — and the check is floored on the count, because a root with no listeners at all passes a
    per-listener test spotlessly. Bind on a stable root that outlives every rebuild; `structure()`
    replaces only the two column interiors, so a listener bound inside a unit card would be thrown
    away, and the alive toggle lives inside one.

    **Dispatch first, page work after**, and `isPrimaryPress` as the shared front gate with the click
    arm's `if (e.detail !== 0) { return; }` for the keyboard double-fire.

    **Nothing disabled.** Not one line of this region writes `.disabled` for anything a student did.
    The Advance control stays enabled on an unaffordable declaration; the alive toggle stays enabled on
    a dead unit. Task 2 asserts both by whole-set comparison.

    Extend the `[S07]` export block including `UI_HANDLED`, *"read off the live registration at the
    moment the section closes."*
  </action>
  <verify>
    <automated>node tests/selftest-node.cjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/selftest-node.cjs` exits 0 at no fewer than 05-09's recorded total / 0 failed.
    - `git diff` over `[S07.1]` is **empty**. Record the confirmation.
    - `grep -c "\.disabled" ` over `[S07.5]` prints `0` — verify by reading and record the reading.
    - Check 68c's floored listener count is recorded, old and new, and the floor moved if it needed to.
    - Check 90b's partition is recorded: every `data-act` on the page, split against `UI_ACTS`,
      `UI_HANDLED` and `App.ops`, both halves asserted. **No fight op appears in `UI_ACTS`.**
    - The held-or-not decision is recorded with its reason.
    - Both naming greps print `0`.
  </acceptance_criteria>
  <done>Every fight control is pressable through a listener inside the error boundary, attached from the four seams with not one line of `[S07.1]` edited, with the page-work / state-work partition written out and readable off the page.</done>
</task>

<task type="auto">
  <name>Task 2: the interaction gate — the partition, the ledger's attributes, the disabled set, and the acceptance run</name>
  <files>tests/selftest-node.cjs</files>
  <read_first>
    - tests/selftest-node.cjs check 68c, 68d/90b, 71c and 72/73 in full — the four shapes to copy
    - tests/selftest-node.cjs check 63b (no dispatchable attribute on a reference card) and check 65
      (the static row count read out of the real markup) and check 62 (:3308-3324, REF-03)
    - tests/selftest-node.cjs :605-640 (matches()'s limits) and the stub `<dialog>` warning
    - tests/selftest-node.cjs the limitations list as plan 05-01 left it
    - the SUMMARYs of plans 05-06 through 05-09 for the ids, the class names, the static-row answer
      and every "record this" reading they produced
  </read_first>
  <action>
    Add the interaction gate's fight checks, numbered from the next free number plan 05-01 actually
    left. **Every one of them drives the page** — a check written against source spelling cannot see
    behaviour reached through a helper, which is Phase 3's WR-01 lesson and it is why the ledger is
    asserted by driving a real Advance and reading rows off the page rather than by grepping `[S06.8]`
    for an `appendChild`.

    **The act partition**, in 68d/90b's shape, extended over the fight region: walk it for every
    `data-act`, split against `UI_ACTS` / `UI_HANDLED` / `App.ops`, and assert both halves. Name the
    fight ops in the check's own label so a move reddens with an explanation rather than a number.

    **The ledger's attributes**, in check 63b's shape and it is *the cheap version of the whole
    focus-hazard section*: no node **on the page** inside the ledger root carries `data-k`,
    `data-act`, `data-amt`, `.brd-value` or `.brd-line--opt`. Walk the region; do not grep the source.

    **`data-k` uniqueness document-wide** for every control this phase added, because
    `withPreservedFocus` takes the first match scoped to `#board` and this phase added the first
    repeated region in the file's history.

    **The disabled set**, in check 71c's shape, extended: compare the **whole** disabled set across a
    fully funded board and one driven to zero action points **and** below every requirement, **with a
    fight running**, and additionally across a board with three units ruled dead. Record the control
    count. The alive toggle on a dead unit must be in neither set.

    **The nothing-lands pair's lesson, inverted for this phase.** Checks 72/73 assert state identity
    **and** page movement in one row, because *"a row that compared only the state would be spotlessly
    green over a pane whose every press did nothing at all — and probe Y proved that is not
    hypothetical."* Here the claim is the opposite — something **does** land — so assert both
    directions in one row too: after pressing Advance, the state moved **and** the page moved. A row
    that only read the state would be green over a press that changed nothing on screen.

    **The static row count**, if plan 05-07 shipped static declaration slots: read it **out of the real
    markup, not the stub**, in check 65's shape and for its recorded reason.

    **Layer C's fingerprint requirement.** Plan 04-05's probe S found that a narrowed fingerprint
    passed every share row except the one whose driving op *changes what is drawn without moving a
    stepper*. Drive a row of that character here — rename an action, or apply a by-hand ruling — and
    assert the fight surface repainted. Plan 05-07 was told to record this obligation; discharge it.

    **REF-03.** Check 62 already asserts that starting a fight leaves every action card on the page.
    Extend rather than duplicate: with a fight running **and** several rounds in the ledger, the action
    cards and the reference band are still on the page and still readable. That is REF-03, and the
    file's own comment says it is *"asserted by starting a fight and reading the cards back … rather
    than left resting on this paragraph."*

    **THE PHASE'S OWN ACCEPTANCE RUN**, in plan 04-07's shape — a run, not a list of assertions. From
    the page, with real presses: start a fight; declare for both sides naming a performer and a unit
    to point at; press Advance; read **six values back off the page** — the round, both pools, one
    unit's health, the ledger's row count, and the what-changed reading; apply a hand ruling and read
    the marker; toggle a unit dead and read that it is still in the roster; press undo and read the
    board come back; reset the fight and read that the build survived. Record every reading.

    **Then the limitations list.** Extend it rather than leaving the next reader to discover:
      - **entry 5** was closed for fight-mode copy by plan 05-01; confirm it still reads correctly now
        that the surface exists;
      - **entry 13** (words a surface paints only after something happens inside it) — a declaration
        slot a student has not opened paints nothing, so name what this gate does drive and what it
        does not;
      - **new entries for everything spatial**, each with a number, because plan 05-11's script is
        built from them: whether the fight region fits above the board on a laptop viewport; whether
        the ledger scrolling on itself keeps `#strip` sticky; whether two more topbar controls wrap
        the cluster and move `[S08]`'s measured bar height; whether past rounds read as past without
        reading as disabled; whether the round and both pools are legible from the back of a room;
        whether a dead unit is legible as dead without colour; whether the three-fact split is clearer
        than one number; and whether D-13's "no figure about both sides" has a mechanical check or
        only a rule (plan 05-09's probe AE answers this — carry its answer through).
  </action>
  <verify>
    <automated>node tests/selftest-node.cjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/selftest-node.cjs` exits 0. Record the interaction-gate count and the next free
      check number, the suite total, shell ids, Layer B literals, `#app`, `FIGHT_FLOOR`, dialogs
      across roots, `PROPOSE_FLOOR`, `PICKER_FLOOR`, `SHARE_FLOOR`, and the perf reading against its
      50 ms budget.
    - The acceptance run's six page-read values are recorded, plus the hand-ruling marker, the dead
      unit still in the roster, the undo, and the reset with the build surviving.
    - The disabled-set comparison is recorded with its control count across all three boards.
    - DELIBERATE-FAILURE PROBE AF, run and recorded verbatim, then reverted: move `advanceRound` into
      `UI_ACTS` and handle it locally. The partition check must redden and **name the op**. **If it
      stays green the walk is not covering the fight region** — fix it and record what it was walking.
    - DELIBERATE-FAILURE PROBE AG, run and recorded verbatim, then reverted: give one ledger row a
      `data-k`. The ledger-attribute check must redden. Then additionally assert what it costs: with
      that `data-k` present, drive a structural rebuild and record where focus lands — that is the
      measured hazard, reproduced in the gate rather than only in a research document.
    - DELIBERATE-FAILURE PROBE AH, run and recorded verbatim, then reverted: make the Advance handler
      dispatch nothing at all. The acceptance run's state-and-page row must redden **on both halves**.
      **A row that reddens on only one half is the 72/73 lesson unlearned** — record which halves fired.
    - DELIBERATE-FAILURE PROBE AI, run and recorded verbatim, then reverted: disable the alive toggle
      on a dead unit. The extended 71c comparison must redden. **If it stays green the comparison is
      not reaching this phase's controls** — fix it and record which controls it was comparing.
    - `grep -ci "counter\|rating\|balanced\|difficulty" cats-vs-mechs.html` prints `0`.
    - `grep -c "verdict\|balanced\|rating\|difficulty" cats-vs-mechs.html` prints `0`.
  </acceptance_criteria>
  <done>The fight surface is asserted by driving it — the partition off the page, the ledger's attributes by walking the region, the disabled set by whole-set comparison across three boards, and a whole round driven from presses with six values read back — and everything no automated check in this repo can reach is a numbered limitations entry that plan 05-11's script is built from.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a student press -> a dispatched op | the only path from a control to state, and the partition is what keeps it the only one |
| a rendered region -> the gate's ability to fail on it | every surface assertion in this phase lives here or nowhere |
| what the gate can reach -> what it claims | the limitations list is the boundary, and an unnamed gap is a claim the gate is not making |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-42 | Elevation of privilege | a fight op parked in `UI_ACTS` and handled locally, so a refusal stops being raised | mitigate | the partition read off the page in 90b's shape with the ops named in the label; probe AF drives the move |
| T-05-43 | Denial of service | a ledger row carrying `data-k` and stealing focus restore | mitigate | the region walked in 63b's shape, plus probe AG reproducing the measured focus hazard in the gate itself |
| T-05-44 | Repudiation | a press that changes state but nothing on screen, or the reverse, passing a one-sided row | mitigate | the acceptance run asserts state identity and page movement in one row, in 72/73's shape; probe AH requires both halves to fire |
| T-05-45 | Repudiation | a control disabled on a dead unit, making a ruling unrecoverable | mitigate | 71c's whole-set comparison extended over a board with units ruled dead; probe AI drives it |
| T-05-46 | Repudiation | a spatial failure shipping because nothing here can see it and nothing names it | mitigate | numbered limitations entries, each one an item in plan 05-11's script |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | zero packages installed by this phase |
</threat_model>

<verification>
- `node tests/selftest-node.cjs` exits 0, with every floor and every scan line recorded.
- `git diff` over `[S07.1]` is empty.
- Probes AF, AG, AH and AI each run, recorded verbatim, reverted.
- The acceptance run's readings are recorded value by value.
- Both naming greps print 0.

<human-check>
This plan is the last chance to name what a person has to check, and the limitations entries it writes
**are** plan 05-11's script. Anything spatial, anything about legibility on a projector, and anything
about whether a sentence reads as a fact or as an opinion is unreachable here by construction and must
leave this plan as a numbered item rather than as an impression.
</human-check>
</verification>

<success_criteria>
- Every fight control is pressable, inside the error boundary, attached from the four seams, with
  `[S07.1]` untouched.
- The page-work / state-work partition is readable off the page and no fight op is in `UI_ACTS`.
- The ledger carries no attribute anything dispatches on or writes to, asserted by walking the
  rendered region.
- Nothing is disabled for anything a student did, across three boards including one with units ruled
  dead.
- A whole round is driven from real presses with six values read back off the page.
- Everything this repo cannot reach is a numbered limitations entry.
</success_criteria>

<output>
Create `.planning/phases/05-fight-loop-playtest/05-10-SUMMARY.md` when done, recording: the full gate
output with every floor and scan line; the interaction-gate count and next free check number; the
empty `[S07.1]` diff; the partition as read off the page; the disabled-set comparison and its control
count across three boards; the acceptance run's readings value by value; the held-or-not decision and
its reason; probes AF, AG, AH and AI verbatim including where focus landed in AG; the full new
limitations list; and both naming-grep readings.
</output>
</content>
