---
phase: 05-fight-loop-playtest
plan: 09
type: execute
wave: 9
depends_on: ["05-08"]
files_modified:
  - cats-vs-mechs.html
autonomous: true
requirements: [FIGHT-05, FIGHT-06, FIGHT-07, FIGHT-10, PROJ-05]
section_ownership: >
  cats-vs-mechs.html — a new `[S06.9] RENDER — FIGHT MODE ON THE BOARD` sub-region registered on
  `SYNC_HOOKS` and on `[S06]`'s return, its `[S00]` line, and `[C14.3]`'s `.dc-` rules. It appends
  its own nodes into `#strip` and into each unit card from its own builder, changing NOT ONE LINE of
  `[S06.1]` or `[S06.3]` — the same sub-region contract `[S06.5]`, `[S06.6]`, `[S06.7]` and `[S06.8]`
  all keep. One declared exception: `unitCard`'s fight branch, an append into a region plan 02-01
  owns, in the register plan 03-05 used when it appended reference cards into `buildColumn`. Fills
  the notice node plan 05-06 reserved. Presses nothing.

must_haves:
  truths:
    - "A unit at zero health is marked on the board and stays in the roster, and the control that toggles it alive again is not inside the thing the marking hides."
    - "A number set by hand is marked as one on the board, and nothing about that marking is stored on the unit."
    - "A student editing the build while a fight is running is told, on screen, at the moment of the edit, that the edit applies to the build and not to this fight."
    - "The projection stays on screen through the whole fight, still describing the allocation it has always described, with the fight's own live figures beside it and labelled as a different thing."
  artifacts:
    - path: "cats-vs-mechs.html"
      provides: "[S06.9] — the dead marker, the by-hand marker, the mid-fight line and the live fight reading"
      contains: "[S06.9]"
    - path: "cats-vs-mechs.html"
      provides: "[C14.3] the .dc- rules — a dead unit legible as dead without relying on colour"
      contains: "[C14.3]"
  key_links:
    - from: "the by-hand marker"
      to: "the current round's ruling list"
      via: "derived at render time; nothing marks provenance on the unit"
      pattern: "past"
    - from: "the live fight reading"
      to: "App.model.turnsToWipe's third argument"
      via: "the parameter added two phases ago 'so Phase 5 can project a fight in progress'"
      pattern: "turnsToWipe"
---

<objective>
The board in fight mode, and the one requirement in this phase whose right answer two documents
disagree about.

**FIGHT-05 and FIGHT-06** are already half-shipped and the file says so. `DEFAULTS.tokens.dead` ships
as vocabulary *"with NO renderer consumer … the concrete proof that this renderer can already draw
Phase 5's dead marker — a rectangle carrying a skull — without any of Phase 5's mechanic being
built."* And the zero-tally hide pass at `:7386-7440` is FIGHT-06 already decided: *"a line the board
is built on is never touched here … A unit at zero health is something a student meant, and it stays
on screen."* That pass also carries the bug this plan must not repeat, in its own words: *"the rule
above took the control away on the last press of the − that reached zero: the button under the
student's finger went inside a `display:none` ancestor, which drops keyboard focus to the body."* **A
dead unit whose alive-toggle is inside the thing the death hides is that bug again.**

**FIGHT-07's marker** is the technique with no analog in this repo — nothing here marks a value's
provenance — and plan 05-05 already settled where the fact lives: in the round's ruling list, not on
the unit. This plan derives the marker from it and stores nothing.

**FIGHT-10's notice** has two shipped shapes to choose between and the file has already reconciled
them once: `#tok-pick-names` is *"a LINE and not a dialog … a line costs no click, adds no surface and
leaves removal one undoable commit, while saying the thing D-17 had nothing to say."* That is this
notice exactly.

**PROJ-05 is the disagreement.** `05-CONTEXT.md`'s D-22 assumes the projection reads the fight.
`05-RESEARCH.md`, written later and with the ROADMAP in hand, argues the opposite: *"a strip that
silently tracks the fight has nothing left to be contradicted"*, and the roadmap's whole teaching
moment is *"you guessed 3–5, it took 8."* This plan reconciles them rather than picking a side, and
puts the reconciliation to the developer at 05-11.

Purpose: the board tells the truth about a fight without deciding anything about it.
Output: `[S06.9]`, `[C14.3]`, and one requirement answered two ways on purpose.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-fight-loop-playtest/05-CONTEXT.md
@.planning/phases/05-fight-loop-playtest/05-RESEARCH.md
@.planning/phases/05-fight-loop-playtest/05-PATTERNS.md
@.planning/phases/05-fight-loop-playtest/05-05-SUMMARY.md
@.planning/phases/05-fight-loop-playtest/05-06-SUMMARY.md
@.planning/phases/05-fight-loop-playtest/05-08-SUMMARY.md
@./CLAUDE.md
@cats-vs-mechs.html
@tests/selftest-node.cjs

<interfaces>
<!-- Extracted from the live artifact. Re-grep before trusting a line number. -->

[S06.1] unitCard :6838-6877 — where the marker and the toggle go. `setup` already gates the remove
  button (:6843-6852), so a fight-only control is the mirror branch.
[S06.1] buildColumn :7072 derives setup = (state.fight === null); :7089-7107 is plan 03-05's
  cross-plan append and its paragraph is the REGISTER for declaring one.
[S06.1] amountFor :7147-7180 — the scope branches. The student-made-type arm sits ABOVE the
  `if (!unit) return 0` early return and the comment says why. A `dead` amount is a per-unit 0-or-1
  and the placement question is identical.
  TOKEN_IDS.indexOf(amt) === -1 keeps the five built-in ids out of the tally arm.
[S06.1] labelFor :7201-7212 — hasOwnProperty AND a typeof string test, falling back to the shipped
  health label. fillProposal :9124-9130 records why that fallback is a TRAP for a message: it would
  "print a name that is actively wrong."
[S06.1] the zero-tally hide pass :7386-7440 — read all three of its dependencies AND its third input
  (:7422-7440), the focus-drop bug. syncRow :7264-7292 reads `animate` off row.children.length
  BEFORE its loop, so 0 -> 1 appends without the entry class.
[S01] TOKEN_IDS = ['hp','ap','shield','dmg','dead'] :1871; DEFAULTS.tokens.dead :2362-2368 —
  rect, ink-dim, a skull glyph, shipping with no consumer FOR THIS PHASE.

[S06.3] syncProjection :8143-8197 — reads state.build and ONLY state.build (:8182). Built once,
  written every frame. projIgnores :8103-8120 and ignoredText :8127-8135, and the four
  silent-failure rules at :8102-8109 that any node added near it must check off.
[S02] turnsToWipe(attacker, target, activeUnits) :2809-2827 — the third argument exists and NO
  CALLER PASSES IT. Its comment :2797-2801: "activeUnits is forwarded untouched so Phase 5 can
  project a fight in progress." bestPair(faction, activeUnits) :2604-2621 forwards it.
[S02] aliveCount :2733; apSpent :2742 (floored in the derivation).
D-13 forbids a figure about BOTH sides; D-06 whole numbers only. Both bound whatever is added here.

#tok-pick-names :1186-1205 — the permanent LINE and not a dialog, D-17 and D-12 reconciled, and
  "IT REPORTS AND IT DISABLES NOTHING."
#reset-ask :1650-1712 — the ONE time the file overruled that policy, and the narrow test its own
  comment states: "does undo stop being able to recover this while the student is still working?"
  A mid-fight notice does not meet that test.
[S05] sideFromBuild :4917-4924 copies build -> fight ONCE, at startFight. MEASURED: with a fight
  running, setUnitMaxHp moves build and leaves fight untouched. There is no retroactive path because
  there is no code that would take one.
[S03] commit() :2939 snapshots {build, fight} together, so ONE Ctrl+Z after a mid-fight build edit
  rewinds the build edit and leaves the fight — correct — but a Ctrl+Z CHAIN eventually un-starts the
  fight (D-08, :2999-3001, asserted at :13719-13722). The notice must not contradict that.

[C07]'s standing rule, restated in [C12] and [C13]: state is said TWICE — outline and tick, class and
  aria — never colour alone. UX-02's 18px floor binds a fourth time.
</interfaces>
</context>

<the_line_this_phase_walks>
**Nothing on this board decides anything.**

- The dead marker draws the **stored flag**. It never reads health. `aliveCount`'s comment says it
  once and `setUnitHp`'s says it again: death is never inferred. A unit at zero health that is still
  alive draws as alive, and that is the Shield ruling being representable.
- The by-hand marker draws a **fact about what the student did**. It does not say the number is wrong,
  or unusual, or overridden by the tool — it says a person set it.
- The mid-fight line states a **consequence**, in the register `#reset-ask`'s own line uses: *"a plain
  statement of consequence with no colour and no alarm."* It reports and it disables nothing.
- Nothing here announces a fight is over, names a side, or renders a terminal state (D-26). A board on
  which every unit of one side is marked dead is a thing a student **reads**.
- No figure added here compares the two sides against each other or against a threshold (D-13), and
  every figure is a whole number (D-06). The live reading must never become the loudest thing on
  screen — that is the projection's own shipped rule and it binds anything placed beside it.
</the_line_this_phase_walks>

<naming_trap>
    grep -ci "counter\|rating\|balanced\|difficulty" cats-vs-mechs.html   # must print 0
    grep -c  "verdict\|balanced\|rating\|difficulty" cats-vs-mechs.html   # must print 0

Measured clean, offered as a copy deck: `Cat 4 is at zero health. Mark it dead, or rule that it
survived.` · `Marked dead. Toggle it back if your ruling says otherwise.` · `Still on the roster,
ruled dead.` · `Set by hand` · `Changed by hand, not by the rule.` · `This number was set by hand.` ·
`You changed the build while a fight was running. The change applies to the build, not to this
fight.` · `Edits to the build take effect the next time you start a fight.` · `4 of 9 still standing`
· `no longer standing` · `survived` · `removed from play` · `at zero` · `zero health` · `marked dead`
· `toggled alive` · `a ruling` · `the table ruled` · `you rule`.

`override` is clean as a **rendered word** and banned as a **state key** — plan 05-05 settled that and
this plan stores nothing anyway.
</naming_trap>

<tasks>

<task type="auto">
  <name>Task 1: the dead marker, the alive toggle, and the by-hand marker</name>
  <files>cats-vs-mechs.html</files>
  <read_first>
    - cats-vs-mechs.html :6830-6890 (unitCard, and the setup branch that gates the remove button)
    - cats-vs-mechs.html :7140-7185 (amountFor's scope branches and the paragraph about the arm that
      sits above the early return)
    - cats-vs-mechs.html :7255-7300 (syncRow, and `animate` read off children.length before the loop)
    - cats-vs-mechs.html :7380-7445 (the zero-tally hide pass — all three dependencies AND the third
      input, which is the focus-drop bug this task must not repeat)
    - cats-vs-mechs.html :2340-2375 (DEFAULTS.tokens.dead and its no-consumer paragraph)
    - cats-vs-mechs.html :7089-7107 (plan 03-05's cross-plan append and the register for declaring one)
  </read_first>
  <action>
    Open `[S06.9] RENDER — FIGHT MODE ON THE BOARD` as a sub-region in `[S06.6]`'s shape, on
    `SYNC_HOOKS`, on `[S06]`'s return, with its `[S00]` line and the cheap exit
    `if (state.fight === null) { return; }`. Its banner keeps the sub-region contract: it changes
    nothing in `[S06.1]` through `[S06.8]`.

    **One cross-plan edit is declared rather than hidden.** `unitCard` is `[S06.1]`'s, and the
    alive-toggle has to be a child of the card it belongs to. Declare the append in plan 03-05's
    register — that plan appended reference cards into `buildColumn` and wrote a paragraph saying so,
    and *wave ordering, not overlapping ownership, is what makes it safe.*

    **The dead marker draws the shipped vocabulary.** `DEFAULTS.tokens.dead` ships a rectangle with a
    skull and has waited for this phase with no consumer; the file put it there as *"the concrete
    proof that this renderer can already draw Phase 5's dead marker."* Draw it through the existing
    `makeToken` / `styleFor` path, and extend `amountFor` so a `dead` amount is a per-unit 0-or-1.
    The placement question is **identical** to the one `amountFor`'s comment already answers about a
    student-made side-scope type: get the arm on the wrong side of the `if (!unit) return 0` early
    return and it is unreachable forever. Read that paragraph and place accordingly.

    **It draws the flag, never the health.** A unit at zero health that a student has ruled alive
    draws as alive. A unit at full health a student has ruled dead draws as dead. Both are the point
    and both were asserted in plan 05-05; this is their rendered half.

    **FIGHT-06 is already decided and must not be re-decided.** The hide pass's own words: *a line the
    board is built on is never touched here … a unit at zero health is something a student meant, and
    it stays on screen.* The dead marker is a marker, not a removal.

    **The alive toggle, and the bug not to repeat.** The hide pass's third input records it: *the rule
    took the control away on the last press of the − that reached zero: the button under the student's
    finger went inside a `display:none` ancestor, which drops keyboard focus to the body.* So: the
    toggle is **not** inside anything the dead state hides, collapses or dims out of reach, and it is
    reachable in both directions — a dead unit's toggle must be pressable to bring it back. Say so in
    the comment and name the bug, because the shape that causes it looks tidy.

    The toggle is a control and takes a `data-k` **unique per unit** in the file's slash-path idiom
    (`fg/alive/c1`), never the student's text. Plan 05-10 presses it.

    **FIGHT-07's marker, derived and not stored.** Read the current round's ruling list and mark the
    values it names. Nothing is stored on the unit, and plan 05-05's suite already asserted that the
    question *"was this value set by hand this round?"* is answerable from state alone — that row
    exists precisely so this plan is not tempted to store a flag.

    Mark it in the `overPill` register: **state said twice**, in the class **and** in an accessible
    name, never in colour alone; and if a glyph is used it is a real node, not a pseudo-element. And
    take `.ae-prop-over` versus `.ae-prop-row`'s lesson — the marked thing gets its own class and is
    **not** selected by the class the ordinary thing uses, because a shared class is how a later
    selector reaches something it was not written for.

    **Every assembled sentence is one node per fragment**, with `data-lbl` on a token type's name and
    `data-anm` on an action's. A dead-unit line naming a departed token type has `labelFor`'s
    fallback trap in it — `fillProposal`'s comment records that the fallback would *"print a name that
    is actively wrong"* — so refuse by name rather than asking `labelFor`.
  </action>
  <verify>
    <automated>node tests/selftest-node.cjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/selftest-node.cjs` exits 0 at no fewer than 05-08's recorded total / 0 failed,
      interaction gate 147 of 147, dialogs 145 across 4 unchanged.
    - `#app` and `FIGHT_FLOOR` re-measured; `FIGHT_FLOOR` moved with a history line. Record both and
      the `#app` delta, noting the per-unit-card string arithmetic the `#app` floor's comment records
      ("each unit card is worth EXACTLY 7 strings") and whether it still holds in fight mode.
    - Layer C clean across both harvests. Record the scan lines.
    - `node -e`: a unit at zero health with `alive === true` draws **as alive**; a unit at full health
      with `alive === false` draws **as dead**; a unit driven to zero by an Advance draws as alive
      until ruled. Record all three.
    - `node -e`: every unit, dead or alive, is still present in its column — count the cards before
      and after marking three units dead. Record both counts.
    - `node -e`: the alive toggle for a dead unit is present, not hidden, and has no hidden ancestor.
      Walk the ancestor chain and record it.
    - `node -e`: every alive toggle's `data-k` is unique **document-wide**. Record the count and the
      duplicate count (zero).
    - `node -e`: after a by-hand health change, that unit's health carries the marker in **both** its
      class and an accessible name; after an Advance, the marker is gone from the new round. Record
      both.
    - DELIBERATE-FAILURE PROBE AB, run and recorded verbatim, then reverted: draw the dead marker from
      `hp === 0` instead of the flag. The alive-at-zero row must redden. **If it stays green the row
      is reading the same source as the renderer** — fix it and record what it was reading.
    - DELIBERATE-FAILURE PROBE AC, run and recorded verbatim, then reverted: put the alive toggle
      inside a node the dead state hides. Record what the gate says — `matches()` has no `[hidden]`
      support, so the hidden-ancestor walk must be written by hand; **if nothing reddens, the walk is
      not walking** and that is the finding.
    - Both naming greps print `0`.
  </acceptance_criteria>
  <done>A dead unit is marked with the vocabulary that shipped for it two phases ago, stays in the roster, and can be toggled back with a control the marking does not hide — and a by-hand value is marked from the round's own record with nothing stored on the unit.</done>
</task>

<task type="auto">
  <name>Task 2: the mid-fight line, the live fight reading beside the projection, and [C14.3]</name>
  <files>cats-vs-mechs.html</files>
  <read_first>
    - cats-vs-mechs.html :1186-1205 (#tok-pick-names — the permanent line and the D-17/D-12
      reconciliation) and :1705-1720 (#reset-ask's own line: "a plain statement of consequence with
      no colour and no alarm")
    - cats-vs-mechs.html :8095-8200 (syncProjection in full, projIgnores, ignoredText, and the four
      silent-failure rules any node placed near it must check off)
    - cats-vs-mechs.html :2775-2830 (turnsToWipe, its third argument and the comment that names this
      phase) and :2600-2625 (bestPair forwarding it)
    - cats-vs-mechs.html :2995-3005 (undo crossing the fight boundary, D-08)
    - .planning/phases/05-fight-loop-playtest/05-CONTEXT.md D-22, and 05-RESEARCH.md § Open
      Questions 1 — READ BOTH, THEY DISAGREE
  </read_first>
  <action>
    **FIGHT-10's notice is a permanent LINE, not a toast and not a modal.** D-17 declined confirmations
    twice by name, and the one time that policy met a competing requirement the answer was
    *"a permanent LINE beside the button … explicitly not a dialog"*, because a line costs no click,
    adds no surface, and says the thing D-17 had nothing to say. `#reset-ask` is the single exception
    in the file and its own comment states the narrow test that justified it — *does undo stop being
    able to recover this while the student is still working?* A mid-fight notice does not meet it.

    Fill the node plan 05-06 reserved, and put a second instance where the edit actually happens —
    **beside the roster steppers that are still live during a fight**, because a line in a region the
    student is not looking at is not a notice. It must be visible **at the moment of the edit**.

    The copy is measured clean: *"You changed the build while a fight was running. The change applies
    to the build, not to this fight."* Two things it must not say: it must not promise the fight is
    "unaffected" in a reassuring register — the gate permits it and it reads as comfort rather than
    fact — and **it must not contradict D-08.** Undo is shared across both slices, so one Ctrl+Z after
    a mid-fight build edit rewinds the build edit and leaves the fight, which is correct, but a Ctrl+Z
    **chain** will eventually un-start the fight. That is existing, documented, intended behaviour,
    asserted at `:13719-13722`, and a notice implying the two are independent would be wrong. Say what
    is true and stop.

    It reports and it **disables nothing** — `#tok-pick-names`' own closing line, and it binds here.

    **PROJ-05, and this is the decision this plan owes an explicit answer to, because two project
    documents disagree.**

    `05-CONTEXT.md`'s D-22 assumes the projection reads the fight while a fight runs, and its reason
    is sound: a projection that stayed visible while silently describing the *build* would be quietly
    wrong at exactly the moment a student is reading it. `05-RESEARCH.md` argues the opposite and its
    reason is also sound: the projection is a statement about **the allocation the student made**, the
    whole pedagogy is that the fight contradicts it, and *a strip that silently tracks the fight has
    nothing left to be contradicted.* The ROADMAP agrees with research — *"you guessed 3–5, it took
    8"* only lands if the guess stays on screen.

    **Reconcile rather than choose.** Keep `[S06.3]`'s existing figures reading `state.build`,
    untouched, and add a **second, adjacent, explicitly labelled** reading of the fight as it actually
    stands — survivors per side off `aliveCount`, points spent and left off `apSpent`, and
    `turnsToWipe`'s **third argument** finally passed. Both readings are labelled for what they
    describe, so neither is quietly wrong and the contradiction stays visible, which is the thing the
    roadmap is built around.

    That discharges D-22's actual consequence — *"the currently-unused `activeUnits` parameter gets
    used. If the developer prefers the projection keep describing the build, that parameter should be
    **retired** rather than left unused — an unused parameter that names this phase is a trap for the
    next reader either way."* It is used. Write the reconciliation into the comment beside it, naming
    D-22, naming research's counter-argument, and naming this as **the first PROJ-05 question at plan
    05-11**, so the developer can collapse it to either single answer cheaply.

    **The bounds hold either way and both must be honoured:** D-13 forbids a figure about both sides
    at once, so there is no shared bar and no midpoint; D-06 forbids a decimal; and the live reading
    must never be the loudest element on screen, which is the projection's own shipped rule. Add
    every node following the admission line's four silent-failure rules by name: no `data-k`; no
    `data-amt` and not a `.brd-value`; not `.brd-line--opt`; built once, `hidden` and empty together.

    **`[C14.3]`** — the `.dc-` rules, `[C13]`'s banner shape, inside `[C14]`'s frame:
      - a dead unit legible as dead **without relying on colour**: the state is in the class and in an
        accessible name, and the marker is a real node. `ink-dim` is the shipped token the `dead`
        vocabulary already names — reuse it, and derive anything further with `color-mix()`;
      - the alive toggle at UX-02's 18px floor, permanent and visible, never an icon alone and never a
        `title=` tooltip, and **never inside anything the dead state hides**;
      - the by-hand marker distinguishable from an ordinary value without colour alone;
      - the mid-fight line in `#reset-ask`'s register — a plain statement with no colour and no alarm.
        It is not a warning and must not look like one;
      - no `url(` anywhere including in a comment; no inline SVG and no `createElementNS` — a shape is
        a `clip-path`; no new hex; no `text-wrap: balance`.
  </action>
  <verify>
    <automated>node tests/selftest-node.cjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/selftest-node.cjs` exits 0, gate 147 of 147, dialogs 145 across 4 unchanged.
    - `#app` and `FIGHT_FLOOR` re-measured and recorded; `FIGHT_FLOOR` moved with a history line.
    - Layer C clean across both harvests. Record the scan lines.
    - `node -e`: editing the build with a fight running renders the line; editing with no fight running
      renders nothing. Record both. Record the line's text verbatim.
    - `node -e`: the notice's node carries no `data-k`, no `data-amt`, is not a `.brd-value` and is not
      `.brd-line--opt`, and was built once. Record all five.
    - `node -e`: **`[S06.3]`'s two shipped figures are byte-identical before and during a fight on the
      same board.** Record both pairs. This is the row that reddens if the strip quietly starts
      tracking the fight.
    - `node -e`: the live reading exists during a fight, is absent outside one, passes
      `turnsToWipe`'s third argument, contains no decimal, and contains no figure that names both
      sides in one statement. Record all five.
    - `node -e`: check 62's existing claim still holds — starting a fight leaves every action card on
      the page while the setup-only Add button goes away. REF-03 stays pre-satisfied. Record it.
    - DELIBERATE-FAILURE PROBE AD, run and recorded verbatim, then reverted: make `[S06.3]` pass
      `activeUnits` into its **own** figures. The byte-identical row must redden. **If it stays green
      the row is comparing the wrong two readings** — record which two it was comparing.
    - DELIBERATE-FAILURE PROBE AE, run and recorded verbatim, then reverted: render the live reading as
      a single statement naming both sides. Record whether any row catches it — **D-13 has no
      mechanical check today and if nothing reddens that is the finding**; add the row here rather
      than leaving it, or record explicitly that it goes to plan 05-10's gate work.
    - `grep -c "url(" cats-vs-mechs.html` prints `0`; no inline SVG, no `createElementNS`, no new hex,
      no `text-wrap: balance`.
    - Both naming greps print `0`.
  </acceptance_criteria>
  <done>A build edit made mid-fight says so on screen as a line that disables nothing and does not contradict D-08, and the projection stays on screen describing the allocation it has always described with the fight's own figures beside it, labelled, using the parameter that was added for this phase two phases ago.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the stored alive flag -> the marker on screen | inferring death from health here would undo D-00d everywhere upstream |
| the build slice -> the fight slice, during a fight | the mechanism already prevents a retroactive edit; the notice is what makes it legible |
| the projection -> the fight | the whole pedagogy is that these two disagree, and a surface that hid the disagreement would be worse than one that was wrong |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-37 | Repudiation | the dead marker drawn from health rather than the flag, making a Shield ruling invisible | mitigate | drawn from the stored flag; three driven rows covering both directions; probe AB drives the health-derived spelling |
| T-05-38 | Denial of service | the alive toggle hidden by the state it toggles, dropping keyboard focus to the body | mitigate | the toggle is outside anything the dead state hides; a hand-written hidden-ancestor walk, because `matches()` has no `[hidden]`; probe AC drives the tidy-looking shape that causes it |
| T-05-39 | Repudiation | a per-value provenance flag stored on a unit, which check 73c bans by name and which would drift | mitigate | the marker is derived from the round's ruling list; plan 05-05's row already proved it derivable |
| T-05-40 | Information disclosure | the projection quietly switching to describe the fight, so nothing is left to be contradicted | mitigate | `[S06.3]`'s figures asserted byte-identical before and during a fight; the live reading is a separate labelled node; probe AD drives the switch |
| T-05-41 | Repudiation | a live reading that compares both sides in one statement, which D-13 forbids and no check enforces | mitigate | probe AE tests for the gap and the plan closes it or names where it is closed |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | zero packages installed by this phase |
</threat_model>

<verification>
- `node tests/selftest-node.cjs` exits 0, gate 147 of 147, dialogs unchanged.
- Layer C clean across both harvests; `FIGHT_FLOOR` re-measured and moved with a history line.
- `[S06.3]`'s two shipped figures are byte-identical before and during a fight.
- Probes AB, AC, AD and AE each run, recorded verbatim, reverted. Probes AC and AE are gap-finding
  probes and their outcome is recorded whichever way it goes.
- Both naming greps print 0.

<human-check>
Items for plan 05-11, and PROJ-05 is the largest of them: whether two readings side by side reads as
**one honest surface** or as two numbers a student has to reconcile; whether the developer wants D-22
collapsed to a single answer in either direction, which is now a one-comment change either way;
whether a dead unit is legible as dead from the back of a room without relying on colour; whether the
mid-fight line is seen at the moment of the edit or is somewhere the student is not looking; and
whether the by-hand marker reads as *a person set this* rather than as *the tool disagrees*.
</human-check>
</verification>

<success_criteria>
- A dead unit is marked from the stored flag, stays in the roster, and its toggle is reachable in both
  directions and not hidden by the state it toggles.
- A by-hand value is marked from the round's own record with nothing stored on the unit.
- A mid-fight build edit says so on a permanent line that disables nothing and does not contradict
  D-08.
- The projection's shipped figures are unchanged during a fight, the fight's own figures are beside
  them and labelled, `turnsToWipe`'s third argument is used, and the reconciliation of D-22 against
  research is written down and queued for the developer.
</success_criteria>

<output>
Create `.planning/phases/05-fight-loop-playtest/05-09-SUMMARY.md` when done, recording: the three
alive/dead driven readings; the card counts before and after marking; the alive toggle's ancestor
walk and its `data-k` uniqueness count; `[S06.3]`'s two figures before and during a fight; the live
reading's five readings; the mid-fight line verbatim; the PROJ-05 reconciliation as written into the
file; probes AB, AC, AD and AE verbatim including the outcome of the two gap-finding ones; the new
`#app` and fight-harvest counts and `FIGHT_FLOOR`; and both naming-grep readings.
</output>
</content>
