---
phase: 05-fight-loop-playtest
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - cats-vs-mechs.html
autonomous: true
requirements: [FIGHT-01, FIGHT-14, SHARE-07]
section_ownership: >
  cats-vs-mechs.html — `[S00]`'s table of contents ([S06] and [S09] sub-region lines); `[S01]`'s
  fight caps and the `[S01]` export block; the whole of `[S03]`'s slice-shape banner; `[S05]`'s
  `sideFromBuild` / `startFight` / `endFight` and a new `resetFight`, their dispatch arms, the
  DELIBERATELY ABSENT block at :6397-6412, and the `[S05]` export block; `[S09.3]`'s fight rows where
  the slice shape moved under them; a new `[S09.12]` suite opened above any no-DOM bracket, and its
  line in `[S09]`'s banner index. Touches no line of `[S02]`, `[S04]`, `[S06]`, `[S07]`, `[S08]`, the
  `<style>` block or the static shell. Writes no `advanceRound` — plan 05-04 owns it.

must_haves:
  truths:
    - "Starting a fight produces a slice that carries this phase's round record and the students' own token tallies, and every key in it survives a JSON round trip as an integer, a string or a boolean."
    - "Resetting the fight puts both rosters back to what the build says and leaves the build untouched — in one commit, therefore one Ctrl+Z."
    - "A build code produced with a fight running is byte-identical to the one produced without, because the codec cannot see the fight and this plan did not teach it to."
    - "No banner in the file names a key the file no longer has, and no banner names an op as absent that this plan added."
  artifacts:
    - path: "cats-vs-mechs.html"
      provides: "[S03]'s slice shape carrying `round`, `decl`, `past`, and per-side `ap` / `units` / `tally`, with `turn` and `log` retired by name"
      contains: "fight: null |"
    - path: "cats-vs-mechs.html"
      provides: "[S05].resetFight — one commitStructural rebuilding both sides from the build, leaving the build alone"
      contains: "resetFight"
    - path: "cats-vs-mechs.html"
      provides: "[S09.12] opened above any no-DOM bracket, with the saved-state bookend and protoIntact"
      contains: "[S09.12]"
  key_links:
    - from: "[S05].resetFight"
      to: "sideFromBuild"
      via: "the same builder startFight uses, so a reset and a fresh start cannot diverge"
      pattern: "sideFromBuild"
    - from: "[S09.12]"
      to: "App.ops and App.state exports"
      via: "asserted against the exported name, never a re-typed copy"
      pattern: "App\\.ops\\."
---

<objective>
Grow the slice this phase writes into, and rewrite every banner that describes it — in the same change
that makes the old description untrue.

This is the most heavily pre-provisioned phase in the project's history. `[S03]`'s banner already
declares a `fight` shape. `startFight`, `endFight`, `setUnitHp` and `setAlive` already ship and are
already exported. `sideFromBuild` already copies `shield` into the fight slice and its comment says
the writer *"arrives with the phase that has a surface to spend from — Phase 5, plan 05-01."*
That is this plan and the next four.

It is also the phase with the most tripwires aimed at it, and two of them fire here.
`[S05]:6397-6412` is a DELIBERATELY ABSENT block naming *"turn and round advance, action-point
spending, damage application and the fight record — Phase 5, plan 05-01"* **by number**. And
`[S03]`'s banner declares `fight: { round, turn, cats, mechs, log }` — a shape carrying a `turn` key
that encodes alternating turns, which the developer's round loop of 2026-08-28 supersedes, and a
`log` array with no writer and no consumer. `[S03]`'s own banner states the standard both are
measured against: **a banner that quietly lies is worse than no banner.**

Purpose: the slice, the lifecycle, and the four rulings the file has been deferring to this phase by
name. Output: `[S03]` honest again, `resetFight` shipped, `[S09.12]` open with rows that can fail.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-fight-loop-playtest/05-CONTEXT.md
@.planning/phases/05-fight-loop-playtest/05-RESEARCH.md
@.planning/phases/05-fight-loop-playtest/05-PATTERNS.md
@.planning/phases/05-fight-loop-playtest/05-01-SUMMARY.md
@./CLAUDE.md
@cats-vs-mechs.html
@tests/selftest-node.cjs

<interfaces>
<!-- Extracted from the live artifact. Re-grep before trusting a line number. -->

[S03] the banner's shape block, :2869-2873 — the text being rewritten:
    fight: null | { round, turn, cats, mechs, log },
    fight side shape: { ap, units: [ { id, hp, shield, alive } ] }
[S03] slice lifetimes, :2878-2882 — "`build` round-trips through the share code and the undo stack;
    `fight` is undo-only and never shared; `ui` is in neither."  <- stays true and gets stronger
[S03] :2850-2866 names exactly two writers outside [S05]: restore() and commitInitial().
    Phase 5 needs no third. Say so.
[S03] commit() :2938 snapshots {build, fight} together; thaw() :2923 is JSON.parse(JSON.stringify(x))
    and IS the enforcement of "integers only, JSON-clonable".
    UNDO_LIMIT 30 (:2888), COALESCE_MS 500 (:2892).

[S05] sideFromBuild :4917-4924, and its comment :4893-4916 — which defers THREE things to this plan
    by name: the shield writer, whether a tally is spendable during a fight, and setUnitHp's clamp.
    return { ap: faction.ap,
             units: faction.units.map(u => ({ id: u.id, hp: u.maxHp, shield: u.shield, alive: true })) };
[S05] startFight :5144-5158 — commitStructural, with the already-running refusal INSIDE the mutator
    (:5127-5136) so the throw leaves no phantom undo step.
[S05] endFight :5159-5167 — "build is left untouched, so 'run it again' always restarts from the same
    allocation the student spent the exercise arriving at."
[S05] resetToDefaults :5179-5183 — "One commit, therefore one undo entry (D-12). The undo stack is
    never cleared here: a mis-clicked reset has to be recoverable."   <- resetFight's exact model
[S05] commitStructural :5040-5043 and its rule :5029-5035.
[S05] fightOf(s) :4890-4893 — the in-mutator "is there a fight" guard; it already throws.
[S05] requireSide :4600, int() :4545/:4557, findUnit :4870, nextUnitId :5053 (the "scan every id
    already in use, take the largest suffix and add one" idiom that avoids the banned noun).
[S05] tallyOwner :6265-6284 — the two-call shape: ruled once on the live board, again on the
    detached copy inside the mutator where it cannot throw.
[S05] dispatch arms :6330-6395; startFight and endFight have arms, setAlive and setUnitHp DO NOT.
[S05] export block :6442-6506.

[S01] MAX_CUSTOM_TYPES :1926-1930 — the cap-comment idiom: a named var, exported, with the
    arithmetic that chose it above it. MAX_ALLOC, MAX_ACTION_XF etc :2156-2158.
[S01] DEFAULTS :2303, and :2285-2287 which names plan 05-03 as the retune. Phase-5 plan numbering
    has since moved: the retune is plan 05-11. That line needs correcting.
[S01] DEFAULTS.tokens.dead :2362-2368 — ships as vocabulary with NO renderer consumer, for this phase.

[S09] banner index :13035-13051 — "this list is the only index of them". Last entry is [S09.11].
[S09.11] :17962-17985 — the model for a suite written wholly above any no-DOM bracket, including its
    opening declaration and the savedAll = JSON.stringify(App.state.get()) / App.state.restore
    bookend.
[S09.10] protoIntact() — reuse the shape, do not derive a second one.
[S09.3] fight rows: :13611 three slices; :13663-13668 startFight and the refused restart;
    :13672-13681 aliveCount reads the flag; :13719-13722 undo crosses the fight boundary;
    :13855-13863 "shield is a BUILD write. Phase 5 owns the fight slice's own copy" <- plan 05-05's.

tests/selftest-node.cjs check 73c :5921-5961 — walks the WHOLE state at any depth and reddens on a
    key matching /propos|override|caster|target|pending/i, AND pins by string equality:
      Object.keys(state).sort().join(',') === 'build,fight,ui'
      Object.keys(state.build).join(',') === 'schema,cats,mechs,tokens'
      Object.keys(state.build.cats).join(',') === 'id,name,ap,units,actions'
    The FIGHT slice is NOT key-pinned by that check. It IS walked by the name test.
    Check 74 reads Object.keys(App.ops) and drives every arm blind with {side:'cats', unitId:'c1'}.
</interfaces>
</context>

<the_line_this_phase_walks>
**The tool does bookkeeping and projection; the students are the rules engine.** That is PROJECT.md's
oldest Out of Scope entry and it is the thing most likely to erode under implementation pressure, so
it gets drawn here, in the section that holds the shape, before anything can be written into it.

This plan adds **no** resolution of any kind. It adds a place for a declaration to sit and a place for
a resolved round to be recorded. It does not decide whether an action happens, whether a counter
applies, whether an effect fires, or whether a unit is dead. `setUnitHp`'s existing comment already
states the rule this plan must not weaken: *zero health prompts a death ruling from the table, it
never auto-kills (D-00d)* — and `alive` is a stored flag precisely so a Shield ruling is
representable.

`resetFight` is bookkeeping too: it puts the numbers back to what the student's own build says. It
does not decide the fight is over. **Nothing in this phase decides a fight is over** (D-26).
</the_line_this_phase_walks>

<naming_trap>
    grep -ci "counter\|rating\|balanced\|difficulty" cats-vs-mechs.html   # must print 0
    grep -c  "verdict\|balanced\|rating\|difficulty" cats-vs-mechs.html   # must print 0

`counter` is a whole-document ban, so **`roundCounter` and `turnCounter` are unwritable identifiers**
and a comment about "a loop counter" fails the build. `round`, `roundIndex` and `countRounds` measure
clean. `rating` hides in **iterating** — write *walking* or *reading each*. And **the round is never a
"Day"**; the developer rejected it and `Day` measures clean, so only this line stops it.

Test labels in `[S09.12]` are string literals and Layer B reads them, now including the eleven
patterns plan 05-01 added. Do not write `t.eq('the winner is recorded', …)`.

Measured-clean vocabulary for this plan: `fight`, `round`, `Round 1`, `start the fight`,
`reset the fight`, `previous board`, `past rounds`, `rounds so far`, `history`, `ledger`, `record`,
`declare`, `declaration`, `undeclared`, `resolve`, `spent`, `pool`, `alive`, `standing`, `dead`.
</naming_trap>

<tasks>

<task type="auto">
  <name>Task 1: [S03]'s shape rewritten, and four rulings the file deferred to this plan by name</name>
  <files>cats-vs-mechs.html</files>
  <read_first>
    - cats-vs-mechs.html :2846-2900 (the whole of [S03]'s banner — the shape block, the slice
      lifetimes, the two-writers paragraph and the "a banner that quietly lies" line)
    - cats-vs-mechs.html :4890-4960 (fightOf, sideFromBuild and the comment that defers three
      decisions to this plan by name, setUnitShield's comment naming `setFightShield`)
    - cats-vs-mechs.html :5090-5170 (setUnitHp's clamp paragraph, setAlive, startFight, endFight)
    - cats-vs-mechs.html :1920-1935 (MAX_CUSTOM_TYPES — the cap-comment idiom) and :2280-2300
      (DEFAULTS' header comment naming a plan number that has moved)
    - .planning/phases/05-fight-loop-playtest/05-RESEARCH.md § Q1 and § Assumptions Log A1
    - .planning/phases/05-fight-loop-playtest/05-CONTEXT.md D-24 and D-26
  </read_first>
  <action>
    Rewrite `[S03]`'s shape block and add the slice's new keys. The shape becomes, stated in the
    banner's own register:

        fight: null | { round, decl, past, cats, mechs }
        fight side shape: { ap, units: [ { id, hp, shield, alive } ], tally }

    **Four rulings, each written into the file beside the code, each with its alternatives, and each
    going back to the developer at plan 05-11.**

    1. **`turn` is retired, not repurposed.** THE ROUND LOOP is simultaneous declaration: one Advance
       resolves both sides, so there is no side whose turn it is. A key named after a concept the file
       no longer has is exactly the quiet lie the banner forbids. Remove it from the shape block, from
       `startFight`'s seed, and from any `[S09.3]` row that names it. Write into the banner what
       FIGHT-09's readable fact became instead: **the round number and both pools**, which is a
       statement about the board rather than about a turn order.

    2. **`log` is folded into `past`.** `fight.log` ships as `[]` with no writer and no consumer.
       FIGHT-08 wants each round's actions and every override recorded in readable order; FIGHT-14
       wants earlier rounds on screen. `past[i].did` plus `past[i].hand` **is** that record, and it is
       the same object the ledger renders. Two structures both claiming to be what happened will
       disagree. Delete the field with no writer rather than giving one to a field with no consumer,
       and say that in the comment so the next reader does not restore it.

    3. **A student-invented tally IS spendable in a fight, on the same terms as a shipped token
       type** — D-24, and the reason is Phase 2.1's: *the vocabulary stops being ours and starts being
       theirs*, and a type that worked everywhere except in the fight would reintroduce the second
       tier 2.1 removed, at the one moment it matters most. So **`sideFromBuild` now copies the tally
       bag** into the fight side. Its own comment currently says *"Whether a tally should be spendable
       DURING a fight is a fight-semantics ruling, and Phase 5 owns it"* — replace that sentence with
       the ruling and its consequence: the fight carries its own copy, so nothing in this phase ever
       writes to `build`, and the codec stays unable to see a fight because there is nothing of the
       fight in the slice it reads. Copy the bag through the file's own deep-copy idiom
       (`JSON.parse(JSON.stringify(x))`, as `thaw` and `defaults()` both use), and copy it with
       `hasOwnProperty` on every step for the reason `dropTally` already records — from Phase 4 a bag
       can arrive from a pasted build code rather than from this file.

    4. **`setUnitHp`'s clamp stays `MAX_ALLOC` and does not become the unit's own `maxHp`.** Its
       comment left the decision *"consciously, not missed"* to this plan. Rule it: the clamp stays,
       because a heal that cannot overshoot and a shield that cannot read as temporary health are both
       rulings, and rulings belong to the table. Rewrite the comment from a deferral into the
       decision, with the alternative named.

    **The keys are chosen against check 73c, which walks the whole state at any depth.** `decl`,
    `past`, `did`, `hand`, `was`, `by`, `at`, `act`, `side`, `tok`, `from`, `to`, `hit` are all
    measured clean. `caster`, `target`, `pending` and `override` are **refused as object keys** — they
    are fine as words on the page and `WHO_SAID` already renders two of them. Write that distinction
    into the banner in one sentence, because it is the single most surprising constraint in this
    phase and nothing else in the file says it. **Do not amend 73c.** Its job is to keep the proposal
    off the slices and widening it for this phase costs that guarantee; plan 05-04 explains in the
    suite why a declaration is a different object with a different lifetime.

    Add `MAX_PAST_ROUNDS` to `[S01]` in the `MAX_CUSTOM_TYPES` idiom — a named exported `var` with the
    arithmetic above it. Ship it at **30**, matching `UNDO_LIMIT`, and give the honest reason: the
    measurement says the page can hold far more (one more past board costs a flat 1.9–2.1 ms to 60
    rounds and 26,132 nodes, and `sync()` does not move at all at 50), so this is a **readability**
    cap and not a performance one. Say that explicitly rather than implying a performance reason the
    measurement does not support. Note the second-order fact the measurement did find: `commit()`
    snapshots the whole fight slice on every commit, so thirty undo entries over a thirty-round
    ledger is roughly 640 KB of snapshot strings — affordable, watched by the perf gate at 50 ms per
    100 commits, and a reason to cap rather than a reason to move the ledger out of state.

    Finally, correct `[S01] DEFAULTS`' header comment: it names *plan 05-03* as the retune and the
    phase's plan numbering has moved. The retune is **plan 05-11**, and per D-25 **no plan before it
    touches `DEFAULTS.cats.ap`.**

    Extend the `[S01]` export block.
  </action>
  <verify>
    <automated>node tests/selftest-node.cjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/selftest-node.cjs` exits 0 at no fewer than 1051 passed / 0 failed, interaction gate
      147 of 147, shell ids 96, `#app` 127, dialogs 145 across 4 roots, `FIGHT_FLOOR` still met.
    - Check 73c is **green and unmodified** — confirm by reading it, and record that it was not
      touched. Its key-name walk must report zero over a state with a fight running and a declaration
      in it.
    - `node -e` over the artifact: after `startFight()`, `Object.keys(state.fight).sort().join(',')`
      reads `cats,decl,mechs,past,round` and there is no `turn` and no `log` anywhere in the slice.
      Record the reading.
    - `JSON.parse(JSON.stringify(state))` is deep-equal to `state` with a fight running and a tally
      set — the JSON-clonable rule holds. Record it.
    - `App.serialize.encode(state.build)` produces a byte-identical code before and after
      `startFight()` on the same board. Record both strings.
    - DELIBERATE-FAILURE PROBE E, run and recorded verbatim, then reverted: name the declaration
      array `pending` instead of `decl`. Check 73c must redden and name `state.fight.pending`. **If it
      stays green, the walk is not reaching the fight slice** — record that, because the whole key
      naming in this phase rests on it.
    - DELIBERATE-FAILURE PROBE F, run and recorded verbatim, then reverted: put a function on the
      fight slice. The JSON-clonable row must redden. If nothing reddens, the row is asserting
      nothing — fix the row and record what was wrong.
    - Both naming greps print `0`.
  </acceptance_criteria>
  <done>`[S03]`'s banner describes the slice the file actually has, four decisions the file deferred to this plan by name are made and written down with their alternatives, and the codec still cannot see a fight because there is nothing of the fight anywhere it reads.</done>
</task>

<task type="auto">
  <name>Task 2: the lifecycle — resetFight in one commit, and the DELIBERATELY ABSENT block rewritten</name>
  <files>cats-vs-mechs.html</files>
  <read_first>
    - cats-vs-mechs.html :5029-5045 (commitStructural and its rule)
    - cats-vs-mechs.html :5119-5185 (setAlive, startFight with its in-mutator refusal, endFight,
      resetToDefaults and its one-commit / never-clear-the-stack paragraph)
    - cats-vs-mechs.html :6330-6395 (the dispatch router and its arm style) and :6397-6440 (the whole
      DELIBERATELY ABSENT block, all four kinds of absence it records)
    - cats-vs-mechs.html :6442-6506 (the [S05] export block)
    - .planning/phases/05-fight-loop-playtest/05-RESEARCH.md § Common Pitfalls 5
  </read_first>
  <action>
    **`resetFight` — one op, one commit, one Ctrl+Z.** SHARE-07 asks for a fight reset that keeps the
    build. The naive spelling is `endFight()` then `startFight()` and it costs **two undo entries**
    (measured), and `startFight` **throws** if a fight is already running, so the order is forced and
    a mis-ordered pair raises the error panel mid-demo.

    Write it as one `commitStructural` that rebuilds both sides from `sideFromBuild(s.build[side])`,
    sets the round back to 1, empties `decl` and empties `past`, and **leaves `s.build` alone** — in
    `resetToDefaults`' exact register, including its two sentences: one commit therefore one undo
    entry, and the undo stack is never cleared because a mis-clicked reset has to be recoverable.
    Reuse `sideFromBuild` rather than writing a second builder, so a reset and a fresh start cannot
    diverge. Guard with `fightOf(s)` **inside** the mutator, for the reason `startFight`'s own
    paragraph gives: the mutator runs on the detached copy before anything is recorded, so the throw
    leaves no phantom undo step.

    **Do not reuse the reset confirmation.** D-17's test is narrow and written down — *does undo stop
    being able to recover this while the student is still working?* — and a fight reset leaves the
    build intact and one Ctrl+Z away. It does not meet the test. Write that sentence beside the op,
    because the confirmation is four thousand lines away and looks reusable.

    Add dispatch arms, one key each, in the router's existing style: `resetFight`. Note in the comment
    that `startFight` and `endFight` already have arms and `setAlive` and `setUnitHp` do **not** —
    plan 05-05 adds those two, deliberately, alongside the surface that presses them.

    **Then rewrite the DELIBERATELY ABSENT block at :6397-6412.** Its first clause names this phase's
    ops by number — *"turn and round advance, action-point spending, damage application and the fight
    record — Phase 5, plan 05-01"* — and this plan is where that stops being true. Leaving it standing
    beside the ops it says do not exist is precisely the banner that quietly lies.

    Rewrite it into its **positive** form, which is the 03.1-04 precedent for turning a shipped
    tripwire in the open. State what now exists and, more importantly, what still does not and why:

      - the fight record and the fight lifecycle exist, in this plan;
      - **round advance exists in plan 05-04 and is one op** — there is exactly one applier in this
        file and it is `advanceRound`;
      - **there is still no writer that decides anything.** No op picks an action, picks a target,
        decides whether a relationship applies, decides whether an effect fires, or decides whether a
        unit is dead. `REFERENCE.beats` has no consumer in `[S05]` and must gain none; `keywords` has
        no writer at all and must gain none. That is PROJECT.md's Out of Scope entry restated where
        the pressure to break it actually lands.
      - the other three kinds of absence the block already records — `loadBuildCodeAtBoot` having no
        arm, `openTokenPicker` being page work, and there being no writer for an action's `dmg` or
        `keywords` — are **unchanged and must stay**. Check 74 drives every exported op blind and
        reads `dmg`/`keywords` drift after each one; this phase adds ops to that walk and every one of
        them must leave both alone.

    Extend the `[S05]` export block.
  </action>
  <verify>
    <automated>node tests/selftest-node.cjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/selftest-node.cjs` exits 0 at no fewer than 1051 passed / 0 failed, interaction gate
      147 of 147.
    - Check 74's readings are recorded: exported-op count (was 48), dispatch arms driven of acts
      tried (was 18 of 57), action records read (was 425). Every new op is in the walk and none
      moves `dmg` or `keywords`.
    - `node -e`: from a mid-fight board with damage applied, `App.ops.resetFight()` puts both sides
      back to the build's numbers, `App.state.undoDepth()` moved by exactly **1**, one `undo()`
      restores the mid-fight board byte-for-byte, and `state.build` is byte-identical throughout.
      Record all four readings.
    - `App.ops.resetFight()` with no fight running throws, and `App.state.undoDepth()` is unchanged
      by the throw — no phantom entry. Record both.
    - `grep -c "Phase 5, plan 05-01" cats-vs-mechs.html` prints `0` — the promissory note is
      rewritten, not annotated.
    - DELIBERATE-FAILURE PROBE G, run and recorded verbatim, then reverted: implement `resetFight` as
      `endFight(); startFight();`. The undo-depth row must redden with a delta of 2. **If it stays
      green the row is asserting nothing** — fix the row and record what was wrong with it.
    - Both naming greps print `0`.
  </acceptance_criteria>
  <done>A fight resets in one commit and one Ctrl+Z with the build untouched, and the block that promised these ops did not exist now states which one does, which still does not, and why the ones that still do not are the requirement rather than the gap.</done>
</task>

<task type="auto">
  <name>Task 3: [S09.12] opened above the bracket, holding the slice contract and the lifecycle rows</name>
  <files>cats-vs-mechs.html</files>
  <read_first>
    - cats-vs-mechs.html :13035-13051 ([S09]'s banner index — "the only index of them")
    - cats-vs-mechs.html :17962-17990 ([S09.11]'s opening declaration — the "wholly above any no-DOM
      bracket, and that is a decision rather than an accident" paragraph, and the savedAll bookend)
    - cats-vs-mechs.html [S09.10]'s protoIntact() helper — reuse the shape
    - cats-vs-mechs.html :13600-13730 and :13850-13870 ([S09.3]'s existing fight rows, so this suite
      duplicates none of them, and the shield tripwire at :13855-13863 which is plan 05-05's to turn)
    - tests/selftest-node.cjs :381-438 (SUITE_FLOOR and its measurement history)
  </read_first>
  <action>
    Open `[S09.12]`, named `the fight loop`, and register it in `[S09]`'s banner index — that index is
    the only list of the suites.

    **Write the whole suite above any `typeof document === 'undefined'` bracket and say so in the
    opening comment**, copying `[S09.11]`'s declaration in shape and in force. The reasoning transfers
    exactly: the slice, the ops and the arithmetic are **state work** — they read no page, write no
    page and need none — so every row here runs in the terminal harness and can therefore fail a
    build. Five suites in this file stop at that bracket and every row below one of them is
    documentation until somebody opens the page with `#selftest`.

    State the corollary in the same comment, because it is this phase's central split and it will be
    tested: **every surface assertion in this phase belongs in `tests/selftest-node.cjs`'s interaction
    gate instead.** The declaration surface's presses, the ledger's growth, the dead marker, the
    mid-fight notice and Layer C's fight-mode reading are all page work and none of them may be
    written here. Name the plans that carry them — 05-07 through 05-10 — so the rule has owners.

    Use `[S09.11]`'s `savedAll = JSON.stringify(App.state.get())` / `App.state.restore(savedAll)`
    bookend, and drive a **real board through the shipped ops** rather than writing a state literal —
    a literal agrees with whatever its author believed the shape was; a driven board agrees with this
    file. Reuse `[S09.10]`'s `protoIntact()` shape and check it after **every** path rather than once
    at the end, for the reason that comment already gives.

    The rows this task adds:

    **The slice contract.** After `startFight()`: the key set is exactly `cats,decl,mechs,past,round`;
    `round` is 1; `decl` and `past` are both empty arrays; each side carries `ap`, `units` and
    `tally`; each unit carries `id`, `hp`, `shield`, `alive` and nothing else; every leaf is an
    integer, a string or a boolean; and the whole thing survives `JSON.parse(JSON.stringify(...))`
    identically. One row asserting the absence of `turn` and `log` **by name**, labelled so a reader
    who never saw this plan understands that the absence is the decision.

    **The tally crosses into the fight.** Create a student-made token type through the shipped ops,
    give a unit a tally of it, start a fight, and assert the fight side carries that number — D-24's
    no-second-tier rule, asserted rather than intended. Then assert the reverse direction: changing
    the build's tally mid-fight does **not** move the fight's copy, which is FIGHT-10 at the state
    level and the reason the copy exists at all.

    **The codec cannot see a fight.** `encode(state.build)` is byte-identical before `startFight()`,
    after it, after a declaration exists in `decl`, and after `past` has entries. This is SHARE-07
    proved structurally rather than argued, and it is the row that reddens the day somebody teaches
    `encode` to read `App.state` instead of taking a slice.

    **The lifecycle.** `startFight` refuses a second start and leaves no undo entry when it does
    (`[S09.3]` already has the refusal — assert the **undo depth** here, which it does not).
    `resetFight` is one entry, restores byte-for-byte on one undo, leaves `build` untouched, and
    throws with no phantom entry when no fight is running. `endFight` leaves `build` untouched.

    **The bad-input table**, in `[S09.7]`/`[S09.10]`'s form: `resetFight` and `endFight` driven with
    no fight; every op driven with a side of `'__proto__'`, `'constructor'`, `''`, `5`, `null` and
    `{}`, each refused by `requireSide` **by name** rather than resolving on `Object.prototype`; and
    `protoIntact()` after each.

    **`t.info` measurements, not gates:** `MAX_PAST_ROUNDS`, `UNDO_LIMIT`, `COALESCE_MS`, and the
    byte size of the fight slice on the shipped board — so the numbers appear in every report and
    plans 05-04 and 05-08 have something to read a delta against.

    Re-measure the suite total, move `SUITE_FLOOR` in `tests/selftest-node.cjs`, and add a line to its
    history paragraph in the style the existing lines use.
  </action>
  <verify>
    <automated>node tests/selftest-node.cjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/selftest-node.cjs` exits 0 at **no fewer than 1080 passed / 0 failed** (1051 baseline
      plus this suite's rows), interaction gate 147 of 147, shell ids 96, `#app` 127, dialogs 145
      across 4 roots. Record the measured total and the delta from 1051.
    - `SUITE_FLOOR` has moved and its history paragraph carries a new line naming this plan and the
      measured total. Record old and new.
    - **Every row of `[S09.12]` runs in the terminal harness** — count the suite's rows in the `node`
      output and record the count. Zero rows skipped.
    - DELIBERATE-FAILURE PROBE H, run and recorded verbatim, then reverted: teach `sideFromBuild` to
      skip the tally bag. The D-24 row must redden. Then teach `encode` to read `App.state.get()`
      instead of its argument: the byte-identical row must redden. **If either stays green the row is
      asserting nothing** — fix the row, not the expectation, and record what was wrong with it.
    - Both naming greps print `0`.
  </acceptance_criteria>
  <done>`[S09.12]` exists, runs entirely in CI, pins the slice this phase writes into, proves a student-made tally reaches the fight and the codec does not, and its opening comment states which half of this phase may never be written here.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a student press -> `App.state.commit` | the only writer of state reachable from a student action, no exceptions |
| a caller-supplied side / unit id -> a property lookup | `__proto__` resolves on `Object.prototype`, which is not frozen |
| the fight slice -> the build code | must remain uncrossable; the codec takes the build slice as an argument |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Tampering | prototype pollution through a fight-side or unit id | mitigate | `requireSide` / `findUnit` outside the commit; a bad-input row per key per write shape; `protoIntact()` after every path |
| T-05-06 | Information disclosure | the fight slice leaking into a shareable build code, putting attacker-controlled round records one paste away from a write path | mitigate | `encode` byte-identical across four fight states, asserted; `[S04]`'s banner unchanged and untouched by this plan |
| T-05-07 | Repudiation | a banner describing a slice the file no longer has | mitigate | `[S03]`'s shape block rewritten in the same change; the `turn`/`log` absence asserted by name |
| T-05-08 | Denial of service | `resetFight` costing two undo entries, so a mis-press is unrecoverable in one step | mitigate | one `commitStructural`; the undo-depth delta asserted at exactly 1; probe G drives the naive spelling |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | zero packages installed by this phase |
</threat_model>

<verification>
- `node tests/selftest-node.cjs` exits 0 at no fewer than 1080 / 0, gate 147 of 147.
- Check 73c is green and was not modified; its key-name walk reports zero over a fight state.
- Probes E, F, G and H each run, recorded verbatim, reverted.
- The build code is byte-identical across four fight states.
- Both naming greps print 0.

<human-check>
Nothing in this plan is reachable by a person — it renders nothing. Two of its four rulings are
judgement calls the developer has not seen: retiring `turn` and folding `log` into `past` are both
irreversible in the cheap direction only, and D-24's tally copy is a CONTEXT assumption taken while
the developer was away. All three are items at plan 05-11.
</human-check>
</verification>

<success_criteria>
- `[S03]`'s banner describes the slice the file has, with `turn` and `log` retired by name rather than
  left as fields nobody writes.
- A student-made token type is spendable in a fight on the same terms as a shipped one, and the fight
  carries its own copy so nothing in this phase writes to `build`.
- `resetFight` is one commit, one Ctrl+Z, build untouched.
- The DELIBERATELY ABSENT block states what exists, what still does not, and why the absences are the
  requirement.
- `[S09.12]` runs every row in CI and its opening comment names the half of this phase that may not be
  written there.
</success_criteria>

<output>
Create `.planning/phases/05-fight-loop-playtest/05-02-SUMMARY.md` when done, recording: the measured
suite total and its delta from 1051; old and new `SUITE_FLOOR`; the fight slice's key set and byte
size after `startFight`; the build code before and after starting a fight; check 74's three readings;
the four rulings as written, each with the alternative it declined; probes E, F, G and H verbatim;
and both naming-grep readings.
</output>
</content>
