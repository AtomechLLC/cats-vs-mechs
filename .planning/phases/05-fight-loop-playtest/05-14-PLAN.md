---
phase: 05-fight-loop-playtest
plan: 14
type: execute
wave: 14
gap_closure: true
depends_on: ["05-13"]
files_modified:
  - cats-vs-mechs.html
  - tests/selftest-node.cjs
autonomous: true
requirements: [FIGHT-03, FIGHT-09, FIGHT-12, FIGHT-13, UX-02]
section_ownership: >
  cats-vs-mechs.html — `[S06.7]` rewritten whole (its banner, every `fg*` helper, `fightBar` and
  `syncFight`); `[C14.1]` rewritten whole; `[S07.5]`'s press arms, its `FG_PICK_KEY` table and the
  never-disable paragraph in its banner. tests/selftest-node.cjs — the `fg*` DRIVE helpers
  (`fgPick`, `fgDeclare`, `fgAdvancePress`, `fgAliveBtn`, `fgSideRootOf`) and **check 95, turned**.
  It may change any other fight row's DRIVE where the control it presses no longer exists, and it
  may change **no other row's CLAIM** — plan 05-16 owns every claim, `FIGHT_FLOOR`, check 92's
  drive, row 102's values, `tests/browser-checks.mjs` and the limitations list.
  **It builds the battlefield container and paints NOTHING inside it** — `[S06.11]` and `[C14.4]`
  are plan 05-15's and this plan does not open them. It touches no `[S01]`–`[S05]` region, no
  `[S06.1]`–`[S06.6]`, `[S06.8]`, `[S06.9]` or `[S06.10]` region, no `[S07.1]`–`[S07.4]` or
  `[S07.6]` region, no `[C14]`, `[C14.2]`, `[C14.3]` or `[C15]` rule, and no static shell markup or
  shell id.

must_haves:
  truths:
    - "The fight tab shows the round spanning both columns, and then each side as a column reading top to bottom: the side, its battlefield cluster, its team resources, and one picker row per unit."
    - "Each picker row is the unit's name and one button per action that side carries. One click declares; clicking it again undoes it; clicking a different action on the same row replaces it. Both sides are on screen at once."
    - "An action carrying a target-directed term declares in ONE click, pointed at the lowest-health living enemy by default; the row says what it lands on and carries a change-target button that starts the lights-up flow."
    - "As declarations accumulate the displayed team resources deplete by what has been spoken for, and nothing has resolved — pressing Advance is still the only thing that lands anything."
    - "An action's button is disabled if and only if its requirement terms are unmet, or the previewed remaining pool cannot pay its cost, or the performer has been ruled dead. Nothing else on the page is disabled by anything a student did."
    - "There is exactly ONE way to declare on this page."
  artifacts:
    - path: "cats-vs-mechs.html"
      provides: "[S06.7] as the declaration grid, [C14.1] as its rules, [S07.5]'s presses rewired, and the empty flagged battlefield container plan 05-15 fills"
      contains: "[S06.7]"
    - path: "tests/selftest-node.cjs"
      provides: "check 95 turned in the open, and the fg* helpers driving the new grid"
      contains: "95."
  key_links:
    - from: "an action button's disabled state"
      to: "App.model.affordability / App.model.spokenFor / unit.alive"
      via: "re-decided from state on every repaint, never toggled by a handler"
      pattern: "affordability"
    - from: "[S07.5]"
      to: "App.model.defaultAt"
      via: "the surface computes the default and passes it as `at`; the op never does"
      pattern: "defaultAt"
    - from: "[S07.5]"
      to: "App.ops.dispatch('declare' / 'clearDeclaration')"
      via: "one press, one op; the undo path finds its index from state"
      pattern: "clearDeclaration"
---

<objective>
**The surface D-27 drew, the layout its addendum ordered, and the two rules it knowingly overrules.**

> [ROUND]
> [CATS]
> [C1] [ACTION] [ACTION] [ACTION] ← buttons to pick one
> [C2] [ACTION] [ACTION] [ACTION] ← buttons to pick one
> etc
> same thing on the other side.
> As actions are selected, deplete the resources. Re clicking an action should undo that preview.
> picking another action should undo any changes and apply the new one. Disable any actions whose
> requirements are not met
> Show both sides at the same time in columns.

And the addendum:

> Place 'team' resources above the action picker.
> Default target to the lowest health enemy. Add a button to change target (rather than require a
> picker every time)

**Column layout per side, top to bottom: `[SIDE]` → battlefield cluster → team resources → action
picker rows. `[ROUND]` spans above both columns.** This plan builds all of it except what goes
inside the battlefield cluster: it builds that container empty and flagged, and plan 05-15 fills it.
That is `#fight-said`'s and `#share-said`'s shipped shape — a node reserved for a later plan, with
the reason written at its site.

Two shipped rules stop being true here, **deliberately, on the developer's word**, and both are
turned in the open on the 03.1-04 precedent — recorded red, rewritten to assert the new contract,
recorded green:

1. **D-23** — *"a declared cost that exceeds the pool is reported, never prevented."* An
   orchestrator assumption, overruled by the person it was standing in for.
2. **The never-disable rule on the fight region** — check 95 walks 147 controls across three boards
   and asserts none is disabled; `[S07.5]`'s own banner describes the property rather than spelling
   it so a grep over the region answers zero.

**The overrule is scoped to fight declaration and nowhere else.** The never-disable rule REMAINS in
force on the build and proposal surfaces — ACT-06's comment, `[S06.5]`'s banner and every check
there are untouched, and this plan must be able to prove it. Advance is never disabled. The fight
reset is never disabled. The change-target button is never disabled. The alive toggle is never
disabled — that direction is what makes a Shield ruling recoverable at all. `#fight-start` keeps its
one sanctioned disable, which is the tool bounding what it can do to itself.

Purpose: the grid, the layout, and the honest turn.
Output: `[S06.7]` rewritten, `[C14.1]` rewritten, `[S07.5]` rewired, check 95 turned.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-fight-loop-playtest/05-REDIRECT-D27.md
@.planning/phases/05-fight-loop-playtest/05-CONTEXT.md
@.planning/phases/05-fight-loop-playtest/05-PATTERNS.md
@.planning/phases/05-fight-loop-playtest/05-07-SUMMARY.md
@.planning/phases/05-fight-loop-playtest/05-10-SUMMARY.md
@.planning/phases/05-fight-loop-playtest/05-12-SUMMARY.md
@.planning/phases/05-fight-loop-playtest/05-13-SUMMARY.md
@./CLAUDE.md
@cats-vs-mechs.html
@tests/selftest-node.cjs

<interfaces>
<!-- Read off the live artifact. Re-grep every line number before trusting it. -->

[S06.7] :12323-13363 — the region being rewritten. Keep, because each is load-bearing:
  `fgSay(box, parts)` / `fgSayOver` — ONE NODE PER FRAGMENT, `{said:...}` for the artifact's words
    and `{word:..., act:|tok:}` for the student's, the marked node carrying the exemption channel.
    Its paragraph is the whole argument and it must survive the rewrite intact: a line built as one
    string would either redden CI the day a student names an action after a comparative word, or be
    wholly exempt and take every word the ARTIFACT chose out of the only layer that can read
    render-time copy.
  `fgGoneTerm(state, action)` — ACT-07's refusal, naming the action by its LIVE name and the term by
    the **id** it still carries (`labelFor` would print the shipped label for a departed type).
  `fgHandOff(root)` — the keyboard handed forward when the focused control is disabled.
  `fgUnitName`, `fgAction`, `fgTerms`, `fgCaster` (the build-shaped shim: `maxHp` from the fight's
    `hp`, and it is FORCED not chosen — handing the fight side over unchanged makes
    `presentOnCaster` sum a field that is not there and hand a requirement line NaN, on a projector,
    silently), `fgSettle`, `fgUnitOn`, `fgSig`, `fgRest`, `fightBar`, `syncFight`.
  `FIGHT_SIDES`, `FIGHT_POOL_IDS`, `FIGHT_DECL_IDS`, `FIGHT_NOBODY_WORD`.
  `withPreservedFocus(root, fn)` scoped to `#fightbar` — the declaration nodes are rebuilt, so the
    node the keyboard is on can be destroyed under it.
  `root.dataset.fgUp` (build-once flag) and `root.dataset.fgSig` (repaint gate; **nothing ever reads
    a value back out of it**).
  Retiring: `fgFillActs`/`fgFillPicks` as CHOOSERS, `fgFillReport`'s single-selected-action shape,
  `fgFillDecls`' separate list and its per-line Clear, `fgPill`'s three kinds, `fgBuildSide`'s five
  groups, and the Declare button.
[S06.7]'s banner paragraphs that must be REWRITTEN rather than deleted: "IT DISABLES NOTHING A
  STUDENT DID", "NOTHING HERE IS EVER DISABLED", "AND NO GATE IN THIS REPOSITORY WATCHES THAT RULE
  ON THIS REGION YET" (plan 05-10 closed it; check 95 exists), and "THE SLOTS ARE NOT STATIC
  MARKUP" (still true, and the arithmetic behind it changes).
[S07.5] :17865-18251 — `FG_DECL_IDS`, `FG_PICK_KEY = {act:'fgAct', by:'fgBy', at:'fgAt'}`,
  `fgSideRoot`, `fgUnitOrNull`, `pressChoose` (page work: move one attribute, then
  `App.render.fightBar(App.state.get())` so the pressed control does not look unpressed for a
  frame), `pressDeclare`, `pressClear`, `pressAlive`, `onFightPress`/`onFightClick`,
  `onBoardFightPress`/`onBoardFightClick`, `onFightKeyDown`, `bindFight`, `bindBoardFight`.
  **Nothing is in `UI_ACTS` and nothing is in `HOLD_ACTS`, and both absences are written down.**
  The banner paragraph that must be rewritten: the one describing the disable property rather than
  spelling it, and the sentence naming D-23.
[C14.1] :1404-1616 — `.fg-legend` (18px floor), `.fg-acts`/`.fg-picks` wrap rather than shrink,
  `.fg-sides{max-height:34vh;overflow-y:auto;padding:2px}` (the bound, with its measurement table
  and the sticky-safety argument), `.fg-pill` / `.fg-pill--on` / `.fg-pill-name` / `.fg-check`,
  `.fg-say` and `.fg-word` computed-IDENTICAL on purpose, `.fg-apart`.
  **[C14]'s frame is not this block's** — no width, no margin, no overflow and no position beyond
  the one bound, so the shorthand trap and the sticky gotcha stay closed.
[S02] `affordability(faction, action)` → `{apCost, apHave, met:[{tok,need,have}]}`; requirements are
  read on the CASTER SIDE, never on the unit. `spokenFor`, `needsAt` and `defaultAt` arrive from
  plan 05-13. `labelFor(state, tokenId)` :9860 is `[S06.1]`'s and every `[S06.*]` sub-region shares
  one function scope, so it is callable directly.
[S05] `declareAction(side, actionId, by, at)` with replace-per-performer, `clearDeclaration(index)`
  (never clamped), `clearDeclarations()`. **The op never computes a default `at`.**
[S06.5] `missingTerm(state, live)` :11435 — the first term naming a departed type, or null.
`refActions(state, side)` :9540 — the side's action list, read never re-derived.
[S03] `state.fight[side].tally` — side-scope student-invented tallies (D-24: anything that walks
  token types walks ALL of them, shipped and student-made alike, with no second tier).
tests/selftest-node.cjs fight helpers :8255-8310 — `fgPress`, `fgSideRootOf`, `fgOne`, `fgPick`,
  `fgDeclare`, `fgAdvancePress`, `fgAliveBtn`, `fgLeaves`, `fgActNamed`, and the played board built
  at :8315-8320. `disabledIn(root)` :5947. Check 95 :8600-8686. Next free check number **103** at
  the time of writing, plus whatever 05-12 took — **verify by reading before you number.**
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: the column — [ROUND] above both, then side, battlefield slot, team resources, picker rows</name>
  <files>cats-vs-mechs.html, tests/selftest-node.cjs</files>
  <action>
Rewrite `[S06.7]` so `#fightbar` draws D-27's sketch in the addendum's order, and rewrite `[C14.1]`
to lay it out. **Remove the old declaration form in this same task** — there must never be two ways
to declare, not even between two commits of one plan.

**`[ROUND]` spans above both columns.** Draw the round figure inside `#fightbar`, beside
`#fight-head`, outside `.fg-sides` so it spans rather than repeating per side. The topbar readout
(`#round-label` / `#round-count`) stays and keeps being written — this plan owns no shell markup —
and **the reason is now a real one rather than a shrug: the topbar is the ONE reading present in
both views**, so a student on the board tab still sees the round and both pools. Write that down,
and record it as a question for the playtest anyway.

**Then, per side, inside that side's existing `#decl-{side}` root, top to bottom** (no new shell id;
the two roots and `.fg-sides` are exactly the containers this needs, which is why the ids do not
move):

1. **the side** — its faction name, and its still-standing reading, as today.

2. **the battlefield cluster — built EMPTY and FLAGGED, and this plan paints nothing inside it.**
   One container per side, class-only (`.fg-field`), no id: nothing queries it by id, nothing
   dispatches off it and no reading is taken from it here, so the three-part rule is a cost with
   nothing to buy — the same argument `.fg-band` records about itself. It is **built once and never
   replaced** by any later pass in this region, because plan 05-15 owns everything inside it and a
   `replaceChildren` on its parent would throw the battlefield away every repaint. Write the reason
   at the site and name plan 05-15 as its owner, in `#fight-said`'s and `#share-said`'s shipped
   register for a node reserved for a later plan. `fgRest`'s teardown may empty it with the rest of
   the root — that is correct, and say so, because a rebuilt container is a container `[S06.11]`
   repaints.

3. **team resources** — the addendum's own words, *"Place 'team' resources above the action
   picker."* Per side, and **every resource the side holds, not only action points** (D-24: anything
   that walks token types walks all of them, shipped and student-made alike, with no second tier):
     - action points as **two readings**: what this round's declarations have spoken for, and what is
       left to spend. Task 3 writes what they say; in THIS task leave them reading as they do today
       and say in the commit that task 3 owns them.
     - every entry in `state.fight[side].tally`, each with its own token label from `labelFor` and
       the token-name exemption channel on the label node, so a renamed type follows and a student's
       word never reddens CI.
   The topbar's `#pool-cats` / `#pool-mechs` keep being written for the reason above.

4. **one picker row per unit** on that side's fight roster, in roster order. Each row is:
     - **the unit's name, as a LABEL and not a control.** D-27's sketch draws `[C1]` in brackets,
       but the addendum moves the only job a unit control had — being the click surface for
       targeting — onto the battlefield. A control with nothing to do is worse than a label, and two
       nodes for one unit would need two `data-k`s and check 94b counts them. **Record this as a
       decision and put it to the developer at the playtest**: if the row's unit should be pressable,
       that is one arm. The name carries **no exemption marker**, which is `unitPick`'s shipped
       ruling: no op in this file renames a unit, so it is a word the ARTIFACT says.
     - **one button per action that side carries**, from `refActions(state, side)` — read, never
       re-derived. `data-fg="act"`, `data-fg-side`, `data-fg-by` = the row's unit id,
       `data-fg-val` = the action id, `data-k="fg/act/{side}/{unit}/{action}"`.
       Each carries the action's name in a node marked `data-anm` (the student's word, exempt from
       the walk) **and its cost as separate nodes** — the figure, and the token's label marked with
       the exemption channel so a renamed token follows. An action with no cost says so in the
       artifact's own words rather than showing a blank.
     - the state of the row said **three times and in colour none of them**, on the declared
       button: the class, `aria-pressed`, and a tick that is a REAL NODE. `[C07]`'s rule, restated
       in `[C12]`, `[C13]` and `[C14.1]`, and the reason is the room at the back.
     - **when this unit's standing declaration names somebody, a reading on the row saying so**,
       built with `fgSay`'s one-node-per-fragment idiom and the shipped wording, **followed by the
       change-target button** (task 2 gives that button its behaviour; build it here, and in this
       task it declines quietly, with the decline written down rather than left as a fall-through).
     This reading replaces the separate "Declared so far" list; the list and its per-line Clear both
     go, because the pressed button IS the declaration and a second spelling of it would drift.

5. the round controls (`Advance the round`, `Reset this fight`, and the
   `Nothing resolves until you advance.` note) stay where `fgBuildRound` puts them — appended last,
   **below** `#fight-said`, for the reason written there: FIGHT-10's line sits between the sides and
   the Advance a student is about to press.

**Both sides visible at once, in columns.** `.fg-sides` keeps its flex row. **Re-derive both dials.**
The surface now grows with the ROSTER (one row per unit, one battlefield cluster) rather than with a
chooser list, and `[C14]`'s side-by-side split gives `#fightbar` a 736px basis whose arithmetic
(696px of content for two 340px columns) was measured against the old content. **Both numbers are
now almost certainly wrong, and a third region is coming in 05-15**, so measure, set them from the
measurement, and write into `[C14.1]` that the battlefield's arrival will move them again and that
plan 05-15 owes the re-measure. Measure in real Chrome and real Edge at 1920x1080 and 1366x768, on
9-and-3 and again at 24-a-side. **Do not solve anything by turning a dial down without saying what
it costs.** Note that under the tab the fight no longer competes with the board for one viewport, so
a dial that was buying headroom may not need to any more.

**Rewrite the retiring paragraphs rather than deleting them.** `[S06.7]`'s "IT DISABLES NOTHING A
STUDENT DID", "NOTHING HERE IS EVER DISABLED", and the paragraph saying no gate watches the rule —
each becomes a paragraph stating what is true now, naming D-27 and naming the scope of the overrule.
Task 3 writes the disable; in THIS task, state that it is coming and that nothing is disabled yet,
so no comment is ahead of the code.

**`[S07.5]`, rewired in this same task** — a grid nothing can press is not a surface:
  - `data-fg="act"` → **declare, undo or replace, decided from STATE and never from `aria-pressed`.**
    Read `state.fight.decl` for a standing declaration by this row's unit:
      * none → dispatch `declare`. **`at` is `App.model.needsAt(action)` ? `App.model.defaultAt(...)`
        : `null`** — the surface computes the default and passes it, and plan 05-13's paragraph
        beside the op says why the op must never do it;
      * one naming THIS action → find its index in the live list and
        `App.ops.dispatch('clearDeclaration', { index })`. Read the index off state; never off an
        attribute, and never clamped — `clearDeclaration`'s own paragraph says why;
      * one naming a DIFFERENT action → dispatch `declare` again with the new action and a freshly
        computed default `at`; plan 05-13's op replaces it in one commit, so one press is one undo
        entry. **The old action's target does not carry over** — a target chosen for Slash is not a
        target chosen for Lasers, and the default is recomputed. Say so.
    The flag is read off state and not off the page, for `fgAliveOn`'s stated reason: the renderer
    writes `aria-pressed` FROM the state, so reading it back asks the page what the state is through
    a copy the page made.
  - `data-fg="at"` (the change-target button) → task 2's arm. **In this task it declines quietly.**
  - `data-fg="declare"`, `"clear"`, `"by"` and the old `"at"` chooser arms and the `FG_PICK_KEY`
    table **retire with the controls they served**. `fgUnitOrNull` survives if task 2 still needs it;
    if not, retire it with a sentence.
  - `advance`, `reset` and the board's `alive` arm are **untouched**.
  - `onFightKeyDown`'s scope is `[data-fg]` and `[data-dc]` and does not move.
  - Still nothing in `UI_ACTS` and nothing in `HOLD_ACTS`, and both absences stay written down.
  - Edit **not one line** of `[S07.1]`–`[S07.4]` or `[S07.6]`. Prove it by line span and an empty
    diff.

**And rewrite the harness's DRIVE helpers in the same commit**, so the suite never goes red for a
control that stopped existing: `fgPick` and `fgDeclare` press the new grid (find the action button
by `data-fg-side` + `data-fg-by` + `data-fg-val`, never by position). Change **no row's claim** —
only what it presses. Where a row's claim genuinely cannot survive, leave it red and hand it to
plan 05-16 **by number**, in the summary, with the reading; do not delete it and do not weaken it.

Drive the whole of it in real Chrome and real Edge at both viewports: a full round declared on both
sides by real clicks and advanced, with page errors and console errors `[]`.
  </action>
  <verify>
    <automated>node tests/selftest-node.cjs</automated>
  </verify>
  <done>
`node tests/selftest-node.cjs` exits 0 or exits 1 **only** on rows explicitly handed to 05-16 by
number with their readings recorded. The column reads top to bottom in the addendum's order, proved
by walking `#decl-cats`'s children and recording the sequence. Exactly one way to declare exists:
grep the region and record that there is no Declare control and no chooser. `.fg-field` exists per
side, is empty, and is not replaced by any repaint — driven over ten frames and asserted by node
identity. `[S07.1]`–`[S07.4]` and `[S07.6]` diffs proved empty by line span. Both dials re-measured
in two browsers at two viewports with the table written into `[C14.1]`. `#strip` still `sticky`, its
ancestors still `visible`, in both browsers.
  </done>
</task>

<task type="auto">
  <name>Task 2: default, don't ask — the target the tool picks and the button that changes it</name>
  <files>cats-vs-mechs.html, tests/selftest-node.cjs</files>
  <action>
D-27's addendum, verbatim: *"Default target to the lowest health enemy. Add a button to change target
(rather than require a picker every time)."* The two-click-every-time flow the earlier interpretation
call described is **superseded**; the lights-up flow survives as the CHANGE path only.

**The one-click path is task 1's and is already shipped by it.** This task builds the change path.

**The change-target button.** Drawn on any picker row whose standing declaration names somebody, or
whose action `needsAt` and whose target came back null. `data-fg="at"`, `data-fg-side`,
`data-fg-by` = the row's unit id, `data-k="fg/at/{side}/{unit}"`. Its label is a permanently visible
18px text label in the artifact's own words (UX-02: never icon-only, never a `title=`), screened
against all three word lists and the eight. **It is never disabled** — it is not one of the three
disable conditions and this tier does not add a fourth.

**The lights-up flow, reusing the bookkeeping that already ships.** A form's transient selection is
not state — the shell comment on `#act-edit-propose` argues it in full and `[S09.3]` pins the `ui`
key set so there is nowhere legitimate for it to go. So the half-made retarget lives on the acting
side's `#decl-{side}` root, in the two attributes that are already there:
  - `dataset.fgAct` — the action whose target is being changed;
  - `dataset.fgBy` — the unit whose declaration it is.
`dataset.fgAt` retires; there is no third attribute, because the completing click IS the target and
it dispatches immediately. **Do not introduce any attribute or state key matching
`/propos|override|caster|target|pending/i`** — `by` and `at` are the vocabulary, check 73c walks
state at any depth, and the two attributes above already clear it.

`fgSettle` is amended to settle these two rather than three, and keeps its rule: **it settles to
NOTHING and never to somebody else.** An action a student removed or a unit that left the board
leaves the half-made retarget standing on nobody, which is the position they can read and correct.
`fgSig`'s third slot follows the attributes it fingerprints — and it stays **wide before the code
that draws the thing**, which is 03.1-05's recorded lesson.

**Where the targets light: the BATTLEFIELD, not the picker rows.** The addendum is explicit —
*"the battlefield is read-only except as the click surface for the change-target flow, whose targets
light there."* This plan owns the two root attributes and the press arm; **plan 05-15 draws the lit
state and the unit shapes that carry it.** So in this task:
  - write the two attributes and repaint;
  - handle `data-fg="bf"` presses — the battlefield's per-unit control, whose key is
    `fg/bf/{side}/{unit}` and whose spelling is fixed HERE so plan 05-15 builds to it. Record the
    contract in `[S07.5]`'s banner as a named requirement on plan 05-15, in the register plan 05-07
    used to hand requirements to plan 05-10;
  - until 05-15 ships, the flow is reachable and asserts through the state and the attributes, and
    the row's own reading is what a student sees change. Say so; do not fake a lit state in this
    region.

**The presses**, in `[S07.5]`:
  - `data-fg="at"` on a row with a standing declaration → **page work**: write the two attributes,
    call `App.render.fightBar(App.state.get())` so the pressed control does not look unpressed for a
    frame, dispatch nothing. If the two attributes already name THIS row, **clear them** — pressing
    the change-target button twice cancels the change and leaves the original declaration standing.
  - `data-fg="bf"` while a retarget is half-made **and the pressed unit is on the opposing side** →
    dispatch `declare` with the same `side`, `act` and `by` and the pressed unit as `at`, then clear
    the two attributes. Plan 05-13's op replaces in place, so `act` and `by` stand and only `at`
    moves, in one commit and one undo entry.
  - `data-fg="bf"` at any other time, or on a unit that is not a legal pick → **a press that has
    nothing to do declines quietly.** `clearDeclarations` returning `false` over an empty list is
    the shipped precedent, and an error panel raised in the middle of a workshop is exactly what a
    disabled button would have been avoiding.
  - `data-fg="act"` while a retarget is half-made → the action press wins and the half-made retarget
    is cleared with it. *"Re-click of the declared action cancels, target and all."*

**AND THIS IS WHERE D-27's TARGETING NARROWS A RECORDED RULING — write it at the site rather than
only in this plan.** `declareAction`'s own paragraph and 03.1-07's asymmetry say `at` may name **any
unit on the board, either side**, because *"a heal that lands on a friend and a hit that lands on a
foe are the same record with a different unit chosen, and the student chooses"*, and *"who an action
may be pointed at is exactly the sort of question this tool does not answer."* Two things narrow it
now: `defaultAt` picks an **enemy**, and the change flow lights the **opposing** side. So a
heal-shaped action — one whose `xf` term has a positive delta and lands on a target — gets an enemy
by default and can only be pointed at an enemy from this surface. **Ship it as the developer wrote
it**, but:
  - put the "which side may be picked" choice behind **one predicate with one line in it**, so
    widening it to both sides is one edit;
  - write the ruling it narrows, the one-line widening, **and the heal-shaped case as the concrete
    example that will surface it** in the comment beside it;
  - leave `declareAction` and `unitAnywhere` untouched, so the OP still accepts either side and the
    restriction lives only where a student can see it;
  - name it in the summary as the playtest question it is.

Add the interaction-gate rows for this flow, numbered from the next free number (verify by reading):
  - **an action that `needsAt` declares in ONE press**, and the record's `at` is the lowest-health
    living enemy — asserted against `App.model.defaultAt`'s own answer on the same board, so the row
    reads the derivation rather than re-implementing it;
  - an action that does not `needsAt` declares in one press with `at: null`;
  - **the default skips a dead-ruled enemy** and **does not skip a zero-health enemy nobody ruled**,
    read off the RECORD after a real press (05-13 asserts the derivation; this row asserts the
    surface actually uses it);
  - pressing the change-target button dispatches **nothing** — the state is byte-identical — and
    writes the two attributes;
  - pressing it twice clears them and leaves the declaration standing, unchanged;
  - a `data-fg="bf"` press on an opposing unit while half-made moves **only** `at`: `act` and `by`
    stand, the list does not grow, and **one undo takes the retarget back**;
  - a `data-fg="bf"` press at rest moves neither the state nor the page and leaves the error panel
    shut;
  - a `data-fg="act"` press while half-made clears the attributes;
  - **nothing anywhere is disabled by any of it** — reuse `disabledIn` and compare the set across
    the whole flow. This row is the never-disable rule still holding on everything task 3 does not
    touch, asserted BEFORE task 3 turns it, so the turn has a control run to be read against.

**Keep ACT-07's refusal reachable.** `fgGoneTerm`'s sentence — *"Cannot fire Sting. It names t1,
which is no longer a token type on this board. Nothing has changed."* — names the action by its LIVE
name and the term by the **id** it still carries, and it must still be rendered somewhere a student
looking at this surface will read it. Put it in the side's own reading box. **The button stays
enabled**: a departed term is not one of the three disable conditions.

**PROBE AN — the half-made retarget written into `state.fight`.** Run after this task's commit,
record verbatim, revert from a scratchpad `cp` snapshot. Expect `[S09.3]`'s key-set row and/or check
73c to redden. **If the suite stays green, the rows are asserting nothing — fix them and record what
was wrong.**

**PROBE AW — the default computed from `hp` alone, ignoring the `alive` flag.** Expect the
skips-a-dead-ruled-enemy row to redden. Same protocol. **If it stays green, fix the row and record
what was wrong** — this is D-00d, the file's oldest ruling about that flag.
  </action>
  <verify>
    <automated>node tests/selftest-node.cjs</automated>
  </verify>
  <done>
Suite green (or red only on rows handed to 05-16 by number). The one-click declare and the
change-target flow driven in real Chrome and real Edge with real clicks at both viewports, page
errors `[]`. `disabledIn` over the whole flow returns a set with no `=true` except `#fight-start`'s.
Both probes recorded verbatim, tree clean after each revert. The narrowing of 03.1-07's ruling
written at the site with its one-line widening and the heal-shaped example. The `data-fg="bf"` key
contract written into `[S07.5]`'s banner as a named requirement on plan 05-15.
  </done>
</task>

<task type="auto">
  <name>Task 3: the spoken-for resources, the disable contract, and the two overruled rules turned in the open</name>
  <files>cats-vs-mechs.html, tests/selftest-node.cjs</files>
  <action>
**The preview.** Rewrite each side's team-resources block so its action-point readings deplete as
declarations accumulate, from `App.model.spokenFor` — computed at render time, storing nothing. Two
readings, distinguished four ways as the shipped pair already is (different words, different weight,
a border, a tint derived with `color-mix()` from an existing `[C00]` token — three of the four
survive a projector with the colour washed out, and the two readings are also different POSITIONS,
which is the fourth):
  - what this round's declarations have spoken for;
  - what is left to spend.
The wording is the artifact's and it is screened against all three of the gate's word lists before
it is written, plus the eight that are mechanically clean and still unshippable. **This is FIGHT-09's
"spent visibly distinct from available", and it is the resolution of the apSpent-reads-zero blocker**
plan 05-07 measured and 05-10's acceptance run read back verbatim. Write that lineage into the
comment: the number moves now because the reading changed, not because `advanceRound` did — its
same-commit refill is untouched and nothing resolves until Advance.

The side's tally readings sit beside them and are **not** depleted, because no shipped cost is paid
out of a tally pool — `affordability` has a figure for action points and for nothing else, and D-16's
stance is that a cost this report cannot price is said plainly rather than dropped. Say so at the
site so the asymmetry reads as a decision.

**The disable contract, written as an `if and only if` and implemented as one.** An action button on
this grid is disabled **iff**:
  (a) `App.model.affordability(fgCaster(state, side), action)` reports a requirement term whose
      `have < need` — requirements read on the CASTER SIDE, which is the shipped machinery's own
      stated rule and not a change this plan makes; **or**
  (b) the pool available to this row cannot pay the action's cost, where the pool available is
      `fight[side].ap − spokenFor(side) + (the cost this row's own standing declaration already
      spoke for)`. **The `+` is load-bearing and the reason goes in the comment**: replacing an
      action frees the one it replaces, and without it a unit's own declared action would disable
      itself and re-click-to-undo would be unreachable. A cost `affordability` cannot price
      (`apCost === null`) does **not** disable — the tool cannot show it cannot pay, so it does not
      act as though it can; **or**
  (c) the row's unit has been ruled dead (`alive === false`). D-27: *"this is driven by the
      student's own ruling, so it is bookkeeping, not adjudication."* **A unit at zero health that
      nobody ruled is NOT dead and its actions are NOT disabled** — D-00d, and this is the second
      place in the phase where a tidy implementation loses it.

**And nothing else on this page is disabled by anything a student did.** Advance stays enabled on a
side that cannot pay. Reset stays enabled. The change-target button stays enabled. The alive toggle
stays enabled on a unit already marked — the direction that makes a Shield ruling recoverable. The
battlefield's unit controls are never disabled; unlit is not disabled. `#fight-start` keeps its one
sanctioned disable. Every one of these is asserted, not asserted around.

**Re-decided from state on every repaint, never toggled by a handler** — `setEditorEnabled`'s rule
and its reason: re-deciding is what makes an undo move it too. `[S07.5]` writes no `disabled`
anywhere, and that property is now stronger than the grep it replaces: **the disable is a RENDER
decision.** Say it in `[S07.5]`'s banner in place of the paragraph that described the property
rather than spelling it, and give it a numbered row of its own so it cannot be lost to a plan
acceptance line again.

**Visibly distinct, and never a judgement word.** The disabled treatment is said in at least two
non-colour channels — the shipped `.brd-btn:disabled` opacity is one, and it is **not enough on its
own** because [C07]'s rule is that a state is never in colour or dimming alone. Add a second
structural channel in `[C14.1]` (a border treatment is the cheapest that survives a projector) and
measure both in a real browser. **No text anywhere says why**, on the button or beside it: the
reason is legible as arithmetic — the cost is on the button, the team resources are directly above
it, and the side's requirement readings carry `affordability`'s shipped sentences verbatim
(`"Slash needs 99 Health of 27. Requirement not met."`), one line per action that a requirement
disables, deduplicated per action rather than per unit. That wording is deliberately the SAME wording
the authoring dialog uses: a student who read the cost of an action there and reads it again here
must not have to work out whether the two sentences mean the same thing.

## THE TWO TURNS, IN THE OPEN — 03.1-04's precedent, and the protocol is not optional

**Turn 1 — check 95.** Before rewriting it: ship the disable, run the suite, and **record check 95's
RED output verbatim**, with its control count and its three-board comparison. That red is the
evidence the row was watching something. Then rewrite it — **rewritten, never deleted; a deleted
boundary assertion is an Out of Scope entry that has quietly stopped being enforced** (72b's own
words, and the treatment `[S09.10]`'s two rows got in the same register). The new row asserts the
new contract precisely, in both directions, over the same three like-for-like boards it already
builds (a fight running and exactly one declaration standing, differing only in what the side can
pay, what it can meet, and who has been ruled dead):

  - **the set OUTSIDE the grid's action buttons is identical across all three boards** — that is the
    never-disable rule still in force on everything the overrule does not reach, and it is the half
    most likely to erode;
  - **inside the grid, disabled is exactly the three conditions** — for each of the three boards,
    compute the expected disabled set from `(a) or (b) or (c)` independently of the render and
    compare it to what the page actually shows, **both directions**: nothing disabled that should
    not be, nothing enabled that should be;
  - the Advance entry is still `fg/advance=false`; the change-target buttons are enabled; the alive
    toggles are still enabled on the units that were ruled; the one `=true` outside the grid is
    still `#fight-start` and is still **named** rather than tolerated;
  - the row's label says which rule it replaced, on whose word, and that the never-disable rule
    remains in force on the build and proposal surfaces.
Record it **GREEN** afterwards, with the reading.

**Turn 2 — the `[S07.5]` disabled-grep-0 assertion.** It shipped as an acceptance line in plan
05-10, not as a numbered check, which is exactly how a boundary gets forgotten. Re-home it as a
numbered row that asserts the stronger property the new contract makes true: **no handler writes a
disabled state — every disabled state on the page is written by the render tier and is re-decided
from state on every repaint.** Drive it: disable a set through a real declaration, take a frame that
changes nothing, and assert the set is re-derived rather than sticky; then drive an **undo** and
assert the set follows it back. Record the old acceptance line's reading, the new row's reading, and
say in the summary that the grep still answers zero over `[S07.5]` and now answers to a check.

**Prove the scope of the overrule.** Add a row, or extend an existing one, asserting that the
never-disable rule on the **build and proposal surfaces** is untouched: `disabledIn` over the
proposal pane and over the board's own controls, compared across a funded and a starved board, still
identical. If a shipped check already makes that claim, name it and record its reading rather than
adding a second.

**PROBE AP — the disable widened to the Advance control when a side cannot pay** (probe W's exact
violation, driven again against the NEW row). Expect the rewritten 95 to redden on its
outside-the-grid clause. **If it stays green, the row is asserting nothing — fix it and record what
was wrong.**

**PROBE AQ — condition (b)'s `+ own pledge` removed**, so a unit's own declared action disables
itself. Expect the rewritten 95 to redden on its inside-the-grid clause. **If it stays green, fix
the row and record what was wrong.**

**PROBE AR — the alive toggle disabled on a unit ruled dead** (probe AI, driven again). Expect 95
and 98 to redden, naming the toggles by key. **If it stays green, fix the row and record what was
wrong.**

**PROBE AX — condition (c) driven off `hp === 0` instead of the stored `alive` flag.** Expect the
new 95's inside-the-grid clause to redden in both directions on a board holding one zero-health
unruled unit and one full-health ruled unit. **If it stays green, fix the row and record what was
wrong.**

Each probe: run after this task's commit, record verbatim, revert with a `cp` from a scratchpad
snapshot, and confirm `git status --short` is clean. **`git checkout --` is never used.**
  </action>
  <verify>
    <automated>node tests/selftest-node.cjs</automated>
  </verify>
  <done>
Suite green at the end, with check 95's RED recorded verbatim before the rewrite and GREEN after.
The interaction gate at its new count, recorded. Four probes recorded verbatim, each with a clean
tree after its revert. The disabled treatment measured in two non-colour channels in real Chrome and
real Edge. No rendered word explains a disable, and no test label carries one of the eight
mechanically-clean-but-unshippable words. `disabledIn` over the proposal pane and the board is
unchanged across boards, recorded.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| student press → op | `declare` and `clearDeclaration` are the only two ops this surface sends |
| render tier → `disabled` | the first place in this file where a student's own board can take a control away |
| a default target → the adjudication line | the tool proposes a target; the student may change it in one press |
| student's action names → the no-verdict gate | every rendered fragment crosses Layer C |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-57 | Elevation of privilege | the overrule leaking off the fight grid onto Advance, Reset, the change-target button, the alive toggle, the board or the proposal pane | mitigate | check 95's outside-the-grid clause compared as a whole set across three like-for-like boards; **probes AP and AR drive it** |
| T-05-58 | Denial of service | a unit's own declared action disabling itself, making re-click-to-undo unreachable | mitigate | condition (b)'s `+ own pledge`, with the reason at the site; **probe AQ drives it** |
| T-05-59 | Tampering | a handler toggling `disabled`, so an undo leaves a stale disable on the page | mitigate | the disable is re-decided from state every repaint; the re-homed grep row drives an undo and reads the set back |
| T-05-60 | Information disclosure | a rendered word explaining a disable and reading as a judgement | mitigate | no explanatory text on or beside a disabled button; the reason is arithmetic already on the page; every string screened against all three word lists and the eight |
| T-05-61 | Repudiation | a student's action name reddening CI on a legitimate replay, now that names sit inside a button with a cost | mitigate | `fgSay`'s one-node-per-fragment idiom kept intact; `data-anm` on the name node and the exemption channel on the token label; the button itself is never marked, because marking it would take the tick out of the harvest |
| T-05-62 | Tampering | the half-made retarget written into `state`, so it rides in a build code or an undo entry | mitigate | it lives on the two root attributes only; **probe AN drives the violation** |
| T-05-71 | Elevation of privilege | condition (c) or the default read off `hp === 0` rather than the stored `alive` flag, so a Shield ruling becomes unrepresentable | mitigate | D-00d written at both sites; **probes AW and AX drive both** |
| T-05-72 | Information disclosure | the change-target flow silently becoming the only way to name a target, hiding that `at` may name either side | mitigate | the narrowing, its one-line widening and the heal-shaped example written at the site; `declareAction` and `unitAnywhere` untouched; named as a playtest question |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | zero packages installed; Playwright resolved through `PLAYWRIGHT_DIR` |
</threat_model>

<verification>
- `node tests/selftest-node.cjs` exits 0 at the end of task 3. Any row handed to plan 05-16 is
  handed **by number, with its reading**, and the suite is green over it — never left red.
- `advanceRound` and `ruleRound` byte-identical: prove by line span and an empty diff.
- `[S07.1]`–`[S07.4]`, `[S07.6]`, `[S06.1]`–`[S06.6]`, `[S06.8]`–`[S06.10]`, `[C14]`, `[C14.2]`,
  `[C14.3]`, `[C15]` and the static shell all diff **empty** — prove by line span. `[C14.4]` and
  `[S06.11]` do not exist yet and are not opened here.
- Stub-drift unchanged at the post-05-12 count — this plan adds no shell id.
- Both naming greps 0; `url(` 0; `createElementNS|<svg` 0; `innerHTML` 0; the hex pattern over
  `[C14.1]` 0. One classic `<script>`, one `<style>`.
- Every rendered word screened against Layer A's, Layer B's and Layer C's lists **and** against
  `contested`, `one-sided`, `blowout`, `lopsided`, `even`, `close`, `tight`, `behind`. A round is
  never a "Day". No state-object key matches `/propos|override|caster|target|pending/i`.
- `DEFAULTS.cats.ap` untouched (D-25).
- Real Chrome and real Edge, 1920x1080 and 1366x768, every task: layout, `#strip` sticky and pinning,
  a full round by real clicks, page errors and console errors `[]`.
</verification>

<success_criteria>
A student opens the fight tab, sees the round above both sides, each side's battlefield slot, its
team resources, and one row per unit. They press an action and it is declared — pointed at the
weakest enemy without being asked — and that side's resources drop by what it will cost, and nothing
has happened yet. They press the change button and pick somebody else. They press the action again
and it all comes back. An action they cannot pay for, cannot meet, or whose unit they have ruled dead
is visibly out of reach, and the page does not say a word about why beyond the numbers already on it.
Then they press Advance, and that is the only thing that has ever landed anything.
</success_criteria>

<output>
Create `.planning/phases/05-fight-loop-playtest/05-14-SUMMARY.md` when done.
</output>
