---
phase: 01-foundation-data-state-funnel-undo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cats-vs-mechs.html
  - tests/selftest-node.cjs
autonomous: true
requirements: [ALLOC-08, UX-04]
section_ownership:
  owns:
    - "cats-vs-mechs.html — <head>, entire <style> block, <body> static shell markup"
    - "script region [S00] APP (namespace + hasFlag)"
    - "script region [S01] DATA"
    - "script region [S02] MODEL"
    - "script region [S09] SELFTEST — harness, report renderer, gate, sub-blocks [S09.1] and [S09.2]"
    - "script regions [S04] SERIALIZE / [S06] RENDER / [S07] INTERACTIONS — banner + declared no-op stub surface only (real bodies are Phase 2 and Phase 4)"
    - "script regions [S03] STATE / [S05] OPS / [S08] BOOT — banner + declared stub surface only; plan 01-02 replaces the stub bodies"
    - "script region [S10] LAUNCH"
    - "tests/selftest-node.cjs (dev-only, never shipped)"
  must_not_touch:
    - "nothing — this plan creates the file"
must_haves:
  truths:
    - "Double-clicking cats-vs-mechs.html opens a styled dark page in a modern desktop browser with zero console errors and zero outbound requests"
    - "Opening the file with no hash renders no self-test report at all"
    - "Opening the file with #selftest renders a readable pass/fail report, styled with the course design tokens, listing every Workshop 16 default assertion by name"
    - "The report confirms the Workshop 16 board loads exactly: Cats 9 units at 3 HP with Slash 1 damage / Hairball Slowdown / Screech Confuse; Mechs 3 units at 6 HP and 3 shield with Fly Evade / Lasers 3 damage Range / Recharge Shield; 3 faction action points each"
    - "DEFAULTS is deep-frozen: a nested write throws in strict mode and changes nothing"
    - "App.data.defaults() returns a fresh deep copy on every call; mutating the result never reaches DEFAULTS"
    - "App.model derivations are pure, take state as an argument, and return integers for the shipped defaults"
  artifacts:
    - path: "cats-vs-mechs.html"
      provides: "The whole artifact skeleton: head, style, static shell, TOC comment, all ten script regions, frozen board data, pure model, self-test harness"
      contains: "[S01] DATA"
    - path: "cats-vs-mechs.html"
      provides: "Course design tokens inherited verbatim from the sibling artifacts"
      contains: "--accent:#5cc8ff"
    - path: "cats-vs-mechs.html"
      provides: "Deep-frozen Workshop 16 defaults"
      contains: "deepFreeze"
    - path: "tests/selftest-node.cjs"
      provides: "Dev-only headless runner: forbidden-pattern scan of the HTML plus App.selftest.run()"
      contains: "selftest"
  key_links:
    - from: "cats-vs-mechs.html [S09] SELFTEST"
      to: "App.data.defaults()"
      via: "suite [S09.1] asserting the board numbers"
      pattern: "App\\.data\\.defaults\\(\\)"
    - from: "cats-vs-mechs.html [S09] SELFTEST"
      to: "App.model"
      via: "suite [S09.2] asserting eHP and damage-throughput derivations"
      pattern: "App\\.model\\.factionEhp"
    - from: "cats-vs-mechs.html [S08] BOOT start()"
      to: "App.selftest.report(App.selftest.run())"
      via: "exact-match hash flag gate App.hasFlag('selftest')"
      pattern: "App\\.hasFlag\\('selftest'\\)"
    - from: "tests/selftest-node.cjs"
      to: "cats-vs-mechs.html script body"
      via: "vm.runInNewContext with no document and no location"
      pattern: "runInNewContext"
---

<objective>
Create `cats-vs-mechs.html` — the whole file skeleton, the frozen Workshop 16 board data, the pure
derivation layer, and the self-test harness that proves the data loads exactly as the board
specifies.

Purpose: ROADMAP Phase 1 success criteria 1 and 2. This plan makes the artifact exist, open offline,
and prove its own data. It also lays the section scaffold that every later phase writes into —
retrofitting structure into a 3,000-line single file never happens (PITFALLS.md Pitfall 12).

Output: `cats-vs-mechs.html` (opens by double-click, styled, silent on a normal open, a readable
pass/fail report under `#selftest`) and `tests/selftest-node.cjs` (dev-only headless gate).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-data-state-funnel-undo/01-CONTEXT.md
@.planning/phases/01-foundation-data-state-funnel-undo/01-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Contracts this plan CREATES. Plan 01-02 and every later phase consume these exact names. -->
<!-- Do not explore the codebase for them — there is no codebase. These are the contracts. -->

Global namespace (region [S00] APP):
  App                              — the single global; every section hangs off it
  App.hasFlag(name) -> boolean     — exact-match URL hash flag test; safe when `location` is undefined

Region [S01] DATA:
  App.data.DEFAULTS                — deep-frozen Workshop 16 board object
  App.data.defaults() -> object    — fresh deep copy of DEFAULTS (JSON deep copy)
  App.data.deepFreeze(o) -> o      — recursive Object.freeze; exported because [S03] STATE reuses it

Region [S02] MODEL (all pure, all take state or a slice as an argument):
  App.model.unitEhp(unit) -> int
  App.model.factionEhp(faction) -> int
  App.model.bestDamage(faction) -> int
  App.model.factionDps(faction, activeUnits) -> int
  App.model.aliveCount(fightSide) -> int
  App.model.apSpent(buildFaction, fightSide) -> int

Region [S09] SELFTEST:
  App.selftest.suite(name, fn)     — register a suite; fn receives an assert object `t`
  App.selftest.run() -> { records: [{suite, name, pass, actual, expected}], passed, failed }
  App.selftest.report(result)      — renders into #selftest-report using DOM APIs + textContent only

Region [S08] BOOT (stub here, real body in plan 01-02):
  App.boot.attempt(label, fn)      — run fn now inside try/catch; non-terminal
  App.boot.start()                 — entry point; called once from region [S10] LAUNCH

Declared no-op stub surfaces (call sites must exist now so they are never retrofitted):
  App.serialize.scheduleUrlSync()  — Phase 4 (plan 04-01) fills this
  App.render.structure(state)      — Phase 2 (plan 02-01) fills this
  App.render.sync(state)           — Phase 2 (plan 02-01) fills this
  App.interactions.bind()          — Phase 2 (plan 02-02) fills this

State shape (defined and built in plan 01-02; recorded here because [S02] MODEL takes it as an
argument and the [S03] STATE banner must document it):
  { build: { schema, cats, mechs }, fight: null | { round, turn, cats, mechs, log }, ui: { kbdNav } }
  fight side shape: { ap, units: [ { id, hp, alive } ] }
  There is NO `mode` key anywhere. Mode is derived from `fight !== null`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the file skeleton — head, style, static shell, section scaffold, self-test harness, headless runner</name>
  <files>cats-vs-mechs.html, tests/selftest-node.cjs</files>

  <read_first>
    - `C:\Projects\GameDesignSkills\GameFeelDirectionCourse\game-feel-study-guide.html` lines 1-35 (doctype/head/`:root` tokens/base rules), 88-118 (`.card`, `.grid`, `.eyebrow`, `.muted`, `.faint`, `.callout` + modifiers), 142-143 (`.ex` pill), 178-185 and 663-709 (style close, body open, script placement) — read with `sed -n` on those ranges, do not read all 709 lines
    - `C:\Projects\GameDesignSkills\GameFeelDirectionCourse\game-feel-types-frameworks.html` lines 8-14 (the appended-token-line convention) and 87-91 (`.pro`/`.con` pass/fail treatment)
    - `.planning/phases/01-foundation-data-state-funnel-undo/01-PATTERNS.md` — the whole file; it quotes every excerpt you need and names the exact conventions to match
    - `./CLAUDE.md` — the "What NOT to Use" table is binding
  </read_first>

  <action>
Create `cats-vs-mechs.html` at the project root (D-17: flat kebab-case, matching the siblings).

**Head.** Copy the sibling head block byte-for-byte — uppercase `<!DOCTYPE html>`, `<html lang="en">`,
`<head>` with exactly `<meta charset="UTF-8">` then `<meta name="viewport" content="width=device-width, initial-scale=1.0">`,
then `<title>`, then `<style>`, with no indentation on the `<head>` children. Set the title to
`Cats vs Mechs — Workshop 16 Interactive Sample`. Add no favicon, no `<link>`, no description meta.

**Style block.** One `<style>` in `<head>`, tag at column 0, contents indented 2 spaces. Organise it
with CSS banner comments whose tokens mirror the JS region tokens: `[C00] TOKENS`, `[C01] BASE`,
`[C02] SHELL`, `[C08] BOOT — ERROR PANEL`, `[C09] SELFTEST REPORT`.

  - `[C00] TOKENS` — copy the fourteen colour/radius custom properties from the study guide `:root`
    verbatim and in order: `--bg:#0e1014`, `--bg-2:#15181f`, `--panel:#191d26`, `--panel-2:#1f2530`,
    `--ink:#e8ebf2`, `--ink-dim:#a4adbe`, `--ink-faint:#6c7689`, `--line:#2a3140`, `--accent:#5cc8ff`,
    `--accent-2:#ff7eb6`, `--gold:#ffd166`, `--green:#5bd99c`, `--violet:#b98cff`, `--radius:14px`.
    Then, on their own line with the comment `/* artifact-specific */`, append `--coral:#ff8a5c`
    (verbatim from the second sibling) and `--maxw:1280px` (raised from the siblings' 980/1000 because
    this is a two-faction board, per STACK.md §5 constraint 2). Never hardcode a hex that could be a
    `var(--…)` or a `color-mix()` of one.
  - `[C01] BASE` — copy `*{box-sizing:border-box}`, `html{scroll-behavior:smooth}`, the `body` rule
    (the two-layer `radial-gradient(1200px 700px at 80% -10%, #1a2030 0%, var(--bg) 55%) no-repeat fixed,var(--bg)`
    background, `color:var(--ink)`, the `font:16px/1.6 "Segoe UI",system-ui,…` shorthand, and
    `-webkit-font-smoothing:antialiased`) and both `a` rules verbatim. Do not paraphrase the gradient —
    it is the single strongest "part of the course set" signal. Bump the base font size from `16px` to
    `17px` in the font shorthand for projector legibility (PITFALLS Pitfall 11) and leave everything
    else identical. Add one artifact-wide utility: `.num{font-variant-numeric:tabular-nums}` so every
    later counter inherits stable digit widths.
  - `[C02] SHELL` — `.shell{max-width:var(--maxw);margin:0 auto;padding:28px 22px}`, a `.shell-head`
    title treatment, and the inherited utilities copied verbatim from the study guide: `.card`,
    `.card.tight`, `.grid`/`.g2`/`.g3` with their media query, `.eyebrow`, `.muted`, `.faint`,
    `.callout` plus the `.accent`/`.gold`/`.green`/`.pink` modifiers and `.callout b`, and the `.ex`
    pill. These keep their sibling names for recognizability. Every NEW class in this file gets a
    section prefix (`st-`, `err-`) — that is the fake-scoping discipline from PITFALLS Pitfall 12.
  - `[C08] BOOT — ERROR PANEL` — style the panel that plan 01-02 will drive. `.err-panel` is positioned
    `fixed` at the bottom-right (`right:18px;bottom:18px;max-width:520px;z-index:50`) over `--panel-2`
    with the `.callout.pink` left rule, so it is unmissable and never overlaps the board root.
    `.err-title` bold `--ink`; `.err-message` `--ink-dim`; `.err-detail` is a `<textarea>` styled from
    `--panel`/`--line`, `width:100%`, `min-height:96px`, `font-family:ui-monospace,Consolas,monospace`,
    `font-size:12.5px`, `resize:vertical`, `user-select:text`; `.err-actions` a flex row with 8px gap;
    `.err-btn` a bordered pill on `--panel`; `.err-btn--danger` uses `--accent-2` for its border and text.
    Add `.err-panel[hidden]{display:none}` explicitly so the `hidden` attribute wins over `display:flex`.
  - `[C09] SELFTEST REPORT` — model the rows on the second sibling's `.pro`/`.con` treatment:
    `.st-report` builds on `.card`; `.st-summary` is large (`font-size:20px`) and uses `.num`;
    `.st-rows{display:flex;flex-direction:column;gap:6px}`; `.st-row{border:1px solid var(--line);border-radius:9px;padding:8px 12px;font-size:14px}`;
    `.st-row--pass{border-left:3px solid var(--green)}`; `.st-row--fail{border-left:3px solid var(--coral)}`;
    `.st-row b{display:block;font-size:11px;text-transform:uppercase;letter-spacing:.6px;margin-bottom:3px}`
    with `--green` / `--coral` respectively; `.st-detail{color:var(--ink-dim);font-family:ui-monospace,Consolas,monospace;font-size:12.5px}`.
    Add `.st-report[hidden]{display:none}`.

**Body — static shell only.** Container roots must be hand-written and stable so Phase 2 can bind
delegated listeners once and never rebind. Write exactly these elements with exactly these ids:
`<main class="shell" id="app">` containing a `.shell-head` with an `<h1>` reading `Cats vs Mechs` and
a `.muted` line reading `Workshop 16 — allocation and fight sandbox`; then
`<div class="board" id="board">` containing `<p class="muted" id="board-empty">The allocation board renders here.</p>`;
then `<section class="card st-report" id="selftest-report" hidden>` containing a `.eyebrow` reading
`Developer self-test`, `<div class="st-summary" id="selftest-summary"></div>` and
`<div class="st-rows" id="selftest-rows"></div>`.
After `</main>`, write the error panel as static markup so it can display even when init throws:
`<div class="callout pink err-panel" id="err-panel" hidden role="alert">` containing a `.eyebrow`
reading `Something went wrong`, `<div class="err-title" id="err-title"></div>`,
`<div class="err-message" id="err-message"></div>`,
`<textarea class="err-detail" id="err-detail" readonly aria-label="Error details"></textarea>`,
and `<div class="err-actions">` with `<button type="button" class="err-btn" id="err-dismiss">Dismiss and continue</button>`
and `<button type="button" class="err-btn err-btn--danger" id="err-reset">Reset to Workshop 16 defaults</button>`.
Follow the siblings' accessibility bar: `aria-label` on icon-only controls and the textarea, nowhere else.

**Script.** One classic `<script>` immediately before `</body>` — no `type`, no `src`, no `defer`, no
second block. Tag at column 0, contents indented 2 spaces.

Open it with a table-of-contents comment listing every region in order with a one-line purpose and the
owning plan or phase, plus a short note recording the two deliberate divergences from the siblings:
this file introduces a single `App` namespace and uses `'use strict'` inside each section IIFE
(ARCHITECTURE.md Pattern 1), because at 3,000 lines the siblings' bare-top-level-`const` style stops
working. Region order is fixed and must never change:
`[S00] APP`, `[S01] DATA`, `[S02] MODEL`, `[S03] STATE`, `[S04] SERIALIZE`, `[S05] OPS`,
`[S06] RENDER`, `[S07] INTERACTIONS`, `[S08] BOOT`, `[S09] SELFTEST`, `[S10] LAUNCH`.

Each region gets the banner form prescribed by ARCHITECTURE.md lines 173-177 combined with the
grep-token discipline from PITFALLS.md line 370 and the `#region` outline markers from
ARCHITECTURE.md lines 120-143. Concretely, for every region: a bar comment
`/* ===================== [SNN] NAME =====================` followed by a purpose line, a `deps:` line
naming the other regions it calls (or `deps: none`), an `owner:` line naming the plan or phase that
fills it, and a closing bar; then `// #region [SNN] NAME` before the code and
`// #endregion [SNN] NAME` after it. Searching `[S03]` must jump to exactly one place.

Region bodies to write in this task:

  - `[S00] APP` — `var App = {};` (a `var` declaration, so the headless runner can reach it through a
    `vm` context global) and `App.hasFlag = function (name) {...}`. `hasFlag` returns `false` when
    `typeof location === 'undefined'`; otherwise it strips a single leading `#` from `location.hash`,
    decodes nothing, splits on `,`, trims each element, and returns whether the resulting array
    contains `name` by **exact string equality**. It must never use `String.prototype.includes`,
    `indexOf` on the raw hash, a regular expression, or any substring test — a substring gate is
    trivially trippable by a pasted link (threat T-01-01). Hash content must never reach `innerHTML`,
    `eval`, `Function`, or a DOM sink of any kind.
  - `[S03] STATE`, `[S05] OPS` — banner only, plus a single line comment
    `// STUB — implemented by plan 01-02`. The `[S03]` banner's purpose lines must document the state
    shape from the `<interfaces>` block above, including the rule that there is no `mode` key because
    mode is derived from `fight !== null`.
  - `[S04] SERIALIZE` — banner plus `App.serialize = Object.freeze({ scheduleUrlSync: function () {} });`
    with a comment marking it a declared no-op until Phase 4 plan 04-01. This call site exists now so
    `commit()` never has to be retrofitted.
  - `[S06] RENDER` — banner plus `App.render = Object.freeze({ structure: function () {}, sync: function () {} });`
    marked as a declared no-op until Phase 2 plan 02-01. The two-tier split (rare structural rebuild,
    per-tick keyed reconcile) is recorded in the banner's purpose lines so Phase 2 inherits the
    decision rather than re-deciding it.
  - `[S07] INTERACTIONS` — banner plus `App.interactions = Object.freeze({ bind: function () {} });`
    marked as a declared no-op until Phase 2 plan 02-02.
  - `[S08] BOOT` — banner plus a stub IIFE exporting exactly two functions:
    `attempt(label, fn)` which currently just calls `fn()` inside a `try`/`catch` that `console.error`s,
    and `start()` whose entire body is `if (App.hasFlag('selftest')) { App.selftest.report(App.selftest.run()); }`.
    Mark both `// STUB — plan 01-02 replaces these bodies; the selftest gate line must survive.`
  - `[S09] SELFTEST` — the harness. An IIFE holding a private `suites` array. `suite(name, fn)` pushes
    `{name, fn}`. `run()` walks the suites, calling each `fn` with an assert object `t` exposing
    `t.eq(label, actual, expected)` (deep-compares with `JSON.stringify`), `t.ok(label, condition)` and
    `t.throws(label, fn)` (passes when `fn` throws); each assertion pushes
    `{suite, name, pass, actual, expected}` onto a records array; a suite function that throws pushes a
    single failing record named `suite threw` rather than aborting the run. `run()` returns
    `{records, passed, failed}`. `report(result)` writes into the static shell: `#selftest-summary`
    gets `result.passed + ' passed, ' + result.failed + ' failed'` via `textContent`; `#selftest-rows`
    is cleared with `replaceChildren()` and repopulated with one `div.st-row` per record built through
    `document.createElement` and `textContent` only. **No `innerHTML` anywhere in this file** — the
    report echoes assertion labels and actual/expected values, and later phases will feed it decoded
    build data (threat T-01-02). Sort failing records first so a failure is visible without scrolling.
    Below the harness, leave the three sub-block markers as comments in this order:
    `/* --- [S09.1] SUITE: board defaults — owner plan 01-01 --- */`,
    `/* --- [S09.2] SUITE: model derivations — owner plan 01-01 --- */`,
    `/* --- [S09.3] SUITE: state contract — owner plan 01-02 --- */` (empty for now).
  - `[S10] LAUNCH` — exactly one guarded statement:
    `if (typeof document !== 'undefined') { App.boot.start(); }`. The guard is what lets the headless
    runner load the script without a DOM.

**Headless runner.** Create `tests/selftest-node.cjs`. Open it with a comment stating it is dev-only
and must never be shipped or referenced from the HTML (CLAUDE.md keeps optional dev tooling in a
sibling `tests/` folder). It must: read `cats-vs-mechs.html`; scan it for forbidden patterns and exit
non-zero listing any hit — the patterns are `https?://`, `<link`, ` src=`, `type="module"`, `fetch(`,
`XMLHttpRequest`, `@import`, `url(`, `innerHTML`, `eval(` and `new Function` (this is the mechanical
proof of "no outbound requests, no external dependencies, no DOM injection sink" for UX-04 and threats
T-01-02 and T-01-03, so no comment in the HTML may contain a URL either); extract the single
`<script>`…`</script>` body with a non-greedy match; run it with `vm.runInNewContext` in a sandbox
containing only `console`, `setTimeout`, `clearTimeout`, `queueMicrotask` and a
`requestAnimationFrame` shim that defers to `setTimeout(fn, 0)` — deliberately **no** `document` and
**no** `location`, so `[S10] LAUNCH` stays inert and `App.hasFlag` takes its undefined-location path;
then call `sandbox.App.selftest.run()`, print one line per record as `PASS`/`FAIL` plus the suite and
assertion name and, for failures, the actual and expected values; and `process.exit(failed ? 1 : 0)`.
The in-file `#selftest` report remains the primary surface (D-04, D-06); this runner exists so the
same assertions are checkable from a terminal.
  </action>

  <verify>
    <automated>node tests/selftest-node.cjs</automated>
    <automated>grep -c "^// #region \[S" cats-vs-mechs.html</automated>
    <human-check>Double-click `cats-vs-mechs.html` from the file manager. A dark, styled page with the "Cats vs Mechs" heading appears. DevTools Console shows zero errors; the Network tab shows zero requests. No self-test report is visible.</human-check>
  </verify>

  <acceptance_criteria>
    - `cats-vs-mechs.html` exists at the project root and `tests/selftest-node.cjs` exists
    - `node tests/selftest-node.cjs` exits 0 and prints no forbidden-pattern hits (zero suites registered at this point is a pass)
    - `grep -c "^// #region \[S" cats-vs-mechs.html` returns exactly `11`
    - `grep -c "^// #endregion \[S" cats-vs-mechs.html` returns exactly `11`
    - Each of the exact strings `[S00] APP`, `[S01] DATA`, `[S02] MODEL`, `[S03] STATE`, `[S04] SERIALIZE`, `[S05] OPS`, `[S06] RENDER`, `[S07] INTERACTIONS`, `[S08] BOOT`, `[S09] SELFTEST`, `[S10] LAUNCH` appears in the file, and they appear in that order
    - `grep -c "deps:" cats-vs-mechs.html` returns at least `11`
    - The file contains the exact substrings `--accent:#5cc8ff`, `--coral:#ff8a5c`, `--maxw:1280px`, and `radial-gradient(1200px 700px at 80% -10%, #1a2030 0%, var(--bg) 55%)`
    - `grep -c 'id="err-panel"\|id="err-title"\|id="err-message"\|id="err-detail"\|id="err-dismiss"\|id="err-reset"' cats-vs-mechs.html` returns `6` distinct matching lines' worth of ids (each id appears exactly once)
    - The file contains `id="selftest-report"`, `id="selftest-summary"`, `id="selftest-rows"`, `id="board"`, `id="app"` — each exactly once
    - `grep -c "innerHTML\|eval(\|new Function\|https\?://\|<link\|type=\"module\"" cats-vs-mechs.html` returns `0`
    - `grep -c "<script" cats-vs-mechs.html` returns `1` and `grep -c "<style" cats-vs-mechs.html` returns `1`
    - The `[S00] APP` region contains `App.hasFlag` and the file contains no `location.hash.includes` and no `location.hash.indexOf`
    - `[S10] LAUNCH` contains the exact substring `typeof document !== 'undefined'`
    - `[S08] BOOT` contains the exact substring `App.hasFlag('selftest')`
    - The three sub-block markers `[S09.1]`, `[S09.2]` and `[S09.3]` each appear exactly once
    - Opening the file with no hash leaves `#selftest-report` carrying the `hidden` attribute
  </acceptance_criteria>

  <done>The artifact opens offline by double-click as a styled dark page with no console errors and no network activity, every section banner and stub surface is in place, and the headless runner loads the script and exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Frozen Workshop 16 defaults in [S01] DATA, proved by suite [S09.1]</name>
  <files>cats-vs-mechs.html</files>

  <read_first>
    - `cats-vs-mechs.html` — the file you just created; read the `[S01] DATA` banner and the `[S09]` harness so you call `suite`/`t.eq` with the signatures you defined
    - `.planning/phases/01-foundation-data-state-funnel-undo/01-CONTEXT.md` — D-01 (the board numbers), D-02 (single frozen DEFAULTS, one-place edit), D-03 (damage and keywords on actions, health on units, action points on the faction)
    - `.planning/PROJECT.md` § Context → "Source material" and "Stat model divergence from the board" — the transcription of the Workshop 16 board image
    - `.planning/ROADMAP.md` § Phase 1 success criterion 2 — the exact assertion list
  </read_first>

  <action>
Fill region `[S01] DATA` with an IIFE opening `'use strict'` and assigning a frozen public surface to
`App.data`. It depends on nothing (`deps: none`) and is never mutated.

**`deepFreeze(o)`** — a recursive walk that `Object.freeze`s the object and every nested object and
array. `Object.freeze` is shallow, and D-02 requires the defaults deep-frozen at load. Export it,
because `[S03] STATE` reuses exactly this function for its mutation guard rather than writing a second
copy (duplicated logic is PITFALLS Pitfall 12's number-one concrete failure).

**`DEFAULTS`** — one object literal, deep-frozen at definition, holding the whole board. This is the
single-source-of-truth object D-02 requires, and Phase 5's playtest retune (FIGHT-11) must be a
one-place edit here. Shape:

  - `schema: 1`
  - `cats: { id: 'cats', name: 'Cats', ap: 3, units: [...], actions: [...] }`
  - `mechs: { id: 'mechs', name: 'Mechs', ap: 3, units: [...], actions: [...] }`

  Unit shape is `{ id, name, maxHp, shield }` — health attaches to the unit, action points attach to
  the faction (D-03, PROJECT.md's deliberate divergence from the board's grouped "Resources"). Cats
  get nine units with ids `c1` through `c9`, names `Cat 1` through `Cat 9`, `maxHp: 3`, `shield: 0`.
  Mechs get three units with ids `m1` through `m3`, names `Mech 1` through `Mech 3`, `maxHp: 6`,
  `shield: 3`. Generate the unit arrays with an explicit loop or `Array.from` — do not hand-write nine
  near-identical literals.

  Action shape is `{ id, name, dmg, keywords }` where `keywords` is an array of lowercase effect ids.
  Cats actions in board order: `slash` / `Slash` / `dmg: 1` / `keywords: []`;
  `hairball` / `Hairball` / `dmg: 0` / `keywords: ['slowdown']`;
  `screech` / `Screech` / `dmg: 0` / `keywords: ['confuse']`.
  Mechs actions in board order: `fly` / `Fly` / `dmg: 0` / `keywords: ['evade']`;
  `lasers` / `Lasers` / `dmg: 3` / `keywords: ['range']`;
  `recharge` / `Recharge` / `dmg: 0` / `keywords: ['shield']`.

  Every numeric value in `DEFAULTS` is an integer. Add nothing else: the counter map (Slash < Fly,
  Hairball < Lasers, Fly < Recharge) and the effect-keyword card copy are `data.reference`, owned by
  Phase 3 plan 03-02, and must not be written here. Keyword **ids** on actions are in scope; keyword
  **descriptions** are not.

  Record in a comment above `DEFAULTS` that these are D-01's literal reading of the board's token
  rows, that they are a starting point rather than a conclusion, and that Phase 5 plan 05-03's
  playtest gate is the mechanism that retunes them.

**`defaults()`** — returns `JSON.parse(JSON.stringify(DEFAULTS))`, a fresh unfrozen deep copy on every
call. Use the JSON round-trip rather than `structuredClone`: it is faster on this payload
(ARCHITECTURE.md measured 0.0053 ms versus 0.0140 ms) and it enforces the JSON-clonable state
invariant by construction — a function or a `Map` that sneaks into the board data disappears loudly
instead of surviving silently.

Then fill sub-block `[S09.1] SUITE: board defaults` in region `[S09]` with
`App.selftest.suite('board defaults', function (t) { ... })` asserting, each as its own named
assertion so the report reads as a checklist:

  - `cats unit count` is 9; `cats unit maxHp` — every Cat has `maxHp` 3; `cats unit shield` — every
    Cat has `shield` 0; `cats faction ap` is 3
  - `mechs unit count` is 3; `mechs unit maxHp` — every Mech has `maxHp` 6; `mechs unit shield` —
    every Mech has `shield` 3; `mechs faction ap` is 3
  - `cats actions` — the mapped `[id, name, dmg, keywords]` tuples deep-equal Slash/1/[],
    Hairball/0/['slowdown'], Screech/0/['confuse'] in that order
  - `mechs actions` — likewise Fly/0/['evade'], Lasers/3/['range'], Recharge/0/['shield']
  - `unit ids unique` — the set of all twelve unit ids has size 12
  - `defaults are integers` — a recursive walk over `defaults()` finds no non-integer number, no
    `NaN`, no `undefined` and no function
  - `DEFAULTS is deep-frozen` — `t.throws` on an assignment to `App.data.DEFAULTS.cats.units[0].maxHp`
    (strict mode makes a frozen write throw `TypeError`), and a follow-up `t.eq` proving the value is
    still 3
  - `defaults() is a fresh deep copy` — two calls are not the same object reference, mutating the
    first call's nested unit does not change the second call's, and does not change `DEFAULTS`
  </action>

  <verify>
    <automated>node tests/selftest-node.cjs</automated>
    <automated>grep -c "Hairball\|Screech\|Recharge\|Lasers\|Slash\|Fly" cats-vs-mechs.html</automated>
  </verify>

  <acceptance_criteria>
    - `node tests/selftest-node.cjs` exits 0 and prints at least 14 `PASS` lines under suite `board defaults`, with zero `FAIL` lines
    - Opening `cats-vs-mechs.html#selftest` in a browser shows a report whose summary reads `N passed, 0 failed` and whose rows include the named assertions `cats unit count`, `mechs unit maxHp`, `cats actions`, `mechs actions`, `DEFAULTS is deep-frozen` and `defaults() is a fresh deep copy`
    - The file contains the exact substrings `'slash'`, `'hairball'`, `'screech'`, `'fly'`, `'lasers'`, `'recharge'`, `'slowdown'`, `'confuse'`, `'evade'`, `'range'` and `'shield'`
    - The file contains `App.data.deepFreeze` as an exported member, and `[S01] DATA` contains exactly one `DEFAULTS` object literal
    - `grep -c "maxHp: 3\|maxHp: 6" cats-vs-mechs.html` shows both values present exactly once each (the unit arrays are generated, not hand-written twelve times)
    - The file contains no counter-map data and no effect-card description text: `grep -ci "counter\|slash < fly" cats-vs-mechs.html` returns `0`
    - Every number inside `DEFAULTS` is an integer, proved by the passing `defaults are integers` assertion
  </acceptance_criteria>

  <done>The Workshop 16 board loads from one deep-frozen object, `defaults()` hands back an independent copy every call, and the self-test report confirms every board number and action by name.</done>
</task>

<task type="auto">
  <name>Task 3: Pure derivations in [S02] MODEL, proved by suite [S09.2]</name>
  <files>cats-vs-mechs.html</files>

  <read_first>
    - `cats-vs-mechs.html` — the `[S02] MODEL` banner and the `[S01] DATA` surface you just wrote
    - `.planning/research/ARCHITECTURE.md` lines 162-221 (Pattern 1: the IIFE shape, the frozen public surface, and the call-time `App.x.y()` cross-reference rule) and line 95 plus the component table around lines 88-101 (the `model` contract: pure, takes state as an argument, never reads `App.state`, never touches the DOM)
    - `.planning/research/STACK.md` lines 177-186 — the rule that derived values stay out of state
    - `.planning/ROADMAP.md` § Phase 3 plan 03-01 — the boundary: turns-to-wipe ranges, visible arithmetic and the projection panel belong there, not here
  </read_first>

  <action>
Fill region `[S02] MODEL` with an IIFE opening `'use strict'` assigning a frozen surface to
`App.model`. Banner `deps: none` — every function takes the state or a slice as an argument and
must never read `App.state`, never touch the DOM, and never store a derived value. Reference other
sections only at call sites as `App.x.y()`, never by destructuring at IIFE body scope
(ARCHITECTURE.md's ordering-fragility rule).

Functions, with these exact names and signatures:

  - `unitEhp(unit)` returns `unit.maxHp + unit.shield` — effective HP of one unit
  - `factionEhp(faction)` returns the sum of `unitEhp` across `faction.units`
  - `bestDamage(faction)` returns the highest `dmg` among `faction.actions`, or 0 when there are none
  - `factionDps(faction, activeUnits)` returns `Math.min(faction.ap, activeUnits) * bestDamage(faction)`
    — throughput is capped by the shared action-point pool, not by unit count, which is the whole point
    of PROJECT.md's faction-level AP model. When `activeUnits` is `undefined`, default it to
    `faction.units.length`
  - `aliveCount(fightSide)` returns the number of units in `fightSide.units` whose `alive` is `true` —
    it reads the stored flag and must never infer death from `hp === 0` (D-00d, PITFALLS Pitfall 9)
  - `apSpent(buildFaction, fightSide)` returns `buildFaction.ap - fightSide.ap`

Every return value is an integer. Do not add a turns-to-wipe function, a range formatter, a rounding
helper, a percentage, a comparison between the two sides, or anything that could read as a rating —
the projection and its arithmetic are Phase 3 plan 03-01, and PROJECT.md's no-verdict constraint is
the project's most likely accidental scope violation.

Then fill sub-block `[S09.2] SUITE: model derivations` with
`App.selftest.suite('model derivations', function (t) { ... })` asserting against a fresh
`App.data.defaults()`:

  - `cats eHP` is 27 (nine units at 3 + 0)
  - `mechs eHP` is 27 (three units at 6 + 3) — assert the value, and note in a comment that the two
    sides matching on this axis is a coincidence of the board's numbers, not a balance claim
  - `cats best damage` is 1; `mechs best damage` is 3
  - `cats throughput` — `factionDps(cats)` is 3 (three action points, capped below nine units, times 1)
  - `mechs throughput` — `factionDps(mechs)` is 9 (three action points, three units, times 3)
  - `throughput respects the ap cap` — `factionDps(cats, 1)` is 1, proving the `Math.min` cap fires
    from the unit side too
  - `model is pure` — call `factionEhp` twice on the same object and assert the input is unchanged by
    deep-comparing a JSON snapshot taken before the call
  - `alive is read, not inferred` — build a throwaway fight side literal
    `{ ap: 3, units: [{ id: 'c1', hp: 0, alive: true }, { id: 'c2', hp: 3, alive: false }] }` and
    assert `aliveCount` is 1: the zero-HP unit counts as alive and the full-HP unit counts as dead,
    because the flag is authoritative
  - `apSpent` — with a build faction of `ap: 3` and a fight side of `ap: 1`, the result is 2
  - `all derivations return integers` — every function above returns a value satisfying
    `Number.isInteger`
  </action>

  <verify>
    <automated>node tests/selftest-node.cjs</automated>
    <automated>grep -c "App\.state" cats-vs-mechs.html</automated>
  </verify>

  <acceptance_criteria>
    - `node tests/selftest-node.cjs` exits 0, printing at least 11 `PASS` lines under suite `model derivations` and zero `FAIL` lines across all suites
    - Opening `cats-vs-mechs.html#selftest` shows rows named `cats eHP`, `mechs eHP`, `mechs throughput`, `alive is read, not inferred` and `all derivations return integers`, all marked pass
    - `grep -c "App\.state" cats-vs-mechs.html` returns `0` (the `[S03]` banner may describe the state shape in prose but `[S02] MODEL` contains no reference to `App.state`)
    - The `[S02] MODEL` region contains the exact exported names `unitEhp`, `factionEhp`, `bestDamage`, `factionDps`, `aliveCount`, `apSpent` and its public surface is wrapped in `Object.freeze`
    - The file contains no `turnsToWipe`, no `balance`, no `verdict`, no `rating`, no `score`: `grep -ci "verdict\|balanced\|rating\|difficulty" cats-vs-mechs.html` returns `0`
    - `App.model` contains no `document`, no `querySelector`, no `getElementById` reference
  </acceptance_criteria>

  <done>eHP and damage-throughput derivations exist as pure argument-taking functions, the self-test proves their values against the shipped board, and the `alive`-is-a-flag contract has a named passing assertion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This is an offline, zero-dependency, single-file HTML artifact with no backend, no auth, no accounts
and no PII. The realistic ASVS L1 surface is narrow and is listed honestly below rather than padded
with inapplicable web-application threats.

| Boundary | Description |
|----------|-------------|
| pasted link → `location.hash` | The only attacker-controllable input reaching this plan's code. A classmate can paste any link into the course Discord thread. |
| error/assertion text → DOM | Assertion labels and, in later phases, decoded build data are echoed into the report and the error panel. |
| the shipped file → the machine | Zero runtime dependencies means the supply chain is the file itself. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | `App.hasFlag` in `[S00] APP` | mitigate | Exact-match token comparison against a `,`-split, trimmed hash. No `String.includes`, no `indexOf` on the raw hash, no regex substring test. A link ending `#notselftest` or `#selftestx` must not open the developer report. Enforced by the acceptance criterion asserting `location.hash.includes` appears zero times. |
| T-01-02 | Tampering / Elevation | `[S09] SELFTEST` report renderer and the `[C08]`/`[C09]` surfaces | mitigate | All report and panel text is written with `document.createElement` plus `textContent`, and the error detail field is a `<textarea>` written through `.value`. `innerHTML` is banned file-wide and the ban is machine-checked by `tests/selftest-node.cjs`, which also rejects `eval(` and `new Function`. |
| T-01-03 | Tampering | supply chain / offline integrity | mitigate | The file references nothing external. `tests/selftest-node.cjs` fails the build on `https?://`, `<link`, ` src=`, `type="module"`, `fetch(`, `XMLHttpRequest`, `@import` and `url(` — including inside comments. Zero runtime dependencies is the mitigation; the scan is the proof. |
| T-01-04 | Denial of Service | `[S01] DATA` | accept | The defaults are compile-time integer literals with no input path. Range-clamping untrusted integers (roster-size caps, `NaN` rejection) belongs to the build-code decode path, which is Phase 4 plan 04-01 and out of Phase 1's scope. |
| T-01-SC | Tampering | package installs | n/a | This plan installs nothing. There is no npm, pip or cargo step; `tests/selftest-node.cjs` uses only Node built-ins (`fs`, `vm`, `path`). No package legitimacy gate applies. |

The much larger untrusted-input surface — decoding a build code pasted from Discord — arrives in
Phase 4 and is explicitly out of scope here. PITFALLS.md § Security Mistakes prescribes its
mitigations (decode to integers only, clamp every value, hard-cap roster size, never `eval`), and
plan 04-01 owns them.
</threat_model>

<verification>
1. `node tests/selftest-node.cjs` exits 0 with zero `FAIL` lines and zero forbidden-pattern hits.
2. Double-click `cats-vs-mechs.html` from a file manager on a machine with networking disabled: a
   styled dark page appears, DevTools Console shows zero errors, the Network panel shows zero
   requests, and no self-test report is visible (ROADMAP criterion 1, D-05).
3. Open `cats-vs-mechs.html#selftest`: a readable pass/fail report appears, styled with the course
   tokens, summarising `N passed, 0 failed` and listing every board and model assertion by name
   (ROADMAP criterion 2, D-06).
4. Open `cats-vs-mechs.html#notselftest` and `cats-vs-mechs.html#selftestx`: no report appears
   (threat T-01-01).
5. `grep -c "innerHTML\|https\?://\|<link\|type=\"module\"" cats-vs-mechs.html` returns `0`.
</verification>

<success_criteria>
- `cats-vs-mechs.html` opens offline by double-click as a styled page with no console errors and no
  outbound requests (ROADMAP criterion 1; UX-04)
- `#selftest` renders a readable pass/fail report confirming the Workshop 16 defaults load exactly as
  the board specifies (ROADMAP criterion 2; ALLOC-08)
- The section scaffold is complete: eleven regions, in fixed order, each with a banner carrying a
  `deps:` and `owner:` line, `#region`/`#endregion` markers, and declared no-op stub surfaces at every
  Phase-2/Phase-4 call site so nothing is retrofitted later
- Regions `[S03] STATE`, `[S05] OPS` and `[S08] BOOT` contain only banners and marked stubs, ready for
  plan 01-02 to fill in place
</success_criteria>

<output>
Create `.planning/phases/01-foundation-data-state-funnel-undo/01-01-SUMMARY.md` when done.
</output>
