---
phase: 02-allocation-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cats-vs-mechs.html
autonomous: true
requirements: [ALLOC-04, ALLOC-05, ALLOC-07, UX-02, UX-05]

# SECTION OWNERSHIP (binding — everything lives in one file)
#   [S01] DATA      — the `tokens` vocabulary map + SHAPES/COLORS/TOKEN_IDS allowlists ONLY
#   [S06] RENDER    — the whole region, converted from frozen literal to IIFE
#   [S09.3]         — ONE assertion edited (the build-keys row), nothing else
#   [S09.4]         — new suite, this plan creates it
#   <body>          — static shell markup additions
#   <style>         — [C00] additions, [C03] BOARD, [C04] UNIT, [C05] TOKENS-VIZ, [C06] STEPPER
# NOT owned here: [S05] OPS, [S07] INTERACTIONS, [C07] PICKER, tests/selftest-node.cjs
#
# DELIBERATE DEVIATION FROM ROADMAP's PLAN SPLIT — stated per the planner brief:
#   ROADMAP assigns the `[S01] DATA` token-vocabulary additions and `[C05] TOKENS-VIZ`
#   to plan 02-03. Both move HERE, to 02-01, because 02-01 must render the board's four
#   token types (ALLOC-04) in wave 1 and cannot do that from a vocabulary that does not
#   exist yet. The alternative — 02-01 hardcodes token classes and 02-03 rewrites
#   `makeToken` inside `[S06]` — is exactly the cross-plan section edit the ownership
#   rule exists to prevent. 02-03 keeps the `GLYPHS` curated set, `setTokenStyle`,
#   `[C07] PICKER` and the picker surface. See 02-03's frontmatter for the mirror note.
#
# WAVE ORDERING, NOT FILE PARTITIONING:
#   All three Phase 2 plans modify cats-vs-mechs.html. They are therefore assigned
#   strictly sequential waves (02-01 -> 02-02 -> 02-03) rather than run in parallel.
#   No two plans in this phase may execute concurrently.

user_setup: []

must_haves:
  truths:
    - "Opening the file shows two faction columns side by side, Cats left and Mechs right, with a visibly reserved empty strip between them."
    - "Each unit card shows its health as green squares and its shield as blue squares; each faction header shows its action points as yellow triangles and its best damage as red diamonds."
    - "A row of 12 or more tokens shows a count and one token instead of 12 separate tokens."
    - "A structural rebuild leaves the page scroll position and the focused element unchanged, and the focus ring stays visible after keyboard navigation."
    - "Adding one health point animates exactly one token; crossing the compaction threshold animates nothing."
    - "`node tests/selftest-node.cjs` still exits 0 with zero failures."
  artifacts:
    - path: "cats-vs-mechs.html"
      provides: "[S06] RENDER as an IIFE with structure() and sync() implemented"
      contains: "App.render = (function ()"
    - path: "cats-vs-mechs.html"
      provides: "the render document guard that keeps the Node harness green"
      contains: "typeof document === 'undefined'"
    - path: "cats-vs-mechs.html"
      provides: "the token vocabulary as data in [S01] DATA"
      contains: "tokens: {"
    - path: "cats-vs-mechs.html"
      provides: "static shell roots that outlive every rebuild"
      contains: "id=\"col-cats\""
    - path: "cats-vs-mechs.html"
      provides: "the four static token-appearance buttons, one per board token type"
      contains: "openTokenPicker"
    - path: "cats-vs-mechs.html"
      provides: "[C05] TOKENS-VIZ shape rules built from CSS, not SVG"
      contains: "clip-path:polygon"
  key_links:
    - from: "[S03] state.frame()"
      to: "[S06] App.render.structure / App.render.sync"
      via: "the already-shipped invalidate/frame path"
      pattern: "App\\.render\\.(structure|sync)\\(state\\)"
    - from: "[S06] makeToken"
      to: "[S01] DEFAULTS.tokens"
      via: "state.build.tokens lookup at render time, never stored"
      pattern: "build\\.tokens"
    - from: "[S06] render.sync"
      to: "state.ui.kbdNav"
      via: "document.body.dataset.kbd, the reader ui.kbdNav has been waiting for"
      pattern: "dataset\\.kbd"
---

<objective>
Put the allocation board on screen. This plan delivers the static shell roots, the whole
`<style>` surface the board needs, the token vocabulary as data, and both tiers of
`[S06] RENDER` — the rare focus/scroll-preserving structural rebuild and the per-frame keyed
reconcile that never destroys the node under the cursor.

Purpose: everything Phase 2 does afterwards (steppers, delta typing, roster editing, the
appearance picker) operates a board that has to exist first. This plan is also where
ALLOC-07's *non-count* half is won or lost: focus, scroll and token-animation stability are
properties of the renderer, not of the event handlers.

Output: a board that displays the Workshop 16 defaults in the board's own token vocabulary,
legible at projector distance, with `node tests/selftest-node.cjs` still at exit 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/02-allocation-surface/02-CONTEXT.md
@.planning/phases/02-allocation-surface/02-RESEARCH.md
@.planning/phases/02-allocation-surface/02-PATTERNS.md
@cats-vs-mechs.html
@tests/selftest-node.cjs

Read these sections of research in full before writing any code:
- 02-RESEARCH.md §1 "Two-Tier Render Mechanics" — `withPreservedFocus`, the three sync rules,
  the `data-k` / `data-act` table, who requests a structural frame
- 02-RESEARCH.md §4 "Token Vocabulary, Shapes and the Picker" — CSS shape table, the
  three-layer token node, the vocabulary literal, compaction, the damage-diamond question
- 02-RESEARCH.md §5 "Projector Legibility as a Concrete Spec" — the measured contrast table
  and the size table. These are numbers, not suggestions.
- 02-RESEARCH.md §6 "CSS Techniques" — nesting, `color-mix()`, the two sticky gotchas
- 02-RESEARCH.md pitfalls P-04, P-06, P-07, P-08, P-10, P-11
- 02-PATTERNS.md §"02-01 — [S06] RENDER" and §"02-01 — the <style> block"

<interfaces>
<!-- The BINDING contract this plan writes into the DOM and plan 02-02 reads out of it. -->
<!-- 02-02 has no other way to find a control. Do not rename anything in this table. -->

| Node | `data-k` (identity, focus restore) | `data-act` | other dataset |
|---|---|---|---|
| Unit health input | `{side}/{unitId}/maxHp` | `maxHp` | `data-side`, `data-unit` |
| Unit health − | `{side}/{unitId}/maxHp-` | `nudgeMaxHp` | `data-side`, `data-unit`, `data-step="-1"` |
| Unit health + | `{side}/{unitId}/maxHp+` | `nudgeMaxHp` | `data-side`, `data-unit`, `data-step="1"` |
| Unit shield input | `{side}/{unitId}/shield` | `shield` | `data-side`, `data-unit` |
| Unit shield − | `{side}/{unitId}/shield-` | `nudgeShield` | `data-side`, `data-unit`, `data-step="-1"` |
| Unit shield + | `{side}/{unitId}/shield+` | `nudgeShield` | `data-side`, `data-unit`, `data-step="1"` |
| Unit remove | `{side}/{unitId}/rm` | `removeUnit` | `data-side`, `data-unit` |
| Faction AP input | `{side}/ap` | `ap` | `data-side` |
| Faction AP − | `{side}/ap-` | `nudgeAp` | `data-side`, `data-step="-1"` |
| Faction AP + | `{side}/ap+` | `nudgeAp` | `data-side`, `data-step="1"` |
| Add unit | `{side}/add` | `addUnit` | `data-side` |
| Undo (top bar) | `undo` | `undo` | — |
| Token-appearance edit × 4 (static, `#topbar`) | `tok/{tokenId}` | `openTokenPicker` | `data-tok` |

**`openTokenPicker` is not an op and never will be.** Every other `data-act` in this table
names a function in `[S05] OPS`. `openTokenPicker` is pure page work — fill a dialog and call
`showModal()` — and `[S05]`'s banner states that layer "never reads or writes the page." It is
therefore routed by `[S07.1]`'s `UI_ACTS` allowlist, which plan 02-02 builds and plan 02-03
registers a handler into. It must never reach `App.ops.dispatch`, whose
`default: throw new Error('Unknown op: ' + act)` arm would be caught by `App.boot.wrap` and
raise the styled error panel on a student's first click. This plan's whole obligation is to
render the four buttons with the attributes above. Do not add a dispatch case for it, here or
anywhere.

Existing surfaces this plan calls and must not duplicate:

```js
App.state.get()                      // frozen state: { build, fight, ui }
App.data.DEFAULTS                    // deep-frozen board; this plan adds DEFAULTS.tokens
App.model.unitEhp(unit)              // unit.maxHp + unit.shield
App.model.factionEhp(faction)
App.model.bestDamage(faction)        // pure, integer, currently has NO consumer — this plan is it
App.model.factionDps(faction, n)
App.state.invalidate({structural:true})   // already called by undo(), restore(), boot.start()
```

The stub this plan replaces (cats-vs-mechs.html, `[S06]`), which must become an IIFE
because it now needs private helpers:

```js
App.render = Object.freeze({
  structure: function () {},
  sync: function () {}
});
```

The document guard to copy verbatim from `[S09] report()` or `[S08] byId()`:

```js
if (typeof document === 'undefined') { return; }
```
</interfaces>
</context>

<open_questions_resolved>
Two of the phase's five open questions are answered here. The other three are in 02-02.

**Q-2 — where do the red damage diamonds live? RESOLVED: a read-only per-faction damage row
in the faction header, rendered from `App.model.bestDamage(state.build[side])`.**
Reasoning: ROADMAP criterion 2 requires red diamonds on screen in Phase 2, but damage attaches
to actions and action/effect cards are Phase 3's REF-02. `bestDamage` is a shipped pure
derivation with no consumer at all, so this needs no new state, no new op and no new
derivation. It satisfies the vocabulary requirement and leaves Phase 3's per-action cards
entirely unbuilt. Constraint: it is read-only in Phase 2 (no stepper) and its label is the
plain word "damage" — never a score, a grade or a judgement, per PROJ-06 and the grep gate.

**Q-3 — should `--maxw` rise for the board? RESOLVED: no. Keep `--maxw:1280px` for `.shell`
and add a separate `--boardw:1600px` that applies to `#board` only.**
Reasoning: `--maxw` is the sibling artifacts' document measure and UX-05 requires this file to
read as their sibling; the self-test report and the page header should keep it. D-01 requires
both faction columns fully visible on a ~1920px projector, which 1280 wastes. Two variables,
no conflict, and a rehearsal can move one number.

**Q-4 — is `--tok: 22px` countable at the back of a room? RESOLVED: ship 22px, make it a dial.**
Reasoning: research marks projector sizes LOW confidence and says only a rehearsal settles it.
`--tok:22px` and `--tok-gap:6px` go in `[C00]` with a comment naming them as the rehearsal
dial, and no JavaScript may hardcode either number. The rehearsal itself is on 02-03's
human-verify checkpoint.
</open_questions_resolved>

<tasks>

<task type="auto">
  <name>Task 1: Static shell roots, layout and the projector legibility spec ([C00] additions, [C03] BOARD, [C04] UNIT, [C06] STEPPER)</name>
  <files>cats-vs-mechs.html — &lt;body&gt; shell markup (#topbar, #col-cats, #strip, #col-mechs) and &lt;style&gt; blocks [C00] additions, [C03] BOARD, [C04] UNIT, [C06] STEPPER</files>

  <read_first>
    - `cats-vs-mechs.html` lines 1–130 (the whole `<style>` block and the current `<body>` shell)
    - `cats-vs-mechs.html` lines 1013–1050 (`[S08] boot.start()` — note the `#throwhandler`
      branch reads `byId('board-empty')`; this is why `#board-empty` must survive)
    - `../game-feel-study-guide.html` lines 7–178 (the sibling stylesheet whose visual language
      UX-05 requires this file to match)
    - `.planning/phases/02-allocation-surface/02-RESEARCH.md` §5 and §6
    - `.planning/phases/02-allocation-surface/02-PATTERNS.md` §"02-01 — the `<style>` block"
  </read_first>

  <action>
Add the static shell roots and the CSS the board needs. No JavaScript in this task.

**`<body>` markup.** Inside `<main class="shell" id="app">`, between `.shell-head` and
`#selftest-report`, the tree becomes: `<div id="topbar">` then `<div class="board" id="board">`.
`#topbar` holds the artifact title on the left and a control cluster on the right (D-04). The
cluster holds two things in Phase 2.

First, a button with `data-k="undo"`, `data-act="undo"`, `type="button"`, text `Undo`.

Second, a labelled group — visible label text `Token appearance` — holding exactly four
`type="button"` buttons, one per **board** token type, with visible text `Health`, `Action`,
`Shield`, `Damage` and the attributes `data-k="tok/{tokenId}"`, `data-act="openTokenPicker"`,
`data-tok="{tokenId}"` for `hp`, `ap`, `shield` and `dmg` in that order. The fifth vocabulary
entry, `dead`, gets no button: Task 2 ships it as data with no renderer consumer, so an edit
control for it would edit something invisible.

**These four buttons are static markup, not rendered, and that is the point.** A `data-k` must
be unique, because `withPreservedFocus` (Task 3) restores focus by `[data-k="…"]` and takes the
first match. A token-edit button rendered inside a unit card would produce one `tok/hp` per
unit, and one inside a faction header would produce two `tok/ap` per board — silently breaking
focus restore, which is half of ALLOC-07. Static markup in `#topbar` gives exactly one of each
by construction, sits inside `#app` so 02-02's delegated listener reaches it, and means plan
02-03 needs no edit anywhere in the board renderer. Style them in `[C03]` at the 18px label
minimum, with real text; never icon-only, never hover-revealed (UX-02).

`openTokenPicker` is a **UI-only act** — see the note under the contract table above. This plan
renders the buttons and stops there; 02-02 builds the `UI_ACTS` path that carries them and
02-03 registers the handler that opens the picker.

Phase 4 adds reset and share to the same cluster, Phase 5 adds turn state; leave a comment
saying so.

`#board` gains four static children in this order: `<section id="col-cats">`,
`<aside id="strip">`, `<section id="col-mechs">`, and the existing
`<p class="muted" id="board-empty">`. **Keep `#board-empty` — do not delete it.**
`[S08] boot.start()`'s `#throwhandler` rehearsal branch reads it by id, `[S08]` is owned by
plan 01-02 and this plan may not edit it, and a guarded `if (empty)` that silently stops
working is a rehearsal affordance rotting in place. `render.structure()` will set
`boardEmpty.hidden = true` in Task 3 instead.

`#strip` is D-02's reserved region for Phase 3's projection. It is *visibly* reserved, not
zero-width: a `1px dashed var(--ink-faint)` border, `--radius` corners, and a centred
`var(--ink-dim)` label at 18px reading `Projection lands here in Phase 3`. Per
`<specifics>` in 02-CONTEXT.md, an instructor demoing Phase 2 must see where it goes rather
than watch the layout shift when Phase 3 arrives.

Keep the shipped indentation, the `class`-then-`id` attribute order, and two-space nesting —
copy the existing `#err-panel` / `#selftest-report` blocks as the formatting analog.

**`[C00] TOKENS` additions.** Add to `:root`, in the `/* artifact-specific */` group:
`--boardw:1600px`, `--topbar-h:64px`, `--tok:22px`, `--tok-gap:6px`. Write a comment above
`--tok` naming it and `--tok-gap` as the projector-rehearsal dial (Q-4) and stating that no
JavaScript hardcodes either number. Do not touch `--maxw`. Do not add a single new hex —
research §5 measured that all four board token colours already exist in `:root` and clear
7:1 or better on `--panel`.

**`[C03] BOARD`.** New banner block in the `<style>` block, placed after `[C02] SHELL`,
following the shipped `/* ===================== [CNN] NAME ===================== */` form.

- `#topbar { position:sticky; top:0; z-index:20; min-height:var(--topbar-h); display:flex;
  align-items:center; justify-content:space-between; gap:16px; background:var(--bg);
  border-bottom:1px solid var(--line) }`.
- `#board { display:grid; grid-template-columns:1fr minmax(220px, 320px) 1fr; gap:18px;
  align-items:start }` — research §6's prescribed track list for D-01.
- Board width escape from the 1280px shell, without `transform` (a transform on an ancestor
  of a sticky element is exactly the kind of silent breakage P-10 is about):
  `#board { width:min(var(--boardw), calc(100vw - 44px));
  margin-left:calc((100% - min(var(--boardw), calc(100vw - 44px))) / 2) }`.
  The `44px` is `.shell`'s `22px` horizontal padding, doubled.
- `#strip { position:sticky; top:var(--topbar-h); align-self:start }`. `align-self:start` is
  load-bearing — research §6 G-2: a grid item stretches to the row height by default and a
  stretched sticky item has nowhere to move, so sticky appears to do nothing.
- **Write research §6 G-1 as a comment directly above the two sticky rules:** no ancestor of
  `#topbar` or `#strip` may carry `overflow` other than `visible`, because a sticky element
  sticks to its nearest scrolling ancestor and one `overflow:hidden` added later to clip a
  long token row kills both with no error anywhere. Keep any clipping on `.tok-row` itself.
- Keep `@media(max-width:760px) { #board { grid-template-columns:1fr } }` consistent with the
  shipped breakpoint at `[C02]`.

**`[C04] UNIT`.** Faction column, faction header, unit card and the add/remove chrome.
Class prefixes are mandatory (Pitfall 12): `.brd-` for board/column chrome, `.unit-` for the
unit card. Sizes are the research §5 table, not adjustable by taste:

- numeric readouts: `font-size:24px; font-weight:700`, and every one carries the shipped
  `.num` class so `tabular-nums` stops the value reflowing while the ramp runs;
- unit and faction labels: `font-size:18px` minimum;
- nothing at or below 14px may carry information — confine that size to decoration;
- unit card: `background:var(--panel); border:1px solid var(--line);
  border-radius:var(--radius)`, matching the shipped `.card`;
- `.unit-card:focus-within { border-color:var(--accent) }` — research §6 chose
  `:focus-within` over `:has()` deliberately; do not use `:has()` anywhere.

**The remove control — this corrects 02-CONTEXT.md `<specifics>`.** That note suggests "a quiet
outline rather than a filled destructive button." A `--line` outline measures **1.29:1 against
`--panel`** (research §5), which is functionally invisible on a projector and fails UX-02
outright. Keep the intent, change the pixels: `1px solid var(--ink-faint)` border (3.69:1,
clears WCAG SC 1.4.11's 3:1 floor for UI components) with `var(--ink-dim)` glyph and text
(7.47:1), and `border-color:var(--accent-2); color:var(--accent-2)` on `:hover` and `:focus`.
Minimum target `32px × 32px`. `opacity:1` at all times — **no `.unit-card:hover .unit-rm` rule
may exist anywhere in the file** (D-06 + UX-02 forbid hover-only affordances).

**`[C06] STEPPER`** (styles only — 02-02 owns the behaviour). `.stp-` prefix.
`+`/`−` buttons are `44px × 44px` (WCAG 2.5.5 AAA — at 44px the ALLOC-07 stress test stops
being a test of the student's aim). The numeric field is `type="text"` styled with a **fixed
width** so the layout cannot reflow mid-ramp; do not use `field-sizing:content`.
`user-select:none` on `.stp-btn`, `.tok-row` and the unit-card chrome — research §2 F-6, rapid
clicking otherwise drag-selects labels.

**Focus ring, both halves.** `:focus-visible { outline:2px solid var(--accent);
outline-offset:2px }` **and** `body[data-kbd="1"] :focus { outline:2px solid var(--accent);
outline-offset:2px }`. Both, not either. Research §1 Addition 2 / P-08: programmatic `focus()`
does not re-arm `:focus-visible`, so after a structural rebuild the ring vanishes even though
focus is correct — which fails ROADMAP criterion 3 literally. Task 3 writes the `data-kbd`
attribute; 02-02 writes the `ui.kbdNav` flag that drives it.

Use CSS nesting (Baseline Widely Available 2026-06-11) and `color-mix()` for derived borders
and tints — but keep any colour that *carries meaning* at its pure token value so the measured
contrast ratios hold. Nesting depth 2 maximum. This block sets the house style for both; there
is no prior instance in the repo or the siblings.

**Forbidden in this task:** any `url(`, any `https?://`, any `<link`, any `transform` on
`#board` or an ancestor of a sticky element, any `overflow` other than `visible` on an
ancestor of `#topbar` or `#strip`, any `:has()`, any `container-type`, any new hex colour.
  </action>

  <verify>
    <automated>node tests/selftest-node.cjs</automated>
  </verify>

  <acceptance_criteria>
    - `node tests/selftest-node.cjs` exits 0 and prints `81 passed, 0 failed` (this task adds
      no assertions; the count is unchanged).
    - `grep -c 'id="col-cats"\|id="col-mechs"\|id="strip"\|id="topbar"' cats-vs-mechs.html`
      prints `4`.
    - `grep -c 'id="board-empty"' cats-vs-mechs.html` prints `1` — the element still exists.
    - `grep -c 'data-act="openTokenPicker"' cats-vs-mechs.html` prints `4` — one static
      token-edit button per board token type, and no more.
    - `grep -c "case 'openTokenPicker'" cats-vs-mechs.html` prints `0` — it is not an op and
      never enters `[S05] dispatch`.
    - `grep -c 'data-k="tok/hp"' cats-vs-mechs.html` prints `1`, and the same for `tok/ap`,
      `tok/shield` and `tok/dmg` — every `data-k` is unique, which focus restore depends on.
    - `grep -c "C03\] BOARD\|\[C04\] UNIT\|\[C06\] STEPPER" cats-vs-mechs.html` prints `3`.
    - `grep -c "align-self:start\|align-self: start" cats-vs-mechs.html` is at least `1`.
    - `grep -c "\-\-boardw\|--topbar-h\|--tok:\|--tok-gap" cats-vs-mechs.html` is at least `4`.
    - `grep -c 'body\[data-kbd="1"\]' cats-vs-mechs.html` prints `1`.
    - `grep -ci "hover .unit-rm\|hover .brd-rm" cats-vs-mechs.html` prints `0` — no hover-only
      reveal of the remove control.
    - `grep -c ":has(\|container-type\|field-sizing" cats-vs-mechs.html` prints `0`.
    - `grep -ci "counter\|rating\|balanced\|difficulty" cats-vs-mechs.html` prints `0`.
    - `grep -c "verdict\|balanced\|rating\|difficulty" cats-vs-mechs.html` prints `0`.
    - `grep -cE "innerHTML|eval\(|new Function|https?://|<link|type=\"module\"" cats-vs-mechs.html`
      prints `0`.
  </acceptance_criteria>

  <done>The file still opens as one self-contained document; `#topbar`, `#col-cats`,
  `#strip`, `#col-mechs` exist as static children of `#app`/`#board`; `#topbar` carries Undo
  plus exactly four unique `openTokenPicker` buttons; the three new `[CNN]` blocks carry the
  research §5 sizes and the corrected remove-control treatment; the Node gate is unchanged at
  81/0.</done>
</task>

<task type="auto">
  <name>Task 2: The token vocabulary as data ([S01] DATA) and its pixels ([C05] TOKENS-VIZ)</name>
  <files>cats-vs-mechs.html — [S01] DATA (SHAPES/COLORS/TOKEN_IDS + DEFAULTS.tokens), [S09.1] new rows, [S09.3] the build-keys assertion, &lt;style&gt; [C05] TOKENS-VIZ</files>

  <read_first>
    - `cats-vs-mechs.html` lines 186–268 (`[S01] DATA` — `deepFreeze`, `makeUnits`, the
      `DEFAULTS` literal and its board-source comment at 224–228)
    - `cats-vs-mechs.html` lines 1380–1395 (`[S09.3]`, specifically the
      `'the board has its three build keys'` assertion — this task edits that one row)
    - `cats-vs-mechs.html` lines 1240–1300 (`[S09.1]` board-defaults suite — the analog for
      the new vocabulary assertions and for `allIntegers`)
    - `.planning/phases/02-allocation-surface/02-RESEARCH.md` §4, especially the CSS shape
      table, the three-layer token node, the vocabulary literal and compaction (D-20/21/22)
    - `.planning/phases/02-allocation-surface/02-PATTERNS.md` §"02-03 — `[S01] DATA` token
      vocabulary" (the analog is the same even though the ownership moved to this plan)
  </read_first>

  <action>
**`[S01] DATA` — the vocabulary.** Add three module-scope allowlist arrays inside the
`App.data` IIFE, above `DEFAULTS`, and export all three from the frozen return object so
`[S05]` (02-03) and `[S09]` can assert against the constants rather than against bare
literals — the same reason `[S03]` exports `UNDO_LIMIT` and `COALESCE_MS`:

- `SHAPES = ['sq', 'rect', 'tri', 'dia', 'circ', 'hex']`
- `COLORS = ['green', 'gold', 'accent', 'coral', 'violet', 'accent-2', 'ink-dim']`
- `TOKEN_IDS = ['hp', 'ap', 'shield', 'dmg', 'dead']`

Add a `tokens` key to the `DEFAULTS` literal, **as the last key**, a peer of `cats` and
`mechs`, so it inherits the deep freeze for free. Entries, exactly:
`hp` → shape `sq`, color `green`, glyph `''`;
`ap` → shape `tri`, color `gold`, glyph `''`;
`shield` → shape `sq`, color `accent`, glyph `''`;
`dmg` → shape `dia`, color `coral`, glyph `''`;
`dead` → shape `rect`, color `ink-dim`, glyph `'\u{1F480}'`.

The first four are the board's own vocabulary read literally (ALLOC-04, unchanged in
appearance). `dead` ships as vocabulary data with **no renderer consumer** — it is D-09's
concrete proof (a rectangle with a skull emoji) that the renderer is capable of Phase 5's
dead marker without building any of Phase 5's mechanic. Write that in a comment, following
the precedent already set for `shield` and `log` at `[S05]` lines 686–689. Write the emoji as
a `\u{...}` escape with a trailing comment naming it, not as a literal character.

Extend the board-source comment above `DEFAULTS` to name the vocabulary and say that D-13 puts
it in `build` because it round-trips through Phase 4's build code (SHARE-08).

**The shipped assertion this breaks — fix it in this task, not later.**
`cats-vs-mechs.html:1392` asserts `Object.keys(App.state.get().build)` equals
`['schema','cats','mechs']`. `Object.keys` returns an ordered array and `t.eq` compares arrays
positionally, so adding `tokens` turns that row red immediately. Update the expected value to
`['schema', 'cats', 'mechs', 'tokens']` and change the label from
`'the board has its three build keys'` to `'the board has its build keys'` so the label does
not encode a number that will move again in Phase 4.

**New assertions in `[S09.1]`** (the board-defaults suite, plan 01-01's — this plan appends
rows, it does not restructure the suite):
- every id in `App.data.TOKEN_IDS` has an entry in `d.tokens`;
- every `d.tokens[id].shape` is in `App.data.SHAPES` and every `.color` is in
  `App.data.COLORS`;
- the four board types read back exactly: `hp` = square/green, `ap` = triangle/gold,
  `shield` = square/accent, `dmg` = diamond/coral;
- `d.tokens` survives the existing `allIntegers` walk (strings are permitted by that walker,
  so this should pass — assert it so a future numeric field cannot sneak a float in);
- `App.data.DEFAULTS.tokens` is frozen (a `t.throws` write, matching the shipped
  `'DEFAULTS is deep-frozen'` row).

**`[C05] TOKENS-VIZ`** — a new `<style>` banner block, `.tok-` prefix. The node is three
layers, and the reason is D-10: emoji metrics differ per OS and must not be allowed to
influence row width or the compaction threshold, so the glyph is removed from flow entirely
rather than measured.

- `.tok` — a fixed `var(--tok)` square, `position:relative`, `flex:0 0 auto`, background set
  from the colour token. The box is what carries size.
- `.tok-s` — the shape layer. `position:absolute; inset:0`. `clip-path` and `transform` are
  applied **here**, never to `.tok`, so a triangle's clip does not clip the glyph and a
  diamond's rotation is cancellable.
- `.tok-g` — the glyph layer, created **only when the vocabulary entry has a non-empty glyph**.
  `position:absolute; inset:0; display:grid; place-items:center;
  font-size:calc(var(--tok) * .68); line-height:1;` with an emoji font stack of
  `"Segoe UI Emoji","Apple Color Emoji","Noto Color Emoji",sans-serif`. Because it is out of
  flow inside a fixed box, no emoji on any OS can change the box, the row width or the
  threshold.

Shape rules, one class per `SHAPES` id, from research §4's table:
`sq` — nothing, the box is the shape;
`rect` — `width:calc(var(--tok) * 1.7)` on `.tok`;
`tri` — `clip-path:polygon(50% 0, 100% 100%, 0 100%)`;
`dia` — `transform:rotate(45deg)` on `.tok-s`, with `transform:rotate(-45deg)` on `.tok-g` so
the glyph stays upright;
`circ` — `border-radius:50%`;
`hex` — `clip-path:polygon(25% 0,75% 0,100% 50%,75% 100%,25% 100%,0 50%)`.

**Inline SVG is not an option here and is not a matter of preference.** The SVG namespace is
the literal string `http://www.w3.org/2000/svg` and `tests/selftest-node.cjs`'s `absolute URL`
rule fails the build on it; hoisting the namespace to a constant fails the same rule;
`clip-path:url(#id)` and `mask-image:url(…)` fail the `url(` rule. Concatenating the namespace
to slip past the scan is the deliberate bypass the harness documents itself as not catching —
do not. Write a one-line comment above the shape rules recording this, so the next person does
not spend an afternoon rediscovering it.

Colour rules, one class per `COLORS` id, each setting `background:var(--{color})`. Use
`color-mix(in srgb, var(--{color}) 35%, transparent)` for a token border if one is wanted, but
keep the **fill** at the pure token value so research §5's measured 7–11:1 ratios hold.

Row and compaction:
- `.tok-row { display:flex; flex-wrap:wrap; gap:var(--tok-gap); align-items:center;
  user-select:none }`.
- `.tok-count` — the compacted form (D-21): the count text at the same 24px/`.num` treatment
  as the other numeric readouts, followed by exactly one `.tok` at normal size.
- Entry animation `.tok--in`: a short scale/opacity pop, plus
  `@media (prefers-reduced-motion: reduce) { .tok--in { animation:none } }`.
- **No transition or animation may be attached to the mode swap** (D-22). A value oscillating
  at 11↔12 would strobe. Do not use `display:none` to hide either mode — per CSS Animations
  L1 that cancels animations on every descendant and restarts them on the way back (P-09).
  Task 3 swaps modes via `replaceChildren()` and a `data-mode` flag instead.
  </action>

  <verify>
    <automated>node tests/selftest-node.cjs</automated>
  </verify>

  <acceptance_criteria>
    - `node tests/selftest-node.cjs` exits 0, prints `0 failed`, and the passed count is at
      least `86` (81 shipped + at least 5 new vocabulary rows).
    - `grep -c "the board has its build keys" cats-vs-mechs.html` prints `1` and
      `grep -c "the board has its three build keys" cats-vs-mechs.html` prints `0`.
    - `node -e "const {execSync}=require('child_process');"` is not needed; instead:
      `node tests/selftest-node.cjs | grep -c "FAIL"` prints `0`.
    - `grep -c "TOKEN_IDS\|SHAPES\|COLORS" cats-vs-mechs.html` is at least `6` (declared and
      exported).
    - `grep -c "clip-path:polygon\|clip-path: polygon" cats-vs-mechs.html` is at least `2`
      (`tri` and `hex`).
    - `grep -c "1F480" cats-vs-mechs.html` prints `1` — the `dead` vocabulary entry exists.
    - `grep -c "url(" cats-vs-mechs.html` prints `0`.
    - `grep -c "createElementNS" cats-vs-mechs.html` prints `0`.
    - `grep -c "prefers-reduced-motion" cats-vs-mechs.html` prints `1`.
    - `grep -ci "counter\|rating\|balanced\|difficulty" cats-vs-mechs.html` prints `0`.
    - `grep -c "verdict\|balanced\|rating\|difficulty" cats-vs-mechs.html` prints `0`.
  </acceptance_criteria>

  <done>`App.data.DEFAULTS.tokens` exists with five entries; `SHAPES`, `COLORS` and
  `TOKEN_IDS` are exported allowlists; the `build`-keys assertion is updated in the same task
  that broke it; `[C05] TOKENS-VIZ` draws all six shapes from CSS with the glyph out of flow;
  the Node gate is green with more assertions than it started with.</done>
</task>

<task type="auto">
  <name>Task 3: [S06] RENDER — both tiers, focus/scroll preservation, keyed token reconcile</name>
  <files>cats-vs-mechs.html — [S06] RENDER (whole region), [S09.4] new suite</files>

  <read_first>
    - `cats-vs-mechs.html` lines 841–858 (the `[S06]` stub and its banner — keep the banner,
      replace the body)
    - `cats-vs-mechs.html` lines 281–329 (`[S02] MODEL` — the smallest namespaced-IIFE
      instance in the file; copy this wrapper shape exactly)
    - `cats-vs-mechs.html` lines 1179–1218 (`[S09] report()` — the only function in the repo
      that builds a page subtree from data, and the source of both the DOM-building idiom and
      the `typeof document === 'undefined'` guard)
    - `cats-vs-mechs.html` lines 885–895 (`[S08] byId` — the same guard factored into a helper)
    - `cats-vs-mechs.html` lines 495–530 (`[S03] frame()` — the caller; note `structure()` runs
      before `sync()` in the same frame and that the structural flag is cleared only after
      `structure()` returns)
    - `.planning/phases/02-allocation-surface/02-RESEARCH.md` §1 in full, plus P-04, P-06,
      P-07, P-08
  </read_first>

  <action>
Convert `[S06]` from a frozen object literal to a namespaced IIFE returning a frozen surface
(the `[S02] MODEL` shape), because it now needs private helpers. Keep the existing banner text
and the `#region` / `#endregion` markers; update the `owner:` line to `Phase 2, plan 02-01`
if it is not already exact. Cross-section references are resolved at call time as `App.x.y()`
and never captured at section-body scope — `[S03]`'s lazy `cur` exists specifically to enforce
that rule.

**The document guard comes first, before anything else in this task.** Both `structure()` and
`sync()` begin with `if (typeof document === 'undefined') { return; }`. `tests/selftest-node.cjs`
loads the script into a bare `vm` sandbox with no `document`, and `[S09.3]` calls
`App.state.flush()`, which calls `frame()`, which calls both of these. Without the guard the
currently-green 81-assertion gate goes red the moment render stops being a no-op. This is the
single most likely way this plan breaks the build.

**Private helper — `withPreservedFocus(container, fn)`.** Research §1's verified helper, with
its two named additions:

- Before `fn()`: record `document.activeElement`, its `dataset.k`, its `selectionStart` if it
  has one, `container.scrollTop`, and `window.scrollY`.
- After `fn()`: re-set `container.scrollTop` **after the content exists** (scrollTop clamps to
  0 when content shrinks, i.e. exactly when a unit is removed), then restore window scroll.
  Use `window.scrollTo({ top: wy, left: 0, behavior: 'instant' })`, **not**
  `window.scrollTo(0, wy)` — the shipped `html { scroll-behavior:smooth }` at `[C01]` would
  otherwise animate the restore and it would not land on the same frame. Write that reason as
  a comment; it is invisible otherwise.
- Re-focus by `[data-k="…"]` using `CSS.escape` on the key, with
  `el.focus({ preventScroll:true })` — a bare `focus()` was measured jumping the page 1,278px
  (ARCHITECTURE T18). Restore the caret with `setSelectionRange` when the element has it.
- **Addition 1, the removed-unit fallback (P-07).** The commonest structural trigger in this
  phase is removing the unit whose remove button currently has focus, so the keyed node is gone
  by construction and the published helper returns silently, leaving focus on `<body>`. Before
  `fn()`, also record the active element's `dataset.act` and `dataset.side`, and its ordinal
  position within
  `container.querySelectorAll('[data-act="' + act + '"][data-side="' + side + '"]')`.
  After `fn()`, if the exact `data-k` is missing, take that same list again, clamp the recorded
  ordinal to `list.length - 1`, and focus that element; if the list is empty, focus
  `[data-k="' + side + '/add"]`. Deterministic, and it means keyboard removal of three units
  does not mean three re-Tabs from the top.
- **Addition 2, the ring.** Handled in `sync()` below, not here.

**`structure(state)` — the rare rebuild.** Wrapped in `withPreservedFocus(board, …)`. It
replaces the interiors of `#col-cats` and `#col-mechs` only — never `#board` itself, never
`#app`, because 02-02 binds its delegated listeners to `#app` and those must outlive every
rebuild. Use `replaceChildren()` as the clear (already shipped, already passes the scan; the
markup sinks are all gate-forbidden). Every node is `document.createElement` and every string
is `textContent`.

Per side it builds, in order:
1. a faction header carrying the faction name at 18px+; the shared action-point stepper (`−`
   button, `.num` text input, `+` button) per the `data-k`/`data-act` contract table above;
   an **empty** `.tok-row` container for AP tokens; a read-only damage row with an empty
   `.tok-row` container and the plain label `damage` (Q-2); and an eHP readout from
   `App.model.factionEhp` labelled in plain words.
2. one unit card per unit in `state.build[side].units`, each carrying the unit name, a health
   stepper trio, a shield stepper trio, a remove button, and **empty** `.tok-row` containers
   for health and shield.
3. an Add button per side, `data-k="{side}/add"`, `data-act="addUnit"`, text
   `+ Add Cat` / `+ Add Mech` (D-06).

**`structure()` builds no token nodes at all** — every `.tok-row` comes out empty and `sync()`
fills it. That is what makes "a Tier-1 rebuild repopulates from an empty row, so nothing
animates" true, and it is a rule, not an optimisation.

Set `disabled` on each side's Add button when that side already has 24 units, and on every
remove button on a side that has exactly 1 unit. A disabled button dispatches no pointer
event, which is the cheap half of the guard; 02-02 adds the throwing backstop in `[S05]`.

**D-05 — the roster controls are setup-only.** `structure()` builds each side's Add button and
every per-unit remove button **only when `state.fight === null`**. A null fight slice *is* setup
(the `[S05] fightOf` comment already establishes that mode is derived, never stored). Roster
shaping and fight bookkeeping are different activities and the UI must not blur them. Phase 2
ships no way to start a fight from the page, but `App.ops.startFight()` is already callable, so
building the condition now costs one branch and saves Phase 5 a re-layout.

Finally, set `boardEmpty.hidden = true` (the `#board-empty` paragraph kept in Task 1) once
both columns have content.

**`sync(state)` — the per-frame reconcile.** Three rules, all of them load-bearing:

- **Rule 1 (this *is* D-19, and P-06).** A private `setValue(el, n)` writes
  `el.value = String(n)` **only if `el !== document.activeElement`**. Any commit anywhere
  schedules a frame and that frame writes every input; a student halfway through typing `+5`
  must not get `3` back.
- **Rule 2.** A private `syncTokens(row, n, tokenId)` grows and shrinks **by delta only** —
  `while (row.children.length > n) { row.lastElementChild.remove(); }` then
  `while (row.children.length < n) { row.appendChild(makeToken(...)); }`. Node identity is the
  animation contract: 10 → 11 keeps all ten original nodes and appends exactly one.
- **Rule 3 (P-04).** Never re-append, never reorder, never `row.append(...allNodes)`, never
  `replaceChildren(...existingNodes)`. `appendChild` on a node that is already a child is a
  move — a remove plus an insert — and removal from the document cancels the element's CSS
  animations, so every moved token replays its pop. On a game-feel course artifact that is a
  correctness bug, not a polish item. Write this as a comment on the reconcile.

A private `syncRow(row, n, tokenId, tokenStyle)` handles compaction (D-20/21/22). Threshold is
a named module constant `COMPACT_AT = 12`, exported on the frozen `App.render` surface so
`[S09]` asserts against the constant rather than a bare `12`. Below the threshold: `n` token
nodes. At or above: exactly two children — a `.tok-count` span whose `textContent` is
`n + '×'` (the multiplication sign) and one `.tok`. Mode is tracked in
`row.dataset.mode` (`'t'` / `'c'`); when the mode changes, `row.replaceChildren()` first, so
crossing the threshold in either direction rebuilds from empty and therefore **animates
nothing**. The shipped defaults (Cats 3 HP, Mechs 6 HP + 3 shield, both AP 3) all sit under
12, so the default board always shows the real token vocabulary — which is the point.

A private `makeToken(tokenStyle, animate)` builds the three-layer node from a
`{shape, color, glyph}` record: a `.tok` with `tok--{shape}` and `tok--{color}` classes, a
`.tok-s` child, and a `.tok-g` child **only when `glyph` is a non-empty string**. The shape and
colour ids are enum values mapped to a fixed class suffix — never a caller string interpolated
raw into `className`. The `animate` flag adds `tok--in`, and it is true **only** when the token
is being appended to a row that already had children in the same mode. Adding one health point
animates exactly one token; everything else animates nothing.

`sync()` reads the vocabulary as `state.build.tokens[tokenId]` at render time. Nothing derived
is stored in state — `eHP`, damage counts and AP counts are all computed here from
`App.model.*` on every frame, per the `[S02]` banner and PROJECT.md's standing rule.

**The kbdNav reader (Addition 2 / P-08).** `sync()` sets
`document.body.dataset.kbd = state.ui.kbdNav ? '1' : ''`. `ui.kbdNav` has shipped since
Phase 1 with `ops.setUi('kbdNav', …)` allowlisted and no reader; this is the reader, and
02-02 is the writer. Task 1's `body[data-kbd="1"] :focus` rule is what it lights up.

**`[S09.4]` — a new suite**, registered with the lighter one-line banner form
`/* --- [S09.4] SUITE: render contract — owner plan 02-01 --- */`. It must be DOM-gated the
way research prescribes and `01-REVIEW-FIX` already did for its boot probe: at the top,
`if (typeof document === 'undefined') { t.info('render', 'skipped — no DOM'); return; }`, so
the Node run stays green and a browser run gains real coverage. Assertions that do **not**
need a DOM go above that gate: `App.render.COMPACT_AT === 12`, and `App.render.structure` /
`App.render.sync` are both functions. Assertions below the gate: after
`App.render.structure(App.state.get())`, the rendered unit-card count per side equals
`state.build[side].units.length`; a health `.tok-row` holds `maxHp` children; forcing a row to
12 puts it in `data-mode="c"` with exactly two children. Snapshot the whole state at suite
start and hand it back with `App.state.restore(savedAll)` as the last act, exactly as
`[S09.3]` does — a suite that leaves the board dirty is a suite an instructor cannot open
mid-demo.

**Forbidden in this task:** any markup sink (`innerHTML`, `outerHTML`, `insertAdjacentHTML`,
`document.write`, `srcdoc`, `DOMParser`), any `createElementNS`, any per-node
`addEventListener` (that is `[S07]`'s job and 02-02 owns it), any write to state, any stored
derived value, any second `requestAnimationFrame` (`App.state.invalidate` already coalesces),
and any escalation from `sync()` into `structure()` — the structural trigger belongs to
`[S05]`, and hiding it inside `sync()` inverts the tier ownership.

**Comment-prose gate.** This task writes the most comment prose in the phase and the ramp/
reconcile vocabulary is exactly where it fires. `rating` is a substring of *accelerating*,
*generating*, *operating* and *iterating*; `counter` is a substring of *encounter* and of the
ordinary loop-variable name. Safe: *count*, *counting*, *acceleration*, *accelerates*, *tally*,
*index*. Re-run both greps after writing.
  </action>

  <verify>
    <automated>node tests/selftest-node.cjs</automated>
  </verify>

  <acceptance_criteria>
    - `node tests/selftest-node.cjs` exits 0 and prints `0 failed`. This is the primary gate:
      it can only pass if the `typeof document === 'undefined'` guard is present, because
      `[S09.3]` calls `App.state.flush()` in a document-free sandbox.
    - `grep -c "App.render = (function ()" cats-vs-mechs.html` prints `1` — the region is an
      IIFE, not a frozen literal.
    - `grep -c "typeof document === 'undefined'" cats-vs-mechs.html` is at least `4`
      (`[S09] report`, `[S08] byId`, `render.structure`, `render.sync`).
    - `grep -c "withPreservedFocus" cats-vs-mechs.html` is at least `2`.
    - `grep -c "preventScroll" cats-vs-mechs.html` is at least `1`.
    - `grep -c "behavior: 'instant'\|behavior:'instant'" cats-vs-mechs.html` prints `1`.
    - `grep -c "lastElementChild" cats-vs-mechs.html` is at least `1` — the shrink half of the
      delta reconcile.
    - `grep -c "COMPACT_AT" cats-vs-mechs.html` is at least `3` (declared, used, exported).
    - `grep -c "dataset.kbd" cats-vs-mechs.html` prints `1`.
    - `grep -c "S09.4\] SUITE" cats-vs-mechs.html` prints `1`.
    - `grep -cE "innerHTML|outerHTML|insertAdjacentHTML|DOMParser|createElementNS|document\s*\.\s*write" cats-vs-mechs.html`
      prints `0`.
    - `grep -c "requestAnimationFrame" cats-vs-mechs.html` prints `1` — still only `[S03]`'s.
    - `grep -ci "counter\|rating\|balanced\|difficulty" cats-vs-mechs.html` prints `0`.
    - `grep -c "verdict\|balanced\|rating\|difficulty" cats-vs-mechs.html` prints `0`.
    - `grep -cE "https?://|<link|type=\"module\"|url\(" cats-vs-mechs.html` prints `0`.
  </acceptance_criteria>

  <done>Both render tiers are implemented; a structural rebuild preserves focus, caret and
  scroll and falls back deterministically when the focused node is removed; the token reconcile
  grows and shrinks by delta and never re-appends; compaction flips at 12 without animating;
  `body[data-kbd]` is written from `ui.kbdNav`; the Node gate is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `state.build` → DOM | Every value the renderer writes originates in state. From Phase 4 that state can come from a pasted build code, so the renderer must treat it as untrusted text. |
| `state.build.tokens` → `className` | Shape and colour ids become CSS class suffixes. An arbitrary string here would be attacker-controlled markup context. |
| Node `vm` sandbox → `[S06]` | The dev harness loads the script with no `document`. The renderer must degrade rather than throw. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | `[S06] makeToken` writing `className` | mitigate | Shape/colour are looked up against `App.data.SHAPES` / `App.data.COLORS` and mapped to a fixed class suffix; a caller string is never interpolated raw into `className`. Unknown ids fall back to the `hp` entry rather than being pasted through. |
| T-02-02 | Tampering | Any text reaching the page from `state` | mitigate | `document.createElement` + `textContent` only. No markup sink exists anywhere in the file and `tests/selftest-node.cjs` enforces that document-wide. |
| T-02-03 | Denial of Service | `syncRow` with a very large token count | mitigate | Compaction at `COMPACT_AT = 12` caps the node count per row at 2 regardless of the value; `[S05]`'s `int()` clamp caps the value at 99 independently. |
| T-02-04 | Denial of Service | `render` throwing inside the rAF frame | accept | Already contained: `[S03] schedule()` wraps every frame in `App.boot.wrap`, so a render throw surfaces the styled panel and leaves the last good page on screen. No new boundary is added. |
| T-02-05 | Tampering | Reintroducing a gate-blocked technique (SVG namespace, `url()`) to draw a shape | mitigate | Shapes come from `clip-path:polygon()` / `transform:rotate()` / `border-radius`; a comment above the shape rules records that the namespace string and `url(` are both gate-failures, and the acceptance greps re-check. |
| T-02-01-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — this plan installs zero packages and the repo has zero runtime dependencies by UX-04. No install task exists, so no legitimacy checkpoint is required. |
</threat_model>

<verification>
Run from the repo root after every task:

1. `node tests/selftest-node.cjs` — exits 0, `0 failed`, passed count never decreases.
2. `grep -ci "counter\|rating\|balanced\|difficulty" cats-vs-mechs.html` — prints `0`.
3. `grep -c "verdict\|balanced\|rating\|difficulty" cats-vs-mechs.html` — prints `0`.
4. `grep -cE "innerHTML|outerHTML|insertAdjacentHTML|eval\(|new Function|https?://|<link|type=\"module\"|url\(|createElementNS|DOMParser|srcdoc" cats-vs-mechs.html` — prints `0`.
5. The file is still exactly one `<style>` block and one classic `<script>` block:
   `grep -c "<style>" cats-vs-mechs.html` prints `1`, `grep -c "<script>" cats-vs-mechs.html`
   prints `1`.

Browser confirmation (recorded in the SUMMARY, not gated here — 02-03's checkpoint is where a
human signs it off): open `cats-vs-mechs.html` by double-click and confirm two columns with the
reserved strip between them, no console errors; open `cats-vs-mechs.html#selftest` and confirm
the report renders all green.
</verification>

<success_criteria>
- Two faction columns render side by side with a visibly reserved centre strip and a sticky top
  bar holding an Undo button (D-01 through D-04).
- Health renders as green squares, shield as blue squares, action points as yellow triangles,
  best damage as red diamonds — all drawn from `state.build.tokens`, none hardcoded (ALLOC-04).
- A row of 12 or more tokens renders as a count plus one token, and crossing that threshold in
  either direction animates nothing (ALLOC-05, D-20/21/22).
- A structural rebuild preserves window scroll, `#board` scroll, the focused element, its caret
  and its focus ring; when the focused element is removed, focus lands on a deterministic
  neighbour rather than `<body>` (ALLOC-07, P-07, P-08).
- Growing a token row by one appends exactly one node and re-appends nothing (ALLOC-07, P-04).
- Every value and label meets the research §5 size and contrast spec, and nothing is conveyed by
  hover alone (UX-02); no new hex colour was introduced (UX-05).
- `node tests/selftest-node.cjs` exits 0 with more assertions than the 81 it started with.
</success_criteria>

<output>
Create `.planning/phases/02-allocation-surface/02-01-SUMMARY.md` when done.

Record in it: the final `data-k` / `data-act` table as actually written (02-02 depends on it),
the assertion count before and after, the exact `[CNN]` blocks added, and any place the
research spec was departed from and why.
</output>
