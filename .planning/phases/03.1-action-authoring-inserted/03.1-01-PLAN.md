---
phase: 03.1-action-authoring-inserted
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cats-vs-mechs.html
  - tests/selftest-node.cjs
autonomous: true
requirements: [ACT-02, ACT-03, ACT-04]
section_ownership: >
  tests/selftest-node.cjs Layer C region (:2742-2833) and its floor;
  cats-vs-mechs.html [S06.2] two label-writing lines (:3619, :3720);
  [S01] DATA constants + DEFAULTS actions + the [S01] export block;
  [S09.1] board defaults and the [S09.3] key-set rows.

must_haves:
  truths:
    - "Every dialog outside #app is read by Layer C of the PROJ-06 gate, and a dialog that is not read fails the run rather than passing silently."
    - "A student who names a token type after a comparative word still leaves the gate green."
    - "All six shipped actions carry a cost, a requirement list and a transformation list, and the shipped board is byte-identical."
    - "A field added to the action record that is wrong is caught by an assertion, not by a reader."
  artifacts:
    - path: "tests/selftest-node.cjs"
      provides: "Layer C harvest over dialog roots, bidirectional against the stub's dialog nodes, with its own floor"
      contains: "DIALOG_ROOTS"
    - path: "cats-vs-mechs.html"
      provides: "[S01] ACTION_ID_DIGITS/PATTERN/CAPTURE, ACTION_IDS, MAX_CUSTOM_ACTIONS, MAX_ACTION_REQ, MAX_ACTION_XF, MIN_XF_DELTA/MAX_XF_DELTA, XF_WHO; the six shipped actions in the new form"
      contains: "ACTION_ID_CAPTURE"
  key_links:
    - from: "tests/selftest-node.cjs Layer C"
      to: "every <dialog> the stub builds"
      via: "a named DIALOG_ROOTS list checked in both directions"
      pattern: "DIALOG_ROOTS"
    - from: "[S01] DEFAULTS[side].actions"
      to: "[S09.1] board defaults"
      via: "a tuple that projects cost/req/xf, so a wrong field reddens"
      pattern: "cost.*req.*xf"

deliberate_half_delivery:
  - "ACT-05 is HALF-DELIVERED across this phase ON PURPOSE (CONTEXT D-05b). This phase ships authoring and a proposal the student can edit and override. Application happens on Advance, in Phase 5 (FIGHT-12..16). No plan in this phase writes a transformation into any slice. That absence is asserted, not assumed — see plan 03.1-07."
---

<objective>
Make the gate able to see this phase's work, then grow the action record so an action can carry
a cost, a requirement and a transformation.

Two things happen in wave 1 because both are prerequisites that nothing else can be written
safely without:

1. **Layer C of the PROJ-06 verdict gate cannot see a dialog.** It harvests `#app`; `</main>`
   closes at `cats-vs-mechs.html:692` and `<dialog id="tok-picker">` opens at `:728`, a sibling
   outside it. 96 strings rendered into the picker are invisible to the gate today, and assembled
   copy (`'target ' + name + ' -' + n`) is invisible to Layers A and B as well. Every surface this
   phase builds lives in a dialog and every line of its copy is assembled. The gate is extended
   **before a word of that copy exists**, and it is extended bidirectionally so the dialog plan
   03.1-05 adds cannot escape it.
2. **The action record.** Adding `cost` / `req` / `xf` to the shipped six inside `DEFAULTS` was
   measured at 520 rows, 0 failed — which is the problem, not the reassurance. Nothing asserts the
   new fields, so nothing can notice them being wrong. The rows that would go red without them are
   part of this plan, not a follow-up.

Purpose: the two prerequisites every later plan in this phase depends on.
Output: a gate that reads dialogs, and an action record that can carry a student's rule.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-action-authoring-inserted/03.1-CONTEXT.md
@.planning/phases/03.1-action-authoring-inserted/03.1-RESEARCH.md
@.planning/phases/03.1-action-authoring-inserted/03.1-PATTERNS.md
@./CLAUDE.md
@cats-vs-mechs.html
@tests/selftest-node.cjs

<interfaces>
<!-- Extracted from the live artifact. Use these directly; no exploration needed. -->

[S01] DATA, the shipped record (cats-vs-mechs.html:1067-1090):
  DEFAULTS[side] has exactly the keys  id, name, ap, units, actions
  an action today is  { id, name, dmg, keywords }
  six ids: slash, hairball, screech (cats) / fly, lasers, recharge (mechs)

[S01] the id-shape trio to mirror (:929-943):
  TOKEN_ID_DIGITS -> TOKEN_ID_PATTERN, TOKEN_ID_CAPTURE   (width written ONCE)
  RESERVED_KEYS = deepFreeze(['__proto__','constructor','prototype'])   (reuse, do not duplicate)
  MAX_CUSTOM_TYPES = 6                                    (:945-949, the cap-constant idiom)
  MAX_ALLOC is the existing unsigned bound; every int() call site today floors at 0

[S01] export block to extend: :1186-1202

[S06.2] the two nodes inside the dialog whose text is a student's word:
  :3619  b.appendChild(text('span', 'pk-sw-label', labelFor(state, id)));
  :3720  if (heading) { heading.textContent = labelFor(state, id); }
  The sync label pass is scoped to `board` (:3408), so a data-lbl inside the dialog is an
  EXEMPTION MARKER ONLY and adds no second writer.

tests/selftest-node.cjs Layer C (:2742-2833):
  harvest walks leaves under dom.byId['app'], skipping text where 'lbl' in node.dataset and
  aria-label where 'albl' in node.dataset; floor is check 47 at > 125; check 48 asserts no hits.
  Baseline measured this session: 412 passed / 0 failed, interaction gate 66 of 66,
  stub-drift gate 36 shell ids, Layer C 135 rendered strings.
</interfaces>
</context>

<the_line_this_phase_walks>
This phase must not resolve anything. The student authors the rule; the tool reads it back and
waits. Nothing lands. **`eval` and `new Function` are banned** — an authored rule is a data record
the tool READS, never a string the tool executes. If an implementation finds itself deciding
something, it has left the phase.
</the_line_this_phase_walks>

<naming_trap>
Both greps must read ZERO over `cats-vs-mechs.html` after every task:

    grep -ci "counter\|rating\|balanced\|difficulty" cats-vs-mechs.html
    grep -c  "verdict\|balanced\|rating\|difficulty" cats-vs-mechs.html

`rating` hides inside **operating, iterating, enumerating, separating, decorating, generating** —
all six are natural verbs for this phase. Write "generated", never "generating". Describe, never
quote. Lift approved copy from `03.1-RESEARCH.md` § Vocabulary; do not invent a phrase and hope.
</naming_trap>

<tasks>

<task type="auto">
  <name>Task 1: Layer C reaches every dialog, bidirectionally, with its own floor</name>
  <files>tests/selftest-node.cjs, cats-vs-mechs.html</files>
  <read_first>
    - tests/selftest-node.cjs :2742-2833 (the whole Layer C region, its harvest, its floor and the
      floor's history comment)
    - tests/selftest-node.cjs :711-745 (the stub `<dialog>`, its three members, the dataset-spelling
      warning) and :827-857 (the bidirectional stub-drift gate — the shape to copy)
    - tests/selftest-node.cjs :2843-2900 (the numbered limitations list)
    - cats-vs-mechs.html :3606-3625 and :3714-3725 ([S06.2]'s two labelFor writes)
    - cats-vs-mechs.html :3398-3425 (the sync label pass, scoped to `board`)
    - .planning/phases/03.1-action-authoring-inserted/03.1-RESEARCH.md § Pitfall 1 and § Pitfall 4
  </read_first>
  <action>
    Give the gate reach over the surfaces this phase builds, and make that reach impossible to lose.

    In `cats-vs-mechs.html` `[S06.2]`: the picker list row's `.pk-sw-label` node and the picker's
    editor heading both take their text from `labelFor(state, id)`, and neither carries the
    exemption attribute. Mark both through the file's own spelling — `setData(node, { lbl: id })`
    for the row, and the equivalent for the heading — so the harvest's existing `'lbl' in dataset`
    skip covers them. State in a comment that the sync label pass queries `board`, so inside a
    dialog this attribute marks the channel and appoints no second writer; the value it carries is
    the token id, which is exactly what the attribute already means everywhere else.

    In `tests/selftest-node.cjs` Layer C: lift the harvest into a named function so it can run over
    more than one root, then run it over `#app` (unchanged, keeping check 47's floor of 125 and its
    history comment intact) and again over each dialog root. Introduce a named `DIALOG_ROOTS` list
    and gate it in BOTH directions against the dialogs the stub builds, exactly as the stub-drift
    gate does for shell ids: a dialog the stub builds that is not in `DIALOG_ROOTS` fails, and an
    entry in `DIALOG_ROOTS` the stub does not build fails. That bidirectional check is what forces
    plan 03.1-05's new dialog into the harvest instead of relying on a future author remembering.

    Drive each dialog the way a student reaches it — through the `data-act` handler the static
    markup carries where one exists — then `A.state.flush()`, harvest, and close it. Give the
    dialog harvest its OWN floor and its own numbered check, separate from 125, and open the floor
    comment with the measurement history in the same style the existing floor keeps.

    Feed every harvested dialog string through the same `RENDERED_VERDICT_WORDS` list check 48
    already uses; do not build a second word list.

    Add two control checks so the extension is asserted rather than assumed: one that a student's
    own type name reaching the dialog through `labelFor` does NOT redden (drive a real
    `renameTokenType` to a comparative word, flush, harvest, assert clean, restore), and one that
    reads the harvested dialog string count as non-zero at its floor.

    Record, as two comment lines beside the word list, the two gaps measured this session:
    `/\blead\b/i` does not match "leads" and `/dominat/i` does not match "dominant". Report them;
    do not widen the list in this plan and do not exploit either.

    Extend the numbered limitations list with the entries this phase adds a second instance to
    (close-request behaviour, and words a dialog shows only while open).
  </action>
  <verify>
    <automated>node tests/selftest-node.cjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/selftest-node.cjs` exits 0, reporting no fewer than 412 passed / 0 failed, and
      `interaction gate: 68 of 68` or higher (66 baseline + the new dialog-harvest checks).
    - The run prints a second scan line naming the dialog harvest and its own string count, and that
      count is >= 90 (the picker measured 96 this session).
    - `stub-drift gate: 36 shell ids` is unchanged by this task.
    - DELIBERATE-FAILURE PROBE A, run and recorded in the SUMMARY, then reverted: adding the literal
      word `Winner` to a STATIC string the picker renders (not through `labelFor`) makes the new
      dialog check fail. Record the failing check number and message verbatim.
    - DELIBERATE-FAILURE PROBE B, run and recorded, then reverted: removing one id from
      `DIALOG_ROOTS` while the stub still builds that dialog fails the run, and adding an id the
      stub does not build fails it in the other direction. Record both messages.
    - DELIBERATE-FAILURE PROBE C, run and recorded, then reverted: deleting the `lbl` marker added
      to `.pk-sw-label` and renaming a token type to `Winner` makes check 48 fail — proving the
      exemption is load-bearing rather than decorative.
    - `grep -ci "counter\|rating\|balanced\|difficulty" cats-vs-mechs.html` prints `0`.
    - `grep -c "verdict\|balanced\|rating\|difficulty" cats-vs-mechs.html` prints `0`.
    - `grep -c "eval(\|new Function" cats-vs-mechs.html` prints `0`.
  </acceptance_criteria>
  <done>Every dialog outside `#app` is harvested by Layer C, the list of them is gated in both directions against the stub, the harvest has its own floor with a history comment, and three deliberate-failure probes proved each half is live.</done>
</task>

<task type="auto">
  <name>Task 2: [S01] the action record, the id constants and the signed bound</name>
  <files>cats-vs-mechs.html</files>
  <read_first>
    - cats-vs-mechs.html :920-961 (TOKEN_ID_DIGITS/PATTERN/CAPTURE, RESERVED_KEYS,
      MAX_CUSTOM_TYPES, TOKEN_SCOPES and the "APPENDED, never reordered" paragraph)
    - cats-vs-mechs.html :1060-1100 (DEFAULTS, both factions' actions, the tokens map)
    - cats-vs-mechs.html :1180-1205 (defaults() and the [S01] export block)
    - cats-vs-mechs.html :1640-1675 (int(), the named-bounds comment, MAX_ALLOC)
    - .planning/phases/03.1-action-authoring-inserted/03.1-RESEARCH.md
      § Concrete record shapes for the planner, § Pattern 2, § OQ-3
  </read_first>
  <action>
    Extend `[S01]` so an action can carry a student's rule. Nothing else in the file changes.

    The id-shape trio, mirroring `TOKEN_ID_*` structure-for-structure with the width written ONCE:
    `ACTION_ID_DIGITS`, `ACTION_ID_PATTERN` on prefix `x`, `ACTION_ID_CAPTURE` built beside it from
    the same source. Copy the reason the token trio gives for building the capturing sibling rather
    than rewriting the pattern's text. Reuse `RESERVED_KEYS` — do not duplicate it. Add
    `ACTION_IDS`, a frozen list of the six shipped ids, as the analog of `TOKEN_IDS`.

    The caps, each a named exported constant with the encoding arithmetic in its comment, in the
    `MAX_CUSTOM_TYPES` idiom, and each flagged as a Phase 4 codec dial rather than a taste call:
    `MAX_CUSTOM_ACTIONS = 3` per side; `MAX_ACTION_REQ = 2`; `MAX_ACTION_XF = 2`. The arithmetic to
    write down (from RESEARCH § OQ-3): roughly 20 characters for a compact positional spelling of
    an authored action with one cost, one requirement and one transformation, plus up to 18 more
    for a name at the cap, plus roughly 6 per extra term — about 50 worst case, three per side both
    sides is about 300, against SHARE-04's 512-character budget with roughly 180 already claimed by
    token types. Name `CompressionStream('deflate-raw')` as the escalation path if the dial moves.

    The signed bound: `MIN_XF_DELTA` and `MAX_XF_DELTA`, both derived from the existing `MAX_ALLOC`
    so the number is written once, exported beside it, with the reason the existing named-bounds
    comment gives. Every `int()` call site in the file today floors at 0; a transformation is signed
    by nature and this is the first bound that is not.

    `XF_WHO = deepFreeze(['caster', 'target'])`, given the same "entries may be APPENDED, never
    reordered" paragraph `TOKEN_SCOPES` carries, because Phase 4 may encode the position.

    Rewrite all six shipped actions inside `DEFAULTS` into the new form: each gains
    `cost: [{ tok: 'ap', n: 1 }]`, `req: []` and an `xf` array. Slash gets one entry, target `hp`,
    delta -1. Lasers gets one entry, target `hp`, delta -3. Hairball, Screech and Fly get `xf: []`.
    Recharge gets `xf: []` and a comment saying its shield gain is deliberately left out: the
    `shield` KEYWORD carries that meaning and the table adjudicates it, and writing the gain here
    would name Recharge in plan 03.1-02's admission line on a board where the student has authored
    nothing. `dmg` and `keywords` stay on all six (D-06) — the fields are added, not swapped in.

    Every term is an ARRAY OF RECORDS carrying `tok` as a FIELD. Never an object keyed by token id.
    Write the reason in a comment: a keyed bag re-opens the key position `requireTokenId` exists to
    close, and `Object.create(null)` cannot be the mitigation because it does not survive `thaw()`.

    Extend the `[S01]` export block with every new constant.
  </action>
  <verify>
    <automated>node tests/selftest-node.cjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/selftest-node.cjs` exits 0 at no fewer than 412 passed / 0 failed, interaction gate
      green, stub-drift gate `36 shell ids`.
    - `node -e` over the artifact reports `App.data.DEFAULTS.mechs.actions[1].xf` deep-equal to
      `[{ who: 'target', tok: 'hp', d: -3 }]` and `DEFAULTS.mechs.actions[2].xf` equal to `[]`.
    - `Object.isFrozen(App.data.XF_WHO)` and `Object.isFrozen(App.data.ACTION_IDS)` are both true.
    - `App.data.ACTION_ID_PATTERN.test('x1')` is true; `.test('t1')`, `.test('slash')`,
      `.test('x100')` and `.test('1x')` are all false.
    - The width appears in the source exactly once: `grep -c "ACTION_ID_DIGITS" cats-vs-mechs.html`
      is 3 or fewer (the definition and its two readings), and no second hand-written digit range
      exists in either action pattern.
    - `grep -ci "counter\|rating\|balanced\|difficulty" cats-vs-mechs.html` prints `0`.
    - `grep -c "verdict\|balanced\|rating\|difficulty" cats-vs-mechs.html` prints `0`.
    - `grep -c "eval(\|new Function\|innerHTML\|insertAdjacentHTML\|document.write\|DOMParser" cats-vs-mechs.html` prints `0`.
  </acceptance_criteria>
  <done>All six shipped actions carry `cost`, `req` and `xf`; the id trio, the three caps, the signed bound and the `who` allowlist exist as named exported constants; `dmg` and `keywords` are untouched.</done>
</task>

<task type="auto">
  <name>Task 3: the rows that would go red without Task 2</name>
  <files>cats-vs-mechs.html</files>
  <read_first>
    - cats-vs-mechs.html the `[S09.1]` board-defaults suite, in particular the action tuple
      projection `tuple(a) => [a.id, a.name, a.dmg, a.keywords]`
    - cats-vs-mechs.html the `[S09.3]` state-contract suite's four `Object.keys(build)` rows
    - cats-vs-mechs.html :6623-6634 (assert against the NAMED constant, never a re-typed copy)
    - .planning/phases/03.1-action-authoring-inserted/03.1-RESEARCH.md § Pitfall 2 and § Pitfall 10
  </read_first>
  <action>
    Task 2 is invisible to the suite as written — measured, 520 rows green with every new field
    present and 520 green with every one of them wrong. Close that.

    Widen `[S09.1]`'s action tuple so it projects `cost`, `req` and `xf` as well as the four fields
    it projects today, and state in a comment that a projection which drops a field is a check that
    cannot see it — the same shape as the two review findings Phase 3 paid for.

    Add rows that name the specific facts, each asserted against the exported constant rather than
    a re-typed copy: that Lasers' transformation is exactly one entry, target, health, -3; that
    Slash's is -1; that Hairball, Screech, Fly and Recharge carry an empty transformation list; and
    one row stating in its own label WHY Recharge's shield gain is absent, so the next reader
    restoring it fails a check that explains itself.

    Add rows pinning the caps and the allowlist by name: `MAX_CUSTOM_ACTIONS` is 3,
    `MAX_ACTION_REQ` is 2, `MAX_ACTION_XF` is 2, `XF_WHO` is exactly caster then target in that
    order and is frozen, `MIN_XF_DELTA` is the negation of `MAX_XF_DELTA`, and `ACTION_IDS` names
    exactly the six the board ships with.

    Add the missing faction key-set row RESEARCH § Pitfall 10 recommends, mirroring the four
    existing `Object.keys(build)` rows: `Object.keys(build[side])` is exactly
    `['id', 'name', 'ap', 'units', 'actions']` for both sides. Today a plan can add a key to a
    faction and nothing objects; this phase's own record growth should be loud.

    Add one row asserting every number reachable inside every shipped `cost`, `req` and `xf` is an
    integer, since `NaN` launders through `thaw()` into `null` and the existing integer walk accepts
    `null`.
  </action>
  <verify>
    <automated>node tests/selftest-node.cjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/selftest-node.cjs` exits 0 at no fewer than 425 passed / 0 failed (412 baseline
      plus the new rows), interaction gate green, stub-drift `36 shell ids`.
    - The full in-file suite run with a DOM reports its TOTAL, and that total is asserted rather than
      `failed === 0` — record the TOTAL and the per-suite breakdown in the SUMMARY.
    - DELIBERATE-FAILURE PROBE D, run and recorded, then reverted: changing Lasers' `xf` delta from
      -3 to -4 reddens at least one named row, and changing `MAX_ACTION_XF` from 2 to 4 reddens the
      cap row. Record both messages verbatim. If either probe passes when red was expected, the
      probe is wrong until proven otherwise — fix the row, not the expectation.
    - DELIBERATE-FAILURE PROBE E, run and recorded, then reverted: adding a stray key to
      `build.cats` reddens the new faction key-set row.
    - `grep -ci "counter\|rating\|balanced\|difficulty" cats-vs-mechs.html` prints `0`.
    - `grep -c "verdict\|balanced\|rating\|difficulty" cats-vs-mechs.html` prints `0`.
  </acceptance_criteria>
  <done>Every field Task 2 added is asserted by a row that would go red without it, the faction key set is pinned, and two deliberate-failure probes proved the new rows can fail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| student text -> the record | a name, a token id and a signed amount typed by a student enter `build` |
| student text -> the page | a student's word reaches a rendered string the PROJ-06 gate reads |
| a pasted build code -> the record (from Phase 4) | somebody else's typing arrives as an actions array |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.1-01 | Tampering | the `cost`/`req`/`xf` shape in `[S01]` | mitigate | arrays of records with `tok` as a VALUE — no key position exists, so a caller-supplied `__proto__` cannot reparent anything (Task 2) |
| T-03.1-02 | Tampering | action ids | mitigate | `ACTION_ID_PATTERN` + `ACTION_IDS` + the shared `RESERVED_KEYS` shipped in Task 2; the three-layer gate that uses them lands in plan 03.1-03 |
| T-03.1-03 | Information disclosure | the rendered page | mitigate | Layer C extended over dialog roots, bidirectionally gated, before any of this phase's copy exists (Task 1) |
| T-03.1-04 | Elevation of privilege | an authored rule | mitigate | the rule is a data record the tool reads; `eval` and `new Function` are absent and their absence is a grep in every task's acceptance criteria |
| T-03.1-SC | Tampering | npm/pip/cargo installs | accept | this phase installs zero packages in either file; `tests/selftest-node.cjs` is Node built-ins only. No legitimacy audit applies. Any plan proposing an install has left the constraint |
</threat_model>

<verification>
- `node tests/selftest-node.cjs` exits 0; TOTAL is no fewer than 425 passed / 0 failed; the
  interaction gate reports at least 68 of 68; stub-drift reports 36 shell ids.
- Two scan lines print: `#app` at 135 or more against the floor of 125, and the dialog harvest at
  90 or more against its own floor.
- Both naming greps print 0 over `cats-vs-mechs.html`.
- The five deliberate-failure probes (A-E) are each run, their failure message recorded verbatim in
  the SUMMARY, and reverted.
- The shipped board is unchanged: `App.model.bestDamage` reads 1 for cats and 3 for mechs, and the
  strip's rendered text is identical to a stock run.
</verification>

<success_criteria>
- Layer C reads every dialog outside `#app`, and a dialog that escapes the harvest fails the run.
- The six shipped actions are expressible in the new form and are expressed in it.
- Every new field and constant is asserted by a row that would go red without it.
- Nothing on screen moved.
</success_criteria>

<output>
Create `.planning/phases/03.1-action-authoring-inserted/03.1-01-SUMMARY.md` when done, recording:
the measured dialog-harvest count and the floor chosen for it, the five probe messages verbatim,
the TOTAL and per-suite breakdown from a full DOM run, and the two naming-grep readings.
</output>
