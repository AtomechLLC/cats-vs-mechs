---
phase: 04-share-reset
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cats-vs-mechs.html
autonomous: true
requirements: [SHARE-03, SHARE-04, SHARE-08]
section_ownership: >
  cats-vs-mechs.html [S01] DATA — the wire constants, the index-stability paragraph added to four
  more allowlists, the rewritten codec-budget comment, and the [S01] export block; [S04] SERIALIZE's
  banner and a new [S04.1] sub-region holding the codec's primitives; a new [S09.11] suite and its
  line in [S09]'s banner index; the [S00] table-of-contents [S04] line. `scheduleUrlSync` stays the
  frozen no-op it is today — plan 04-04 owns it. Touches no line of [S02], [S03], [S05], [S06],
  [S07] or [S08].

must_haves:
  truths:
    - "A name a student can type — ASCII, a BMP symbol, an astral emoji, or a name at the 24-code-point cap — survives a trip through the codec's own text primitives and comes back identical."
    - "The seven allowlists the codec encodes by position each carry the promise that makes that legal, and a reorder of any of them turns the run red."
    - "Every character the primitives can produce is inside a named allowlist that excludes the comma, the space and the percent sign by construction."
    - "The 480-of-512 prediction [S01] has carried since Phase 2.1 is replaced by measured figures rather than left standing."
  artifacts:
    - path: "cats-vs-mechs.html"
      provides: "[S01] CODE_VERSION, CODE_SEP, CODE_ALPHABET, CODE_HASH_KEY, CODE_TARGET, CODE_WARN, CODE_LIMIT — each a named exported constant with its arithmetic in the comment"
      contains: "CODE_ALPHABET"
    - path: "cats-vs-mechs.html"
      provides: "[S04.1] hand-rolled UTF-8, base64url, base36, run-length and FNV-1a primitives — pure ECMAScript, no btoa, no TextEncoder"
      contains: "[S04.1]"
    - path: "cats-vs-mechs.html"
      provides: "[S09.11] the build-code suite, opened above any no-DOM bracket so every row runs in CI"
      contains: "build code"
  key_links:
    - from: "[S04.1]"
      to: "[S01]'s allowlists and caps"
      via: "App.data.* at the call site, never captured at section-body scope"
      pattern: "App\\.data\\."
    - from: "[S09.11]"
      to: "the exported constants"
      via: "asserted against the exported name, never a re-typed copy"
      pattern: "App\\.data\\.CODE_"
---

<objective>
Lay down the two things every later plan in this phase reads from, and the suite that watches them.

The codec is the only part of Phase 4 that runs end to end inside CI. `04-PATTERNS.md` § 13b is the
reason: an assertion behind `typeof document === 'undefined'` does not run in the terminal harness,
and a codec is state work, so `[S09.11]` can sit entirely above any bracket. This plan opens that
suite and gives it something to hold before a single byte of grammar exists.

Two obligations this plan discharges that were written on Phase 4's behalf, long before Phase 4:

1. **`[S01]:1694-1714` carries a prediction** — "180 + 300 = 480 against a 512-character design
   target" — and instructs the next plan to move a dial "against measured cost instead of a feeling."
   The measurement now exists. The comment gets replaced by it.
2. **Three allowlists carry an index-stability paragraph and three more do not.** `GLYPHS`,
   `TOKEN_SCOPES` and `XF_WHO` each promise entries may be appended and never reordered "without
   bumping the build code's schema version". `SHAPES`, `COLORS`, `TOKEN_IDS` and `ACTION_IDS` are
   about to be encoded by position too, and they carry nothing. This is the phase that makes all
   seven promises load-bearing, so all seven get the paragraph and a row that reddens on a reorder.

Purpose: the constants, the primitives and the suite frame the rest of the phase is written against.
Output: `[S01]` extended, `[S04]` opened for real, `[S09.11]` alive with rows that can fail.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-share-reset/04-CONTEXT.md
@.planning/phases/04-share-reset/04-RESEARCH.md
@.planning/phases/04-share-reset/04-PATTERNS.md
@./CLAUDE.md
@cats-vs-mechs.html
@tests/selftest-node.cjs

<interfaces>
<!-- Extracted from the live artifact. Use these directly; no exploration needed. -->

[S04] SERIALIZE as it stands today, :2564-2578 — a banner and a frozen no-op, NOT an IIFE:
  banner claims, all four binding: `build` only; a mandatory version prefix; a checksum;
  `deps: App.data`. Table-of-contents entry already exists at :1374.
  App.serialize = Object.freeze({ scheduleUrlSync: function () {} });

[S01] DATA, the allowlists this phase encodes by POSITION:
  :1480  var SHAPES = deepFreeze(['sq','rect','tri','dia','circ','hex']);          — NO paragraph
  :1481  var COLORS = deepFreeze(['green','gold','accent','coral','violet','accent-2','ink-dim']); — NO paragraph
  :1482  var TOKEN_IDS = deepFreeze(['hp','ap','shield','dmg','dead']);            — NO paragraph
  :1548  var TOKEN_SCOPES = deepFreeze(['unit','side']);                           — HAS the paragraph
  :1603  var GLYPHS = deepFreeze([...29 entries, first is '']);                    — HAS the paragraph
  :1690  var ACTION_IDS = deepFreeze([...the six shipped ids, first is 'slash']);  — NO paragraph
  :1747  var XF_WHO = deepFreeze(['caster','target']);                             — HAS the paragraph
  The paragraph to copy verbatim in shape is TOKEN_SCOPES' at :1543-1548.

[S01] the cap-comment idiom to copy: MAX_CUSTOM_TYPES :1532-1536, MAX_TOKEN_NAME :1553-1555,
  and MAX_CUSTOM_ACTIONS / MAX_ACTION_REQ / MAX_ACTION_XF :1694-1714 — the one being rewritten.
  MAX_TOKEN_NAME = 24 (:1555), counted in CODE POINTS with Array.from.
  TOKEN_NAME_REFUSE = /[\p{Cc}\p{Cs}]/u (:1572) — control characters and lone surrogates.
[S01] export block to extend: :1904-1937.

[S02]'s banner :1939-1947 is the posture [S04] inherits through `deps: App.data`: pure functions,
  a slice as an argument, nothing reads the state singleton, nothing produces a sentence.

[S09] banner index :9626-9640 — "the only index of them". Last entry is [S09.10] (plan 03.1-03).
[S09.10] opens at :13291 and its first ten lines are the shape to copy: the DOM-free declaration,
  the saved-state line, and the assert-against-the-exported-name paragraph at :13332-13340.
[S09.10]'s protoIntact() helper is at :13320-13327 — reuse the shape, do not re-derive it.
tests/selftest-node.cjs SUITE_FLOOR :397 = 760, with its measurement history at :381-397.

The sandbox this suite runs in has NO btoa, NO atob, NO TextEncoder, NO TextDecoder, NO URL and NO
  CompressionStream. `encodeURIComponent` IS present. Measured, not assumed.
</interfaces>

<reference_codec>
A COMPLETE reference implementation — `encode` AND `decode`, round-tripped byte-for-byte against six
boards driven through the shipped ops — was written during research and still exists at:

  C:\Users\alexy\AppData\Local\Temp\claude\C--Projects-GameDesignSkills-GameFeelDirectionCourse-CatsVsMech\6955a1c2-1679-4a99-be89-fd3975b5abb0\scratchpad\cm5.cjs

Its primitives are at lines 22-107: `utf8`, `unutf8`, `enc64`, `dec64`, `n36`, `p36`, `rle`, `unrle`,
`fnv`. **Read it and cross-check against it. Do not treat it as the source of truth** — it lives in a
temp directory that will be swept, it is written in modern Node style rather than this file's, and
`04-RESEARCH.md` § The Codec states the grammar independently. If the file is gone, the research
document is complete enough on its own; say so in the SUMMARY and carry on.
</reference_codec>
</context>

<the_line_this_phase_walks>
Nothing in this plan reads or writes the page, reads `App.state`, or produces a sentence. `[S04]`
sits above `[S05]`, `[S06]` and `[S07]` and its banner says `deps: App.data`. A primitive that
reaches for the state singleton or returns a message for a student to read has left the section.

And the standing scope line: this phase adds no verdict, no judgement and no evaluation of a build.
The one number it will eventually put on screen is a character count, which is arithmetic.
</the_line_this_phase_walks>

<naming_trap>
Both greps must read ZERO over `cats-vs-mechs.html` after every task:

    grep -ci "counter\|rating\|balanced\|difficulty" cats-vs-mechs.html
    grep -c  "verdict\|balanced\|rating\|difficulty" cats-vs-mechs.html

`rating` is a **document-wide** substring ban, comments and CSS included. It hides inside
**generating, operating, separating, integrating, decorating** — every one of them a natural verb
for a codec. Write *producing*, *writing*, *building*. Never write "generating a build code".

`counter` is banned document-wide too, so the character readout is never a "character counter" and a
monotonic sequence is never a counter — `[S05]` already avoids the noun and says "scan every id
already in use, take the largest suffix and add one".

Test labels inside `cats-vs-mechs.html` are string literals and **Layer B reads them**. Every
`t.eq(...)` first argument in `[S09.11]` is scanned.

Measured clean against the live lists, and the approved vocabulary for this phase: `share`,
`build code`, `copy`, `copied`, `paste`, `load`, `reset`, `confirm`, `discard`, `start over`,
`Workshop 16 defaults`, `clipboard`, `address bar`, `link`, `characters`, `character count`,
`counting`, `truncated`, `incomplete`, `version`, `schema`, `restore`, `overwrite`, `replace`,
`bookmark`, `reload`, `shorter`, `longer`, `fits`, `budget`, `limit`, `window`.
</naming_trap>

<tasks>

<task type="auto">
  <name>Task 1: [S01] the wire constants, and the seven positional promises made load-bearing</name>
  <files>cats-vs-mechs.html</files>
  <read_first>
    - cats-vs-mechs.html :1476-1490 (SHAPES, COLORS, TOKEN_IDS as they stand)
    - cats-vs-mechs.html :1528-1575 (MAX_CUSTOM_TYPES' comment idiom, TOKEN_SCOPES and its
      index-stability paragraph, MAX_TOKEN_NAME, TOKEN_NAME_REFUSE)
    - cats-vs-mechs.html :1590-1610 (GLYPHS and its paragraph)
    - cats-vs-mechs.html :1686-1750 (ACTION_IDS, the three caps and the comment being rewritten,
      MAX_ALLOC, XF_WHO)
    - cats-vs-mechs.html :1904-1937 (the [S01] export block)
    - .planning/phases/04-share-reset/04-RESEARCH.md § THE CENTRAL QUESTION, ANSWERED and
      § Alphabet and separator budget
  </read_first>
  <action>
    Add the wire constants to `[S01]`, each a named exported `var` with the arithmetic that chose it
    in the comment above it, in the `MAX_CUSTOM_TYPES` idiom.

    `CODE_VERSION = 'v1'` — the WIRE version, carried in the code string. Its comment must state the
    relationship to `build.schema` (:1769), which is the RECORD version and stays 1: they are the
    same number today and they are two different things, and six assertions already pin the `build`
    key set so the wire version may never become a state key.

    `CODE_SEP` — a frozen record naming one character per nesting level, so the rule "a separator may
    never appear in anything it delimits" has something to be asserted against rather than being a
    convention in a comment. Six members: section `~`, record `_`, field `.`, item `-`, term `!`,
    run `*`. Write into its comment that two grammar bugs were found during research and both were
    the same bug one level apart — a record joined its fields with the same character its term lists
    joined items with, and a tally bag joined its items with the character the run-length stream used
    between runs. **Both decoded to a wrong build rather than to an error**, which is the exact
    "loads garbage silently" failure SHARE-03 exists to prevent.

    `CODE_ALPHABET` — a character-class **allowlist** regular expression matching a whole code string,
    covering base36 digits, the base64url alphabet (which already includes `-` and `_`) and the six
    separators. Its comment must name the three characters that are excluded by construction and why,
    because only one of the three reasons is discoverable from the browser:
      - `,` because `App.hasFlag` (:1420-1431) splits `location.hash` on it and compares whole
        tokens, so a comma silently fragments a code and stops `#selftest` working beside one. This
        is the binding alphabet constraint in the whole phase and it is not in CLAUDE.md;
      - a space, because it becomes a percent escape on write-back and the code would not survive
        its own mirror;
      - `%`, because it is ambiguous against percent-encoding.
    An allowlist, never a blocklist — a blocklist is green about every character nobody thought of.

    `CODE_HASH_KEY = 'b'` — the name of the token the build code occupies inside the comma-separated
    hash, so `#selftest,b=v1~…` parses correctly under the existing `hasFlag`. Its comment states
    that a named token makes the boot reader a prefix test rather than a guess about whether an
    unknown token is a flag or a code.

    The three size figures, each with its basis in the comment and each a fact rather than a
    judgement: `CODE_TARGET = 512` (the design target for a board a student actually builds),
    `CODE_WARN = 1800` (D-18's threshold, the point past which the surface says a code is longer than
    a message allows) and `CODE_LIMIT = 2000` (the free-tier message limit — **MEDIUM confidence,
    inherited from CLAUDE.md's two secondary sources rather than from a first-party document**, and
    the comment must say so, because it is the one number in this phase that is not measured).

    Then **rewrite `:1694-1714`'s prediction with the measurement.** That comment instructs the next
    plan to move a dial "against measured cost instead of a feeling". The measurement says the dials
    are not where the characters are. Replace the arithmetic with the six measured round-tripped
    figures — 45 for the shipped board untouched, 295 for a realistic 12v5 student build, 283 for a
    24v24 with every unit a distinct health/shield pair, 675 for a 24v24 that fully exercises what
    Phases 2.1 and 3.1 shipped, and 2,984-3,186 only for a deliberately adversarial board — and with
    the two cost centres that actually dominate at the ceiling: the tally stream and the name lengths.
    State the conclusion the measurement supports: **do not move `MAX_CUSTOM_TYPES`,
    `MAX_CUSTOM_ACTIONS`, `MAX_ACTION_REQ` or `MAX_ACTION_XF`**, because buying budget by taking
    expressiveness away from students cuts against the thing Phases 2.1 and 3.1 exist to give them.
    And state that `CompressionStream` — the escalation path that same comment names — is ruled out,
    and that the reason is not size: it is asynchronous, and an `await` between the click and the
    clipboard write forfeits the Chrome-107 user gesture SHARE-01 depends on.

    Finally, add the index-stability paragraph — the one `TOKEN_SCOPES` carries at :1543-1548, in the
    same words — to `SHAPES`, `COLORS`, `TOKEN_IDS` and `ACTION_IDS`. All four are about to be
    encoded by position. Say in each what a reorder would now cost: a link a classmate shared last
    term silently loads a different board.

    Extend the `[S01]` export block with every new constant.
  </action>
  <verify>
    <automated>node tests/selftest-node.cjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/selftest-node.cjs` exits 0 at no fewer than 789 passed / 0 failed (this task adds
      constants, not rows), interaction gate 117 of 117, stub-drift 73 shell ids, `#app` 127,
      dialogs 144 across 2 roots.
    - `node -e` over the artifact reports `App.data.CODE_ALPHABET.test('v1~Ab.9-x!2*3_w00g')` true
      and `.test('v1~a,b')`, `.test('v1~a b')` and `.test('v1~a%20b')` all false.
    - `Object.isFrozen(App.data.CODE_SEP)` is true and its six values are six DISTINCT characters.
    - All seven positional allowlists — `SHAPES`, `COLORS`, `TOKEN_IDS`, `TOKEN_SCOPES`, `GLYPHS`,
      `ACTION_IDS`, `XF_WHO` — carry the index-stability paragraph. Verify by reading, and record the
      four line numbers where it was added.
    - `grep -c "512-character design target" cats-vs-mechs.html` prints `0` — the prediction is gone,
      not annotated.
    - `grep -ci "counter\|rating\|balanced\|difficulty" cats-vs-mechs.html` prints `0`.
    - `grep -c "verdict\|balanced\|rating\|difficulty" cats-vs-mechs.html` prints `0`.
  </acceptance_criteria>
  <done>The wire constants exist as named exported constants with their arithmetic beside them, the 480-of-512 prediction is replaced by six measured figures, and all seven positionally-encoded allowlists carry the promise that makes positional encoding legal.</done>
</task>

<task type="auto">
  <name>Task 2: [S04] opened as a real section, holding [S04.1]'s primitives and nothing else</name>
  <files>cats-vs-mechs.html</files>
  <read_first>
    - cats-vs-mechs.html :2564-2578 (the whole of [S04] as it stands — banner and frozen no-op)
    - cats-vs-mechs.html :1939-1951 and :2282-2299 ([S02]'s banner and its export block — the exact
      shape an opened section takes: an IIFE with 'use strict' returning one frozen object)
    - cats-vs-mechs.html :2015-2023 (actionApCost — a guard per assumption, each on its own line,
      Array.isArray before any index, Number.isInteger before any arithmetic, null for "this cannot
      be read" rather than a throw or a repair)
    - cats-vs-mechs.html :2218-2220 (soakTotal's `if (!(hit > 0))` — written so NaN is refused by the
      same line)
    - cats-vs-mechs.html :1364-1404 ([S00]'s table of contents)
    - .planning/phases/04-share-reset/04-RESEARCH.md § Unicode — resolved concretely, and § Don't
      Hand-Roll (the two rows that say DO hand-roll, and why)
    - the reference codec's primitives, cm5.cjs lines 22-107, if the file is still there
  </read_first>
  <action>
    Turn `[S04]` from a frozen literal into a section, in `[S02]`'s exact shape: an IIFE opening with
    `'use strict'` and returning one frozen object with one key per exported function. Keep
    `scheduleUrlSync` exactly as it is — a declared no-op — and say in a comment that plan 04-04 fills
    it, so this plan's diff cannot be read as having forgotten it.

    Amend the banner. It keeps its four existing claims and gains three statements this phase owes
    the next reader:
      - **`dmg` and `keywords` are deliberately not encoded.** No `[S05]` op writes either — `dispatch`
        has no arm for them and `createAction` ships every authored action at zero damage and an empty
        keyword list — so both are reconstructible, shipped actions from `DEFAULTS` and authored ones
        from those constants. **The day a later phase adds a writer for either, this becomes silent
        data loss in a codec nobody thought to revisit.** Plan 04-03 builds the tripwire; the banner
        is where the decision is stated.
      - **The checksum is not a security boundary.** Four characters of FNV-1a catch truncation and
        transcription damage — the failures SHARE-03 names — and an author who recomputes it walks
        straight past. That is why every content guard lives *behind* it rather than being replaced
        by it.
      - **The section reads `App.data` and nothing else.** `encode` takes the build slice as an
        argument; it never reaches `App.state`. `[S02]`'s banner is the template and the dependency
        arrows in the table of contents point down only.

    Open `[S04.1]` as a sub-region, carrying the sub-region index in `[S04]`'s own banner the way
    `[S09]`'s banner does — the table of contents names sections, not sub-regions. Name the four
    sub-regions now so later plans land where this one says: `[S04.1]` the primitives, `[S04.2]`
    encode, `[S04.3]` decode, `[S04.4]` the hash mirror.

    Write the primitives, all pure ECMAScript, all synchronous, and all reachable from a sandbox with
    no host globals whatsoever:

      - **UTF-8, by hand, both directions.** A string to an array of byte values and back.
        `btoa`, `atob`, `TextEncoder` and `TextDecoder` are every one of them `undefined` in the
        gate's sandbox, so a codec built on them would be untestable in CI without weakening a
        deliberately bare sandbox — and `TextEncoder` silently substitutes the replacement character
        for a lone surrogate, which is worse than throwing because it loses data without saying so.
        Encode a lone surrogate **reversibly** (the WTF-8 shape: emit its three bytes rather than
        substituting), so a lone surrogate round-trips and can then be refused **by name** by the
        decoder's own `TOKEN_NAME_REFUSE` re-check rather than arriving as a silently different name.
      - **base64url**, no padding, over those bytes. Comment that it is used rather than
        `encodeURIComponent` for two measured reasons: `encodeURIComponent` **throws `URIError`** on a
        lone surrogate — a failure `[S01]:1553-1558` predicted by name — and it costs 288 characters
        for a 24-emoji name where base64url costs 128.
      - **base36**, write and parse. The parse refuses anything that is not a base36 integer rather
        than returning `NaN` for a later line to trip over, in `actionApCost`'s idiom: a guard per
        assumption, each on its own line.
      - **Run-length encode and decode**, over a list of already-stringified payloads. The decode
        takes the total it must fill and **refuses a run structure that does not fill exactly that
        total** — a run length disagreeing with the roster is one of the thirteen refusals the
        research matrix measured.
      - **FNV-1a to base36, truncated to four characters.** Six lines, synchronous.
        `crypto.subtle.digest` is asynchronous and absent from the sandbox both.

    Every function here is pure and returns either a value or `null`/a refusal record. Nothing throws
    a sentence: `[S02]`'s banner forbids the model producing a string and `[S04]` inherits the posture,
    so the page owns every word a student reads.

    Add `[S04.1]`-`[S04.4]` to the `[S00]` table of contents under the existing `[S04]` line, with
    their owning plans.
  </action>
  <verify>
    <automated>node tests/selftest-node.cjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/selftest-node.cjs` exits 0 at no fewer than 789 passed / 0 failed, interaction gate
      117 of 117, stub-drift 73 shell ids.
    - `App.serialize` is frozen, and `typeof App.serialize.scheduleUrlSync === 'function'` still holds
      with the no-op body unchanged.
    - `grep -c "btoa\|atob\|TextEncoder\|TextDecoder\|CompressionStream\|crypto\." cats-vs-mechs.html`
      prints `0`.
    - `grep -c "App.state" cats-vs-mechs.html` inside the `[S04]` region prints `0` — verify by
      reading the region, and record the reading.
    - `grep -c "eval(\|new Function\|innerHTML\|insertAdjacentHTML\|document.write" cats-vs-mechs.html`
      prints `0`.
    - `grep -c "https\?://\|url(" cats-vs-mechs.html` prints `0`.
    - `grep -ci "counter\|rating\|balanced\|difficulty" cats-vs-mechs.html` prints `0`.
    - `grep -c "verdict\|balanced\|rating\|difficulty" cats-vs-mechs.html` prints `0`.
  </acceptance_criteria>
  <done>`[S04]` is an IIFE with a banner stating what it does not encode and why the checksum is not a boundary; `[S04.1]` holds hand-rolled UTF-8, base64url, base36, run-length and FNV-1a primitives that need no host global at all.</done>
</task>

<task type="auto">
  <name>Task 3: [S09.11] opened above the bracket, holding the primitive rows and the reorder tripwire</name>
  <files>cats-vs-mechs.html</files>
  <read_first>
    - cats-vs-mechs.html :9615-9645 ([S09]'s banner and its suite index — "the only index of them")
    - cats-vs-mechs.html :13291-13345 ([S09.10]'s opening: the DOM-free declaration, the saved-state
      line, protoIntact(), and the assert-against-the-exported-name paragraph)
    - cats-vs-mechs.html :9797-9840 ([S09.0] the assertion harness — t.eq / t.ok / t.info and what
      each is for; t.info is a measurement, not a gate)
    - tests/selftest-node.cjs :381-400 (SUITE_FLOOR and its measurement history)
    - .planning/phases/04-share-reset/04-PATTERNS.md § 13a and § 13b
  </read_first>
  <action>
    Open `[S09.11]`, named `build code`, and register it in `[S09]`'s banner index — that index is the
    only list of the suites and a suite missing from it is a suite the next author does not know about.

    **Write the whole suite above any `typeof document === 'undefined'` bracket, and say so in the
    opening comment.** A codec is state work. Five suites in this file stop at that bracket and their
    rows are documentation until somebody opens the page with `#selftest`; this one runs every row in
    CI, and that is the single luckiest fact about this phase. Nothing in `[S09.11]`, in this plan or
    in any later one, may sit below a bracket.

    Reuse `[S09.10]`'s `protoIntact()` shape rather than deriving a second one, and check the
    prototype after every path rather than once at the end — both corruption shapes evaporate or hide,
    so a single check at the bottom reads clean over a run that already went wrong.

    Assert against the EXPORTED constant and the EXPORTED function, never a re-typed copy. A copy
    drifts away from what the file actually runs, and then the suite is green about the copy.

    The rows this task adds:

    **The text primitives.** Each of these strings survives a trip out and back, identical: plain
    ASCII; a name carrying a BMP symbol; a name of astral emoji; a name at exactly `MAX_TOKEN_NAME`
    code points made entirely of astral emoji (48 UTF-16 units — a naive length check would reject a
    legal name, so assert the code-point count with `Array.from`); the empty string; and a lone
    surrogate, which must come back as the same lone surrogate rather than as a replacement character.
    For every one of them, assert the base64url output matches `CODE_ALPHABET`.

    **base36.** A write-then-parse round trip over 0, 1, 35, 36, 99 and a large value; and a refusal
    row per bad input — the empty string, a separator character, a string with a space, `null` and a
    number — where the parse declines rather than producing something arithmetic will accept.

    **Run-length.** A list that collapses to one run, a list that collapses nothing, and a list with
    two runs, each round-tripping to the same list. Then the refusal: a run structure whose lengths do
    not add up to the total it is asked to fill is declined.

    **The checksum.** Four characters, inside `CODE_ALPHABET`, stable across calls, and different for
    a one-character change in the input — assert over several distinct inputs rather than one, because
    a single pair can agree by luck.

    **The separator rule, made mechanical.** The six members of `CODE_SEP` are six distinct
    characters; every one is inside `CODE_ALPHABET`; and none of them is a comma, a space, a percent
    sign or a hash. This is the row that would have caught both grammar bugs research found, one
    level up.

    **The reorder tripwire — the cheapest possible guard against a silent wrong board.** One row per
    positionally-encoded allowlist, asserting its first element and its length against the exported
    constant: `SHAPES`, `COLORS`, `TOKEN_IDS`, `TOKEN_SCOPES`, `GLYPHS`, `ACTION_IDS`, `XF_WHO`. Each
    row's label says in its own words what a reorder costs, so the next reader who moves an entry
    fails a check that explains itself rather than one that only says a number changed.

    **The budget figures as measurements, not gates.** `t.info` rows recording `CODE_TARGET`,
    `CODE_WARN` and `CODE_LIMIT`, so the numbers appear in every report and the size rows plan 04-02
    adds have something to be read beside.

    Re-measure the suite total, move `SUITE_FLOOR` in `tests/selftest-node.cjs`, and add a line to its
    history paragraph in the same style the four existing lines use. A floor that is not moved when
    rows are added is a floor that quietly stops bounding anything.
  </action>
  <verify>
    <automated>node tests/selftest-node.cjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `node tests/selftest-node.cjs` exits 0 at **no fewer than 805 passed / 0 failed** (789 baseline
      plus this suite's rows), interaction gate 117 of 117, stub-drift 73 shell ids, `#app` 127,
      dialogs 144 across 2 roots. Record the measured total and the delta from 789.
    - `SUITE_FLOOR` has moved, and the history paragraph has a new line naming this plan and the
      measured total. Record the old and new values.
    - Every row of `[S09.11]` runs in the TERMINAL harness — confirm by counting the suite's rows in
      the `node` output, and record that count. Zero rows are skipped.
    - DELIBERATE-FAILURE PROBE A, run and recorded verbatim, then reverted: swap the first two entries
      of `SHAPES`, and separately delete the last entry of `COLORS`. Each must redden a named tripwire
      row. Record both messages.
    - DELIBERATE-FAILURE PROBE B, run and recorded, then reverted: change the hand-rolled UTF-8
      encoder to read one UTF-16 unit at a time — the naive spelling that breaks a surrogate pair.
      The astral-emoji row and the 24-emoji-name row must both redden. **If either stays green the row
      is asserting nothing** — fix the row, not the expectation, and record what was wrong with it.
    - DELIBERATE-FAILURE PROBE C, run and recorded, then reverted: change one member of `CODE_SEP` to
      a comma. The separator row must redden. Record the message.
    - `grep -ci "counter\|rating\|balanced\|difficulty" cats-vs-mechs.html` prints `0`.
    - `grep -c "verdict\|balanced\|rating\|difficulty" cats-vs-mechs.html` prints `0`.
  </acceptance_criteria>
  <done>`[S09.11]` exists, runs entirely in CI, proves every primitive round-trips including the two Unicode shapes that silently corrupt elsewhere, and reddens on a reorder of any of the seven allowlists the codec encodes by position.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a student's typed name -> the wire | free text, up to 24 code points, including emoji and lone surrogates, becomes bytes |
| a positional index -> a meaning | an ordinal in a code string resolves to an entry in an `[S01]` allowlist |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | an `[S01]` allowlist reordered after a code was shared | mitigate | the index-stability paragraph on all seven lists plus a `[S09.11]` row per list asserting first element and length; probe A proves the rows can fail |
| T-04-02 | Information disclosure | a lone surrogate silently substituted, changing a student's name without saying so | mitigate | hand-rolled reversible UTF-8 rather than `TextEncoder`; the round-trip row asserts the surrogate comes back identical; probe B proves the row is live |
| T-04-03 | Tampering | a separator appearing inside what it delimits, decoding to a valid WRONG build | mitigate | one character per nesting level in `CODE_SEP`, six-distinct + alphabet-membership rows; probe C proves it |
| T-04-04 | Spoofing | a code string carrying a comma, fragmenting under `hasFlag` and stopping `#selftest` working | mitigate | `CODE_ALPHABET` is an allowlist, not a blocklist, and every primitive's output is asserted against it |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | zero packages installed by this phase; the artifact has no runtime dependency and may not acquire one |
</threat_model>

<verification>
- `node tests/selftest-node.cjs` exits 0 at no fewer than 805 passed / 0 failed, interaction gate
  117 of 117, stub-drift 73 shell ids in both directions, `#app` 127 and dialogs 144 across 2 roots
  — this plan renders nothing, so all four page-side numbers must be UNCHANGED.
- Probes A, B and C each run, their failure message recorded verbatim, and reverted.
- Both naming greps print 0; `btoa`/`TextEncoder`/`CompressionStream`/`crypto.`, every markup sink,
  `https://` and `url(` are all absent.
- The shipped board is unchanged: the two projected figures and every rendered string read exactly as
  they did before this plan.

<human-check>
Nothing in this plan is reachable by a person — it renders nothing and has no surface. Its
LOW-confidence items are all downstream: whether a real browser's `location.hash` round-trips this
alphabet verbatim outside Chrome is rehearsal item 7 in plan 04-08, and Firefox and Safari were never
launchable in the environment that measured any of this.
</human-check>
</verification>

<success_criteria>
- The wire constants exist, are exported, and each carries the arithmetic or the source that chose it
  — including the honest note that the 2,000-character limit is the one figure in this phase inherited
  from secondary sources rather than measured.
- The prediction `[S01]` has carried since Phase 2.1 is replaced by six round-tripped measurements,
  and the conclusion drawn is that no cap moves.
- All seven positionally-encoded allowlists carry the index-stability promise, and a reorder of any
  one of them fails the run.
- `[S04]` is a real section whose banner states what it does not encode, and every primitive in it
  works in a sandbox with no host globals at all.
</success_criteria>

<output>
Create `.planning/phases/04-share-reset/04-01-SUMMARY.md` when done, recording: the measured suite
total and its delta from 789, the old and new `SUITE_FLOOR`, the four line numbers where the
index-stability paragraph was added, the probe A/B/C messages verbatim, whether `cm5.cjs` was still
present and what it was used for, and the two naming-grep readings.
</output>
